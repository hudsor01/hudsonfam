---
phase: 20-foundation-freshness-zod-tailored-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/styles/globals.css
autonomous: true
requirements:
  - AI-RENDER-01
tags:
  - dependencies
  - tailwind-v4
  - streamdown
must_haves:
  truths:
    - "streamdown@^2.5.0 is listed in package.json dependencies"
    - "node_modules/streamdown/ exists after install"
    - "src/styles/globals.css contains the exact single-asterisk @source directive for streamdown"
    - "npm run build succeeds with no errors"
  artifacts:
    - path: "package.json"
      provides: "streamdown dependency declaration"
      contains: '"streamdown"'
    - path: "src/styles/globals.css"
      provides: "Tailwind v4 @source directive so prose classes in Streamdown are scanned"
      contains: '@source "../../node_modules/streamdown/dist/*.js"'
  key_links:
    - from: "src/styles/globals.css"
      to: "node_modules/streamdown/dist/*.js"
      via: "@source directive"
      pattern: '@source\s+"\.\./\.\./node_modules/streamdown/dist/\*\.js"'
---

<objective>
Install the `streamdown@^2.5.0` package and add Tailwind v4's `@source` directive to `globals.css` so that Streamdown's prose utility classes are picked up at build time. This plan unblocks every downstream plan that renders or tests markdown (Plans 02, 04, 05).

Purpose: Foundational wiring. No behavior change is visible yet; the point is that subsequent plans can `import { Streamdown } from "streamdown"` without a missing-module error, and Tailwind v4 will scan Streamdown's dist files for class names so rendered prose has proper typographic styling.

Output:
- Updated `package.json` with `streamdown` in `dependencies`
- Updated `package-lock.json`
- Updated `src/styles/globals.css` with the `@source` directive placed between the existing `@import` lines and the `@custom-variant` line
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-foundation-freshness-zod-tailored-resume/20-CONTEXT.md
@.planning/phases/20-foundation-freshness-zod-tailored-resume/20-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Current top of src/styles/globals.css — needed so executor knows exactly where the @source line lands. -->

From src/styles/globals.css (lines 1–4):
```css
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));
```

Target state after this plan (lines 1–5):
```css
@import "tailwindcss";
@import "tw-animate-css";
@source "../../node_modules/streamdown/dist/*.js";

@custom-variant dark (&:is(.dark *));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install streamdown@^2.5.0</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - /home/dev-server/hudsonfam/package.json
    - /home/dev-server/hudsonfam/.planning/phases/20-foundation-freshness-zod-tailored-resume/20-RESEARCH.md (§Standard Stack — New addition)
  </read_first>
  <action>
Run `npm install streamdown@^2.5.0` in the repo root. This will:
1. Add `"streamdown": "^2.5.0"` to the `dependencies` block of `package.json` (NOT devDependencies — it is a runtime render component).
2. Update `package-lock.json` with the resolved version (expected 2.5.0) and its 16 transitive deps (including `rehype-raw`, `rehype-sanitize`, `rehype-harden`, `marked@17`, `remark-gfm@4`, `mermaid@11`).
3. Peer-deps are compatible with the project's `react@19.2.4` + `react-dom@19.2.4` per RESEARCH.md Q2 verification — NO `--legacy-peer-deps` flag required.

Do NOT also install `date-fns` — it is already at `^4.1.0` in package.json (line 26) and is the same version RESEARCH.md specifies. Do NOT run `npm install --save-dev` — streamdown is a runtime dep.

After install, verify by reading `package.json` and confirming the new line in `dependencies`. If npm emits a peer-dep warning about React 19, note it in the task output and continue — RESEARCH.md explicitly verifies compatibility.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.streamdown) { console.error('streamdown missing from package.json dependencies'); process.exit(1); } console.log('streamdown version:', p.dependencies.streamdown);" && test -d node_modules/streamdown && echo "OK: node_modules/streamdown exists"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"streamdown":\s*"\^2\.5\.' package.json` returns a match
    - `test -d node_modules/streamdown` exits 0
    - `test -f node_modules/streamdown/dist/index.js` exits 0 (or any `.js` file inside `node_modules/streamdown/dist/`)
    - `node -e "require('streamdown')"` exits 0 (module resolves)
  </acceptance_criteria>
  <done>streamdown@^2.5.0 is a runtime dependency, installed on disk, and requires cleanly from Node.</done>
</task>

<task type="auto">
  <name>Task 2: Add Tailwind v4 @source directive for streamdown</name>
  <files>src/styles/globals.css</files>
  <read_first>
    - /home/dev-server/hudsonfam/src/styles/globals.css (lines 1–10 — confirm the current top matches the expected baseline)
    - /home/dev-server/hudsonfam/.planning/phases/20-foundation-freshness-zod-tailored-resume/20-RESEARCH.md (§Q7 — exact incantation + single-asterisk clarification)
  </read_first>
  <action>
Edit `src/styles/globals.css`. Insert exactly ONE new line after the existing `@import "tw-animate-css";` (currently line 2) and before the blank line that precedes `@custom-variant dark (&:is(.dark *));` (currently line 4).

The EXACT line to insert (verbatim, single asterisk — NOT double asterisk, despite CONTEXT.md D-14's typo):

```
@source "../../node_modules/streamdown/dist/*.js";
```

The resulting top of the file must be (lines 1–5):

```css
@import "tailwindcss";
@import "tw-animate-css";
@source "../../node_modules/streamdown/dist/*.js";

@custom-variant dark (&:is(.dark *));
```

Rationale for single asterisk (per RESEARCH.md §Q7 VERIFIED against Context7 /vercel/streamdown): Streamdown's official docs show `dist/*.js`. CONTEXT.md D-14 has a drafting typo with `dist/**/*.js` — use the single-asterisk form from RESEARCH.md which is the authoritative source for this task.

Rationale for the relative path `../../node_modules/`: `src/styles/globals.css` is two directories deep from the repo root; `node_modules/` lives at the repo root. RESEARCH.md Pitfall 5 warns that a wrong relative path fails silently — Tailwind v4 compiles without error but doesn't scan the files.

Do NOT import `streamdown/styles.css`. RESEARCH.md §Q2 verifies this is only needed when the `animated` prop is used; we render static content.

Do NOT modify any other line in globals.css. The `@theme` block, animations, and all existing tokens stay exactly as they are.
  </action>
  <verify>
    <automated>grep -nE '^@source "\.\./\.\./node_modules/streamdown/dist/\*\.js";$' src/styles/globals.css && head -n 5 src/styles/globals.css | grep -q '^@source' && npm run build 2>&1 | tee /tmp/build-20-01.log | tail -20 && grep -qE '(Compiled successfully|✓ Compiled|Generating static pages)' /tmp/build-20-01.log || { echo "build did not complete cleanly — check /tmp/build-20-01.log"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^@source "\.\./\.\./node_modules/streamdown/dist/\*\.js";$' src/styles/globals.css` returns exactly `1`
    - `grep -c '@source' src/styles/globals.css` returns `1` (no accidental duplicates)
    - The first five lines of `src/styles/globals.css` are (exact sequence, verifiable with `head -n 5 src/styles/globals.css`):
      1. `@import "tailwindcss";`
      2. `@import "tw-animate-css";`
      3. `@source "../../node_modules/streamdown/dist/*.js";`
      4. (blank line)
      5. `@custom-variant dark (&:is(.dark *));`
    - No double-asterisk glob (`dist/**/*.js`) appears anywhere in the file: `grep -c 'dist/\*\*' src/styles/globals.css` returns `0`
    - `npm run build` completes without a Tailwind parse error
  </acceptance_criteria>
  <done>globals.css contains the exact `@source` directive with the correct single-asterisk path; production build still compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Untrusted package content materializes into the project's runtime dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Tampering | streamdown npm package | accept | streamdown is a Vercel-authored package (same publisher as Next.js); peer-deps and version verified in RESEARCH.md against npm registry 2026-04-21. No additional supply-chain check added in Phase 20 — accepted as existing project risk level (same as every other npm dep). |
| T-20-01-02 | Information Disclosure | Tailwind `@source` glob | mitigate | Restrict path to exactly `node_modules/streamdown/dist/*.js` — does NOT expand scanning to unrelated packages, does NOT point at application source, so no leaked code is scanned for classes. |
</threat_model>

<verification>
1. `grep -n '"streamdown"' package.json` shows the dep line with `^2.5.x` version
2. `head -n 5 src/styles/globals.css` shows the exact 5-line sequence with the single-asterisk `@source` directive
3. `npm run build` completes without a Tailwind parse error
4. `node -e "require('streamdown')"` exits 0
</verification>

<success_criteria>
- `streamdown` resolves as a runtime dependency
- `src/styles/globals.css` contains the single-asterisk `@source` directive at line 3 (verified by `head -n 5 src/styles/globals.css`)
- `npm run build` completes without a Tailwind parse error
- Plans 02, 04, and 05 are now unblocked (they can `import { Streamdown } from "streamdown"` and get prose styling)
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-freshness-zod-tailored-resume/20-01-SUMMARY.md`
</output>
