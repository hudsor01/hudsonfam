---
phase: 21
plan: 09
type: execute
wave: 5
depends_on: [21-00, 21-01, 21-02, 21-03, 21-04, 21-05, 21-06, 21-07, 21-08]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements_addressed: []
must_haves:
  truths:
    - "ROADMAP.md Phase 21 SC #2 no longer references `.md fallback` — wording reflects the owner's PDF-only override (D-03)"
    - "ROADMAP.md Phase 21 Goal + Plans list reflects the 9-plan breakdown actually shipped (not 'TBD')"
    - "ROADMAP.md Phase 21 progress row shows 9/9 Complete with the correct completion date"
    - "REQUIREMENTS.md AI-ACTION-02 description matches the PDF-only implementation (no `.md` fallback wording)"
    - "REQUIREMENTS.md traceability table marks all 5 Phase 21 REQs as Complete with the plan IDs that delivered them"
    - "STATE.md reflects Phase 21 COMPLETE (9/9) and milestone v3.0 at 2/5 phases done"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 21 section updated + SC #2 wording + plan checklist + progress row"
      contains: "Plan 21-01-PLAN.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "AI-ACTION-02 description + traceability entries for AI-ACTION-01/02, AI-RENDER-04/05/06"
      contains: "Phase 21 ("
    - path: ".planning/STATE.md"
      provides: "Current Position advanced to next phase (Phase 22 candidate); Phase 21 performance-metrics rows"
      contains: "Phase 21"
  key_links: []
---

<objective>
Ship the meta-documentation updates that close out Phase 21: ROADMAP.md reflects the owner's PDF-only override (D-03) and the 9-plan breakdown actually delivered; REQUIREMENTS.md marks all 5 Phase 21 REQs complete in the traceability table + updates the AI-ACTION-02 description to strip the `.md` fallback wording; STATE.md advances the current position and adds Phase 21 performance-metric rows.

Purpose: CONTEXT.md `<deferred>` section explicitly calls this out: "Planner must include a task that updates both files to reflect the locked decision, OR flag it at plan-approval time." RESEARCH Open Question 3 recommends a dedicated plan (not rolled into another). This is that plan.

Output: 3 planning-doc files edited; 1 commit. No code changes, no tests. Traceability complete, owner can confidently begin `/gsd-discuss-phase 22` (Salary Intelligence) next.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-polish-copy-pdf-empty-states-link-out/21-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md Phase 21 goal + SC #2 + Plans list + Progress row</name>
  <read_first>
    - .planning/ROADMAP.md (entire Phase 21 section at lines 155-165)
    - .planning/phases/21-polish-copy-pdf-empty-states-link-out/21-CONTEXT.md §D-03 (owner override — PDF only, no .md fallback)
    - .planning/phases/21-polish-copy-pdf-empty-states-link-out/21-CONTEXT.md §D-04 (scope expansion — n8n workflow + migration + API route)
    - Directory listing: `ls .planning/phases/21-polish-copy-pdf-empty-states-link-out/*-PLAN.md | wc -l` — confirm 10 plans (00 through 09)
  </read_first>
  <action>
    **Step 1: Update the Phase 21 GOAL in ROADMAP.md.**

    The current Goal at line 156:
    > Owner can act on a tailored resume (copy, download) in one click, and every missing AI artifact shows a distinct, explanatory empty state instead of a silent blank section.

    Expand it to reflect the scope expansion (D-04), replacing it with:
    > Owner can act on a tailored resume (copy, download PDF) in one click, and every missing AI artifact shows a distinct, explanatory empty state instead of a silent blank section. Includes an end-to-end PDF pipeline extension (n8n `Job Search: Application Packager` extended with a parallel resume-PDF branch + `ALTER TABLE tailored_resumes ADD COLUMN pdf_data TEXT` migration), a company-website link-out on the sheet header, a color-coded quality-score badge on cover letters, and a bundled Phase 20 revision replacing FreshnessBadge relative-time with a formal America/Chicago M/D/YY date.

    **Step 2: Rewrite Success Criterion #2 at line 161.**

    Current text:
    > Owner clicks "Download PDF" on the tailored resume and receives a `.pdf` file (or a `.md` fallback with `Content-Disposition: attachment` if the n8n pipeline has not yet emitted `pdf_data`)

    Replace with (owner override — PDF only, no fallback):
    > Owner clicks "Download PDF" on the tailored resume and receives a `.pdf` file named `tailored-resume-job-<id>.pdf` (served by the new `/api/jobs/[id]/tailored-resume-pdf` route handler; the `tailored_resumes.pdf_data` column is populated by the extended n8n Application Packager workflow)

    DO NOT touch success criteria #1, #3, #4, #5 — they are unchanged.

    **Step 3: Replace `Plans: TBD` at line 165 with the real 10-plan list.**

    Update to:
    ```
    **Plans:** 10 plans
    Plans:
    - [ ] 21-00-PLAN.md — Phase 20 revision: FreshnessBadge relativeTime → generatedDate + attachFreshness Intl.DateTimeFormat(America/Chicago)
    - [ ] 21-01-PLAN.md — Homelab: ALTER TABLE tailored_resumes ADD COLUMN pdf_data + n8n Application Packager workflow extension (AI-ACTION-02 pipeline)
    - [ ] 21-02-PLAN.md — Zod TailoredResumeSchema.pdf_data + schema-drift EXPECTED map (AI-ACTION-02)
    - [ ] 21-03-PLAN.md — getJobDetail SELECT tr.pdf_data + getTailoredResumePdf helper + /api/jobs/[id]/tailored-resume-pdf route (AI-ACTION-02)
    - [ ] 21-04-PLAN.md — Copy button + Download anchor in TailoredResumeSection (AI-ACTION-01, AI-ACTION-02)
    - [ ] 21-05-PLAN.md — scoreColor/scoreLabel helpers + Quality badge in cover-letter meta row (AI-RENDER-05)
    - [ ] 21-06-PLAN.md — EMPTY_STATE_COPY constant + isCompanyResearchEmpty predicate + empty-state branches on all 3 LLM sections (AI-RENDER-04)
    - [ ] 21-07-PLAN.md — normalizeUrl helper + conditional company anchor in sheet header (AI-RENDER-06)
    - [ ] 21-08-PLAN.md — End-to-end UAT checkpoint (AI-ACTION-01, AI-ACTION-02, AI-RENDER-04)
    - [ ] 21-09-PLAN.md — Meta-doc finalization (ROADMAP + REQUIREMENTS + STATE text updates)
    ```

    **Step 4: Update the Progress table row for Phase 21** at the bottom of the v3.0 section (line 207 area):
    - Change `| 21. Polish (Copy + PDF + Empty States + Link-out) | 0/0 | Not started | - |` to `| 21. Polish (Copy + PDF + Empty States + Link-out) | 10/10 | Complete | <today's date> |`
    - Use the actual date the executor is running this plan; format YYYY-MM-DD.

    **Step 5: Flip the Phase 21 checkbox in the phase-list** at line 127:
    - Change `- [ ] **Phase 21: Polish (Copy + PDF + Empty States + Link-out)**` to `- [x] **Phase 21: Polish (Copy + PDF + Empty States + Link-out)**` and append ` — <today's date>` at the end of the description line (matches Phase 20 pattern at line 126).
  </action>
  <verify>
    <automated>grep -c "md fallback" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "md fallback" .planning/ROADMAP.md` returns `0` (SC #2 reworded; no residue of the old fallback wording anywhere in ROADMAP)
    - `grep -n "Plans: TBD" .planning/ROADMAP.md` returns zero matches under the Phase 21 section — the 10-plan list is present
    - `grep -c "21-00-PLAN.md\\|21-01-PLAN.md\\|21-02-PLAN.md\\|21-03-PLAN.md\\|21-04-PLAN.md\\|21-05-PLAN.md\\|21-06-PLAN.md\\|21-07-PLAN.md\\|21-08-PLAN.md\\|21-09-PLAN.md" .planning/ROADMAP.md` returns `10`
    - `grep -n "^- \\[x\\] \\*\\*Phase 21:" .planning/ROADMAP.md` returns exactly one match (checkbox flipped)
    - The Progress table row for Phase 21 shows `10/10 | Complete` not `0/0 | Not started`: `grep -n "^| 21\\." .planning/ROADMAP.md | grep "Complete"` returns at least one match
    - The updated Goal text mentions "n8n" AND "pdf_data" AND "Application Packager" AND "America/Chicago" (scope-expansion + Phase 20 revision both described)
    - SC #2 text contains `tailored-resume-job-<id>.pdf` (new filename locked in) and does NOT contain the `.md fallback` phrase
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 21 section accurately describes delivered scope: 10-plan breakdown, PDF-only SC, pipeline work folded in, Phase 20 revision called out. Checkbox flipped; progress table updated; zero residue of `.md fallback` wording.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md — AI-ACTION-02 description + traceability for 5 Phase 21 REQs</name>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file, focus on AI-ACTION-02 at line 25 and traceability table starting at line 85)
    - .planning/phases/21-polish-copy-pdf-empty-states-link-out/21-CONTEXT.md §D-03 (PDF-only override)
  </read_first>
  <action>
    **Step 1: Update AI-ACTION-02 description** at line 25.

    Current:
    > - [ ] **AI-ACTION-02**: Owner can download the tailored resume as a PDF file via a button

    Replace with:
    > - [x] **AI-ACTION-02**: Owner can download the tailored resume as a PDF file via a button (`/api/jobs/[id]/tailored-resume-pdf` route serves base64-decoded `tailored_resumes.pdf_data`; n8n Application Packager populates the column via Stirling-PDF)

    Note: also flip the leading `[ ]` → `[x]`.

    **Step 2: Flip the 4 other Phase 21 REQ checkboxes** (lines 23, 16, 17, 18 area):
    - AI-ACTION-01: `- [ ]` → `- [x]`
    - AI-RENDER-04: `- [ ]` → `- [x]`
    - AI-RENDER-05: `- [ ]` → `- [x]`
    - AI-RENDER-06: `- [ ]` → `- [x]`

    **Step 3: Update the Traceability table** (lines 85-110 area).

    For each of the 5 Phase 21 REQs, change the "Pending" status to "Complete (2026-04-22)" (or whatever today's date is) and append the plan IDs that delivered. Replace these 5 rows:

    Current:
    ```
    | AI-RENDER-04 | Phase 21 | Pending |
    | AI-RENDER-05 | Phase 21 | Pending |
    | AI-RENDER-06 | Phase 21 | Pending |
    | AI-ACTION-01 | Phase 21 | Pending |
    | AI-ACTION-02 | Phase 21 | Pending |
    ```

    With (update the date field to today's actual date):
    ```
    | AI-RENDER-04 | Phase 21 (21-06) | Complete (<today's date>) |
    | AI-RENDER-05 | Phase 21 (21-05) | Complete (<today's date>) |
    | AI-RENDER-06 | Phase 21 (21-07) | Complete (<today's date>) |
    | AI-ACTION-01 | Phase 21 (21-04) | Complete (<today's date>) |
    | AI-ACTION-02 | Phase 21 (21-01, 21-02, 21-03, 21-04) | Complete (<today's date>) |
    ```

    AI-ACTION-02 references 4 plan IDs because the end-to-end delivery is split across n8n pipeline (21-01) + Zod schema (21-02) + API route + query helper (21-03) + UI anchor (21-04).

    **Step 4: Update the "Last updated" line** at the bottom (around line 126).

    Replace with:
    > *Last updated: <today's date> — Phase 21 complete (9 plans shipped + 1 homelab-repo plan); AI-ACTION-01, AI-ACTION-02, AI-RENDER-04, AI-RENDER-05, AI-RENDER-06 all marked complete; AI-ACTION-02 description reflects PDF-only owner override (no `.md` fallback)*

    **Step 5: Update the "Coverage" section** if needed. The "v1 requirements: 24 total" count stays the same; just verify no other numbers need updating.
  </action>
  <verify>
    <automated>grep -c "Phase 21 | Pending" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 21 | Pending" .planning/REQUIREMENTS.md` returns `0` (all 5 Phase 21 rows advanced to Complete)
    - `grep -c "Phase 21 (21-" .planning/REQUIREMENTS.md` returns >= 5 (each REQ traces to specific plan IDs)
    - `grep -c "^- \\[x\\] \\*\\*AI-ACTION-01" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "^- \\[x\\] \\*\\*AI-ACTION-02" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "^- \\[x\\] \\*\\*AI-RENDER-04" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "^- \\[x\\] \\*\\*AI-RENDER-05" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "^- \\[x\\] \\*\\*AI-RENDER-06" .planning/REQUIREMENTS.md` returns `1`
    - The AI-ACTION-02 description contains `/api/jobs/[id]/tailored-resume-pdf` AND `Stirling-PDF` (or `Application Packager`); does NOT contain `.md fallback`
  </acceptance_criteria>
  <done>
    All 5 Phase 21 REQs flipped to `[x]` + traced to their delivering plan IDs; AI-ACTION-02 description replaces the stale short text with the real pipeline-aware description. Coverage table reflects current state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md — Current Position, What's Next, performance metrics, decisions carry-forward</name>
  <read_first>
    - .planning/STATE.md (entire file — focus on Current Position at lines 18-23, What's Next at lines 43-56, Performance Metrics table at lines 170-181)
  </read_first>
  <action>
    **Step 1: Update the frontmatter.**

    - Bump `last_updated` to today's ISO timestamp (use `date -u +"%Y-%m-%dT%H:%M:%S.000Z"`)
    - Update `last_activity` to something like `2026-04-22 — Phase 21 COMPLETE (10/10 plans including homelab-repo pipeline work); AI-ACTION-01, AI-ACTION-02, AI-RENDER-04/05/06 all delivered; milestone v3.0 at 2/5 phases complete`
    - Update `progress.total_plans` and `progress.completed_plans` to reflect 8 Phase 20 + 10 Phase 21 = 18 completed (check current numbers; the frontmatter accounting may include milestone-level plan counts, so add the Phase 21 plans without double-counting)

    **Step 2: Rewrite "Current Position" (lines 18-23).**

    Replace with:
    ```
    Phase: 22 (Salary Intelligence Defensive Render) — ready to discuss/plan; or Phase 23 (Owner-Triggered Workflows) per owner preference
    Plan: Phase 21 COMPLETE; next step is `/gsd-discuss-phase 22` or `/gsd-discuss-phase 23`
    Status: Phase 21 shipped end-to-end PDF pipeline (n8n Application Packager + ALTER TABLE + API route), Copy button + toast, 3 empty-states, quality badge, company link-out, and the bundled Phase 20 FreshnessBadge date-format revision. Zero hardcoded colors introduced; full test suite green at ~345+ cases; build clean.
    Last activity: <today's date> — /gsd-execute-phase 21 complete
    ```

    **Step 3: Update "What's Done" (line 27 area).**

    Append a bullet:
    > - v3.0 — AI Integration: Phase 20 complete (8/8 plans, 2026-04-21); Phase 21 complete (10/10 plans, <today's date>); Phases 22/23/24 remaining

    **Step 4: Replace "What's Next" (lines 43-56) with Phase 22 guidance.**

    New content:
    ```
    Phase 22 (Salary Intelligence Defensive Render) is the next candidate:
    - Depends on: Phase 20 only (already satisfied)
    - Parallel-safe with Phase 23 (Owner-Triggered Workflows)
    - Ships BEFORE homelab task #11 lands per v3.0 constraint — uses LEFT JOIN LATERAL tolerant of both job_id and company_name keying
    - 4 REQs: AI-RENDER-03, AI-RENDER-07, AI-DATA-01, AI-DATA-02

    Phase 23 (Owner-Triggered Workflows Pattern Setter) alternative:
    - Depends on: Phase 20 only
    - Establishes the signed-webhook + idempotency-key + sentinel-error pattern Phase 24 will copy
    - 5 REQs: AI-ACTION-03, AI-ACTION-04, AI-SAFETY-02, AI-SAFETY-03, AI-SAFETY-04

    Run `/gsd-discuss-phase 22` or `/gsd-discuss-phase 23` to begin context gathering.

    Phase order:
    - Phase 20: Foundation (Freshness + Zod + Tailored Resume) — COMPLETE (2026-04-21)
    - Phase 21: Polish (Copy + PDF + Empty States + Link-out) — COMPLETE (<today's date>)
    - Phase 22: Salary Intelligence (Defensive Render) — depends on Phase 20 only
    - Phase 23: Owner-Triggered Workflows (Pattern Setter) — depends on Phase 20 only
    - Phase 24: Regenerate Expansion — depends on Phases 22 + 23
    ```

    **Step 5: Append a "Last Session" entry** above the existing entries in that section (so newest is on top). Include: Phase 21 summary (what shipped across the 10 plans), any Rule 1/2/3 deviations, key gotchas, test count delta, next phase hint. Model this after the Phase 20 Plan 20-06 entry at lines 62-66 (but scoped to all of Phase 21).

    Draft content (let the executor fill in real test counts + SHAs + any deviations encountered):
    ```
    <today's date> <time> UTC — /gsd-execute-phase 21 complete. Phase 21 shipped in 10 plans across 5 waves: Plan 21-00 (Phase 20 revision — FreshnessBadge generatedDate), Plan 21-01 (homelab n8n Application Packager + ALTER TABLE tailored_resumes.pdf_data, user-run), Plans 21-02/03 (Zod + schema-drift + API route), Plan 21-04 (Copy + Download anchor), Plan 21-05 (quality badge + scoreColor/scoreLabel), Plan 21-06 (three empty states + EMPTY_STATE_COPY + isCompanyResearchEmpty), Plan 21-07 (company link-out + normalizeUrl), Plan 21-08 (UAT checkpoint), Plan 21-09 (meta docs). Test count: 305 → <final count>; build clean. Key gotchas: [populate during execution]. Next: /gsd-discuss-phase 22.
    ```

    **Step 6: Append Phase 21 performance-metrics rows** to the table at line 170. Add one row per executed plan with actual duration + task count + files modified (the executor populates these after each plan runs). Example row:
    ```
    | 21    | 21-00 | Xm Ys    | 3     | 9     | <today's ISO timestamp> |
    ```

    **Step 7: Append Phase 21 decision entries to "Key Decisions"** (line 96 area). Add 3-5 high-value decisions from CONTEXT.md that became part of the shipped codebase. Keep entries one-line each (the section is a flat bulleted list):
    ```
    - v3.0 Plan 21-00: FreshnessBadge prop renamed relativeTime → generatedDate; attachFreshness swapped formatDistanceToNowStrict for Intl.DateTimeFormat(America/Chicago); amber stale dot + ageDays tooltip behaviorally unchanged; Plan 20-04 + 20-06 summaries annotated with "Revision 2026-04-22" footers
    - v3.0 Plan 21-01: PDF pipeline owned by n8n Application Packager (id broLYdNkyX7y11TK), NOT the Resume Tailor workflow — extended with a parallel resume-PDF branch mirroring the cover-letter Stirling-PDF chain; `ALTER TABLE tailored_resumes ADD COLUMN pdf_data TEXT` lives in homelab repo migrations
    - v3.0 Plan 21-03: New `/api/jobs/[id]/tailored-resume-pdf/route.ts` is a verbatim clone of cover-letter-pdf (32 lines); filename `tailored-resume-job-<id>.pdf` matches cover-letter ID-based convention (resolves RESEARCH Open Question 1)
    - v3.0 Plan 21-05: scoreColor/scoreLabel extracted to src/lib/score-color.ts (resolves RESEARCH Open Question 4); default thresholds 0.6/0.8 on 0-1 scale ship pre-emptively — 0/12 cover_letters have a score today; badge is dead UI until the pipeline grader lands
    - v3.0 Plan 21-07: normalizeUrl helper in src/lib/url-helpers.ts rejects javascript:/data:/file: URIs; lenient TLD check accepts file.txt (rel="noopener noreferrer" guard makes broken target harmless); Zod `z.string().url()` remains explicitly NOT on `CompanyResearchSchema` (D-23 — fail-open would null out the whole row)
    ```

    (The executor should populate any additional Plan-NN decisions that surfaced during actual execution, not just the ones above.)
  </action>
  <verify>
    <automated>grep -c "Phase 21: Polish .* COMPLETE" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "last_activity: 2026" .planning/STATE.md` shows today's date in the frontmatter
    - `grep -n "Phase 21 COMPLETE\\|Phase 21 complete" .planning/STATE.md` returns at least 2 matches
    - `grep -n "^## What's Next" .planning/STATE.md` still exists; subsequent lines reference Phase 22 or Phase 23 as the next candidate
    - `grep -n "^- v3.0 Plan 21-" .planning/STATE.md` returns at least 3 matches (new Phase 21 decisions appended)
    - The Performance Metrics table (line 170 area) has at least one new row starting with `| 21    | 21-`
    - No stale "Phase 21 ready to plan" or "Phase 21 in progress" wording remains: `grep -n "Phase 21 ready to plan\\|Phase 21 in progress" .planning/STATE.md` returns zero matches
  </acceptance_criteria>
  <done>
    STATE.md fully reflects Phase 21 complete; Current Position advanced; What's Next points at Phase 22/23 candidates; Key Decisions captures 5+ Phase 21 technical choices for future readers; Performance Metrics has a row per executed plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meta-doc updates only — no code, no runtime trust boundary changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09-01 | Tampering (stale roadmap drives future planning astray) | ROADMAP.md / REQUIREMENTS.md | mitigate | This plan exists specifically to prevent that: locks PDF-only wording, captures delivered plan IDs in traceability, advances STATE.md so future `/gsd-discuss-phase 22` starts from accurate context. |
</threat_model>

<verification>
- All three files (ROADMAP.md, REQUIREMENTS.md, STATE.md) contain the updates per acceptance criteria
- `grep -rn "md fallback" .planning/` returns zero matches (no residue anywhere in the planning tree)
- `git diff .planning/` shows clean, targeted edits — no accidental deletion of unrelated content
</verification>

<success_criteria>
- ROADMAP.md Phase 21 checkbox flipped, goal updated, SC #2 reworded, plan list populated with 10 entries, progress row shows 10/10 Complete
- REQUIREMENTS.md AI-ACTION-02 description updated, all 5 Phase 21 REQs flipped to `[x]`, traceability rows reference delivering plan IDs
- STATE.md frontmatter updated, Current Position + What's Next + Last Session entries reflect Phase 21 completion, Key Decisions appended with 5+ new entries, Performance Metrics table extended
- Zero `.md fallback` wording anywhere in `.planning/`
- Single clean commit
</success_criteria>

<output>
After completion, create `.planning/phases/21-polish-copy-pdf-empty-states-link-out/21-09-SUMMARY.md` — a short wrap-up summary pointing at the SHAs of each plan's delivery commit (optional but helpful for future readers).
</output>
