---
phase: 23-owner-triggered-workflows-pattern-setter
plan: 08
type: execute
wave: 5
depends_on: ["23-01", "23-02", "23-03", "23-04", "23-05", "23-06", "23-07"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/23-owner-triggered-workflows-pattern-setter/23-SUMMARY.md
autonomous: true
requirements:
  - AI-ACTION-03
  - AI-ACTION-04
  - AI-SAFETY-02
  - AI-SAFETY-03
  - AI-SAFETY-04

must_haves:
  truths:
    - "ROADMAP Phase 23 SC #5 no longer mentions job-outreach (stale — grep confirmed no active call site)"
    - "ROADMAP Phase 23 SC #5 mentions the 3 actual retrofit call sites (reject/dismiss/interested)"
    - "ROADMAP Phase 23 plans list shows 23-01 through 23-08 marked [x] complete"
    - "REQUIREMENTS.md 5 Phase 23 REQs (AI-ACTION-03/04, AI-SAFETY-02/03/04) marked [x] with Plan 23-N citations"
    - "STATE.md Current Position shows Phase 23 CODE COMPLETE"
    - "23-SUMMARY.md exists with Outcome / Deliverables / Grep Gates / Deferred / Follow-Up sections"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 23 marked code-complete; SC #5 corrected; 8 plan checkboxes [x]"
    - path: ".planning/REQUIREMENTS.md"
      provides: "5 Phase 23 REQs marked [x]; traceability table updated with Plan citations"
    - path: ".planning/STATE.md"
      provides: "Current position advanced to Phase 23 CODE COMPLETE"
    - path: ".planning/phases/23-owner-triggered-workflows-pattern-setter/23-SUMMARY.md"
      provides: "Phase-level rollup: outcome, deliverables, grep gates G-1..G-7, deferred, Phase 24 follow-up notes"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Traceability table REQ → Phase/Plan citations"
      pattern: 'AI-ACTION-03|AI-ACTION-04|AI-SAFETY-02|AI-SAFETY-03|AI-SAFETY-04'
---

<objective>
Finalize all planning documents for Phase 23: correct ROADMAP SC #5 stale wording, mark all 5 REQs complete in REQUIREMENTS.md, advance STATE.md to code-complete, and create the phase-level 23-SUMMARY.md. This is a docs-only plan — no source code changes.

Purpose: Close the planning loop so `gsd-verify-work` and future planners have accurate state. The SUMMARY.md documents deferred items (n8n homelab PR, prod UAT), Phase 24 inherit instructions (verbatim pattern copy), and known non-issues (e.g., Why wave 3 plans 23-05 and 23-06 are parallel despite sharing `job-detail-sheet.tsx` as downstream consumer — they touch zero shared files themselves).

Output:
- `.planning/ROADMAP.md` — Phase 23 SC #5 reworded; 8 plan checkboxes [x]; progress row updated
- `.planning/REQUIREMENTS.md` — 5 REQ boxes [x]; traceability table Phase 23 rows added
- `.planning/STATE.md` — Current Position updated; What's Next set to Phase 24
- `.planning/phases/23-owner-triggered-workflows-pattern-setter/23-SUMMARY.md` (NEW)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-owner-triggered-workflows-pattern-setter/23-CONTEXT.md

<!-- All prior SUMMARYs (read only those that exist after prior plans complete) -->
@.planning/phases/23-owner-triggered-workflows-pattern-setter/23-01-SUMMARY.md
@.planning/phases/23-owner-triggered-workflows-pattern-setter/23-02-SUMMARY.md
@.planning/phases/23-owner-triggered-workflows-pattern-setter/23-03-SUMMARY.md
@.planning/phases/23-owner-triggered-workflows-pattern-setter/23-04-SUMMARY.md
@.planning/phases/23-owner-triggered-workflows-pattern-setter/23-05-SUMMARY.md
@.planning/phases/23-owner-triggered-workflows-pattern-setter/23-06-SUMMARY.md
@.planning/phases/23-owner-triggered-workflows-pattern-setter/23-07-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

None applicable — this plan touches only planning documentation files (.md). Zero source code changes. No attack surface introduced or modified.

## STRIDE Threat Register

No threats applicable to documentation-only changes. See Phase 23 SUMMARY.md for the end-to-end threat posture.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 23-08-01: Update ROADMAP.md — SC #5 correction + Phase 23 code-complete markers</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Read `.planning/ROADMAP.md` in full. Locate the Phase 23 section.

    **SC #5 correction** — find the current wording:
    ```
    5. Existing `fireWebhook` callsites (`job-feedback-sync`, `job-company-intel`, `job-outreach`) are retrofit to the new signed + idempotency-keyed helper in the same PR; a CI grep rule asserts every exported function in `src/lib/job-actions.ts` contains `requireRole(["owner"])` (mitigates Pitfall 9)
    ```

    Replace with:
    ```
    5. Existing `fireWebhook` call sites (`job-feedback-sync` × 2 for reject/dismiss, `job-company-intel` for interested-status auto-trigger) are retrofit to the new signed + idempotency-keyed helper in the same PR; `job-outreach` mention was stale — grep confirmed zero active call sites in `src/`; a CI grep rule in `src/__tests__/lib/job-actions.requireRole.test.ts` asserts every exported function in `src/lib/job-actions.ts` contains `requireRole(["owner"])` within 10 lines and that `fireWebhook` is fully deleted (Pitfall 9 + G-7)
    ```

    **Plan list** — update Phase 23 plans section to show 8 plans [x] and their objectives. Find the Phase 23 `**Plans:** TBD` line and replace with:
    ```
    **Plans:** 8 plans (code-complete 2026-04-22; prod UAT deferred to v3.5 per Phase 22 precedent)

    Plans:
    - [x] 23-01-PLAN.md — sendSignedWebhook primitive + HMAC-SHA256 + sentinel cascade (AI-SAFETY-02/-03/-04) — 2026-04-22
    - [x] 23-02-PLAN.md — triggerCompanyResearch + regenerateCoverLetter Server Actions (AI-ACTION-03/-04) — 2026-04-22
    - [x] 23-03-PLAN.md — retrofit 3 fireWebhook call sites; delete helper (G-7) — 2026-04-22
    - [x] 23-04-PLAN.md — CI grep gate: requireRole adjacency + fireWebhook absence (D-12/G-7) — 2026-04-22
    - [x] 23-05-PLAN.md — TriggerCompanyResearchButton client component + fake-timer tests (AI-ACTION-03) — 2026-04-22
    - [x] 23-06-PLAN.md — RegenerateCoverLetterButton + server-baseline predicate + fake-timer tests (AI-ACTION-04) — 2026-04-22
    - [x] 23-07-PLAN.md — mount buttons in job-detail-sheet + G-4 assertions + env docs (AI-ACTION-03/-04) — 2026-04-22
    - [x] 23-08-PLAN.md — meta-doc finalization (ROADMAP + REQUIREMENTS + STATE + SUMMARY) — 2026-04-22
    ```

    **Progress table** — update the Phase 23 row:
    - Plans Complete: 8/8
    - Status: Code Complete (prod UAT → v3.5)
    - Completed: 2026-04-22

    Also update the Phase 23 bullet in the phases list from `[ ]` to `[x]`:
    ```
    - [x] **Phase 23: Owner-Triggered Workflows (Pattern Setter)** — ... — CODE COMPLETE 2026-04-22, prod UAT deferred to v3.5
    ```
  </action>
  <verify>
    <automated>grep -q "job-outreach.*stale\|stale.*job-outreach" .planning/ROADMAP.md && echo "SC5 corrected" || echo "SC5 needs update"</automated>
  </verify>
  <done>
    ROADMAP.md Phase 23 section shows: SC #5 corrected (job-outreach stale noted); 8 plan checkboxes [x]; progress row 8/8 Code Complete 2026-04-22; Phase 23 bullet [x].
  </done>
</task>

<task type="auto">
  <name>Task 23-08-02: Update REQUIREMENTS.md + STATE.md and create 23-SUMMARY.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/23-owner-triggered-workflows-pattern-setter/23-SUMMARY.md</files>
  <action>
    **Part A — REQUIREMENTS.md:**

    Read the file. Find the 5 Phase 23 REQ entries:
    - `[ ] **AI-ACTION-03**` → `[x] **AI-ACTION-03**`
    - `[ ] **AI-ACTION-04**` → `[x] **AI-ACTION-04**`
    - `[ ] **AI-SAFETY-02**` → `[x] **AI-SAFETY-02**`
    - `[ ] **AI-SAFETY-03**` → `[x] **AI-SAFETY-03**`
    - `[ ] **AI-SAFETY-04**` → `[x] **AI-SAFETY-04**`

    In the Traceability section, add or update rows for all 5 Phase 23 REQs:
    | REQ | Phase | Plan | Status | Completed |
    |-----|-------|------|--------|-----------|
    | AI-ACTION-03 | Phase 23 | 23-02 (Server Action), 23-05 (component), 23-07 (mount) | Complete | 2026-04-22 |
    | AI-ACTION-04 | Phase 23 | 23-02 (Server Action), 23-06 (component), 23-07 (mount) | Complete | 2026-04-22 |
    | AI-SAFETY-02 | Phase 23 | 23-01 (webhook primitive), 23-03 (retrofit) | Complete | 2026-04-22 |
    | AI-SAFETY-03 | Phase 23 | 23-01 (webhook primitive), 23-03 (retrofit) | Complete | 2026-04-22 |
    | AI-SAFETY-04 | Phase 23 | 23-01 (sentinel cascade), 23-02 (Server Action return) | Complete | 2026-04-22 |

    Note in a comment that prod UAT (n8n HMAC verification) is deferred to v3.5-P4 per Phase 22 precedent.

    **Part B — STATE.md:**

    Read `.planning/STATE.md`. Update:
    - `Current Position:` → `Phase 23 CODE COMPLETE — 8/8 plans complete (2026-04-22)`
    - `What's Next:` → `/gsd-discuss-phase 24`
    - Add last session entry:
      ```
      2026-04-22: Phase 23 (Owner-Triggered Workflows) code-complete. Delivered:
      sendSignedWebhook HMAC primitive; triggerCompanyResearch + regenerateCoverLetter
      Server Actions; 3 fireWebhook call sites retrofitted (G-7 enforced);
      CI grep gate for requireRole adjacency (D-12); TriggerCompanyResearchButton +
      RegenerateCoverLetterButton client components with fake-timer polling tests;
      both buttons mounted in job-detail-sheet.tsx. 7 UI-SPEC grep gates (G-1..G-7)
      all locked by Vitest assertions. Prod UAT (n8n HMAC verification) deferred
      to v3.5 homelab PR — same as Phase 22 pattern.
      ```
    - Metrics: add row showing test count delta (≥ 395 → ~430+ after Phase 23 new tests)

    **Part C — Create 23-SUMMARY.md:**

    Create `.planning/phases/23-owner-triggered-workflows-pattern-setter/23-SUMMARY.md` using the `$HOME/.claude/get-shit-done/templates/summary.md` template. Content:

    ```markdown
    # Phase 23: Owner-Triggered Workflows (Pattern Setter) — SUMMARY

    **Status:** CODE COMPLETE — 2026-04-22
    **Prod UAT:** Deferred to v3.5-P4 (n8n HMAC verification is a homelab-repo PR — see §Awaiting Upstream)

    ## Outcome

    Phase 23 shipped the HMAC-signed + idempotency-keyed + sentinel-error-scrubbing webhook
    pattern that every owner-triggered action in this app will inherit. Two new owner-triggered
    buttons now live in the job detail sheet:

    - "Research this company" — fires company-research workflow for jobs with no company_research row
    - "Regenerate cover letter" — fires regenerate-cover-letter workflow and polls until generated_at advances past server-side baseline

    All existing `fireWebhook` call sites replaced with the new signed helper in the same PR.
    A CI grep rule locks the requireRole invariant on all current and future Server Actions.

    ## Deliverables

    | Artifact | Plan | REQs |
    |----------|------|------|
    | `src/lib/webhooks.ts` — sendSignedWebhook + ErrorSentinel + WebhookResult | 23-01 | AI-SAFETY-02, AI-SAFETY-03, AI-SAFETY-04 |
    | `src/lib/job-actions.ts` — triggerCompanyResearch + regenerateCoverLetter exports | 23-02 | AI-ACTION-03, AI-ACTION-04 |
    | `src/lib/job-actions.ts` — fireWebhook deleted; 3 call sites retrofitted | 23-03 | AI-SAFETY-02, AI-SAFETY-03 |
    | `src/__tests__/lib/job-actions.requireRole.test.ts` — CI grep gate D-12 + G-7 | 23-04 | (enforcement gate) |
    | `src/app/(admin)/admin/jobs/trigger-company-research-button.tsx` | 23-05 | AI-ACTION-03 |
    | `src/app/(admin)/admin/jobs/regenerate-cover-letter-button.tsx` | 23-06 | AI-ACTION-04 |
    | Both buttons mounted in `job-detail-sheet.tsx` + G-4 tests + env docs | 23-07 | AI-ACTION-03, AI-ACTION-04 |

    ## Grep Gates Enforced (G-1..G-7)

    All 7 UI-SPEC grep gates are locked by Vitest assertions running on every `npm test`:

    | Gate | What It Asserts | Test File |
    |------|-----------------|-----------|
    | G-1 | `aria-busy` present on both polling buttons | trigger-company-research-button.test.tsx, regenerate-cover-letter-button.test.tsx |
    | G-2 | No raw Tailwind color class names in button components | same |
    | G-3 | Sentinel strings displayed verbatim as "Error: {sentinel}" | same |
    | G-4 | Both buttons inside their respective SectionErrorBoundary | job-detail-sheet.test.tsx |
    | G-5 | Button labels match ROADMAP SC verbatim | trigger-*.test.tsx, regenerate-*.test.tsx |
    | G-6 | `Date.now()` absent from regenerate-cover-letter-button.tsx | regenerate-cover-letter-button.test.tsx |
    | G-7 | `fireWebhook` absent from job-actions.ts | job-actions.requireRole.test.ts |

    ## Deferred to v3.5-P4

    - **HMAC verification in n8n** — hudsonfam ships the signing side; n8n must validate the
      `X-Hudsonfam-Signature` header and reject timestamps >5 min old + duplicate X-Idempotency-Key
      values within 24h. This is a homelab-repo PR; linked below under §Awaiting Upstream.
    - **Prod UAT smoke tests** — end-to-end click flow in production (real n8n webhook call,
      real LLM run, real polling completion) deferred to v3.5 window, same as Phase 22 precedent.

    ## Awaiting Upstream (homelab repo)

    A homelab PR must add HMAC verification to 4 n8n workflows:
    - `job-feedback-sync` (reject + dismiss)
    - `job-company-intel` (interested status auto-trigger + new triggerCompanyResearch)
    - `regenerate-cover-letter` (new workflow entry point)

    Verification code template:
    ```javascript
    // n8n Code node — verify incoming HMAC (D-02 canonical format)
    const crypto = require("crypto");
    const secret = process.env.N8N_WEBHOOK_SECRET;
    const sig = $input.first().headers["x-hudsonfam-signature"];
    const ts = $input.first().headers["x-hudsonfam-timestamp"];
    const rawBody = JSON.stringify($input.first().body);
    const canonical = `${ts}.${$webhookNode.webhookPath}.${rawBody}`;
    const expected = "sha256=" + crypto.createHmac("sha256", secret).update(canonical).digest("hex");
    if (sig !== expected) throw new Error("HMAC verification failed");
    if (Date.now() - parseInt(ts) > 300_000) throw new Error("Timestamp expired");
    ```

    ## Follow-Up Notes for Phase 24 Planner

    Phase 24 (Regenerate Expansion) inherits the Phase 23 pattern verbatim:

    1. **`regenerateTailoredResume(jobId)` Server Action** — same shape as `regenerateCoverLetter`:
       `requireRole(["owner"])` first; read `tailored_resumes.generated_at` as baseline before webhook;
       fire `sendSignedWebhook("regenerate-tailored-resume", { job_id }, randomUUID())`;
       return `{ ok: true, baseline }` or `{ ok: false, sentinel }`.

    2. **`regenerateSalaryIntelligence(jobId)` Server Action** — same shape; baseline field is
       `salary_intelligence.search_date` (Phase 22 D-03 uses `search_date` not `generated_at`);
       fire `sendSignedWebhook("regenerate-salary-intelligence", { job_id }, randomUUID())`.

    3. **Phase 24 button components** — copy `RegenerateCoverLetterButton` verbatim; change:
       `regenerateCoverLetter` → `regenerateTailoredResume` / `regenerateSalaryIntelligence`;
       `cover_letter.generated_at` → `tailored_resume.generated_at` / `salary_intelligence.search_date`;
       label → "Regenerate tailored resume" / "Regenerate salary intelligence";
       icon → `FileText` / `TrendingUp` (or planner's choice).

    4. **Phase 24 may refactor to a factory component** — at N=4 polling surfaces (cover letter +
       tailored resume + salary intelligence + any future), extract a generic `RegenerateButton`
       factory accepting `action`, `predicate`, and `label` props. Phase 23 deliberately ships
       2 separate files (N=2) per CONTEXT.md D-04.

    ## Known Non-Issues

    - **Wave 3 plans 23-05 and 23-06 are parallel** — they touch zero shared files
      (each creates one new component file + one new test file; job-detail-sheet.tsx is
      modified in Wave 4 Plan 23-07 which depends on both). No file conflict.

    - **N8N_WEBHOOK_BASE const removed from job-actions.ts** — Plan 23-03 deleted it.
      `webhooks.ts` reads `process.env.N8N_WEBHOOK_URL` directly. No duplication.

    - **fireWebhook is fire-and-forget; sendSignedWebhook in retrofit sites is also fire-and-forget**
      (via `void` prefix) — this is intentional per D-11. The status-sync webhooks are side effects;
      making them awaited + sentinel-checked is a separate Phase 24/25 decision once observability
      is wired. The two new owner-triggered actions ARE awaited and sentinel-checked.

    ## Self-Check

    - [x] All 5 Phase 23 REQs marked [x] in REQUIREMENTS.md
    - [x] ROADMAP SC #5 stale job-outreach mention corrected
    - [x] All 8 plan checkboxes [x] in ROADMAP.md
    - [x] STATE.md advanced to Phase 23 CODE COMPLETE
    - [x] 7 grep gates documented and all locked by Vitest assertions
    - [x] Deferred items (n8n verification, prod UAT) documented with rationale
    - [x] Phase 24 planner has verbatim copy instructions for all 3 regenerate actions
    ```
  </action>
  <verify>
    <automated>grep -E "AI-ACTION-03|AI-ACTION-04|AI-SAFETY-02|AI-SAFETY-03|AI-SAFETY-04" .planning/REQUIREMENTS.md | grep -c "\[x\]"</automated>
  </verify>
  <done>
    REQUIREMENTS.md has all 5 Phase 23 REQs marked [x]. STATE.md shows Phase 23 CODE COMPLETE. 23-SUMMARY.md exists with all required sections. `grep -c "\[x\]" .planning/REQUIREMENTS.md | grep -E "^[5-9]|^[1-9][0-9]"` confirms at least 5 [x] boxes for Phase 23 REQs.
  </done>
</task>

<task type="auto">
  <name>Task 23-08-03: Commit Plan 23-08 atomic</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/23-owner-triggered-workflows-pattern-setter/23-SUMMARY.md</files>
  <action>
    Run:

    ```bash
    gsd-sdk query commit "docs(23-08): Phase 23 code-complete — ROADMAP SC#5 fix + REQ traceability + STATE + SUMMARY" \
      .planning/ROADMAP.md \
      .planning/REQUIREMENTS.md \
      .planning/STATE.md \
      ".planning/phases/23-owner-triggered-workflows-pattern-setter/23-SUMMARY.md"
    ```

    If SDK handler unavailable, fall back to:

    ```bash
    git add .planning/ROADMAP.md \
             .planning/REQUIREMENTS.md \
             .planning/STATE.md \
             ".planning/phases/23-owner-triggered-workflows-pattern-setter/23-SUMMARY.md"
    git commit -m "$(cat <<'EOF'
    docs(23-08): Phase 23 code-complete — ROADMAP SC#5 fix + REQ traceability + STATE + SUMMARY

    - ROADMAP.md: Phase 23 SC #5 reworded — drops stale 'job-outreach' mention
      (grep confirmed zero active call sites); lists 3 actual retrofit targets
      (reject/dismiss/interested). 8 plan checkboxes marked [x]. Progress row
      8/8 Code Complete 2026-04-22. Phase 23 bullet [x]. Plans count updated.

    - REQUIREMENTS.md: flip 5 Phase 23 REQ checkboxes to [x] (AI-ACTION-03/-04,
      AI-SAFETY-02/-03/-04). Traceability table rows added citing specific Plans
      for each REQ. Note: prod UAT (n8n HMAC verification) deferred to v3.5-P4
      per Phase 22 precedent — homelab-repo PR concern.

    - STATE.md: advance Current Position to Phase 23 CODE COMPLETE; What's Next
      → /gsd-discuss-phase 24; last session entry added; test-count metrics row
      updated.

    - 23-SUMMARY.md (NEW): phase-level rollup — Outcome, Deliverables table,
      Grep Gates G-1..G-7 with test file citations, Deferred to v3.5-P4 (n8n
      HMAC verification), Awaiting Upstream (homelab PR + verification template),
      Follow-Up Notes for Phase 24 Planner (verbatim copy instructions for
      regenerateTailoredResume + regenerateSalaryIntelligence), Known Non-Issues,
      Self-Check.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>git log -1 --format="%s" | grep -q "docs(23-08):"</automated>
  </verify>
  <done>
    Single atomic commit with `docs(23-08):` subject. All 4 planning files staged.
  </done>
</task>

</tasks>

<verification_criteria>
- [ ] ROADMAP.md Phase 23 SC #5 no longer references `job-outreach` as an active call site
- [ ] ROADMAP.md has 8 plan checkboxes marked [x] under Phase 23
- [ ] ROADMAP.md Phase 23 bullet in phases list is `[x]`
- [ ] REQUIREMENTS.md has all 5 Phase 23 REQs marked [x]: AI-ACTION-03, AI-ACTION-04, AI-SAFETY-02, AI-SAFETY-03, AI-SAFETY-04
- [ ] REQUIREMENTS.md traceability table has Phase 23 rows for all 5 REQs with plan citations
- [ ] STATE.md shows Phase 23 CODE COMPLETE as current position
- [ ] `23-SUMMARY.md` exists and contains all required sections
- [ ] Single atomic commit with subject `docs(23-08):`
</verification_criteria>

<success_criteria>
Plan 23-08 is complete when:
1. All 5 Phase 23 REQs marked [x] in REQUIREMENTS.md with plan citations
2. ROADMAP SC #5 corrected (job-outreach stale; 3 actual call sites documented)
3. STATE.md current position is Phase 23 CODE COMPLETE
4. 23-SUMMARY.md exists with 7 grep gate table, deferred items, Phase 24 follow-up notes
5. docs(23-08) commit lands
</success_criteria>

<output>
Phase 23 is CODE COMPLETE after this plan. Next: `/gsd-discuss-phase 24` (Regenerate Expansion — tailored resume, salary intelligence, silent-success state).
</output>
