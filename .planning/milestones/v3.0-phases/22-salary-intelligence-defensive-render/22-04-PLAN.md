---
phase: 22-salary-intelligence-defensive-render
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-jobs-schema.ts
autonomous: true
requirements:
  - AI-DATA-02
tags:
  - schema-drift
  - ci
  - pre-push-hook
  - expected-map

must_haves:
  truths:
    - "scripts/check-jobs-schema.ts EXPECTED map contains `salary_intelligence` entry with all 7 columns (id, search_date, report_json, raw_results, llm_analysis, created_at, updated_at)"
    - "npm run test:schema runs through 7 additional column checks and exits 0 against the live n8n DB (or skips gracefully if JOBS_DATABASE_URL unset)"
    - "Pre-push hook (installed via scripts/install-hooks.sh per Plan 20-08) now catches drift on salary_intelligence columns"
  artifacts:
    - path: scripts/check-jobs-schema.ts
      provides: salary_intelligence EXPECTED map entry
      contains: "salary_intelligence"
  key_links:
    - from: scripts/check-jobs-schema.ts
      to: n8n Postgres salary_intelligence table
      via: information_schema.columns query
      pattern: "salary_intelligence: \\["
---

<objective>
Add a `salary_intelligence` entry to the EXPECTED map in `scripts/check-jobs-schema.ts` with the 7 live-DB columns captured in CONTEXT.md §Phase Boundary. This satisfies D-04 and extends the Plan 20-08 pre-push drift guard to cover the new Phase 22 table.

Purpose: When Phase 22 ships + downstream n8n task #11 starts producing rows, any future column-drop/rename on the upstream schema fails the pre-push hook loudly, preventing silent fail-open from masking a real problem. Parallel-safe with Plans 22-01 / 22-02 / 22-03 (different file).

Output: Single entry added to the EXPECTED map (4-line diff); `npm run test:schema` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-salary-intelligence-defensive-render/22-CONTEXT.md
@.planning/phases/22-salary-intelligence-defensive-render/22-PATTERNS.md
@.planning/phases/20-foundation-freshness-zod-tailored-resume/20-08-SUMMARY.md

<interfaces>
<!-- Existing EXPECTED map at scripts/check-jobs-schema.ts:18-48 (read in full before editing) -->

Current shape:
```typescript
const EXPECTED: Record<string, string[]> = {
  jobs: [ ... 22 columns ... ],
  cover_letters: [ ... 7 columns ... ],
  company_research: [ ... 13 columns ... ],
  tailored_resumes: [ "id", "job_id", "content", "pdf_data", "model_used", "generated_at" ],
  recruiter_outreach: [ ... 11 columns ... ],
  applications: [ "id", "job_id", "applied_date", "status" ],
};
```

Live-DB columns for `salary_intelligence` (from CONTEXT.md §Phase Boundary — verified 2026-04-22 via kubectl exec psql):
- id, search_date, report_json, raw_results, llm_analysis, created_at, updated_at

Row count: 0 (pending n8n task #11). Structure is stable regardless of row count.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream n8n workflows → salary_intelligence table | Schema may drift (columns renamed, removed, added) outside our control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Tampering | Upstream drops or renames `salary_intelligence.search_date` (or any other column) without notice | mitigate | Pre-push hook fails loudly with a precise error naming the missing column + table; developer pulls in the change before pushing |
| T-22-04-02 | Availability | EXPECTED map out-of-date with reality, hook always fails or always passes falsely | accept | Human-audited; script pattern (EXPECTED subset vs actual set) inherently tolerates upstream additions without failing — only missing columns fail |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 22-04-01: Add salary_intelligence entry to EXPECTED map</name>
  <files>scripts/check-jobs-schema.ts</files>
  <read_first>
    - scripts/check-jobs-schema.ts (all 104 lines — EXPECTED map at lines 18-48 is the exact structure to extend)
    - .planning/phases/22-salary-intelligence-defensive-render/22-PATTERNS.md §10 (`scripts/check-jobs-schema.ts` (EDIT — add `salary_intelligence` to EXPECTED map))
    - .planning/phases/22-salary-intelligence-defensive-render/22-CONTEXT.md §Phase Boundary (live-DB psql dump showing 7 columns)
    - .planning/phases/22-salary-intelligence-defensive-render/22-CONTEXT.md §Decisions D-04
  </read_first>
  <behavior>
    - EXPECTED map grows from 6 table entries to 7.
    - New entry: `salary_intelligence: ["id", "search_date", "report_json", "raw_results", "llm_analysis", "created_at", "updated_at"]`.
    - `npm run test:schema` (when JOBS_DATABASE_URL is set) queries `information_schema.columns` for `salary_intelligence` and confirms all 7 columns exist — exits 0.
    - When JOBS_DATABASE_URL is unset (local dev default), the existing skip-gracefully path at line 51-56 still applies — exits 0 with warning.
  </behavior>
  <action>
    1. Open `scripts/check-jobs-schema.ts`.
    2. Find the EXPECTED map (starts line 18 `const EXPECTED: Record<string, string[]> = {`). Locate the `tailored_resumes` entry (line 37-39). AFTER the `tailored_resumes` closing `],` and BEFORE the `recruiter_outreach` entry, insert:
       ```typescript
         salary_intelligence: [
           "id", "search_date", "report_json", "raw_results", "llm_analysis",
           "created_at", "updated_at",
         ],
       ```
       Position after `tailored_resumes` preserves pipeline-chronological ordering (matches existing convention).
    3. No other edits. The `main()` function iterates via `Object.entries(EXPECTED)` so insertion order doesn't change behavior — the 7 new column checks join the existing ~56 checks (total becomes ~63; verify via the `OK — verified X tables, Y columns.` line in output).
    4. Implements D-04.
  </action>
  <verify>
    <automated>grep -A3 "salary_intelligence:" scripts/check-jobs-schema.ts | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "salary_intelligence:" scripts/check-jobs-schema.ts` returns exactly 1 match (the EXPECTED map entry).
    - `grep -A3 "salary_intelligence:" scripts/check-jobs-schema.ts` output contains each of: `id`, `search_date`, `report_json`, `raw_results`, `llm_analysis`, `created_at`, `updated_at`.
    - File count: the file grew by 3-4 lines (1 entry × 3-4 lines including the array formatting).
    - `Object.keys(EXPECTED).length` is now 7 (jobs + cover_letters + company_research + tailored_resumes + salary_intelligence + recruiter_outreach + applications). Verify by running the script; the `[test:schema] OK` output line mentions `7 tables`.
  </acceptance_criteria>
  <done>EXPECTED map has the new entry. Next task runs the script to confirm.</done>
</task>

<task type="auto">
  <name>Task 22-04-02: Verify `npm run test:schema` against live n8n DB OR graceful skip</name>
  <files>(script execution only — no file edits)</files>
  <read_first>
    - scripts/check-jobs-schema.ts (after Task 22-04-01)
  </read_first>
  <action>
    Run the script in one of two modes depending on env availability:

    **Mode A (JOBS_DATABASE_URL set — ideal):**
    ```bash
    npm run test:schema
    ```
    Expected output contains:
    - `[test:schema] connecting to <host>`
    - `[test:schema] OK — verified 7 tables, <N> columns.` where N is approximately 63 (columns sum across all EXPECTED entries).

    **Mode B (JOBS_DATABASE_URL unset — local dev default):**
    ```bash
    npm run test:schema
    ```
    Expected output contains:
    - `[test:schema] JOBS_DATABASE_URL not set — skipping drift check (non-failure)`
    - Exit code 0.

    Per the Plan 20-08 SUMMARY, local dev runs Mode B — that's the default and acceptable. The pre-push hook runs in Mode B on personal machines without JOBS_DATABASE_URL set, which is expected.

    If Mode A is available AND the live DB is reachable, prefer Mode A for verification. Otherwise Mode B is the valid acceptance path.

    No file edits. This task verifies the script executes; any drift errors indicate real upstream problems that need separate investigation.
  </action>
  <verify>
    <automated>npm run test:schema</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:schema` exits 0 (either Mode A with `OK — verified 7 tables` or Mode B with the skip warning).
    - Mode A output (if present): contains `7 tables` in the final OK line.
    - Mode B output (if no DB access): exits 0 with skip message.
    - No drift errors reported (`Expected column '<col>' on table 'salary_intelligence'; not found` — if this appears in Mode A, investigate: either the live DB has drifted OR the EXPECTED entry has a typo).
  </acceptance_criteria>
  <done>Script runs clean. The pre-push hook will catch any future salary_intelligence column drift.</done>
</task>

<task type="auto">
  <name>Task 22-04-03: Commit — feat(22-04): schema-drift EXPECTED map gains salary_intelligence</name>
  <files>(git commit)</files>
  <read_first>(none)</read_first>
  <action>
    Run:
    ```bash
    gsd-sdk query commit "feat(22-04): add salary_intelligence to schema-drift EXPECTED map (D-04; 7 columns: id, search_date, report_json, raw_results, llm_analysis, created_at, updated_at)" \
      scripts/check-jobs-schema.ts
    ```
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "feat(22-04)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` shows `feat(22-04)` and `salary_intelligence`.
    - `git diff HEAD~1 HEAD --name-only` lists exactly 1 file: `scripts/check-jobs-schema.ts`.
  </acceptance_criteria>
  <done>Commit landed. Schema-drift guardrail complete for Phase 22.</done>
</task>

</tasks>

<verification>
- Script exits 0 via `npm run test:schema` (either Mode A or Mode B).
- EXPECTED map has exactly 7 keys after this plan.
</verification>

<success_criteria>
- D-04 satisfied.
- `salary_intelligence: [7 cols]` entry in EXPECTED map.
- Pre-push hook extension is automatic (no install-hooks.sh re-run needed — the existing hook runs `npm run test:schema` which reads the EXPECTED map at runtime).
</success_criteria>

<output>
After completion, create `.planning/phases/22-salary-intelligence-defensive-render/22-04-SUMMARY.md` with:
- 4-line diff to scripts/check-jobs-schema.ts
- Whether Mode A or Mode B was used for verification
- Pre-push hook continues to work without re-install
</output>
