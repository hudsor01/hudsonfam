---
phase: 31-recipes-experience
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - src/components/public/menu-provider.tsx
  - src/components/public/add-to-menu-button.tsx
  - src/components/public/menu-indicator.tsx
  - src/app/(public)/layout.tsx
  - src/app/(public)/recipes/page.tsx
  - src/app/(public)/recipes/[slug]/page.tsx
  - src/app/(public)/my-menu/page.tsx
  - src/app/(public)/my-menu/menu-client.tsx
  - src/styles/globals.css
autonomous: true
requirements: [RECIPE-05]
must_haves:
  truths:
    - "An 'Add to menu' control appears on recipe listing items and on the detail page"
    - "Adding a recipe updates a floating 'My Menu (N)' indicator visible across the public site"
    - "The menu persists across reload and navigation via localStorage (no login)"
    - "/my-menu lists picked recipes grouped by course/category with per-item remove"
    - "/my-menu has clear-all and print actions; print reuses the RECIPE-03 print CSS"
  artifacts:
    - path: "src/components/public/menu-provider.tsx"
      provides: "React Context provider holding menu items, persisted to localStorage"
      contains: "use client"
    - path: "src/components/public/menu-indicator.tsx"
      provides: "floating My Menu (N) indicator linking to /my-menu"
      contains: "use client"
    - path: "src/app/(public)/my-menu/menu-client.tsx"
      provides: "client menu view grouped by category with remove/clear/print"
      contains: "use client"
    - path: "src/app/(public)/my-menu/page.tsx"
      provides: "server page exporting metadata (tab title), renders the client menu view"
      contains: "metadata"
  key_links:
    - from: "src/app/(public)/layout.tsx"
      to: "menu-provider.tsx + menu-indicator.tsx"
      via: "MenuProvider wraps main + MenuIndicator mounted inside the provider"
      pattern: "MenuProvider"
    - from: "src/components/public/add-to-menu-button.tsx"
      to: "menu-provider.tsx"
      via: "useMenu() context hook"
      pattern: "useMenu"
    - from: "src/app/(public)/my-menu/menu-client.tsx"
      to: "localStorage menu"
      via: "useMenu() reads persisted items"
      pattern: "useMenu"
---

<objective>
Add the build-your-own-menu feature (RECIPE-05): pick recipes from anywhere, see a floating "My Menu (N)" indicator, and review/print them at /my-menu.

Purpose: The family can assemble a meal/menu across many recipes without an account, persisted locally, and print the whole menu.
Output: A React Context menu provider (localStorage-backed) wrapping the public site, an "Add to menu" control on cards + detail, a floating indicator, and a /my-menu page (server wrapper for the tab title + client view) grouped by course with remove/clear/print.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-recipes-experience/31-CONTEXT.md

@src/app/(public)/layout.tsx
@src/app/(public)/recipes/page.tsx
@src/app/(public)/recipes/[slug]/page.tsx
@src/components/public/mobile-nav.tsx
@src/styles/globals.css

<interfaces>
<!-- Extracted from codebase. -->

From src/lib/recipes.ts:
- RecipeMeta { slug, frontmatter: { title, category, ... } }
- RecipeIndexEntry = { slug, title, category } (added in 31-01)

From src/app/(public)/layout.tsx (server component, default-exports PublicLayout({ children })) — the EXACT current body region that MenuProvider must wrap:
```tsx
<main className="flex-1">{children}</main>
```
This sits between the `</nav>` and the `<footer>`. MenuIndicator uses useMenu() so it MUST be rendered INSIDE MenuProvider. The required edit is to replace that single `<main>` line with:
```tsx
<MenuProvider>
  <main className="flex-1">{children}</main>
  <MenuIndicator />
</MenuProvider>
```
MenuProvider + MenuIndicator are client components; PublicLayout stays a server component (client components accept server-rendered children).

From src/app/(public)/recipes/page.tsx (server, after 31-01): renders listing li items with a Link per recipe; AddToMenuButton (client) is added alongside each title.
From src/app/(public)/recipes/[slug]/page.tsx (server, after 31-02): AddToMenuButton added in header; mark it no-print.

From src/styles/globals.css: @media print + .no-print exist after 31-02. /my-menu reuses the .no-print / .print-recipe conventions; add a .print-menu region only if needed.

Client pattern: mobile-nav.tsx. Context pattern: standard React createContext + useContext hook.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build MenuProvider context (localStorage-backed) and useMenu hook</name>
  <files>src/components/public/menu-provider.tsx</files>
  <read_first>
    - src/components/public/mobile-nav.tsx (client component pattern)
    - src/styles/globals.css (tokens)
  </read_first>
  <action>
    Create src/components/public/menu-provider.tsx with "use client" as the first line. Define MenuItem = { slug: string; title: string; category: string }. Create a React Context with value { items: MenuItem[]; add(item): void; remove(slug): void; clear(): void; has(slug): boolean; count: number }. localStorage key: hudson-menu storing JSON MenuItem[]. Hydrate from localStorage in a useEffect on mount (empty initial state to avoid SSR hydration mismatch), persist on every change. add is idempotent by slug (no duplicates). Export MenuProvider component and a useMenu() hook that throws if used outside the provider.
    Read defensively: JSON.parse in try/catch, validate it is an array of objects with string slug/title/category; corrupt data falls back to an empty list.
    No new dependencies.
  </action>
  <verify>
    <automated>grep -q "use client" src/components/public/menu-provider.tsx && grep -q "hudson-menu" src/components/public/menu-provider.tsx && grep -q "useMenu" src/components/public/menu-provider.tsx && echo OK</automated>
  </verify>
  <done>menu-provider.tsx exports MenuProvider + useMenu; menu persisted to hudson-menu localStorage key; add idempotent; clear/remove/has/count available. Initial state is empty; localStorage hydration occurs in useEffect only (no read during render / no lazy useState initializer) to avoid SSR hydration mismatch.</done>
</task>

<task type="auto">
  <name>Task 2a: Build AddToMenuButton + floating MenuIndicator client components</name>
  <files>src/components/public/add-to-menu-button.tsx, src/components/public/menu-indicator.tsx</files>
  <read_first>
    - src/components/ui/button.tsx (Button + variants)
    - src/components/public/menu-provider.tsx (useMenu API from Task 1)
    - src/styles/globals.css (theme tokens — no raw color names)
  </read_first>
  <action>
    Create src/components/public/add-to-menu-button.tsx ("use client"): props { slug, title, category }. Uses useMenu(); shows "Add to menu" when not in menu and an "In menu / Remove" toggle when present (has(slug)); min-h-11 / >=44px tap target; styled with Button + theme tokens. Toggling calls add/remove.
    Create src/components/public/menu-indicator.tsx ("use client"): uses useMenu(); renders a fixed-position floating element (bottom-right, z-index above content) as a Link to /my-menu labeled "My Menu (N)" where N = count; hidden when count === 0; min-h-11; carries the no-print class. Use theme tokens (e.g. bg-primary text-primary-foreground or bg-card border-border) — no raw color names.
    Both components import useMenu from "@/components/public/menu-provider". They are NOT mounted in this task — wiring happens in Task 2b.
  </action>
  <verify>
    <automated>grep -q "use client" src/components/public/add-to-menu-button.tsx && grep -q "use client" src/components/public/menu-indicator.tsx && grep -q "useMenu" src/components/public/add-to-menu-button.tsx && grep -q "useMenu" src/components/public/menu-indicator.tsx && echo OK</automated>
  </verify>
  <done>add-to-menu-button.tsx and menu-indicator.tsx are client components using useMenu(); AddToMenuButton toggles add/remove with a >=44px target; MenuIndicator is a fixed floating Link to /my-menu showing count, hidden at count 0, carrying no-print.</done>
</task>

<task type="auto">
  <name>Task 2b: Wire MenuProvider + MenuIndicator into the layout and AddToMenuButton into the recipe pages</name>
  <files>src/app/(public)/layout.tsx, src/app/(public)/recipes/page.tsx, src/app/(public)/recipes/[slug]/page.tsx</files>
  <read_first>
    - src/app/(public)/layout.tsx (the exact `<main className="flex-1">{children}</main>` line between </nav> and <footer>)
    - src/app/(public)/recipes/page.tsx (listing li items)
    - src/app/(public)/recipes/[slug]/page.tsx (header area; mark new control no-print)
    - src/components/public/menu-provider.tsx, add-to-menu-button.tsx, menu-indicator.tsx (Task 1 + 2a)
  </read_first>
  <action>
    Edit src/app/(public)/layout.tsx: import MenuProvider from "@/components/public/menu-provider" and MenuIndicator from "@/components/public/menu-indicator". Replace the EXACT existing line `<main className="flex-1">{children}</main>` with:
      `<MenuProvider><main className="flex-1">{children}</main><MenuIndicator /></MenuProvider>`
    (preserve the existing className on `<main>`). MenuIndicator MUST be a child of MenuProvider since it calls useMenu(). PublicLayout stays a server component; do not add "use client".
    Edit src/app/(public)/recipes/page.tsx: render AddToMenuButton next to each recipe Link in the listing li (pass r.slug, r.frontmatter.title, r.frontmatter.category). Keep the listing readable.
    Edit src/app/(public)/recipes/[slug]/page.tsx: render AddToMenuButton in the header near the breadcrumbs/print button; wrap it with the no-print class.
    Colors via tokens only; do not remove shadcn components.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "MenuProvider" "src/app/(public)/layout.tsx" && grep -q "MenuIndicator" "src/app/(public)/layout.tsx" && grep -q "AddToMenuButton" "src/app/(public)/recipes/page.tsx" && grep -q "AddToMenuButton" "src/app/(public)/recipes/[slug]/page.tsx" && echo OK</automated>
  </verify>
  <done>Build exits 0; in layout.tsx MenuIndicator is rendered INSIDE MenuProvider (provider wraps both `<main>` and `<MenuIndicator />`, so useMenu() resolves with no "must be used within MenuProvider" runtime error on any public page); AddToMenuButton appears on listing items and the detail page (detail one marked no-print).</done>
</task>

<task type="auto">
  <name>Task 3: Build /my-menu page (server metadata wrapper + client view) grouped by course with remove/clear/print</name>
  <files>src/app/(public)/my-menu/page.tsx, src/app/(public)/my-menu/menu-client.tsx, src/styles/globals.css</files>
  <read_first>
    - src/components/public/menu-provider.tsx (useMenu API)
    - src/app/(public)/recipes/page.tsx (category grouping pattern with a Map)
    - src/styles/globals.css (existing @media print + .no-print from 31-02)
    - src/components/ui/button.tsx (Button)
  </read_first>
  <action>
    Create the page as a SERVER wrapper + CLIENT view (a `"use client"` page cannot export metadata, so the tab title needs a server parent):
    (1) src/app/(public)/my-menu/page.tsx — a server component (NO "use client"). Export `const metadata: Metadata = { title: "My Menu | Hudson Family", description: "Your saved recipe menu." }` (import type Metadata from "next"). Default-export a component that renders `<MenuView />` imported from "./menu-client". Keep it a server component so the metadata export is honored and the browser tab title is correct.
    (2) src/app/(public)/my-menu/menu-client.tsx — `"use client"` first line, exports `MenuView`. Read items via useMenu() (from "@/components/public/menu-provider"); group them by category into a Map (mirror the grouping in recipes/page.tsx) and render each group as a section with a heading and a list of items. Each item links to /recipes/{slug} and has a Remove button (min-h-11) calling remove(slug). Add a "Clear all" button (calls clear()) and a "Print menu" button calling window.print(); both buttons and the back-to-recipes link carry the no-print class. Show an empty state ("Your menu is empty — add recipes from the recipes page") when count === 0, with a Link to /recipes.
    NOTE: MenuView calls useMenu(), so /my-menu must render inside MenuProvider. It does — /my-menu is under the (public) route group whose layout.tsx wraps children in MenuProvider after Task 2b. No separate provider needed here.
    Wrap the printable region (heading + grouped items) in a class the print CSS keeps; mark all chrome no-print so the print reuses RECIPE-03 behavior. If the existing .print-recipe selector does not already cover the menu region, add the /my-menu printable container class to the existing @media print rule in globals.css (extend the selector list — do NOT duplicate the whole block; reuse .no-print hiding). Keep large readable type and theme tokens only.
  </action>
  <verify>
    <automated>grep -q "metadata" "src/app/(public)/my-menu/page.tsx" && grep -q "use client" "src/app/(public)/my-menu/menu-client.tsx" && grep -q "useMenu" "src/app/(public)/my-menu/menu-client.tsx" && grep -q "window.print" "src/app/(public)/my-menu/menu-client.tsx" && grep -q "no-print" "src/app/(public)/my-menu/menu-client.tsx" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>/my-menu has a server page.tsx exporting metadata (tab title "My Menu | Hudson Family") that renders the client MenuView; MenuView groups picked recipes by category with per-item remove, has clear-all + print (reusing the @media print CSS), shows an empty state, runs inside the layout's MenuProvider, and build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → localStorage | menu list written/read entirely client-side |
| client → route param | /recipes/{slug} links built from stored slugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Tampering | hudson-menu localStorage payload | mitigate | JSON.parse in try/catch + validate array of {slug,title,category} strings in menu-provider; corrupt data resets to empty. Slugs only used in next/link href to /recipes/{slug}; getRecipeBySlug path-traversal guard 404s anything malformed |
| T-31-06 | Information Disclosure | menu contents | accept | Public recipe titles/categories only; no PII, no auth context |
| T-31-SC | Tampering | npm installs | mitigate | No new packages installed in this plan |
</threat_model>

<verification>
- grep -rn "useMenu" src/app/(public)/my-menu returns the menu reads.
- grep -q "hudson-menu" src/components/public/menu-provider.tsx (persistence).
- npm run build exits 0; npm test exits 0.
</verification>

<success_criteria>
- RECIPE-05: "Add to menu" on cards + detail; React Context + localStorage (hudson-menu, no login); floating "My Menu (N)" indicator mounted INSIDE MenuProvider so it works sitewide; /my-menu (server metadata wrapper + client view) grouped by course with remove, clear-all, and print reusing the RECIPE-03 print CSS.
</success_criteria>

<output>
Create `.planning/phases/31-recipes-experience/31-03-SUMMARY.md` when done
</output>
