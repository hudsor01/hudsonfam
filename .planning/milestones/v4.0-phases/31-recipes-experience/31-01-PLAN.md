---
phase: 31-recipes-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/recipes.ts
  - src/__tests__/lib/recipes.test.ts
  - src/app/(public)/recipes/page.tsx
  - src/app/(public)/recipes/[slug]/page.tsx
  - src/components/public/recipe-search.tsx
autonomous: true
requirements: [RECIPE-01, RECIPE-04]
must_haves:
  truths:
    - "On /recipes a visible search affordance is present and Cmd/Ctrl+K opens it"
    - "Typing in the search filters recipes by name and category instantly"
    - "Selecting a search result navigates to /recipes/{slug}"
    - "Each recipe detail page shows breadcrumbs: Recipes > Category > Recipe"
    - "Each recipe detail page shows prev/next links within its chapter in book order, omitted at chapter boundaries"
  artifacts:
    - path: "src/components/public/recipe-search.tsx"
      provides: "cmdk client search dialog over a {slug,title,category} index"
      contains: "use client"
    - path: "src/lib/recipes.ts"
      provides: "getRecipeIndex + getChapterNeighbors + exported anchor() helpers"
      exports: ["getRecipeIndex", "getChapterNeighbors", "anchor"]
  key_links:
    - from: "src/app/(public)/recipes/page.tsx"
      to: "src/components/public/recipe-search.tsx"
      via: "server passes index prop to client search"
      pattern: "RecipeSearch"
    - from: "src/app/(public)/recipes/[slug]/page.tsx"
      to: "getChapterNeighbors"
      via: "server-computed prev/next"
      pattern: "getChapterNeighbors"
    - from: "src/app/(public)/recipes/[slug]/page.tsx"
      to: "anchor"
      via: "breadcrumb category link reuses listing anchor() hash"
      pattern: "anchor"
---

<objective>
Add recipe search (RECIPE-01) and breadcrumbs + prev/next navigation (RECIPE-04).

Purpose: Make 1,000 recipes navigable. Family can jump to any recipe by name from the listing, and move through a chapter in book order from any detail page.
Output: A cmdk search dialog on the listing, three new exported helpers in recipes.ts (getRecipeIndex, getChapterNeighbors, anchor), breadcrumbs and chapter prev/next on the detail page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-recipes-experience/31-CONTEXT.md

@src/lib/recipes.ts
@src/app/(public)/recipes/page.tsx
@src/app/(public)/recipes/[slug]/page.tsx
@src/components/public/mobile-nav.tsx

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From src/lib/recipes.ts (existing):
- `interface RecipeMeta { slug: string; frontmatter: RecipeFrontmatter }`
- `RecipeFrontmatter` has: `title: string`, `category: string`, `order: number`, `ingredients: string[]`, `instructions: string[]`
- `getPublishedRecipes(): Promise<RecipeMeta[]>` — already sorted by `order` then title
- `getAllRecipes(): Promise<RecipeMeta[]>` — published + drafts under dev, same sort
- `getAllCategories(): Promise<string[]>` — categories in book order

From src/app/(public)/recipes/page.tsx (existing, lines ~17-22) — the LOCAL `anchor()` helper to extract:
```typescript
function anchor(category: string): string {
  return category
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-|-$/g, "");
}
```
The listing renders category section headings with `id={anchor(c)}`. Breadcrumb category links MUST use the SAME function so the `/recipes#<hash>` target matches.

From src/components/ui/command.tsx (cmdk, installed):
- Exports: `Command, CommandDialog, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem, CommandShortcut, CommandSeparator`
- `CommandDialog` takes `open` / `onOpenChange` props.

From src/components/ui/breadcrumb.tsx (installed):
- Exports: `Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator, BreadcrumbEllipsis`

From src/__tests__/lib/recipes.test.ts (the ESTABLISHED test file — project convention is tests under src/__tests__/, importing from "@/lib/recipes"):
- Already tests `normalizeFrontmatter` and `filterByVisibility` (pure helpers). New `computeChapterNeighbors` cases are appended to THIS file.

Client component pattern: see mobile-nav.tsx — `"use client"` first line, `useState`, next/link, next/navigation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add search-index, chapter-neighbor, and exported anchor() helpers to recipes.ts</name>
  <files>src/lib/recipes.ts, src/__tests__/lib/recipes.test.ts</files>
  <read_first>
    - src/lib/recipes.ts (the file being modified — note getPublishedRecipes is already order-sorted, and the existing filterByVisibility pure-helper split pattern)
    - src/__tests__/lib/recipes.test.ts (existing test patterns; tests import from "@/lib/recipes" — append new cases here, do NOT create a new test file)
    - src/app/(public)/recipes/page.tsx (lines ~17-22: the local anchor() helper being extracted)
  </read_first>
  <behavior>
    - getRecipeIndex returns an array of `{ slug, title, category }` for every published recipe, preserving book order.
    - getChapterNeighbors(slug): given a slug, returns `{ prev, next }` where each is `{ slug, title } | null`, computed within the SAME category, in book order.
    - First recipe in a chapter has `prev = null`; last has `next = null`; a slug not found returns `{ prev: null, next: null }`.
    - Both helpers must operate on the same sorted, published source the listing uses (getPublishedRecipes).
    - anchor("Main Dishes") === "main-dishes"; leading/trailing separators trimmed; identical output to the helper currently inlined in recipes/page.tsx.
  </behavior>
  <action>
    Add to src/lib/recipes.ts:
    (1) Export a pure helper `anchor(category: string): string` — move the exact slugify logic currently inlined at recipes/page.tsx lines ~17-22 (lowercase, `/[^a-z0-9]+/g`→"-", trim leading/trailing "-") into recipes.ts as an exported function. This becomes the single source so listing section ids and breadcrumb category hashes cannot drift. Then update recipes/page.tsx to import { anchor } from "@/lib/recipes" and DELETE the local copy (the listing's `id={anchor(c)}` usage stays, now resolving to the imported helper).
    (2) `getRecipeIndex(): Promise<{ slug: string; title: string; category: string }[]>` — call getPublishedRecipes, map each RecipeMeta to the lightweight shape.
    (3) `getChapterNeighbors(slug: string): Promise<{ prev: { slug: string; title: string } | null; next: { slug: string; title: string } | null }>` — get published recipes, filter to the target recipe's category, find the index of `slug` within that filtered (already order-sorted) chapter list, return the element before/after or null at boundaries.
    Also add an exported pure helper `computeChapterNeighbors(recipes: RecipeMeta[], slug: string)` that takes an already-sorted RecipeMeta[] and does the category-filter + neighbor math, so it is unit-testable without IO (mirror the filterByVisibility pure-helper split already in this file). getChapterNeighbors composes getPublishedRecipes + computeChapterNeighbors.
    Define a small exported type `RecipeIndexEntry = { slug: string; title: string; category: string }` for reuse by the search component.
    No new dependencies. Do not change existing exports.
    Append test cases to src/__tests__/lib/recipes.test.ts (the existing file): a describe block for computeChapterNeighbors covering first-in-chapter (prev null), last-in-chapter (next null), a middle item (both set), and an unknown slug ({prev:null,next:null}); optionally an anchor() case. Build the fixture RecipeMeta[] inline like the existing tests; import the new helpers from "@/lib/recipes".
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/recipes.test.ts</automated>
  </verify>
  <done>src/__tests__/lib/recipes.test.ts (the established file — same path as verify) has cases for computeChapterNeighbors covering first/last/middle of a chapter and unknown slug, all green; getRecipeIndex, getChapterNeighbors, and anchor exported; recipes/page.tsx imports anchor from "@/lib/recipes" with no local copy remaining.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build cmdk RecipeSearch client component and mount it on the listing</name>
  <files>src/components/public/recipe-search.tsx, src/app/(public)/recipes/page.tsx</files>
  <read_first>
    - src/components/ui/command.tsx (cmdk exports + CommandDialog open/onOpenChange API)
    - src/components/public/mobile-nav.tsx (client component + state pattern)
    - src/app/(public)/recipes/page.tsx (the listing being modified — server component)
    - src/styles/globals.css (color tokens; use bg-card, text-foreground, text-muted-foreground, border-border, ring — no raw Tailwind colors)
  </read_first>
  <behavior>
    - A visible search button (>=44px tap target, labeled "Search recipes" with a Cmd/Ctrl+K hint) renders near the listing header.
    - Pressing Cmd+K (mac) or Ctrl+K opens the CommandDialog; pressing it again or Escape closes it.
    - Typing filters the passed index by title AND category, case-insensitive, instant (cmdk built-in filtering or a controlled filter).
    - Selecting an item calls router.push(`/recipes/${slug}`) and closes the dialog.
    - Empty query shows a reasonable list; no matches shows CommandEmpty text.
  </behavior>
  <action>
    Create src/components/public/recipe-search.tsx with `"use client"` as the first line. Props: `{ index: RecipeIndexEntry[] }` (import the type from "@/lib/recipes"). Use useState for open, a useEffect keydown listener for Cmd/Ctrl+K (e.metaKey||e.ctrlKey && key==="k", preventDefault, toggle open). Render a visible trigger that is a real `<button>` element (focusable, contains the visible text label "Search recipes" so screen readers announce it — not an icon-only control), styled with theme tokens, min-h-11 for >=44px, including a CommandShortcut-style "⌘K" hint, plus CommandDialog/CommandInput/CommandList/CommandEmpty/CommandGroup/CommandItem from "@/components/ui/command". Each CommandItem value should include title + category so cmdk filtering matches both; onSelect uses useRouter().push to navigate then setOpen(false). Use next/navigation useRouter.
    Edit src/app/(public)/recipes/page.tsx (stays a server component): call `getRecipeIndex()` alongside the existing data fetches, and render `<RecipeSearch index={index} />` inside the header block (after the intro paragraph). Import RecipeSearch from "@/components/public/recipe-search".
    Colors via tokens only — no text-green-*/bg-blue-* etc. Do not remove any shadcn component.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "use client" src/components/public/recipe-search.tsx && grep -q "RecipeSearch" src/app/\(public\)/recipes/page.tsx && echo OK</automated>
  </verify>
  <done>Build exits 0; recipe-search.tsx is a client component using cmdk with a Cmd/Ctrl+K listener and router navigation; listing renders RecipeSearch with the server-built index. The search trigger is a `<button>` (focusable, with a visible text label, not icon-only); CommandDialog keyboard navigation is provided by Radix/cmdk — satisfying CONTEXT's keyboard-accessible + screen-reader-friendly requirement.</done>
</task>

<task type="auto">
  <name>Task 3: Add breadcrumbs + chapter prev/next to the recipe detail page</name>
  <files>src/app/(public)/recipes/[slug]/page.tsx</files>
  <read_first>
    - src/app/(public)/recipes/[slug]/page.tsx (the file being modified)
    - src/components/ui/breadcrumb.tsx (Breadcrumb component exports)
    - src/lib/recipes.ts (getChapterNeighbors + anchor added in Task 1)
  </read_first>
  <action>
    Edit the detail page (server component). After fetching the recipe, also `await getChapterNeighbors(slug)`. Replace or supplement the existing "Back link" block with a Breadcrumb (from "@/components/ui/breadcrumb"): BreadcrumbLink "Recipes" → /recipes, BreadcrumbLink {category} → `/recipes#${anchor(category)}` importing { anchor } from "@/lib/recipes" (the SAME exported helper the listing uses for its section ids — do NOT replicate the slugify logic here), then BreadcrumbPage {title}. Keep "Recipes" linking to /recipes.
    Add a prev/next nav near the footer: when `neighbors.prev` exists render a Link to `/recipes/${prev.slug}` labeled "← {prev.title}"; when `neighbors.next` exists render a Link to `/recipes/${next.slug}` labeled "{next.title} →". At chapter boundaries the missing side simply renders nothing (no disabled placeholder required, but keep layout balanced). Tap targets >=44px (min-h-11 / py-3). Colors via tokens only.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "getChapterNeighbors" src/app/\(public\)/recipes/\[slug\]/page.tsx && grep -q "Breadcrumb" src/app/\(public\)/recipes/\[slug\]/page.tsx && grep -q "anchor" src/app/\(public\)/recipes/\[slug\]/page.tsx && echo OK</automated>
  </verify>
  <done>Build exits 0; detail page renders Recipes › Category › Recipe breadcrumb (category link uses the imported anchor() so its hash matches the listing's section id) and chapter prev/next links computed via getChapterNeighbors, gracefully omitted at boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → route param | `[slug]` is user-controllable in the URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | `getRecipeBySlug` slug param | accept | Existing path-traversal guard in recipes.ts (rejects `/`, `\`, `..`, `_` prefix) already covers neighbor/breadcrumb slugs; no new IO sink added |
| T-31-02 | Information Disclosure | search index payload | accept | Index is `{slug,title,category}` of published recipes only (getPublishedRecipes); no drafts, no PII |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (cmdk/breadcrumb already present) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/lib/recipes.test.ts` green (neighbor helper cases).
- `npm run build` exits 0.
- `npm test` exits 0 (no regressions).
</verification>

<success_criteria>
- RECIPE-01: visible search affordance + Cmd/Ctrl+K open the cmdk dialog; typing filters by name/category; selecting navigates to /recipes/{slug}; trigger is a focusable labeled button (keyboard + screen-reader accessible).
- RECIPE-04: detail page shows `Recipes › Category › Recipe` breadcrumbs (category hash matches listing via shared anchor()) and chapter prev/next in book order with graceful boundaries.
</success_criteria>

<output>
Create `.planning/phases/31-recipes-experience/31-01-SUMMARY.md` when done
</output>
