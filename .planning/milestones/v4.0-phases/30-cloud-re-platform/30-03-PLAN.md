---
phase: 30-cloud-re-platform
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - next.config.ts
  - Dockerfile
  - .dockerignore
  - .github/workflows/build-and-push.yml
  - .github/workflows/ghcr-retention.yml
  - CLAUDE.md
autonomous: false
requirements: [CLOUD-05, CLOUD-06, CLOUD-07, CLOUD-08]
user_setup:
  - service: vercel
    why: "Production host (replaces Docker/GHCR/Flux/K3s)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon pooled endpoint (already in .env.local)"
      - name: DIRECT_DATABASE_URL
        source: "Neon unpooled endpoint (already in .env.local)"
      - name: BETTER_AUTH_SECRET
        source: ".env.local"
      - name: BETTER_AUTH_URL
        source: "https://thehudsonfam.com"
      - name: GOOGLE_CLIENT_ID
        source: ".env.local / Google Cloud console"
      - name: GOOGLE_CLIENT_SECRET
        source: ".env.local / Google Cloud console"
      - name: RESEND_API_KEY
        source: ".env.local"
      - name: OWNER_EMAIL
        source: ".env.local"
      - name: R2_ACCOUNT_ID
        source: ".env.local"
      - name: R2_ACCESS_KEY_ID
        source: ".env.local"
      - name: R2_SECRET_ACCESS_KEY
        source: ".env.local"
      - name: R2_BUCKET
        source: ".env.local"
      - name: R2_ENDPOINT
        source: ".env.local"
    dashboard_config:
      - task: "Link the repo to a Vercel project and enter all env vars (Production scope)"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables"
      - task: "Add thehudsonfam.com as a custom domain on the Vercel project"
        location: "Vercel Dashboard -> Project -> Settings -> Domains"
  - service: cloudflare-dns
    why: "Point thehudsonfam.com at Vercel and retire the Cloudflare Tunnel"
    dashboard_config:
      - task: "Replace the Tunnel CNAME/record with Vercel's required DNS (per Vercel domain instructions); set proxy/DNS-only as Vercel directs; delete the Tunnel route"
        location: "Cloudflare Dashboard -> DNS + Zero Trust -> Tunnels"
must_haves:
  truths:
    - "The repo has no Docker/GHCR/Flux/K8s deploy artifacts"
    - "next.config.ts has no output: standalone"
    - "thehudsonfam.com resolves to the Vercel deployment over valid HTTPS"
    - "On the live URL: Google + email/password sign-in work, 1000 recipes render, restored events/album show, a new photo upload lands in R2"
  artifacts:
    - path: "next.config.ts"
      provides: "Vercel-native config (no standalone)"
      excludes: "output: \"standalone\""
  key_links:
    - from: "GitHub push to main"
      to: "Vercel deployment"
      via: "Vercel git integration (replaces GHCR/Flux/K3s)"
      pattern: "vercel"
    - from: "thehudsonfam.com"
      to: "Vercel"
      via: "Cloudflare DNS (Tunnel retired)"
      pattern: "thehudsonfam.com"
---

<objective>
Delete all homelab deploy artifacts, reconcile Next config for Vercel, deploy to Vercel, cut Cloudflare DNS from the Tunnel to Vercel, and verify the live cloud stack boots clean.

Purpose: The Docker→GHCR→Flux→K3s pipeline is dead with the homelab offline. Vercel git-push deploy replaces it; standalone output is K3s-only and must go. Final verification proves the whole milestone goal — the site is back online on managed cloud.

Output: a clean repo (no Dockerfile/workflows/standalone), a live Vercel deployment, thehudsonfam.com on Vercel over HTTPS, and a verified boot (auth + recipes + content + R2 upload).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cloud-re-platform/30-CONTEXT.md

@next.config.ts
@CLAUDE.md

<interfaces>
<!-- Deploy artifacts present in the repo (verified): -->
  Dockerfile
  .dockerignore
  .github/workflows/build-and-push.yml
  .github/workflows/ghcr-retention.yml

next.config.ts line ~5: `output: "standalone",` — remove this line (Vercel needs no standalone server.js).
The CLAUDE.md "Deployment" section documents the entire GHCR→Flux→K3s pipeline — it must be rewritten for Vercel.
Flux/K8s references in code were confirmed to exist only as comments in next.config.ts and src/lib/auth.ts; auth.ts was cleaned in 30-01.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete deploy artifacts and remove standalone output</name>
  <read_first>next.config.ts, Dockerfile, .github/workflows/build-and-push.yml, CLAUDE.md</read_first>
  <files>next.config.ts, Dockerfile, .dockerignore, .github/workflows/build-and-push.yml, .github/workflows/ghcr-retention.yml, CLAUDE.md</files>
  <action>
  Per CONTEXT CLOUD-05 (delete outright; remove standalone).
  1. Delete Dockerfile, .dockerignore, .github/workflows/build-and-push.yml, .github/workflows/ghcr-retention.yml. If .github/workflows/ is then empty, leave the directory (harmless) or remove it.
  2. In next.config.ts remove the `output: "standalone",` line and its preceding "Standalone output for Docker" comment. Leave all other config (transpilePackages, images, poweredByHeader, reactStrictMode, cacheComponents, experimental) untouched.
  3. Rewrite the CLAUDE.md "Deployment" section: remove the GHCR/Flux/K3s/ImagePolicy/PVC/Tunnel/failure-mode content; replace with the Vercel model (git-push to main auto-deploys; env vars in Vercel project settings; Neon DB, R2 images, Cloudflare DNS → Vercel). Remove the pre-push hook / test:schema mentions only if already gone (they were removed in phase 29 per REQUIREMENTS JOB-06 — verify before editing). Remove the "Top failure modes" homelab subsection.
  4. Confirm `npm run build` still exits 0 without standalone output.
  </action>
  <verify>
    <automated>test ! -e Dockerfile && test ! -e .github/workflows/build-and-push.yml && test ! -e .github/workflows/ghcr-retention.yml && test $(grep -c "output: \"standalone\"" next.config.ts) -eq 0 && npm run build</automated>
  </verify>
  <done>Dockerfile, .dockerignore, both workflows deleted; next.config.ts has no standalone output; CLAUDE.md Deployment section describes Vercel; `npm run build` exits 0.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The repo is Vercel-ready (no homelab artifacts, no standalone). Deploying requires owner credentials.</what-built>
  <how-to-verify>
    Owner actions (Claude cannot do these — they need the Vercel/Cloudflare/Google dashboards):
    1. Vercel: link the hudsor01/hudsonfam repo to a Vercel project (Framework: Next.js; build is default `next build`).
    2. Vercel: enter all env vars (Production scope) listed in this plan's user_setup — DATABASE_URL, DIRECT_DATABASE_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL=https://thehudsonfam.com, GOOGLE_CLIENT_ID/SECRET, RESEND_API_KEY, OWNER_EMAIL, and all five R2_* vars. Values come from .env.local.
    3. Google Cloud console: add the Vercel production URL + https://thehudsonfam.com to the OAuth Authorized redirect URIs / origins for the existing client (so Google sign-in works on the new host).
    4. Push to main (or trigger a Vercel deploy) and confirm the build succeeds in the Vercel dashboard.
  </how-to-verify>
  <resume-signal>Type "deployed" with the Vercel deployment URL once the build is green, or describe build failures.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Vercel deployment is live on its *.vercel.app URL. Cutting the custom domain requires Cloudflare access.</what-built>
  <how-to-verify>
    Owner actions:
    1. Vercel: add thehudsonfam.com as a custom domain on the project (Vercel will show the required DNS record).
    2. Cloudflare DNS: replace the existing Cloudflare Tunnel record for thehudsonfam.com with the record Vercel requires (CNAME/A per Vercel's instructions); set proxy status as Vercel directs. Delete/disable the Tunnel route for thehudsonfam.com.
    3. Wait for DNS propagation; confirm https://thehudsonfam.com loads the Vercel deployment over valid HTTPS (no Tunnel, no cert warning).
  </how-to-verify>
  <resume-signal>Type "dns-cut" once https://thehudsonfam.com serves the Vercel deployment with valid HTTPS.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full cloud stack live: Vercel host, Neon DB, R2 images, Cloudflare DNS → Vercel, Redis removed.</what-built>
  <how-to-verify>
    On the live https://thehudsonfam.com (CLOUD-08 boot verification):
    1. Sign in with Google OAuth — succeeds and lands on /dashboard.
    2. Sign out, sign in with email/password — succeeds (Postgres sessions, no Redis).
    3. Browse recipes — all 1,000 render.
    4. Confirm restored content shows: the 5 events (Easter, Dallas, Game, Memorial, Summer) and the 1 album.
    5. Upload a new photo from the dashboard — it processes and renders, and the objects appear in the R2 bucket.
    6. Spot-check the browser console/network for no Redis/NAS/standalone errors and no 5xx.
  </how-to-verify>
  <resume-signal>Type "verified" once all six checks pass on the live URL, or list failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → Vercel edge | public HTTPS ingress (replaces Cloudflare Tunnel) |
| Vercel → Neon / R2 / Resend | managed-provider creds stored as Vercel env vars |
| OAuth callback → app | Google redirect URIs must match the new host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08 | Information Disclosure | env vars on Vercel | mitigate | Enter secrets only in Vercel env (Production scope), never in the repo; .env.local stays gitignored |
| T-30-09 | Spoofing | OAuth redirect on new host | mitigate | Restrict Google Authorized redirect URIs to the Vercel URL + thehudsonfam.com exactly; BETTER_AUTH_URL pinned to the canonical host |
| T-30-10 | Denial of Service | dangling Cloudflare Tunnel | mitigate | Delete/disable the Tunnel route during DNS cut so no stale homelab path remains reachable |
| T-30-11 | Tampering | deploy pipeline change (GHCR→Vercel) | accept | Vercel git integration deploys from main; access controlled by GitHub + Vercel project membership |
</threat_model>

<verification>
- `test ! -e Dockerfile && test ! -e .github/workflows/build-and-push.yml && test ! -e .github/workflows/ghcr-retention.yml` passes.
- `grep -c 'output: "standalone"' next.config.ts` is 0.
- `npm run build` exits 0.
- https://thehudsonfam.com resolves to Vercel over valid HTTPS (owner-verified).
- Live boot: Google + email/password sign-in, 1000 recipes, restored events/album, new R2 upload (owner-verified).
</verification>

<success_criteria>
- CLOUD-05: Docker/GHCR/Flux/K8s artifacts removed; standalone output gone.
- CLOUD-06: Vercel deploy is live with all env vars; Next config reconciled with Vercel.
- CLOUD-07: thehudsonfam.com resolves to Vercel over HTTPS; Cloudflare Tunnel retired.
- CLOUD-08: app boots clean — auth, DB reads/writes, 1000 recipes, R2 photos verified on the live URL.
</success_criteria>

<output>
Create `.planning/phases/30-cloud-re-platform/30-03-SUMMARY.md` when done.
</output>
