# Requirements: v4.0 — Cloud Re-platform & Recipes Experience (ARCHIVED)

> **Archived 2026-06-02** at milestone close. All 21 requirements satisfied and verified
> (see `.planning/milestones/v4.0-MILESTONE-AUDIT.md` — verdict PASSED). Snapshot of the
> requirements as shipped.

**Defined:** 2026-06-01
**Reference:** CLAUDE.md, `.planning/ui-enhancement-analysis.md`, prior recipes digitization (superpowers track)
**Core Value:** Get thehudsonfam.com back online on managed cloud (homelab is down indefinitely after a flood), strip the now-irrelevant job-search subsystem entirely, and make the 1,000-recipe collection genuinely usable for the family.

## Context

The K3s homelab is offline indefinitely (flood; equipment in storage). All data is **safe but disconnected** — recoverable later, not lost. The site runs entirely on managed providers with no self-hosted dependency.

**Locked stack (all free tier):**

| Concern | Provider | Notes |
|---------|----------|-------|
| Host | Vercel | git-push deploy, replaces Docker→GHCR→Flux→K3s |
| Postgres | Neon | new project created for hudsonfam |
| Images | Cloudflare R2 | 10 GB free, replaces NAS originals + PVC thumbnails |
| Sessions | (dropped) | better-auth on Postgres — Redis removed |
| DNS | Cloudflare → Vercel | replaces Cloudflare Tunnel |
| Auth | better-auth | Google OAuth + email/password, pointed at Neon |

## v4.0 Requirements (all complete — 21/21)

### Phase 29 — Decommission Job Pipeline (JOB-XX)

- [x] **JOB-01**: All job-search admin UI removed — `src/app/(admin)/admin/jobs/` deleted in full; the "Jobs" entry is gone from admin nav; no `/admin/jobs*` route resolves
- [x] **JOB-02**: Job API routes removed — `src/app/api/jobs/` deleted (including the PDF cover-letter / tailored-resume routes)
- [x] **JOB-03**: Job lib modules removed — `jobs-db.ts`, `job-actions.ts`, `job-constants.ts`, `job-freshness.ts`, `jobs-schemas.ts`, `webhooks.ts`, `regenerate-predicates.ts`, `attach-freshness.ts` deleted (each verified to have no non-job consumer before deletion)
- [x] **JOB-04**: Job env vars purged — `JOBS_DATABASE_URL` and `N8N_WEBHOOK_SECRET` removed from `.env.example`, CLAUDE.md, and all docs
- [x] **JOB-05**: All job-related tests removed; the remaining Vitest suite runs green (0 failures, no orphaned mocks/imports)
- [x] **JOB-06**: Schema-drift tooling removed — `scripts/check-jobs-schema.ts`, the `test:schema` npm script, the pre-push hook, and `scripts/install-hooks.sh` (plus their CLAUDE.md mentions) deleted
- [x] **JOB-07**: Job-only dependencies pruned from `package.json` (`@hello-pangea/dnd` kanban) where no non-job code imports them; `next build` stays green

### Phase 30 — Cloud Re-platform (CLOUD-XX)

- [x] **CLOUD-01**: Prisma runs against Neon — `DATABASE_URL` (pooled) + `DIRECT_DATABASE_URL` (direct) point at the new Neon project; `prisma migrate deploy` applies the full schema cleanly; `@prisma/adapter-pg` connects without pool errors
- [x] **CLOUD-02**: Redis fully removed — `ioredis` dependency gone, `REDIS_URL` env gone, `src/lib/auth.ts` uses better-auth's Postgres-backed session store with no Redis code path or fallback branch remaining
- [x] **CLOUD-03**: Images served from Cloudflare R2 — uploads (`src/lib/images.ts`) write originals + derivatives to R2; the public read path serves from R2 via `/api/images/[...path]`; all NAS/PVC filesystem paths removed; gallery degrades gracefully on NoSuchKey (307 → SVG placeholder)
- [x] **CLOUD-04**: Homelab-monitoring admin removed entirely — Glance-replacement dashboard, `/api/dashboard`, `src/lib/dashboard/*`, widgets/tests, and the `/admin` nav link deleted; `/admin` 404s; SONARR/RADARR/JELLYFIN keys unreferenced. (Preserved lookalikes: `dashboard-actions.ts` content CRUD + `weather-widget.tsx`.) Re-add — FUTURE-02.
- [x] **CLOUD-05**: Deploy de-homelab'd — `Dockerfile`, GH Actions build/retention workflows, and Flux/K8s references removed/archived; `bun.lock` matches `package.json`
- [x] **CLOUD-09**: Dependency versions pinned to satisfy the Aikido Safe Chain minimum-package-age gate; `bun install` resolves cleanly with no skip flag; auto-bump source (interactive caret resolution) identified and stopped via exact pins + Renovate security-only
- [x] **CLOUD-06**: Vercel deploy live — project builds and deploys on Vercel with all env vars set; Next.js output reconciled (no standalone-only assumptions)
- [x] **CLOUD-07**: DNS cut to Vercel — `thehudsonfam.com` resolves to Vercel over valid HTTPS via Cloudflare DNS; Tunnel path retired; CAA allows letsencrypt.org
- [x] **CLOUD-08**: App boots clean on cloud — Google OAuth + email/password sign-in works, DB reads/writes work, all 1,000 recipes render, photos load from R2, verified on the live URL

### Phase 31 — Recipes Experience (RECIPE-XX)

- [x] **RECIPE-01**: Recipe search — instant client-side filter/jump across all recipes by name (cmdk), reachable from the listing and keyboard-accessible (Cmd/Ctrl+K)
- [x] **RECIPE-02**: Ingredient + step checkboxes — tap to cross off while cooking, ≥44px tap targets; checked state persists per recipe in localStorage
- [x] **RECIPE-03**: Print / kitchen view — clean one-page printout per recipe via print CSS
- [x] **RECIPE-04**: Breadcrumbs + prev/next — `Recipes › <Chapter> › <Recipe>` breadcrumbs and prev/next within chapter, in book order
- [x] **RECIPE-05**: Build-your-own-menu — "Add to menu" on cards + detail; floating "My Menu (N)" indicator; `/my-menu` grouping by course with remove/clear/print (full cookable recipes on print); localStorage, no login

## Future Requirements (carried forward)

- **FUTURE-01**: Restore homelab Postgres → migrate data into Neon once equipment is back online
- **FUTURE-02**: Re-enable live homelab monitoring once the cluster returns (un-park the CLOUD-04 dashboard)
- **FUTURE-03**: Remaining recipe back-matter — Menu Making prose, the ~100-menu Menus section (pairs with RECIPE-05), and the Table Service section
- **FUTURE-04**: Recipe full-text search across ingredients/steps (RECIPE-01 covers name-search only)

## Out of Scope (as shipped)

- Re-platforming or restoring the homelab itself (awaiting physical recovery)
- Migrating the n8n job-search pipeline anywhere (deleted, not moved)
- New family-content features beyond the recipes experience
- Paid provider tiers (entire stack stays on free tiers)
