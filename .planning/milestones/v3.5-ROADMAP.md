# Milestone v3.5: CI/CD Hardening

**Status:** ✅ SHIPPED 2026-04-25
**Phases:** 25-28
**Total Plans:** 5 (25-01, 26-01, 26-02, 27-01, 28-01)
**Tag:** `v3.5-complete` (commit `f02440c`)

## Overview

Eliminated the recurring "every time I use it I have to fix it" CI/CD DX pattern. Migrated hudsonfam deploy from broken self-hosted Forgejo+Woodpecker (6 moving parts, 5 self-hosted) to the CLAUDE.md-intended GitHub Actions + GHCR pipeline (2 moving parts, both vendor-managed). Unblocked v3.0 production by closing the deploy-blocked gap and executed all deferred v3.0 prod UAT in Phase 28.

## Phases

### Phase 25: Pipeline Build (v3.5-P1)

**Goal:** Ship `.github/workflows/build-and-push.yml` — single-job push-to-main → GHCR pipeline replacing the broken Woodpecker setup.
**Depends on:** Nothing (greenfield workflow file)
**Plans:** 1
**REQs:** CICD-01, CICD-02, CICD-03

Plans:
- [x] 25-01: GitHub Actions workflow + GHCR push + warm-cache layer caching

**Details:**
Single-job workflow (`docker/build-push-action@v5` with `type=gha,mode=max` cache); triggers on `push.branches: [main]` + `workflow_dispatch`; emits two tags (`YYYYMMDDHHmmss` UTC + `:latest`); built-in `GITHUB_TOKEN` for same-repo GHCR push (no PAT). Hotfix detour: `bun.lock` regen unblocked first build (commit `c099b66`).

### Phase 26: Flux Reconfiguration (v3.5-P2)

**Goal:** Reconfigure Flux ImageRepository + ImagePolicy + Deployment to consume from GHCR (replacing the dead Forgejo registry path).
**Depends on:** Phase 25 (GHCR has tags)
**Plans:** 2
**REQs:** CICD-04, CICD-05, CICD-06

Plans:
- [x] 26-01: GHCR pull Secret via two ExternalSecrets sharing one vault key (homepage + flux-system ns)
- [x] 26-02: Flux ImageRepository + Deployment cutover from `git.homelab/forgejo-admin/hudsonfam` to `ghcr.io/hudsor01/hudsonfam`

**Details:**
First templated ExternalSecret in homelab repo (PATTERNS.md Gap 1 closed). Pod live on `ghcr.io/hudsor01/hudsonfam:20260424023904` with `ghcr-pull-credentials` Secret. 4 Rule 3 deviations auto-fixed: ESO `spec.target.type` rejected by installed CRD, Flux `status.latestRef` rename from docs-stated `status.latestImage`, sed greedy regex bug, concurrent IUA promotion forced rebase. Forward-facing intel: `crd-vs-docs-mismatch-pattern.md`.

### Phase 27: Decommission Old Pipeline (v3.5-P3)

**Goal:** Permanently retire the Forgejo+Woodpecker rollback safety net for hudsonfam — clean cluster state + remove `.woodpecker.yaml` + dereg from Woodpecker + purge Forgejo container registry.
**Depends on:** Phase 26 (≥10 min observably-green; D-10 rollback safety window)
**Plans:** 1
**REQs:** CICD-07, CICD-08, CICD-09

Plans:
- [x] 27-01: 6 destructive ops across 4 systems (repo + K8s + Woodpecker + Forgejo registry)

**Details:**
`.woodpecker.yaml` deleted (commit `0eaacc6`). Woodpecker REST DELETE on `/api/repos/2` HTTP 200. Both `forgejo-registry-creds` Secrets purged. 6 Forgejo container versions HTTP 204 (4 timestamp + 2 sha256 manifest digests). Pod stayed `ready=true restarts=0` throughout. T-27-02 PAT-handling discipline observed VERBATIM — no PAT material in transcript or commits.

### Phase 28: End-to-End Smoke + Retroactive UAT (v3.5-P4)

**Goal:** No-op smoke proves the full pipeline travels end-to-end + retroactive UAT closes the v3.0 prod-UAT debt accumulated since 2026-04-22 + CLAUDE.md §Deployment rewrite to live-pipeline truth.
**Depends on:** Phases 25/26/27
**Plans:** 1
**REQs:** CICD-10, CICD-11, CICD-12, CICD-13

Plans:
- [x] 28-01: 4-task close-out (smoke + CLAUDE.md rewrite + Plan 21-08 retro UAT + Phase 22/23/24 retro smoke)

**Details:**
Empty smoke commit `e1ec19a` traveled GitHub→GHCR→Flux→K3s in **11m13s** (vs 15-min budget). CLAUDE.md §Deployment rewrite (commit `dda3af3`; 6/6 D-04 PASS). Plan 21-08 5/5 PASS with 2 trivial inline fixes per D-09 (Radix a11y `12ce076` + metadata duplicate-suffix `91a1705`). Phase 22/23/24 8-check retroactive smoke = 2/8 PASS + 4/8 OBSERVATIONAL-PENDING-N8N + 2/8 N/A — 100% hudsonfam-side green; n8n-side gaps inherited from v3.0 ship state, captured to SEED-006. BUG-1 Cloudflare Rocket Loader incident remediated mid-prep, captured to SEED-007.

---

## Milestone Summary

**Decimal Phases:** None.

**Key Decisions:**
- D-09 (Phase 26): Two-commit safety cadence (secrets-first then ImageRepository cutover) — proven worthwhile under stress when concurrent IUA promotion forced rebase
- D-04 (Phase 26 / Phase 28 CICD-11): Two-ExternalSecret pattern sharing one vault key — explicit alternative to installing a Reflector/Replicator operator
- D-08 (Phase 27): Full decommission, no coexistence — owner-explicit (2026-04-22)
- D-09 (Phase 28): Trivial-fix-in-Phase-28 vs v3.5.1 deferral — applied 2× for surface-level bugs surfaced by retroactive UAT
- D-09 (Phase 28): n8n-side gap dispositioned as inherited v3.0 ship state — does NOT block close

**Issues Resolved:**
- 6-moving-part Forgejo+Woodpecker pipeline → 2-moving-part GitHub Actions + GHCR (per CLAUDE.md original intent)
- v3.0 prod-UAT debt accumulated since 2026-04-22 — cleared via Phase 28
- v3.0 milestone deploy-block — cleared with v3.5 close
- 2 CRD-vs-docs field shape gotchas (ESO + Flux) — captured to intel doc

**Issues Deferred:**
- SEED-006 (n8n-side hardening followup; HMAC verify on 3 existing + 2 new workflows; ~2-4h)
- SEED-007 (Cloudflare Rocket Loader synthetic check; ~30-60m)
- v1beta2 ImageUpdateAutomation CRD deprecation warning — upgrade to v1 deferred

**Technical Debt Incurred:**
- None significant. The 2 inline fixes (`12ce076`, `91a1705`) were trivial code-level patches per D-09 scope. The "concurrent-IUA-rebase pattern" surfaced in Phase 26 + SEED-007 push was permanently fixed via the push-branch separation pattern (homelab commit `47ebaa3`+ — see `.planning/intel/flux-iua-push-branch-separation.md`).

**Forward-Facing Intel:**
- `.planning/intel/crd-vs-docs-mismatch-pattern.md` — when upstream docs and live cluster CRD/REST API disagree, the cluster wins
- `.planning/intel/flux-iua-push-branch-separation.md` — the canonical Flux fix for the human-vs-IUA push contention pattern; documents the 6-iteration implementation history + 5 transferable lessons

---

_For current project status, see `.planning/ROADMAP.md`. Detailed milestone summary at `.planning/milestones/v3.5-cicd-hardening/v3.5-MILESTONE-SUMMARY.md`. Full audit at `.planning/milestones/v3.5-MILESTONE-AUDIT.md`._
