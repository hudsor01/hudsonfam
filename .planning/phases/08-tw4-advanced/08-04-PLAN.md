---
phase: 08-tw4-advanced
type: execute
---

<objective>
Add accessibility variants and logical properties for RTL readiness.

Purpose: Support high-contrast user preferences with contrast-more:/contrast-less: variants and prepare layout for RTL languages by replacing physical margin/padding with logical equivalents.
Output: UI adapts to contrast preferences, all directional spacing uses logical properties.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css — Global styles
@src/components/ui/button.tsx — Button component
@src/components/ui/card.tsx — Card component
@src/components/ui/input.tsx — Input component
@src/components/ui/ — All UI components
@src/app/ — All page components

**Tailwind v4 utilities to apply:**
- `contrast-more:` — Variant for users with `prefers-contrast: more` (increased borders, bolder text)
- `contrast-less:` — Variant for users with `prefers-contrast: less` (reduced visual noise)
- `ms-*` / `me-*` — Logical margin-inline-start / margin-inline-end (replaces ml-*/mr-*)
- `ps-*` / `pe-*` — Logical padding-inline-start / padding-inline-end (replaces pl-*/pr-*)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contrast-more: and contrast-less: variants to key UI elements</name>
  <files>src/app/globals.css, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx</files>
  <action>Add contrast-more: variant styles to Button (increased border width, bolder font weight), Card (stronger border, increased shadow), and Input (thicker border, higher contrast placeholder). Add contrast-less: variants where appropriate (reduced borders, softer shadows). Consider adding global contrast-more: styles in globals.css for base elements. Key patterns: contrast-more:border-2, contrast-more:font-semibold, contrast-more:border-foreground, contrast-less:shadow-none, contrast-less:border-muted.</action>
  <verify>bun run build passes without errors</verify>
  <done>UI adapts to user contrast preferences with contrast-more: and contrast-less: variants</done>
</task>

<task type="auto">
  <name>Task 2: Replace physical ml-*/mr-*/pl-*/pr-* with logical ms-*/me-*/ps-*/pe-* across all components</name>
  <files>all src/components/ui/ components and src/app/ pages</files>
  <action>Search for all instances of ml-*, mr-*, pl-*, pr-* classes across UI components and page files. Replace with logical property equivalents: ml-* becomes ms-*, mr-* becomes me-*, pl-* becomes ps-*, pr-* becomes pe-*. Do NOT replace mx-*, my-*, px-*, py-* as these are already direction-agnostic. Be careful with negative values (-ml-* becomes -ms-*). This is a large-scale find-and-replace operation — work methodically through components first, then pages.</action>
  <verify>bun run build passes without errors</verify>
  <done>All directional margin/padding uses logical properties, layout is RTL-ready</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] Button, Card, Input have contrast-more: variants
- [ ] contrast-less: variants applied where appropriate
- [ ] No remaining ml-*/mr-*/pl-*/pr-* in UI components
- [ ] Logical properties (ms-*/me-*/ps-*/pe-*) used throughout
</verification>

<success_criteria>
- High-contrast mode increases border visibility and text weight
- Low-contrast mode reduces visual noise
- All directional spacing uses logical properties
- Layout renders correctly in both LTR and RTL contexts
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-tw4-advanced/08-04-SUMMARY.md`
</output>
