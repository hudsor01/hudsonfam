---
phase: 08-tw4-advanced
type: execute
---

<objective>
Add scroll snap for photo galleries and 3D hover effects.

Purpose: Make photo galleries swipeable on mobile with native scroll snap, and add subtle 3D depth effects to interactive elements for a polished feel.
Output: Photo galleries snap to items on scroll, cards and thumbnails have perspective-based hover effects.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(dashboard)/dashboard/photos/albums/[id]/page.tsx — Album photo grid
@src/components/ui/card.tsx — Card component

**Tailwind v4 utilities to apply:**
- `snap-x` — Enables horizontal scroll snapping on container
- `snap-mandatory` — Requires snapping to snap points
- `snap-center` — Snaps child to center of container
- `perspective-*` — CSS perspective for 3D transforms
- `rotate-y-*` — Y-axis rotation for card tilt effects
- `transform-3d` — Enables 3D transform context
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scroll snap to mobile photo gallery containers</name>
  <files>album-photo-grid.tsx, photo grid components</files>
  <action>Add snap-x snap-mandatory overflow-x-auto classes to photo gallery containers on mobile breakpoints. Add snap-center to each photo child element. This creates a native swipeable gallery experience on mobile without JavaScript. Use responsive classes to only enable scroll snap on small screens (e.g., max-md:snap-x) while keeping the grid layout on desktop. Ensure proper scroll padding with scroll-px-4 or similar.</action>
  <verify>bun run build passes without errors</verify>
  <done>Photo galleries are swipeable with snap behavior on mobile screens</done>
</task>

<task type="auto">
  <name>Task 2: Add perspective and subtle rotate-y hover effects to photo thumbnails and cards</name>
  <files>album-photo-grid.tsx, card components</files>
  <action>Add perspective class to photo grid and card container elements. Add subtle hover:rotate-y-1 or hover:rotate-y-2 transform to photo thumbnails for a 3D tilt effect on hover. Add transition-transform for smooth animation. Keep effects subtle — no more than 2-3 degrees of rotation. Apply transform-3d to enable the 3D context. Consider adding hover:scale-[1.02] alongside rotation for a combined lift effect. Only apply 3D effects on hover-capable devices using @media (hover: hover).</action>
  <verify>bun run build passes without errors</verify>
  <done>Photo thumbnails and cards show subtle 3D depth effect on hover</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] Photo galleries snap horizontally on mobile
- [ ] Scroll snap only active on mobile breakpoints
- [ ] Photo thumbnails have subtle 3D hover effect
- [ ] Hover effects are smooth with transitions
</verification>

<success_criteria>
- Photo galleries swipeable on mobile with snap-to-item behavior
- Desktop layout unaffected by scroll snap
- 3D hover effects are subtle and smooth
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-tw4-advanced/08-03-SUMMARY.md`
</output>
