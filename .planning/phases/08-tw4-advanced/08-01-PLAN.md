---
phase: 08-tw4-advanced
type: execute
---

<objective>
Add container queries to dashboard cards and photo grids.

Purpose: Make dashboard cards and photo grids responsive to their container width rather than viewport width, enabling truly component-level responsive design.
Output: Dashboard cards and photo grids adapt layout based on container size using @container queries.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/dashboard/widget-card.tsx — WidgetCard wrapper component
@src/components/dashboard/metric-card.tsx — MetricCard component (if exists)
@src/app/(dashboard)/dashboard/photos/albums/[id]/page.tsx — Album photo grid
@src/app/(dashboard)/dashboard/photos/ — Photos pages with grid layouts

**Tailwind v4 container query utilities:**
- `@container` — Establishes a container query context on a wrapper element
- `@sm:` / `@md:` / `@lg:` — Container query breakpoint variants (replace viewport-based sm:/md:/lg: for component-level responsive)
- These allow cards to reflow based on their own width, not the viewport
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @container to WidgetCard and MetricCard wrappers with container-responsive breakpoints</name>
  <files>src/components/dashboard/widget-card.tsx, metric-card.tsx, and other dashboard card wrappers</files>
  <action>Add @container class to WidgetCard and MetricCard wrapper elements to establish container query contexts. Replace viewport-based responsive classes (sm:/md:/lg:) inside these cards with container query equivalents (@sm:/@md:/@lg:) so card internals respond to their container width. This enables cards to reflow correctly whether in a 2-column or 3-column dashboard grid. Search for other dashboard card wrappers that would benefit from container queries.</action>
  <verify>bun run build passes without errors</verify>
  <done>Dashboard cards use container queries and adapt layout to container width</done>
</task>

<task type="auto">
  <name>Task 2: Add container queries to photo grid components</name>
  <files>album-photo-grid.tsx, photos pages</files>
  <action>Add @container to photo grid wrapper elements. Replace viewport-based grid column classes with container query equivalents so photo grids adapt to their container width (e.g., @sm:grid-cols-2 @md:grid-cols-3 @lg:grid-cols-4). This makes photo grids work correctly in both full-width and sidebar layouts without separate responsive logic.</action>
  <verify>bun run build passes without errors</verify>
  <done>Photo grids use container queries and show appropriate column counts based on container width</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] Dashboard cards have @container on wrappers
- [ ] Card internals use @sm:/@md:/@lg: variants
- [ ] Photo grids use container queries for column count
</verification>

<success_criteria>
- Dashboard cards responsive to container width, not viewport
- Photo grids adapt columns based on available container space
- No broken layouts at any container size
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-tw4-advanced/08-01-SUMMARY.md`
</output>
