---
phase: 08-tw4-advanced
type: execute
---

<objective>
Add themed shadow colors and OKLCH gradient interpolation.

Purpose: Replace generic gray shadows with brand-themed colored shadows for depth, and use OKLCH color space for perceptually smooth gradients.
Output: Cards and elevated elements use themed shadow colors, hero and memorial gradients interpolate in OKLCH.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/card.tsx — Card component with shadow
@src/components/ui/input.tsx — Input component (for inset shadow)
@src/components/public/hero.tsx — Hero section with gradient background
@src/app/(public)/richard-hudson-sr/page.tsx — Memorial page with gradients

**Tailwind v4 utilities to apply:**
- `shadow-primary/10` — Colored shadow using primary theme color at 10% opacity
- `shadow-accent/15` — Colored shadow using accent color at 15% opacity
- `inset-shadow-*` — Inset box shadow for input focus states
- OKLCH gradient interpolation via `bg-gradient-to-r` with OKLCH color stops for perceptually uniform gradients
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace generic shadows with themed shadow colors and add inset shadows to inputs</name>
  <files>src/components/ui/card.tsx, src/components/ui/input.tsx, elevated components</files>
  <action>Replace generic shadow/shadow-md/shadow-lg classes on Card and other elevated components with shadow-primary/10 or shadow-accent/15 for brand-themed depth. Add inset-shadow-sm to Input component for subtle inner depth on focus. Search for all shadow-* usage across components and update to use themed shadow colors where appropriate. Keep shadows subtle — use low opacity values (5-15%).</action>
  <verify>bun run build passes without errors</verify>
  <done>Cards show themed colored shadows, inputs have inset shadow on focus</done>
</task>

<task type="auto">
  <name>Task 2: Update gradients to use OKLCH interpolation on hero and memorial sections</name>
  <files>src/components/public/hero.tsx, src/app/(public)/richard-hudson-sr/page.tsx</files>
  <action>Update gradient definitions on the hero section and memorial page to interpolate in OKLCH color space for perceptually smoother color transitions. Replace existing gradient classes or inline styles with OKLCH-interpolated equivalents. This produces more vibrant, natural-looking gradients compared to sRGB interpolation. Search for other gradient usage across the site that would benefit from OKLCH interpolation.</action>
  <verify>bun run build passes without errors</verify>
  <done>Hero and memorial gradients use OKLCH interpolation for smooth, vibrant color transitions</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] Cards use themed shadow colors (shadow-primary/10 or similar)
- [ ] Inputs have inset shadow on focus
- [ ] Hero gradient uses OKLCH interpolation
- [ ] Memorial page gradient uses OKLCH interpolation
</verification>

<success_criteria>
- All elevated components use brand-themed shadow colors
- Input focus states include inset shadow
- Gradients interpolate in OKLCH for smooth transitions
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-tw4-advanced/08-02-SUMMARY.md`
</output>
