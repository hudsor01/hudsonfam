---
phase: 04-public-site-polish
type: execute
---

<objective>
Add entrance animations, replace manual dividers with Separator component, and wrap dashboard sidebar in ScrollArea.

Purpose: Visual polish on public pages, semantic HTML, consistent scrollbar styling.
Output: Animations on key sections, Separator on memorial/homepage, ScrollArea on sidebar.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/styles/globals.css — already has --animate-fade-in-up: fade-in-up 0.4s ease-out and @keyframes fade-in-up
@src/components/public/hero.tsx — homepage hero section
@src/app/(public)/page.tsx — homepage with one border-t divider
@src/app/(public)/richard-hudson-sr/page.tsx — memorial with 4 standalone dividers + 1 section border-t
@src/app/(public)/events/page.tsx — events page
@src/app/(public)/blog/page.tsx — blog listing page
@src/app/(dashboard)/layout.tsx — dashboard sidebar (w-56 aside)
@src/components/ui/separator.tsx — Separator component (Radix-based, horizontal/vertical)
@src/components/ui/scroll-area.tsx — ScrollArea + ScrollBar components
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add motion-safe:animate-fade-in-up to hero sections, blog posts, and events</name>
  <files>src/components/public/hero.tsx, src/app/(public)/richard-hudson-sr/page.tsx, src/app/(public)/blog/page.tsx, src/app/(public)/events/page.tsx</files>
  <action>Add className="motion-safe:animate-fade-in-up" to key sections:
  - Hero component: add to the outer section element
  - Memorial page: add to the hero header element (the relative overflow-hidden one)
  - Blog page: add to the main content wrapper that contains the post grid
  - Events page: add to the main content wrapper
  Use motion-safe: prefix so animations respect prefers-reduced-motion. The animation token --animate-fade-in-up already exists in globals.css.</action>
  <verify>bun run build passes</verify>
  <done>Hero, memorial hero, blog posts section, and events section have subtle fade-in-up entrance animation (respecting reduced motion preference)</done>
</task>

<task type="auto">
  <name>Task 2: Replace manual dividers with Separator and wrap sidebar in ScrollArea</name>
  <files>src/app/(public)/page.tsx, src/app/(public)/richard-hudson-sr/page.tsx, src/app/(dashboard)/layout.tsx</files>
  <action>1. Homepage (page.tsx): Replace `<div className="border-t border-border" />` on line 73 with `<Separator />`. Add Separator import.
  2. Memorial page (richard-hudson-sr/page.tsx): Replace the 4 standalone `<div className="border-t border-border" />` dividers (lines ~279, 308, 345, 379) with `<Separator />`. Do NOT replace border-t on section elements that have other content (like the section on line 465). Add Separator import.
  3. Dashboard layout: Import ScrollArea from @/components/ui/scroll-area. Wrap the nav links area (between the logo div and the mt-auto footer) in `<ScrollArea className="flex-1"><div className="flex flex-col gap-1">...nav links...</div></ScrollArea>`. Keep the aside flex-col structure intact.</action>
  <verify>bun run build passes</verify>
  <done>Manual border-t dividers replaced with Separator on homepage and memorial page. Dashboard sidebar nav wrapped in ScrollArea.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds
- [ ] Homepage has Separator instead of manual divider
- [ ] Memorial page has Separator on 4 divider locations
- [ ] Dashboard sidebar uses ScrollArea
- [ ] Animations use motion-safe prefix
</verification>

<success_criteria>
- All manual standalone dividers on homepage and memorial page use Separator
- Hero, memorial hero, blog, events have fade-in-up animation
- Dashboard sidebar nav wrapped in ScrollArea
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-site-polish/04-01-SUMMARY.md`
</output>
