---
phase: 06-theme-alignment
type: execute
---

<objective>
Fix CSS variable naming to align with shadcn/ui convention and update all component references.

Purpose: shadcn/ui expects --background, --foreground, --primary (no prefix). We use --color-bg, --color-text, --color-primary. This mismatch causes fragile theming and inconsistent component references.
Output: Clean @theme block with shadcn-standard naming, all components using standard classes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-theme-alignment/06-RESEARCH.md
@src/styles/globals.css
@src/components/ui/select.tsx
@src/components/ui/card.tsx
@src/app/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor globals.css @theme to shadcn-standard variable naming</name>
  <files>src/styles/globals.css</files>
  <action>Consolidate the two @theme blocks into one clean block using shadcn-standard variable names:
  - --color-bg → --background (keep --color-bg as alias in @theme inline if needed for backward compat during migration)
  - --color-surface → --card
  - --color-border → --border
  - --color-primary → --primary
  - --color-accent → --accent
  - --color-text → --foreground
  - --color-text-muted → --muted-foreground
  - --color-text-dim → keep as custom (no shadcn equivalent)
  - Add --primary-foreground, --accent-foreground, --card-foreground, --destructive, --destructive-foreground, --muted, --secondary, --secondary-foreground, --input, --ring tokens
  - Remove the HSL sidebar fallback block (lines 149-169) — replace with OKLCH sidebar tokens in @theme
  - Keep all OKLCH values, keep chart colors, keep animation/easing tokens
  - Ensure the shadcn bridge section maps correctly to the new names</action>
  <verify>bun run build passes</verify>
  <done>Single clean @theme block with shadcn-standard naming, no HSL fallbacks, all OKLCH</done>
</task>

<task type="auto">
  <name>Task 2: Update all component/page references to use shadcn-standard classes</name>
  <files>All files in src/ referencing bg-bg, text-text, text-text-muted, text-text-dim, bg-surface</files>
  <action>Search and replace across all src/ files:
  - bg-bg → bg-background
  - text-text → text-foreground (careful: text-text-muted and text-text-dim are different)
  - text-text-muted → text-muted-foreground
  - text-text-dim → text-muted-foreground/70 or keep as custom --color-text-dim if needed
  - bg-surface → bg-card
  - hover:bg-bg → hover:bg-background
  - hover:text-text → hover:text-foreground
  Do NOT change border-border (that's already shadcn standard).
  Run grep to find all instances first, then replace carefully to avoid breaking partial matches.</action>
  <verify>bun run build passes, grep for bg-bg and text-text returns zero matches</verify>
  <done>No custom shorthand color classes remain (bg-bg, text-text, bg-surface), all use shadcn-standard names</done>
</task>

</tasks>

<verification>
- [ ] bun run build succeeds
- [ ] bun run test passes
- [ ] grep -r "bg-bg[^a-z]" src/ returns no matches
- [ ] grep -r "text-text[^-]" src/ returns no matches
- [ ] grep -r "bg-surface" src/ returns no matches
- [ ] No HSL color values in globals.css
</verification>

<success_criteria>
- All CSS variables follow shadcn naming convention
- All component classes use shadcn-standard names
- Single clean @theme block
- Build and tests pass
</success_criteria>

<output>
After completion, create .planning/phases/06-theme-alignment/06-01-SUMMARY.md
</output>
