---
phase: 06-theme-alignment
type: execute
---

<objective>
Fix hardcoded colors and template literal className patterns in shadcn components.

Purpose: alert-dialog.tsx uses bg-red-500 instead of destructive token. card.tsx and badge.tsx use template literals instead of cn(). These break theming and Tailwind Merge.
Output: All components use theme tokens and cn() consistently.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/ui/alert-dialog.tsx
@src/components/ui/delete-button.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded colors with theme tokens</name>
  <files>src/components/ui/alert-dialog.tsx, src/components/ui/delete-button.tsx</files>
  <action>In alert-dialog.tsx: Replace bg-red-500 hover:bg-red-600 on AlertDialogAction with bg-destructive hover:bg-destructive/90 text-destructive-foreground. In delete-button.tsx: Replace any hardcoded red text colors (text-red-*) with text-destructive. Search all src/components/ui/ for any remaining hardcoded color values (bg-red-*, text-red-*, bg-green-*, etc.) and replace with appropriate theme tokens.</action>
  <verify>bun run build passes, grep -r "bg-red-\|text-red-" src/components/ui/ returns no matches</verify>
  <done>Zero hardcoded color values in ui/ components, all use theme tokens</done>
</task>

<task type="auto">
  <name>Task 2: Fix template literal className to use cn()</name>
  <files>src/components/ui/card.tsx, src/components/ui/badge.tsx</files>
  <action>In card.tsx: Replace template literal className construction (lines using ${hover ? ... : ""} and ${className}) with cn() calls. Pattern: cn("base-classes", hover && "hover-classes", className). In badge.tsx: Same fix — replace any template literal className with cn() calls. Verify cn is imported from @/lib/utils in both files. Scan all other ui/ components to ensure none use template literal className patterns.</action>
  <verify>bun run build passes, no template literal className patterns in ui/ components</verify>
  <done>All ui/ components consistently use cn() for className construction</done>
</task>

</tasks>

<verification>
- [ ] bun run build succeeds
- [ ] bun run test passes
- [ ] No hardcoded color values in src/components/ui/
- [ ] All className construction uses cn()
</verification>

<success_criteria>
- Zero hardcoded colors in ui/ components
- All components use cn() consistently
- Build and tests pass
</success_criteria>

<output>
After completion, create .planning/phases/06-theme-alignment/06-02-SUMMARY.md
</output>
