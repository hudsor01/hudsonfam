---
phase: 11-component-integration
type: execute
---

<objective>
Integrate Calendar + Popover as date picker in EventForm.

Purpose: Replace the native datetime-local input with a visual Calendar + Popover date picker following the shadcn date-picker pattern, and add a mini-calendar to the public events page showing event dates highlighted.
Output: Event form uses visual date picker, events page has mini-calendar.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(dashboard)/dashboard/events/event-form.tsx — Event creation/edit form with datetime-local input
@src/app/(public)/events/page.tsx — Public events listing page
@src/components/ui/calendar.tsx — shadcn Calendar component
@src/components/ui/popover.tsx — shadcn Popover component

**Pattern reference:**
- shadcn date-picker pattern: Popover trigger (Button with calendar icon + formatted date), PopoverContent containing Calendar component
- May need date-fns for date formatting (format, parseISO, etc.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace input type="datetime-local" in EventForm with Calendar + Popover date picker</name>
  <files>src/app/(dashboard)/dashboard/events/event-form.tsx, may need date-fns</files>
  <action>Replace the input type="datetime-local" in EventForm with a shadcn date-picker pattern: Popover containing Calendar component. Use a Button as PopoverTrigger displaying the formatted date with a calendar icon. Install date-fns if not already present for date formatting. Wire the Calendar's onSelect to update the form state. Ensure the date value is properly serialized for form submission.</action>
  <verify>bun run build passes without errors</verify>
  <done>EventForm uses a visual Calendar + Popover date picker instead of native datetime-local input</done>
</task>

<task type="auto">
  <name>Task 2: Add optional mini-calendar to public events page showing event dates highlighted</name>
  <files>src/app/(public)/events/page.tsx</files>
  <action>Add a mini-calendar sidebar or section to the public events page that displays a Calendar component with event dates highlighted using the modifiers prop. Fetch event dates and pass them as highlighted/selected days. Clicking a highlighted date should scroll to or filter to that event. Style the highlighted dates distinctly using the calendar's modifier styles.</action>
  <verify>bun run build passes without errors</verify>
  <done>Public events page displays a mini-calendar with event dates visually highlighted</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] EventForm renders Calendar + Popover date picker
- [ ] Date selection updates form state correctly
- [ ] Public events page shows mini-calendar with event dates highlighted
</verification>

<success_criteria>
- Event form uses visual date picker instead of native datetime-local
- Date picker follows shadcn date-picker pattern (Popover + Calendar)
- Public events page has mini-calendar with highlighted event dates
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-component-integration/11-01-SUMMARY.md`
</output>
