---
phase: 11-component-integration
type: execute
---

<objective>
Add Dialog for quick actions + Drawer for mobile.

Purpose: Enable quick event and update creation from the dashboard overview without full page navigation, and implement responsive Dialog+Drawer pattern that uses Dialog on desktop and Drawer on mobile.
Output: Quick actions available without page navigation, mobile-optimized with Drawer.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(dashboard)/dashboard/page.tsx — Dashboard overview page
@src/components/ui/dialog.tsx — shadcn Dialog component
@src/components/ui/drawer.tsx — shadcn Drawer component

**Pattern reference:**
- shadcn responsive Dialog+Drawer pattern: Use useMediaQuery hook to detect viewport width, render Dialog on desktop (min-width 768px) and Drawer on mobile
- Quick action forms should be simplified inline versions of the full creation forms
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dialog on dashboard overview for "Quick New Event" and "Quick New Update"</name>
  <files>src/app/(dashboard)/dashboard/page.tsx</files>
  <action>Add "Quick New Event" and "Quick New Update" action buttons to the dashboard overview page. Each opens a Dialog containing an inline creation form with essential fields only (title, date for events; title, content for updates). Wire form submission to the existing server actions or API routes. Close dialog on successful creation and refresh the page data.</action>
  <verify>bun run build passes without errors</verify>
  <done>Dashboard overview has quick action buttons that open Dialog with inline creation forms</done>
</task>

<task type="auto">
  <name>Task 2: Implement responsive Dialog+Drawer pattern for quick-action forms</name>
  <files>src/app/(dashboard)/dashboard/page.tsx, new quick-action components</files>
  <action>Implement a responsive pattern where quick-action forms render inside Dialog on desktop (min-width 768px) and inside Drawer on mobile. Create a useMediaQuery hook or use an existing one. Build a ResponsiveModal wrapper component that conditionally renders Dialog or Drawer based on viewport. Apply this to both quick-action forms from Task 1.</action>
  <verify>bun run build passes without errors</verify>
  <done>Quick action forms use Dialog on desktop and Drawer on mobile viewports</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] "Quick New Event" button opens inline creation form
- [ ] "Quick New Update" button opens inline creation form
- [ ] Forms render in Dialog on desktop (>= 768px)
- [ ] Forms render in Drawer on mobile (< 768px)
</verification>

<success_criteria>
- Quick actions accessible from dashboard overview without navigation
- Dialog renders on desktop viewports
- Drawer renders on mobile viewports
- Forms submit successfully and close on completion
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-component-integration/11-02-SUMMARY.md`
</output>
