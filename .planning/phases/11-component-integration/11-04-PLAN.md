---
phase: 11-component-integration
type: execute
---

<objective>
Add Popover for user profile menu.

Purpose: Provide a user profile menu accessible via avatar button in both the dashboard sidebar and public site navigation, showing user info and quick actions like settings and sign out.
Output: User profile accessible via Popover in both public nav and dashboard.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/dashboard/app-sidebar.tsx — Dashboard sidebar component
@src/app/(dashboard)/layout.tsx — Dashboard layout
@src/app/(public)/layout.tsx — Public site layout with navigation
@src/components/ui/popover.tsx — shadcn Popover component
@src/components/ui/avatar.tsx — shadcn Avatar component
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add user avatar Popover in dashboard sidebar footer</name>
  <files>src/components/dashboard/app-sidebar.tsx or src/app/(dashboard)/layout.tsx</files>
  <action>Add a user avatar button in the dashboard sidebar footer area that opens a Popover on click. The Popover should display: user name, user email, a link to settings/profile page, and a sign out button. Use the Avatar component for the trigger button showing user initials or image. Style the Popover content with proper spacing, dividers between sections, and consistent typography.</action>
  <verify>bun run build passes without errors</verify>
  <done>Dashboard sidebar has a user avatar button in the footer that opens a Popover with user info and actions</done>
</task>

<task type="auto">
  <name>Task 2: Add user Popover to public site navigation for authenticated users</name>
  <files>src/app/(public)/layout.tsx</files>
  <action>Add a similar user profile Popover to the public site navigation bar, visible only for authenticated users. Use the same Avatar + Popover pattern as the dashboard. Show user name, email, link to dashboard, and sign out button. For unauthenticated users, show a sign-in link instead. Ensure the Popover aligns properly within the navigation bar layout.</action>
  <verify>bun run build passes without errors</verify>
  <done>Public site navigation shows user avatar Popover for authenticated users, sign-in link for others</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] Dashboard sidebar footer shows user avatar button
- [ ] Clicking avatar opens Popover with user info and actions
- [ ] Public nav shows avatar Popover for authenticated users
- [ ] Public nav shows sign-in link for unauthenticated users
- [ ] Sign out button works correctly in both locations
</verification>

<success_criteria>
- User profile Popover accessible in dashboard sidebar
- User profile Popover accessible in public navigation for authenticated users
- Both Popovers show user name, email, and relevant action links
- Sign out functionality works from both locations
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-component-integration/11-04-SUMMARY.md`
</output>
