---
phase: 11-component-integration
type: execute
---

<objective>
Integrate Pagination and Progress components.

Purpose: Replace custom blog pagination with the shadcn Pagination component for consistent styling and accessibility, and add a Progress component to the photo upload page as an upload progress indicator.
Output: Blog uses shadcn pagination, upload has progress indicator.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(public)/blog/page.tsx — Blog listing with existing custom pagination
@src/app/(dashboard)/dashboard/photos/upload/page.tsx — Photo upload page
@src/components/ui/pagination.tsx — shadcn Pagination component (PaginationContent, PaginationItem, PaginationLink, PaginationPrevious, PaginationNext, PaginationEllipsis)
@src/components/ui/progress.tsx — shadcn Progress component
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace custom blog pagination with shadcn Pagination component</name>
  <files>src/app/(public)/blog/page.tsx</files>
  <action>Replace the existing custom pagination implementation in the blog listing page with the shadcn Pagination component. Use PaginationContent, PaginationItem, PaginationLink, PaginationPrevious, PaginationNext, and PaginationEllipsis. Preserve the existing pagination logic (page numbers, total pages, current page from search params). Add ellipsis for large page ranges. Ensure active page is visually indicated via PaginationLink isActive prop.</action>
  <verify>bun run build passes without errors</verify>
  <done>Blog listing uses shadcn Pagination with proper page navigation, ellipsis, and active state</done>
</task>

<task type="auto">
  <name>Task 2: Add Progress component to photo upload page as upload progress indicator</name>
  <files>src/app/(dashboard)/dashboard/photos/upload/page.tsx or upload-form component</files>
  <action>Add the shadcn Progress component to the photo upload page to display upload progress. Wire the Progress value prop to track upload completion percentage. Show the progress bar during active uploads with a percentage label. Handle states: idle (hidden), uploading (animated progress), complete (100% with success indication), error (visual error state).</action>
  <verify>bun run build passes without errors</verify>
  <done>Photo upload page shows a progress bar during file uploads with percentage indication</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] Blog pagination uses shadcn Pagination components
- [ ] Pagination shows correct page numbers, ellipsis, and active state
- [ ] Photo upload page displays Progress bar during uploads
- [ ] Progress bar reflects actual upload percentage
</verification>

<success_criteria>
- Blog pagination is fully replaced with shadcn Pagination component
- Pagination handles edge cases (first page, last page, ellipsis for many pages)
- Upload progress bar shows real-time progress during photo uploads
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-component-integration/11-03-SUMMARY.md`
</output>
