---
phase: 09-sidebar-upgrade
type: execute
---

<objective>
Replace basic static sidebar with full shadcn Sidebar component.

Purpose: The current dashboard sidebar is a static nav list without collapse, mobile responsiveness, or keyboard shortcuts. The shadcn Sidebar component provides a complete solution with SidebarProvider, collapsible modes, mobile overlay, and accessibility.
Output: Fully interactive sidebar with collapse toggle, mobile-responsive overlay, icon-only mode, and Cmd+B keyboard shortcut.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(dashboard)/layout.tsx
@src/components/ui/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app-sidebar component with shadcn Sidebar hierarchy</name>
  <files>src/components/dashboard/app-sidebar.tsx (new)</files>
  <action>Create src/components/dashboard/app-sidebar.tsx using the full shadcn Sidebar component hierarchy:
  - SidebarProvider + Sidebar + SidebarContent + SidebarMenu as the structural foundation
  - Port all existing nav links from the current layout sidebar into SidebarMenuItem + SidebarMenuButton components
  - Add SidebarHeader with the site logo/family name branding
  - Add SidebarFooter with user info (name, avatar) and sign-out action
  - Use SidebarGroup + SidebarGroupLabel to organize nav items into logical sections (e.g., Content, Media, Admin)
  - Ensure all icons are consistent and all links use proper active state detection</action>
  <verify>bun run build passes, component renders without errors</verify>
  <done>app-sidebar.tsx created with full Sidebar hierarchy, nav links ported, header with logo, footer with user info</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dashboard layout to use SidebarProvider with mobile toggle and keyboard shortcut</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <action>Rewrite src/app/(dashboard)/layout.tsx to integrate the new sidebar:
  - Wrap layout content in SidebarProvider from shadcn
  - Replace the old static sidebar markup with the new AppSidebar component
  - Add SidebarTrigger button in the top header area for mobile toggle
  - Configure collapsible prop to "icon" mode so sidebar collapses to icon-only rail
  - Add Cmd+B (Ctrl+B on Windows/Linux) keyboard shortcut to toggle sidebar via SidebarProvider's toggleSidebar
  - Ensure the main content area properly adjusts width when sidebar collapses
  - Test mobile breakpoint behavior (sidebar should overlay as sheet on mobile)</action>
  <verify>bun run build passes</verify>
  <done>Dashboard layout uses SidebarProvider, sidebar is collapsible with icon mode, mobile-responsive with SidebarTrigger, Cmd+B shortcut works</done>
</task>

</tasks>

<verification>
- [ ] bun run build succeeds
- [ ] Sidebar renders with all nav links in correct groups
- [ ] Sidebar collapses to icon-only rail when toggled
- [ ] SidebarTrigger button toggles sidebar on mobile
- [ ] Cmd+B keyboard shortcut toggles sidebar
- [ ] SidebarHeader shows logo, SidebarFooter shows user info
- [ ] Active nav item is visually highlighted
</verification>

<success_criteria>
- Sidebar uses full shadcn Sidebar component hierarchy
- Collapsible icon mode works correctly
- Mobile-responsive with overlay behavior
- Keyboard shortcut (Cmd+B) toggles sidebar
- All existing nav links preserved and functional
</success_criteria>

<output>
After completion, create .planning/phases/09-sidebar-upgrade/09-01-SUMMARY.md
</output>
