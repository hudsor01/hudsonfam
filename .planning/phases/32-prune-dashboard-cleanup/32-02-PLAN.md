---
phase: 32-prune-dashboard-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/dashboard/posts/page.tsx
  - src/app/(dashboard)/dashboard/posts/new/page.tsx
  - src/app/(dashboard)/dashboard/posts/[id]/page.tsx
  - src/app/(dashboard)/dashboard/posts/post-form.tsx
  - src/app/(dashboard)/dashboard/posts/post-actions.tsx
  - src/app/(dashboard)/dashboard/posts/posts-data-table.tsx
  - src/app/(dashboard)/dashboard/posts/columns.tsx
  - src/app/(dashboard)/dashboard/updates/page.tsx
  - src/app/(dashboard)/dashboard/updates/new/page.tsx
  - src/app/(dashboard)/dashboard/updates/new/update-form.tsx
  - src/lib/dashboard-actions.ts
  - src/app/(dashboard)/dashboard/quick-actions.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - prisma/schema.prisma
  - src/__tests__/lib/dashboard-actions.test.ts
  - src/__tests__/mocks/prisma.ts
  - src/__tests__/prod-readiness.test.ts
  - src/__tests__/production-bugs.test.ts
autonomous: false
requirements: [PRUNE-03, PRUNE-04]

must_haves:
  truths:
    - "Dashboard posts and updates CRUD routes no longer exist"
    - "Post/update server actions are removed from dashboard-actions.ts"
    - "BlogPost and FamilyUpdate models (and PostStatus enum) are removed via a migration"
    - "npx prisma generate succeeds and the app boots against the migrated schema"
    - "npm run build and npm test pass with no prisma.blogPost / prisma.familyUpdate usage"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Schema with BlogPost, FamilyUpdate, PostStatus removed; Visibility intact"
    - path: "prisma/migrations"
      provides: "Versioned destructive migration dropping both tables + enum"
      contains: "remove-blog-familyupdate"
    - path: "src/lib/dashboard-actions.ts"
      provides: "Server actions with no createPost/updatePost/deletePost/createUpdate/deleteUpdate/quickCreateUpdate"
  key_links:
    - from: "src/lib/dashboard-actions.ts"
      to: "prisma"
      via: "no blogPost/familyUpdate queries remain"
      pattern: "blogPost|familyUpdate"
    - from: "prisma/schema.prisma"
      to: "generated/prisma"
      via: "prisma generate after migration"
      pattern: "model BlogPost"
---

<objective>
Remove all DB-backed Blog/Updates surfaces: delete the dashboard posts/updates CRUD routes (PRUNE-04), remove their server actions, drop the `BlogPost` + `FamilyUpdate` models and the `PostStatus` enum via a Prisma migration (PRUNE-03, D-01 verify-then-drop), and perform the test surgery (D-06) so the suite stays green. This plan owns everything that depends on the live schema; the public surface is Plan 01 and the dashboard consolidation/rework is Plan 03.

Purpose: Eliminate the DB models and dashboard CRUD so the schema migration applies cleanly with zero residual `prisma.blogPost` / `prisma.familyUpdate` consumers.
Output: Deleted dashboard posts/updates routes, trimmed `dashboard-actions.ts` + `quick-actions.tsx`, a versioned destructive migration, regenerated Prisma client, and surgically reduced tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/32-prune-dashboard-cleanup/32-CONTEXT.md
@.planning/phases/32-prune-dashboard-cleanup/32-RESEARCH.md
@.planning/phases/32-prune-dashboard-cleanup/32-PATTERNS.md
@.planning/phases/32-prune-dashboard-cleanup/32-VALIDATION.md

@.claude/skills/neon-postgres

<scope_note>
ORDER MATTERS for a clean migration. Every consumer of `prisma.blogPost` and `prisma.familyUpdate` must be removed BEFORE running `prisma migrate dev`, or the regenerated client + tsc will flag dead references. Consumers in THIS plan: dashboard posts/updates pages (Task 1), `dashboard-actions.ts` post/update actions + `quick-actions.tsx` QuickUpdateDialog (Task 2), and the dashboard overview's count/recentPosts queries in `src/app/(dashboard)/dashboard/page.tsx` (neutralized in Task 4's pre-migration sweep). This plan owns ONLY the DATA-LAYER de-blog of `dashboard/page.tsx` (removing the blog/update queries so the migration builds green); Plan 03 (Wave 2, depends_on 32-02) then reworks the overview PRESENTATION (3-stat grid, Recent Photos card) on top of that de-blogged base. Both plans therefore touch `dashboard/page.tsx`, which is why it is declared in this plan's files_modified AND Plan 03's — there is no same-wave conflict because Plan 03 is a later wave that explicitly depends on this plan.
</scope_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dashboard posts + updates CRUD routes (PRUNE-04)</name>
  <files>src/app/(dashboard)/dashboard/posts/page.tsx, src/app/(dashboard)/dashboard/posts/new/page.tsx, src/app/(dashboard)/dashboard/posts/[id]/page.tsx, src/app/(dashboard)/dashboard/posts/post-form.tsx, src/app/(dashboard)/dashboard/posts/post-actions.tsx, src/app/(dashboard)/dashboard/posts/posts-data-table.tsx, src/app/(dashboard)/dashboard/posts/columns.tsx, src/app/(dashboard)/dashboard/updates/page.tsx, src/app/(dashboard)/dashboard/updates/new/page.tsx, src/app/(dashboard)/dashboard/updates/new/update-form.tsx</files>
  <read_first>
    - .planning/phases/32-prune-dashboard-cleanup/32-RESEARCH.md §"Dashboard posts routes" + §"Dashboard updates routes"
    - .planning/phases/32-prune-dashboard-cleanup/32-PATTERNS.md §"Pure Deletions" (dashboard route rows)
    - src/app/(dashboard)/dashboard/posts/page.tsx (confirm it imports prisma.blogPost + deletePost + PostsDataTable)
    - src/app/(dashboard)/dashboard/updates/page.tsx (confirm it imports prisma.familyUpdate + deleteUpdate)
  </read_first>
  <action>
    `git rm` the entire `src/app/(dashboard)/dashboard/posts/` directory (7 files: page.tsx, new/page.tsx, [id]/page.tsx, post-form.tsx, post-actions.tsx, posts-data-table.tsx, columns.tsx) and the entire `src/app/(dashboard)/dashboard/updates/` directory (3 files: page.tsx, new/page.tsx, new/update-form.tsx). Remove the now-empty `posts/` and `updates/` directories. Do NOT yet edit `dashboard-actions.ts` (Task 2), `quick-actions.tsx` (Task 2), or `schema.prisma` (Task 4) — deleting the route pages first removes the page-level `prisma.blogPost`/`prisma.familyUpdate` consumers so the build stays compilable through the next tasks.
  </action>
  <verify>
    <automated>find src/app -type d \( -name posts -o -name updates \) -path "*/dashboard/*" | grep -qc . && echo "FOUND_DIRS" || echo "DIRS_GONE"</automated>
  </verify>
  <acceptance_criteria>
    - `find src/app -type d \( -name posts -o -name updates \) -path "*/dashboard/*"` returns nothing
    - the command in <verify> prints `DIRS_GONE`
    - `ls "src/app/(dashboard)/dashboard/posts"` and `ls "src/app/(dashboard)/dashboard/updates"` both fail (ENOENT)
  </acceptance_criteria>
  <done>Both dashboard CRUD route directories and all 10 files are removed; no orphaned posts/updates route dirs remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove post/update server actions and QuickUpdateDialog (PRUNE-04)</name>
  <files>src/lib/dashboard-actions.ts, src/app/(dashboard)/dashboard/quick-actions.tsx</files>
  <read_first>
    - .planning/phases/32-prune-dashboard-cleanup/32-RESEARCH.md §`src/lib/dashboard-actions.ts` + §`src/app/(dashboard)/dashboard/quick-actions.tsx` (exact line-level scope) + §Pitfall 5
    - src/lib/dashboard-actions.ts (locate createPost/updatePost/deletePost + createUpdate/deleteUpdate/quickCreateUpdate)
    - src/app/(dashboard)/dashboard/quick-actions.tsx (locate QuickUpdateDialog + its imports)
  </read_first>
  <action>
    `src/lib/dashboard-actions.ts` (PRUNE-04): delete the `createPost`, `updatePost`, `deletePost` functions (the Posts section) and the `createUpdate`, `deleteUpdate` functions (the Family Updates section) and `quickCreateUpdate` (in the Quick-create section). Keep `createAlbum`, `updateAlbum`, `createEvent`, `updateEvent`, `deleteEvent`, `updateUserRole`, `banUser`, `unbanUser`, `deletePhoto`, `quickCreateEvent`, `createInvite`, `deleteInvite`. Remove any imports that become unused after deleting those functions.
    `src/app/(dashboard)/dashboard/quick-actions.tsx` (Pitfall 5): delete the entire `QuickUpdateDialog` component; remove its imports `quickCreateUpdate` (from `@/lib/dashboard-actions`), `MessageSquarePlus` (from `lucide-react`), and `Textarea` (from `@/components/ui/textarea`) — only if each is now unused. Keep `QuickEventDialog` and all its dependencies. These two edits must land together so the `quickCreateUpdate` import does not dangle.
  </action>
  <verify>
    <automated>grep -nE "createPost|updatePost|deletePost|createUpdate|deleteUpdate|quickCreateUpdate" src/lib/dashboard-actions.ts | grep -vc '^#' | grep -qx 0 && ! grep -q "QuickUpdateDialog\|quickCreateUpdate" "src/app/(dashboard)/dashboard/quick-actions.tsx" && echo ACTIONS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "createPost|updatePost|deletePost|createUpdate|deleteUpdate|quickCreateUpdate" src/lib/dashboard-actions.ts` (comments filtered) returns 0
    - `grep "QuickUpdateDialog\|quickCreateUpdate" "src/app/(dashboard)/dashboard/quick-actions.tsx"` returns 0
    - surviving actions (createAlbum, createEvent, deletePhoto, createInvite, quickCreateEvent, etc.) are intact
    - the command in <verify> prints `ACTIONS_CLEAN`
  </acceptance_criteria>
  <done>All post/update server actions are gone from dashboard-actions.ts; QuickUpdateDialog and its now-unused imports are removed from quick-actions.tsx; no dangling imports.</done>
</task>

<task type="auto">
  <name>Task 3: Test surgery for removed DB/actions (D-06)</name>
  <files>src/__tests__/lib/dashboard-actions.test.ts, src/__tests__/mocks/prisma.ts, src/__tests__/prod-readiness.test.ts, src/__tests__/production-bugs.test.ts</files>
  <read_first>
    - .planning/phases/32-prune-dashboard-cleanup/32-RESEARCH.md §`src/__tests__/lib/dashboard-actions.test.ts`, §`src/__tests__/mocks/prisma.ts`, §`src/__tests__/prod-readiness.test.ts`, §`src/__tests__/production-bugs.test.ts` (line-level surgery maps) + §Pitfall 1, 2, 7
    - src/__tests__/lib/dashboard-actions.test.ts
    - src/__tests__/mocks/prisma.ts
    - src/__tests__/prod-readiness.test.ts
    - src/__tests__/production-bugs.test.ts
  </read_first>
  <action>
    Apply the test surgery from RESEARCH.md §Files to EDIT so the suite references no deleted code.
    `src/__tests__/lib/dashboard-actions.test.ts`: remove the Posts describe block (createPost/updatePost/deletePost), the Family Updates describe block (createUpdate/deleteUpdate), the createPost/updatePost/deletePost/createUpdate/deleteUpdate imports, the `prismaMock.blogPost.*` + `prismaMock.familyUpdate.*` beforeEach setup, and the createPost/deletePost requireRole auth-enforcement cases. Keep album/event/user/photo/invite suites and their auth guards.
    `src/__tests__/mocks/prisma.ts`: remove the `blogPost` mock object and the `familyUpdate` mock object. Keep album, photo, event, inviteToken, user, memory, memorialMedia, memorialContent.
    `src/__tests__/prod-readiness.test.ts`: remove the `getAllPosts, getPostBySlug` import from `@/lib/blog`; delete the SEO—RSS Feed describe block (reads deleted rss route), the sitemap tests asserting `/blog`+`/family` presence and the blog-post-entries test, the "blog page uses remark-gfm" test (reads deleted blog/[slug]/page.tsx), the Blog—Content Handling describe block, and the getPostBySlug path-traversal + empty-slug tests. Keep Memorial, invite-token, Auth, iCal, robots.txt, layout-backlink, seed-photo, event-relative-time tests.
    `src/__tests__/production-bugs.test.ts`: remove the createPost/deletePost/createUpdate/deleteUpdate imports; remove the createPost/createUpdate FormData-validation cases; remove the blogPost mock setup and the createPost/deletePost/deleteUpdate auth-guard cases + role-list tests; remove `(public)/family/page.tsx`, `(dashboard)/dashboard/posts/page.tsx`, `(dashboard)/dashboard/posts/[id]/page.tsx`, `(dashboard)/dashboard/updates/page.tsx` from the `PAGES_USING_PRISMA` array (Pitfall 1); remove the createPost-data-shape and createUpdate-fields schema-alignment tests and the Blog edge-cases describe block; remove the `prismaMock.blogPost.create` + `prismaMock.familyUpdate.create` beforeEach setup. Keep all event/auth/invite/photo tests and ISR tests for surviving pages.
  </action>
  <verify>
    <automated>grep -rnE "blogPost|familyUpdate|getAllPosts|getPostBySlug|createPost|deletePost|createUpdate|deleteUpdate" src/__tests__/ | grep -vc '^#' | grep -qx 0 && npm test -- --run && echo TESTS_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "blogPost|familyUpdate|getAllPosts|getPostBySlug|createPost|deletePost|createUpdate|deleteUpdate" src/__tests__/` (comments filtered) returns 0
    - `PAGES_USING_PRISMA` no longer lists the 4 removed pages
    - `npm test -- --run` exits 0
    - the command in <verify> prints `TESTS_GREEN`
  </acceptance_criteria>
  <done>No test file references any deleted model, lib export, or server action; `npm test -- --run` exits 0.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Verify-then-drop schema migration (PRUNE-03, D-01)</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - .planning/phases/32-prune-dashboard-cleanup/32-RESEARCH.md §"Prisma Migration Mechanics" (verify-then-drop script + migrate dev workflow) + §Pitfall 8
    - .planning/phases/32-prune-dashboard-cleanup/32-PATTERNS.md §`prisma/schema.prisma` (three contiguous blocks to delete)
    - .claude/skills/neon-postgres (Neon + Prisma migration conventions)
    - prisma/schema.prisma (lines around `model BlogPost` ~11, `enum PostStatus` ~28, `model FamilyUpdate` ~84 — confirm Visibility enum ~79 is shared with Event and is NOT touched)
    - prisma.config.ts (confirm migrations use DIRECT_DATABASE_URL)
  </read_first>
  <action>
    PRE-MIGRATION SWEEP: run `grep -rnE "prisma\.blogPost|prisma\.familyUpdate" src/` — it MUST return 0 before proceeding. RESEARCH.md notes `src/app/(dashboard)/dashboard/page.tsx` still contains blog/update count queries; this plan does NOT own a full rework of that file (Plan 03 does), but its `prisma.blogPost.count()`, `prisma.blogPost.count({ where: { status: "PUBLISHED" } })`, `prisma.familyUpdate.count()`, and `recentPosts` queries MUST be neutralized here so the regenerated client does not break the build. Make the MINIMAL edit to `dashboard/page.tsx` data layer: remove those four queries and any `stats`/JSX that directly references their results, leaving the surviving photos/albums/events queries and cards working. (Plan 03 then reworks the overview presentation — Recent Photos card, 3-stat grid — on top of this de-blogged base.) If the sweep still returns matches after this edit, fix them before continuing.
    D-01 VERIFY-THEN-DROP: run the row-count Node script from RESEARCH.md §Prisma Migration Mechanics Step 1 (generated client `./generated/prisma`, `DIRECT_DATABASE_URL`) to print `BlogPost rows` and `FamilyUpdate rows`. If EITHER count > 0, run the Step 2b dump script first, writing `.planning/phases/32-prune-dashboard-cleanup/blogpost-dump.json` and `.planning/phases/32-prune-dashboard-cleanup/familyupdate-dump.json` before any drop. If both are 0, proceed directly.
    SCHEMA EDIT: delete three contiguous blocks from `prisma/schema.prisma` — the `PostStatus` enum, the `BlogPost` model, and the `FamilyUpdate` model. Do NOT touch the `Visibility` enum (shared with `Event`).
    MIGRATE: run `npx prisma migrate dev --name remove-blog-familyupdate` (NOT `db push` — this project uses versioned migrations via `DIRECT_DATABASE_URL` per prisma.config.ts), then `npx prisma generate` to regenerate the client into `./generated/prisma/`. The generated migration SQL should be approximately `DROP TABLE "BlogPost"; DROP TABLE "FamilyUpdate"; DROP TYPE "PostStatus";`.
    This task is `checkpoint:human-action` because `prisma migrate dev` is a destructive production-DB operation against Neon that may prompt for interactive confirmation and is not safely auto-runnable unattended; the executor pauses for the developer to authorize the migration run (and to confirm any non-empty-table dump). After the migration, verify no `PostStatus` import survives (Pitfall 8).
  </action>
  <verify>
    <automated>grep -rnE "prisma\.blogPost|prisma\.familyUpdate|PostStatus" src/ | grep -vc '^#' | grep -qx 0 && ! grep -q "model BlogPost\|model FamilyUpdate\|enum PostStatus" prisma/schema.prisma && grep -q "enum Visibility" prisma/schema.prisma && ls prisma/migrations/*remove-blog-familyupdate* >/dev/null 2>&1 && npx prisma generate && npm run build && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - row-count script ran; if either table was non-empty, dump JSON files exist under `.planning/phases/32-prune-dashboard-cleanup/`
    - `prisma/schema.prisma` has no `model BlogPost`, `model FamilyUpdate`, or `enum PostStatus`; `enum Visibility` is intact
    - a versioned migration dir matching `*remove-blog-familyupdate*` exists under `prisma/migrations/`
    - `grep -rnE "prisma\.blogPost|prisma\.familyUpdate|PostStatus" src/` (comments filtered) returns 0
    - `npx prisma generate` exits 0 and `npm run build` exits 0
    - the command in <verify> prints `MIGRATION_OK`
  </acceptance_criteria>
  <done>Schema migration applied (verify-then-drop honored, dumps written if any rows existed); Prisma client regenerated; no `prisma.blogPost`/`prisma.familyUpdate`/`PostStatus` reference remains in `src/`; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → Neon Postgres (DIRECT_DATABASE_URL) | A destructive DROP TABLE / DROP TYPE migration crosses into the production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Denial of Service (data loss) | `npx prisma migrate dev` dropping BlogPost + FamilyUpdate tables | mitigate | D-01 verify-then-drop: count rows first; if either non-empty, dump rows to `.planning/phases/32-prune-dashboard-cleanup/*.json` BEFORE the drop. No silent data loss. Task is `checkpoint:human-action`/`gate=blocking` so the developer authorizes the destructive run. |
| T-32-04 | Tampering | regenerated Prisma client | accept | `prisma generate` writes to `./generated/prisma/` from the versioned schema; no untrusted input. Build (tsc) is the integrity gate against residual `PostStatus`/model references (Pitfall 8). |
| T-32-SC | Tampering | npm/prisma CLI invocations | accept | No new packages installed in this phase (removal-only); Prisma already a pinned project dependency. No package-legitimacy gate required. |
</threat_model>

<verification>
- Pre-migration: `grep -rnE "prisma\.blogPost|prisma\.familyUpdate" src/` returns 0
- `npx prisma migrate dev --name remove-blog-familyupdate` applied; `npx prisma generate` exits 0
- `prisma/schema.prisma`: no BlogPost/FamilyUpdate/PostStatus; Visibility intact
- `grep -rnE "PostStatus" src/` returns 0 (Pitfall 8)
- `npm run build` exits 0; `npm test -- --run` exits 0
- Dashboard posts/updates route dirs absent; post/update actions absent from dashboard-actions.ts
</verification>

<success_criteria>
- Dashboard posts/updates CRUD routes deleted (PRUNE-04)
- Post/update server actions + QuickUpdateDialog removed (PRUNE-04)
- BlogPost + FamilyUpdate models + PostStatus enum dropped via versioned migration; client regenerated (PRUNE-03)
- D-01 verify-then-drop honored (counts checked; dumps written if non-empty)
- Build + tests green
</success_criteria>

<output>
Create `.planning/phases/32-prune-dashboard-cleanup/32-02-SUMMARY.md` when done
</output>
