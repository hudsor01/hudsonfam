---
phase: 32-prune-dashboard-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["32-02"]
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/components/dashboard/app-sidebar.tsx
  - src/app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "Dashboard navigation shows only Overview, Photos, Events (+ owner: Members, Memorial) — no Posts or Updates"
    - "The dashboard overview shows only surviving content — 3-stat grid (Photos, Albums, Events), Recent Photos, Upcoming Events"
    - "No blog/updates counts, cards, widgets, or quick actions remain on the overview"
    - "Shared dashboard primitives (CollapsibleCard, Card) are reused; no orphaned dead components"
    - "npm run build and npm test pass"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "navLinks with Posts + Updates removed"
    - path: "src/components/dashboard/app-sidebar.tsx"
      provides: "iconMap with Posts/Updates removed; FileText + Bell imports gone"
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Consolidated overview: 3-stat grid + Recent Photos + Upcoming Events"
      contains: "Recent Photos"
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "/api/images"
      via: "Recent Photos thumbnail proxy"
      pattern: "api/images/.*size=thumbnail"
    - from: "src/components/dashboard/app-sidebar.tsx"
      to: "iconMap"
      via: "Posts/Updates keys removed"
      pattern: "iconMap"
---

<objective>
Consolidate the surviving dashboard (D-03): remove the Posts/Updates nav entries (DASH-01), rework the overview to reflect only surviving content with a 3-stat grid + a new Recent Photos card + the existing Upcoming Events card (DASH-02), and clean dead icon imports so no orphaned components/keys remain (DASH-03). Per the UI-SPEC dashboard overview contract.

Purpose: A coherent dashboard after the prune — not just two deleted pages — built on the de-blogged overview base from Plan 02.
Output: Cleaned nav (layout + sidebar iconMap) and a reworked `/dashboard` overview using shared primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/32-prune-dashboard-cleanup/32-CONTEXT.md
@.planning/phases/32-prune-dashboard-cleanup/32-RESEARCH.md
@.planning/phases/32-prune-dashboard-cleanup/32-PATTERNS.md
@.planning/phases/32-prune-dashboard-cleanup/32-UI-SPEC.md
@.planning/phases/32-prune-dashboard-cleanup/32-VALIDATION.md

@.planning/phases/32-prune-dashboard-cleanup/32-02-SUMMARY.md

<scope_note>
Depends on Plan 02 — which removed the dashboard posts/updates routes, post/update server actions, and the overview's `prisma.blogPost`/`prisma.familyUpdate` queries (minimal de-blog of the data layer). This plan reworks the overview PRESENTATION on that de-blogged base: stats grid, Recent Photos card, copy. Build + tests are already green entering this plan. UI scope is dashboard-only: do NOT touch the public navbar/footer (Phase 35) or add new color tokens / shadcn components (UI-SPEC §Phase 32 Scope Boundary). Reuse existing primitives; create no net-new components.
</scope_note>

<interfaces>
Shared primitives available (all in src/components/dashboard/) — use directly, no exploration needed:
- CollapsibleCard (collapsible-card.tsx): title prop + content slot; header px-5 py-3, do not restyle
- Card (src/components/ui/card.tsx): `<Card hover padding="md">` — existing stat-card pattern
- AppSidebar (app-sidebar.tsx): consumes navLinks prop; iconMap maps label → lucide icon

Image proxy URL pattern (PATTERNS.md §Shared Patterns): `src={`/api/images/${photo.id}?size=thumbnail`}` with `unoptimized` on `<Image>`, 200×200 for dashboard thumbnails.

Recent Photos query (UI-SPEC + PATTERNS.md): `prisma.photo.findMany({ orderBy: { createdAt: "desc" }, take: 4, select: { id: true, thumbnailPath: true, createdAt: true, album: { select: { name: true } } } })`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Posts/Updates from dashboard nav (DASH-01)</name>
  <files>src/app/(dashboard)/layout.tsx, src/components/dashboard/app-sidebar.tsx</files>
  <read_first>
    - .planning/phases/32-prune-dashboard-cleanup/32-PATTERNS.md §`src/app/(dashboard)/layout.tsx` (navLinks before/after) + §`src/components/dashboard/app-sidebar.tsx` (iconMap + lucide imports before/after)
    - .planning/phases/32-prune-dashboard-cleanup/32-UI-SPEC.md §"Dashboard Sidebar Nav (DASH-01)"
    - src/app/(dashboard)/layout.tsx
    - src/components/dashboard/app-sidebar.tsx
  </read_first>
  <action>
    `src/app/(dashboard)/layout.tsx`: remove `{ href: "/dashboard/posts", label: "Posts" }` and `{ href: "/dashboard/updates", label: "Updates" }` from the `navLinks` array. Resulting base navLinks: Overview, Photos, Events; the owner-only push of Members + Memorial is unchanged. Leave Suspense, DashboardShell, requireRole, SidebarProvider untouched.
    `src/components/dashboard/app-sidebar.tsx`: remove the `Posts: FileText` and `Updates: Bell` keys from `iconMap`; remove the now-unused `FileText` and `Bell` lucide imports. Surviving iconMap keys: Overview, Photos, Events, Services, Members, Memorial. Leave active-state logic, user popover, and sign-out untouched. Do NOT change `sidebar-*` tokens or active-item styling.
  </action>
  <verify>
    <automated>grep -nE '"Posts"|"Updates"|dashboard/posts|dashboard/updates' "src/app/(dashboard)/layout.tsx" | grep -vc '^#' | grep -qx 0 && grep -nE 'FileText|Bell|Posts:|Updates:' src/components/dashboard/app-sidebar.tsx | grep -vc '^#' | grep -qx 0 && echo NAV_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '"Posts"|"Updates"|dashboard/posts|dashboard/updates' "src/app/(dashboard)/layout.tsx"` (comments filtered) returns 0
    - `grep -nE 'FileText|Bell|Posts:|Updates:' src/components/dashboard/app-sidebar.tsx` (comments filtered) returns 0
    - navLinks base = Overview, Photos, Events; owner push of Members + Memorial intact
    - the command in <verify> prints `NAV_CLEAN`
  </acceptance_criteria>
  <done>Dashboard nav (layout + sidebar iconMap) lists only surviving areas; no dead FileText/Bell imports; no Posts/Updates entries.</done>
</task>

<task type="auto">
  <name>Task 2: Rework dashboard overview — 3-stat grid + Recent Photos card (DASH-02, DASH-03)</name>
  <files>src/app/(dashboard)/dashboard/page.tsx</files>
  <read_first>
    - .planning/phases/32-prune-dashboard-cleanup/32-PATTERNS.md §`src/app/(dashboard)/dashboard/page.tsx` (full edit pattern: imports, queries, stats grid, quick actions, Recent Photos CollapsibleCard, empty states) + §"Shared Patterns" (image proxy URL, CollapsibleCard row/empty, stat card)
    - .planning/phases/32-prune-dashboard-cleanup/32-UI-SPEC.md §"Dashboard Overview Layout Contract" + §"Copywriting Contract"
    - src/app/(dashboard)/dashboard/page.tsx (post-Plan-02 de-blogged base — blog/update queries already removed)
    - src/app/(dashboard)/dashboard/quick-actions.tsx (QuickEventDialog is the surviving dialog)
  </read_first>
  <action>
    Rework the overview presentation on the de-blogged base from Plan 02, per UI-SPEC §Dashboard Overview Layout Contract.
    QUERIES: ensure `Promise.all` fetches only `prisma.photo.count()`, `prisma.album.count()`, `prisma.event.count()`; add the Recent Photos query (`prisma.photo.findMany` with `orderBy createdAt desc`, `take: 4`, selecting id/thumbnailPath/createdAt/album.name); keep the `upcomingEvents` query. Add `import Image from "next/image"`. Confirm `QuickUpdateDialog` is not imported (only `QuickEventDialog`).
    STATS GRID (DASH-02): a 3-entry `stats` array — Photos → `/dashboard/photos`, Albums → `/dashboard/photos/albums`, Events → `/dashboard/events`. Grid wrapper drops `@lg:grid-cols-5`, uses `grid grid-cols-2 @sm:grid-cols-3 gap-4`. Reuse the existing `<Card hover padding="md" className="text-center">` stat-card pattern (NOT MetricCard — keep the existing pattern to avoid scope expansion). Copy labels exactly: "Photos", "Albums", "Events".
    QUICK ACTIONS (DASH-02): keep Upload Photos (`/dashboard/photos/upload`), New Album (`/dashboard/photos/albums/new`), New Event (`/dashboard/events/new`), and `<QuickEventDialog />`. Confirm the New Post / New Update buttons and `<QuickUpdateDialog />` are gone.
    RECENT PHOTOS CARD (DASH-02): replace the old Recent Posts area with `<CollapsibleCard title="Recent Photos">`. When `recentPhotos.length === 0`, render the empty state (heading "No photos yet" / body "Upload your first photo to get started."). Otherwise a 4-col thumbnail grid using `<Image src={`/api/images/${photo.id}?size=thumbnail`} ... unoptimized width=200 height=200 />` linking to `/dashboard/photos`. Keep `<CollapsibleCard title="Upcoming Events">`; upgrade its empty state to the spec form (heading "No upcoming events" / body "Create an event to see it here."). Page title "Dashboard" + subtitle "Manage family content" unchanged.
  </action>
  <verify>
    <automated>grep -nE 'blogPost|familyUpdate|QuickUpdateDialog|Recent Posts' "src/app/(dashboard)/dashboard/page.tsx" | grep -vc '^#' | grep -qx 0 && grep -q "Recent Photos" "src/app/(dashboard)/dashboard/page.tsx" && grep -q "api/images/" "src/app/(dashboard)/dashboard/page.tsx" && npm run build && npm test -- --run && echo OVERVIEW_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'blogPost|familyUpdate|QuickUpdateDialog|Recent Posts' "src/app/(dashboard)/dashboard/page.tsx"` (comments filtered) returns 0
    - overview contains a `Recent Photos` CollapsibleCard rendering `/api/images/${photo.id}?size=thumbnail` thumbnails
    - stats grid has exactly 3 cards (Photos, Albums, Events) and uses `@sm:grid-cols-3` (not `@lg:grid-cols-5`)
    - empty-state copy matches UI-SPEC ("No photos yet" / "Upload your first photo to get started."; "No upcoming events" / "Create an event to see it here.")
    - `npm run build` exits 0 and `npm test -- --run` exits 0
    - the command in <verify> prints `OVERVIEW_OK`
  </acceptance_criteria>
  <done>Overview reflects only surviving content (3 stats + Recent Photos + Upcoming Events), uses shared primitives, no blog/update remnants; build + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard server component → /api/images proxy | Recent Photos thumbnails are fetched via the existing R2 image proxy; photo ids come from the authed dashboard query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Information Disclosure | Recent Photos card on `/dashboard` | accept | The overview is already behind `requireRole(["owner","admin","member"])` in `(dashboard)/layout.tsx`; the Recent Photos query and `/api/images` proxy reuse the existing authenticated dashboard surface — no new exposure. |
| T-32-06 | Tampering | dashboard nav iconMap edit | accept | Pure removal of two static map keys + their icon imports; no user input, no behavior change beyond fewer links. Net reduction in surface. |
</threat_model>

<verification>
- `grep -nE '"Posts"|"Updates"|dashboard/posts|dashboard/updates' "src/app/(dashboard)/layout.tsx"` returns 0
- `grep -nE 'blogPost|familyUpdate' "src/app/(dashboard)/dashboard/page.tsx"` returns 0
- Overview renders 3-stat grid + Recent Photos + Upcoming Events with spec copy
- `npm run build` exits 0; `npm test -- --run` exits 0
</verification>

<success_criteria>
- Dashboard nav lists only Overview, Photos, Events (+ owner: Members, Memorial) — DASH-01
- Overview shows only surviving content: 3 stats, Recent Photos, Upcoming Events — DASH-02
- Shared primitives reused, no dead icon imports or orphaned components — DASH-03
- Build + tests green
</success_criteria>

<output>
Create `.planning/phases/32-prune-dashboard-cleanup/32-03-SUMMARY.md` when done
</output>
