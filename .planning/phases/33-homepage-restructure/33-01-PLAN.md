---
phase: 33-homepage-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/lib/recipes.test.ts
  - src/lib/featured-recipes.ts
  - src/components/public/hero.tsx
  - src/app/(public)/page.tsx
  - src/components/public/sidebar.tsx
  - src/components/public/weather-widget.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03]

must_haves:
  truths:
    - "A visitor sees the Recipes entry point (Browse Recipes CTA + RecipeSearch) above the fold, before Photos and Events"
    - "Up to 6 curated featured recipe cards render with category + title, each linking to /recipes/[slug]; unresolved slugs are silently skipped"
    - "The Photos section renders live photo thumbnails via PhotoGridPreview, or the 'No photos yet' empty state"
    - "The Events section renders live upcoming events, or the 'No upcoming events' empty state"
    - "D-06: The homepage has zero blog/updates imports and renders correctly with content/blog/ and BlogPost/FamilyUpdate absent"
    - "npm run build exits 0 and npm test is green"
  artifacts:
    - path: "src/lib/featured-recipes.ts"
      provides: "FEATURED_RECIPE_SLUGS curated constant (6 verified published slugs)"
      contains: "FEATURED_RECIPE_SLUGS"
    - path: "src/components/public/hero.tsx"
      provides: "Recipes-forward Hero with index prop + CTA row (Browse Recipes + RecipeSearch)"
      contains: "RecipeSearch"
    - path: "src/app/(public)/page.tsx"
      provides: "Recipes-first homepage: Hero -> Recipes -> Photos -> Events, no Sidebar"
      contains: "FEATURED_RECIPE_SLUGS"
    - path: "src/__tests__/lib/recipes.test.ts"
      provides: "Wave-0 regression test asserting every FEATURED_RECIPE_SLUGS entry resolves in getRecipeIndex()"
      contains: "FEATURED_RECIPE_SLUGS"
  key_links:
    - from: "src/app/(public)/page.tsx"
      to: "getRecipeIndex"
      via: "Promise.all server fetch then passed to Hero/RecipeSearch"
      pattern: "getRecipeIndex"
    - from: "src/components/public/hero.tsx"
      to: "RecipeSearch"
      via: "index prop passed Server -> Client leaf"
      pattern: "RecipeSearch index=\\{index\\}"
    - from: "src/app/(public)/page.tsx"
      to: "FEATURED_RECIPE_SLUGS"
      via: "index.find slug lookup, zero extra I/O"
      pattern: "FEATURED_RECIPE_SLUGS"
---

<objective>
Rebuild the public homepage (`src/app/(public)/page.tsx`) to lead with Grandma Hudson's Recipes (Hero CTA + RecipeSearch + featured card row) and surface live Photos and Events as full-width stacked sections, retiring the orphaned `Sidebar` rail and the `WeatherWidget` it carried. Covers HOME-01, HOME-02, HOME-03.

Purpose: The post-Phase-32 homepage is `Hero` + an orphaned `Sidebar` (events/photos rail) floating in a `max-w-7xl` container — the blog featured/recent sections were already stripped. This phase builds the real recipes-first homepage on that clean base, with zero dependency on the removed blog/updates subsystems.

Output:
- New `src/lib/featured-recipes.ts` exporting `FEATURED_RECIPE_SLUGS` (6 verified published slugs).
- Reworked `src/components/public/hero.tsx` (recipes-forward subcopy + `index` prop + CTA row).
- Rewritten `src/app/(public)/page.tsx` (Promise.all fetch, featured-recipe resolution, 4 stacked full-width sections).
- Deleted `src/components/public/sidebar.tsx` and `src/components/public/weather-widget.tsx`.
- New Wave-0 unit test guarding the curated featured slugs against the published recipe index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-homepage-restructure/33-CONTEXT.md
@.planning/phases/33-homepage-restructure/33-RESEARCH.md
@.planning/phases/33-homepage-restructure/33-PATTERNS.md
@.planning/phases/33-homepage-restructure/33-UI-SPEC.md
@.planning/phases/33-homepage-restructure/33-VALIDATION.md

<interfaces>
<!-- Key contracts extracted from the codebase. Use these directly — no further exploration needed. -->

From src/lib/recipes.ts:
```typescript
export type RecipeIndexEntry = { slug: string; title: string; category: string };
export async function getRecipeIndex(): Promise<RecipeIndexEntry[]>; // pure fs read of published MDX frontmatter; safe in a Server Component
```

From src/components/public/recipe-search.tsx (DO NOT MODIFY — "use client"):
```typescript
interface RecipeSearchProps { index: RecipeIndexEntry[]; }
export function RecipeSearch({ index }: RecipeSearchProps): JSX.Element;
// Visible trigger button: inline-flex ... min-h-11 w-full sm:w-auto sm:max-w-xs; Cmd/Ctrl+K opens CommandDialog.
```

From src/components/ui/section-header.tsx (DO NOT MODIFY this phase — see SectionHeader note below):
```typescript
interface SectionHeaderProps extends HTMLAttributes<HTMLDivElement> {
  label?: string; title?: string; subtitle?: string;
  action?: { text: string; href: string };
}
// label mode renders: <h3 className="text-xs font-sans font-semibold tracking-[3px] text-primary uppercase">{label}</h3>
```

From src/components/public/photo-grid-preview.tsx (DO NOT MODIFY):
```typescript
// Accepts photos: { id: string; thumbnailPath: string; title: string | null }[]
// Renders 3-col aspect-square thumbnails via /api/images/${id}?size=thumbnail, links to /photos.
```
</interfaces>

<analogs>
<!-- Concrete analog files the executor MUST read before editing. -->
- `src/app/(public)/recipes/page.tsx` (lines 21–51) — the EXACT `const index = await getRecipeIndex()` then `<RecipeSearch index={index} />` Server→Client prop-pass pattern. Replicate it.
- `src/components/public/sidebar.tsx` — current events + photos render blocks and empty states. Lift these into `page.tsx`; the file is then DELETED.
- `src/app/(public)/page.tsx` (current) — keep the two Prisma queries verbatim; wrap them in Promise.all; remove the `sidebarEvents`/`sidebarPhotos` shim maps.
</analogs>

<section_header_note>
KNOWN DEVIATION (do not fix this phase): `SectionHeader`'s label `<h3>` hardcodes `text-xs`, while UI-SPEC §Typography lists `text-sm` for section labels. `SectionHeader` is shared (dashboard + public); changing it affects all usages. ACCEPT `text-xs` for Phase 33. Do NOT edit `section-header.tsx`. Documented as a Phase 35 candidate. (RESEARCH.md Open Question 1.)
</section_header_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 featured-slug regression test + FEATURED_RECIPE_SLUGS constant</name>
  <files>src/__tests__/lib/recipes.test.ts, src/lib/featured-recipes.ts</files>
  <read_first>
    - `src/lib/featured-recipes.ts` — does NOT exist yet (verified); you are creating it.
    - `src/__tests__/lib/recipes.test.ts` — existing recipes lib test file (imports `vitest` `describe/it/expect`, imports from `@/lib/recipes`). Append a new `describe` block here.
    - `src/lib/recipes.ts` lines 234–241 — `getRecipeIndex()` returns `RecipeIndexEntry[] = { slug, title, category }[]` from published MDX. Type `RecipeIndexEntry` at lines 188–192.
    - Analog test style: existing blocks in `recipes.test.ts` (e.g. `describe("filterByVisibility", ...)`).
    - VALIDATION.md §Wave 0 Requirements and §Per-Requirement Verification Map (HOME-01 unit row).
  </read_first>
  <behavior>
    - Test: `await getRecipeIndex()` then assert `FEATURED_RECIPE_SLUGS.every(s => index.some(e => e.slug === s))` is true (every curated slug resolves to a published recipe).
    - Test: assert `FEATURED_RECIPE_SLUGS.length` is between 1 and 6 inclusive (guards against an empty or oversized curated list).
    - Test: each resolved entry exposes non-empty `title` and `category` strings (the card needs both).
  </behavior>
  <action>
    Create `src/lib/featured-recipes.ts` exporting `export const FEATURED_RECIPE_SLUGS: string[]` with exactly these 6 verified-published slugs in order: `adas-famous-gingerbread`, `almond-cookies`, `coleslaw`, `shepherds-pie`, `jelly-roll`, `baking-powder-biscuits` (per D-02; all confirmed `status: "published"` in `content/recipes/`). Add a top-of-file comment that the family edits this list to change featured recipes and every slug must match a published file in `content/recipes/`.

    In `src/__tests__/lib/recipes.test.ts`, add a `describe("FEATURED_RECIPE_SLUGS", ...)` block importing `getRecipeIndex` from `@/lib/recipes` and `FEATURED_RECIPE_SLUGS` from `@/lib/featured-recipes`. Add the three assertions from `<behavior>`. Use an async `it` that awaits `getRecipeIndex()` once and reuses the result across assertions. This is the VALIDATION.md Wave-0 gap — a regression guard so a future content prune cannot silently break the homepage featured row.

    Do not introduce randomness, sorting, or runtime filesystem reads in `featured-recipes.ts` — it is a static constant module only (no hydration risk, per D-02).
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/hudsonfam && npx vitest run src/__tests__/lib/recipes.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/lib/featured-recipes.ts` succeeds (file exists).
    - `grep -q "FEATURED_RECIPE_SLUGS" src/lib/featured-recipes.ts` exits 0 and all 6 slug strings are present.
    - `npx vitest run src/__tests__/lib/recipes.test.ts` exits 0 with the new `FEATURED_RECIPE_SLUGS` describe block passing.
    - `grep -q "FEATURED_RECIPE_SLUGS" src/__tests__/lib/recipes.test.ts` exits 0.
  </acceptance_criteria>
  <done>`src/lib/featured-recipes.ts` exports the 6-slug constant; the new recipes-test block asserts all 6 resolve in `getRecipeIndex()` with non-empty title+category and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rework Hero — recipes-forward copy, index prop, CTA row</name>
  <files>src/components/public/hero.tsx</files>
  <read_first>
    - `src/components/public/hero.tsx` (current, 15 lines) — pure Server Component, no props, no hooks. Eyebrow `text-xs tracking-[4px]`, H1, blog-era subcopy "Stories, photos, and life updates from our corner of the world".
    - `src/app/(public)/recipes/page.tsx` lines 46–50 — the `{index.length > 0 && (...<RecipeSearch index={index} />...)}` composition pattern.
    - `src/components/public/recipe-search.tsx` lines 44–67 — the existing trigger classes (already `min-h-11 w-full sm:w-auto sm:max-w-xs`); DO NOT modify RecipeSearch.
    - 33-UI-SPEC.md §"1. Hero Section" + §Copywriting Contract — exact element classes and copy.
    - 33-PATTERNS.md §`src/components/public/hero.tsx` — the before/after class diffs and CTA-row JSX.
  </read_first>
  <action>
    Convert `Hero` to accept a prop: `interface HeroProps { index: RecipeIndexEntry[] }` then `export function Hero({ index }: HeroProps)`. Import `Link` from `next/link`, `RecipeSearch` from `@/components/public/recipe-search`, and `type RecipeIndexEntry` from `@/lib/recipes`. Hero stays a Server Component (no `"use client"`); RecipeSearch is the client leaf rendered inside it — this is a Server Component rendering a Client Component child, NOT a server/client violation.

    Make exactly these edits per UI-SPEC §1:
    1. Eyebrow `<p>`: change `text-xs tracking-[4px]` to `text-sm tracking-[4px]` (keep `text-accent mb-3 font-sans uppercase` and the `Est. 2024 • Dallas, Texas` copy).
    2. Keep H1 unchanged: `The Hudson Family` with `text-4xl sm:text-5xl font-serif text-foreground font-normal mb-3 text-balance`.
    3. Replace the subcopy text with the exact UI-SPEC line: `Grandma Hudson's recipes, family photos, and moments that matter — all in one place.` (use `&rsquo;` for the apostrophe and `&mdash;` for the em dash). Set the subcopy class to `text-muted-foreground italic text-base max-w-xl mx-auto text-pretty mb-8` (drop the `sm:text-lg`, add `mb-8` per UI-SPEC). The string "life updates" MUST NOT remain anywhere in the file.
    4. Add a CTA row `<div className="flex flex-col sm:flex-row items-center justify-center gap-3 mt-2">` containing: a primary `<Link href="/recipes">` "Browse Recipes" button classed `bg-primary text-primary-foreground px-6 py-2.5 rounded-lg text-sm font-sans font-semibold hover:bg-primary/90 transition-colors min-h-11 inline-flex items-center`; and `{index.length > 0 && <RecipeSearch index={index} />}`.

    Keep the `<section>` wrapper classes `text-center py-16 sm:py-20 px-5 motion-safe:animate-fade-in-up`. Use only globals.css theme tokens — no raw Tailwind color names (per CLAUDE.md). Reserve `bg-primary` for the CTA button and `text-accent` for the eyebrow per UI-SPEC §Color.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/hudsonfam && grep -q "RecipeSearch" src/components/public/hero.tsx && grep -q 'href="/recipes"' src/components/public/hero.tsx && ! grep -q "life updates" src/components/public/hero.tsx && grep -q "text-sm tracking-\[4px\]" src/components/public/hero.tsx && echo HERO_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "life updates" src/components/public/hero.tsx` returns non-zero (string is GONE).
    - `grep -q "RecipeSearch" src/components/public/hero.tsx` exits 0 and `grep -q "index" src/components/public/hero.tsx` exits 0 (Hero takes index prop + renders RecipeSearch).
    - `grep -q 'href="/recipes"' src/components/public/hero.tsx` exits 0 (Browse Recipes CTA present).
    - `grep -q "text-sm tracking-\[4px\]" src/components/public/hero.tsx` exits 0 (eyebrow corrected from text-xs to text-sm).
    - `grep -vE '^\s*//' src/components/public/hero.tsx | grep -E "text-(red|blue|green|yellow|gray|slate|amber|orange)-[0-9]"` returns non-zero (no raw Tailwind color names).
    - Hero file does NOT contain `"use client"` (stays a Server Component).
  </acceptance_criteria>
  <done>Hero accepts `index: RecipeIndexEntry[]`, renders the recipes-forward subcopy + Browse Recipes CTA + RecipeSearch, with the `text-sm` eyebrow correction and the blog-era "life updates" copy removed.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite page.tsx (4 stacked sections) + delete Sidebar & WeatherWidget atomically</name>
  <files>src/app/(public)/page.tsx, src/components/public/sidebar.tsx, src/components/public/weather-widget.tsx</files>
  <read_first>
    - `src/app/(public)/page.tsx` (current) — keep the `event.findMany` (PUBLIC, startDate gte now, asc, take 5, select id/title/startDate/location) and `photo.findMany` (createdAt desc, take 6, select id/thumbnailPath/title) queries verbatim. The `sidebarEvents`/`sidebarPhotos` shim maps (lines 37–48) are DELETED.
    - `src/components/public/sidebar.tsx` — lift the events `<ul>`/empty-state and photos `<PhotoGridPreview>`/empty-state blocks. NOTE: sidebar uses `event.date` (string) via `new Date(event.date)`; the homepage uses `event.startDate` (Date) directly via `new Date(event.startDate)` — the Prisma result is already a Date, no `.toISOString()` shim. The sidebar's date badge span uses `text-xs`; per UI-SPEC §4 the homepage event row uses `text-accent text-sm font-bold font-sans` and the secondary line uses `text-sm text-text-dim` (text-sm, not the sidebar's text-xs).
    - `src/app/(public)/recipes/page.tsx` lines 21–51 — Promise/await + `<RecipeSearch index={index} />` pattern.
    - 33-PATTERNS.md §`src/app/(public)/page.tsx` — full imports list, Promise.all shape, featured-resolution filter, the recipe-card JSX, photos-section JSX, events-section JSX, and full return skeleton.
    - 33-UI-SPEC.md §Page Structure Contract, §2 Recipes Feature Section, §3 Photos Section, §4 Events Section, §Responsive Behavior, §Copywriting Contract.
  </read_first>
  <action>
    Rewrite `src/app/(public)/page.tsx` as a Server Component (`export default async function HomePage()`, `await connection()`). Imports: `prisma` from `@/lib/prisma`; `connection` from `next/server`; `Hero` from `@/components/public/hero`; `Separator` from `@/components/ui/separator`; `SectionHeader` from `@/components/ui/section-header`; `PhotoGridPreview` from `@/components/public/photo-grid-preview`; `Link` from `next/link`; `getRecipeIndex` and `type RecipeIndexEntry` from `@/lib/recipes`; `FEATURED_RECIPE_SLUGS` from `@/lib/featured-recipes`. Do NOT import `Sidebar` or `WeatherWidget` — they are deleted in this same task.

    Fetch all three in parallel (no waterfall, per RESEARCH Pitfall 4): a single `const [events, photos, index] = await Promise.all([...])` where the array elements are, in order, the existing `prisma.event.findMany` query (where visibility PUBLIC + startDate gte now, orderBy startDate asc, take 5, select id/title/startDate/location), the existing `prisma.photo.findMany` query (orderBy createdAt desc, take 6, select id/thumbnailPath/title), and `getRecipeIndex()`.

    Resolve featured recipes with zero extra I/O (index already in memory, per RESEARCH Critical Finding 2): map `FEATURED_RECIPE_SLUGS` to `index.find((e) => e.slug === slug)` then `.filter((e): e is RecipeIndexEntry => e !== undefined)`. Do NOT call `getRecipeBySlug` — the index lookup is sufficient and avoids extra MDX parses.

    Render this exact section order (D-03): `<Hero index={index} />` → `<Separator />` → Recipes section → `<Separator />` → Photos section → `<Separator />` → Events section. Recipes precedes Photos precedes Events in source order (HOME-01 above-the-fold).

    Each of the three content sections is `<section className="max-w-5xl mx-auto px-4 sm:px-6 py-10 sm:py-14">` with a `SectionHeader` in label mode:
    - Recipes: `<SectionHeader label="RECIPES" action={{ text: "View all recipes", href: "/recipes" }} />`. Below it, render the featured cards only when `featuredRecipes.length > 0`, in a `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4`. Each card is a `<Link key={recipe.slug} href={\`/recipes/${recipe.slug}\`}>` wrapping a `<div className="bg-card border border-border rounded-xl p-4 hover:border-accent/40 hover:shadow-card transition-all group">` with a category `<p className="text-sm text-accent font-sans font-semibold tracking-[2px] uppercase mb-1.5">{recipe.category}</p>` and a title `<h3 className="text-lg font-serif text-foreground leading-snug text-balance group-hover:text-primary transition-colors">{recipe.title}</h3>`. If all featured slugs fail to resolve, hide the card grid — the section still renders its header (graceful skip per D-02).
    - Photos: `<SectionHeader label="PHOTOS" action={{ text: "View all photos", href: "/photos" }} />` then `<div className="bg-card border border-border rounded-xl p-5">` containing `{photos.length > 0 ? <PhotoGridPreview photos={photos} /> : <p className="text-sm text-text-dim italic">No photos yet</p>}`.
    - Events: `<SectionHeader label="EVENTS" action={{ text: "View all events", href: "/events" }} />` then `<div className="bg-card border border-border rounded-xl p-5">` containing, when `events.length > 0`, a `<ul className="space-y-3">` of `<li key={event.id} className="flex gap-3">` rows — each row a `flex-shrink-0 size-10 rounded-lg bg-accent/15` date badge holding `<span className="text-accent text-sm font-bold font-sans">` with the day number (`new Date(event.startDate).toLocaleDateString("en-US", { day: "numeric" })`), a title `<p className="text-sm text-foreground font-medium truncate">{event.title}</p>`, and a meta line `<p className="text-sm text-text-dim">` of `new Date(event.startDate).toLocaleDateString("en-US", { month: "short", day: "numeric" })` followed by `{event.location && \` • ${event.location}\`}`. Empty state: `<p className="text-sm text-text-dim italic">No upcoming events</p>`.

    Use `event.startDate` (Date) DIRECTLY — do NOT recreate the `sidebarEvents`/`sidebarPhotos` shim maps or convert to ISO strings. Do NOT use `EventCard` (incompatible interface, RESEARCH Pitfall 5). Do NOT keep the old `max-w-7xl` container — sections use `max-w-5xl`.

    THEN, in the SAME task (so the build never references a deleted file): delete `src/components/public/sidebar.tsx` and `src/components/public/weather-widget.tsx` (grep-confirmed: Sidebar imported only by page.tsx, WeatherWidget imported only by sidebar.tsx). Remove both files from the filesystem after the page.tsx rewrite so no commit boundary references a deleted module.

    Use only globals.css theme tokens — no raw Tailwind color names (CLAUDE.md). Do not pass event handlers Server→Client (none needed; RecipeSearch is a self-contained client leaf).
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/hudsonfam && grep -q "RecipeSearch\|Hero" "src/app/(public)/page.tsx" && grep -q "FEATURED_RECIPE_SLUGS" "src/app/(public)/page.tsx" && ! grep -qE "Sidebar|sidebarEvents|sidebarPhotos|WeatherWidget" "src/app/(public)/page.tsx" && ! test -f src/components/public/sidebar.tsx && ! test -f src/components/public/weather-widget.tsx && grep -q "No photos yet" "src/app/(public)/page.tsx" && grep -q "No upcoming events" "src/app/(public)/page.tsx" && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Hero" "src/app/(public)/page.tsx"` (RecipeSearch is embedded inside Hero, not imported by page.tsx directly) and `grep -q "FEATURED_RECIPE_SLUGS" "src/app/(public)/page.tsx"` both exit 0.
    - `grep -q "getRecipeIndex" "src/app/(public)/page.tsx"` and `grep -q "Promise.all" "src/app/(public)/page.tsx"` exit 0.
    - Recipes section appears BEFORE Photos and Events in source: `grep -n "RECIPES" / "PHOTOS" / "EVENTS"` line numbers are strictly ascending.
    - `grep -qE "Sidebar|sidebarEvents|sidebarPhotos|WeatherWidget" "src/app/(public)/page.tsx"` returns non-zero (shims + Sidebar/WeatherWidget references gone).
    - `test -f src/components/public/sidebar.tsx` and `test -f src/components/public/weather-widget.tsx` both fail (files deleted, ENOENT).
    - `grep -rnE "Sidebar|WeatherWidget" src/ --include="*.tsx" --include="*.ts"` shows no `public/sidebar` or `public/weather-widget` importers remaining (the shadcn `ui/sidebar` + dashboard `AppSidebar` matches are unrelated and allowed).
    - `grep -q "No photos yet" "src/app/(public)/page.tsx"` and `grep -q "No upcoming events" "src/app/(public)/page.tsx"` exit 0 (both empty states present).
    - `grep -q "startDate" "src/app/(public)/page.tsx"` exits 0 and `grep -q "toISOString" "src/app/(public)/page.tsx"` returns non-zero (uses Date directly, no string shim).
    - `npm run build` exits 0 with the new homepage and no console/build errors.
  </acceptance_criteria>
  <done>The homepage renders Hero → Recipes → Photos → Events as full-width stacked sections from live data with empty states, featured cards resolve via the in-memory index, Sidebar + WeatherWidget files are deleted with no remaining importers, and `npm run build` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → homepage Server Component | Anonymous public read; no auth, no user-supplied input on this page |
| Server Component → client leaf (RecipeSearch) | `index: RecipeIndexEntry[]` serialized across the RSC boundary (plain strings only) |
| /api/images proxy (within PhotoGridPreview) | Photo bytes streamed from R2 through the existing access-controlled proxy — unchanged this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | `prisma.event.findMany` on homepage | mitigate | Query is hard-filtered to `visibility: "PUBLIC"` (unchanged from current page.tsx); no private events leak to the anonymous homepage |
| T-33-02 | Information Disclosure | `prisma.photo.findMany` thumbnails | accept | Latest photos are intentionally public on the homepage; bytes flow through the existing access-controlled `/api/images` proxy (Phase 34 owns the rendering fix). No new exposure surface introduced here |
| T-33-03 | Tampering | `RecipeIndexEntry[]` crossing Server→Client | accept | Only plain `{slug,title,category}` strings cross the boundary; no Dates, no Prisma objects, no functions — no deserialization/hydration tampering vector. RecipeSearch already shipped (Phase 31) and is unmodified |
| T-33-04 | Denial of Service | `getRecipeIndex()` reading ~1003 MDX files per request | accept | Identical cost to the already-live `/recipes` page; `connection()` opts the route into dynamic rendering as it does today; `Promise.all` parallelizes the three fetches to avoid a waterfall. No new attack-amplification surface |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added this phase (RESEARCH §Package Legitimacy Audit: zero new deps). slopcheck N/A; no install tasks present |

No `high`-severity threats. This is a read-only public homepage rendering existing public data; no new auth surface and no new user input beyond the already-shipped client-side RecipeSearch filter.
</threat_model>

<verification>
After all tasks, the following must hold (VALIDATION.md §Per-Requirement Verification Map):

1. **Build green:** `npm run build` exits 0 — no import errors, no TypeScript errors, no console errors on homepage render.
2. **Full suite green:** `npm test` (maps to `vitest run`) exits 0, including the new `FEATURED_RECIPE_SLUGS` block.
3. **HOME-01 (recipes lead):** `grep -q "Hero" "src/app/(public)/page.tsx"` (RecipeSearch is embedded inside Hero) and Recipes section precedes Photos/Events in source; `grep -q "life updates" src/components/public/hero.tsx` returns non-zero (blog-era copy gone).
4. **HOME-02 (live photos/events + empty states):** `page.tsx` retains `event.findMany` and `photo.findMany`; "No photos yet" and "No upcoming events" both present in the render path.
5. **HOME-03 / D-06 (zero blog dep, build-green):** `grep -rnE "getAllPosts|FeaturedPost|PostCard|familyUpdate|lib/blog" "src/app/(public)/"` returns 0 matches; `ls src/components/public/sidebar.tsx src/components/public/weather-widget.tsx` both fail (ENOENT) and no remaining importers exist; `npm run build` exits 0 with no console errors (D-06).
</verification>

<success_criteria>
- `npm run build` exits 0; `npm test` green (incl. new featured-slug test).
- Homepage source renders Hero (recipes-forward) → Recipes (CTA + RecipeSearch + featured cards) → Photos → Events, full-width stacked, in that order.
- Up to 6 featured recipe cards render (title + category, linking to `/recipes/[slug]`); unresolved slugs silently skipped; if all fail the card grid is hidden but the section header remains.
- Photos and Events render live data or their intentional italic empty states.
- `src/lib/featured-recipes.ts`, reworked `hero.tsx`, and rewritten `page.tsx` exist; `sidebar.tsx` and `weather-widget.tsx` are deleted with zero remaining importers.
- Zero blog/updates imports in `(public)/`; no raw Tailwind color names introduced.
</success_criteria>

<output>
Create `.planning/phases/33-homepage-restructure/33-01-SUMMARY.md` when done.
</output>
