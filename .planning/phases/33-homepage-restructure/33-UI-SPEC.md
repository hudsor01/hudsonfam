---
phase: 33
slug: homepage-restructure
status: draft
shadcn_initialized: true
preset: new-york
created: 2026-06-02
---

# Phase 33 — UI Design Contract: Homepage Restructure

> Visual and interaction contract for the recipes-first homepage. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | new-york (existing `components.json`) |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font | Serif: Georgia/Times New Roman (`--font-serif`); Sans: system-ui (`--font-sans`) |

Source: `components.json` + `src/styles/globals.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, date badge internal padding |
| sm | 8px | Between meta lines (date + location in event rows), card inner gaps |
| md | 16px | Section horizontal padding (mobile), card internal padding |
| lg | 24px | Vertical gap between sections on mobile, card padding on desktop (`p-5` = 20px maps here) |
| xl | 32px | Vertical section spacing on sm+ |
| 2xl | 48px | Hero vertical padding (mobile `py-16` = 64px; desktop `py-20` = 80px — treated as 2xl+ zone) |
| 3xl | 64px | Hero top/bottom padding on desktop (`sm:py-20` = 80px; nearest token at 64px) |

Exceptions:
- Hero `py-16` (64px) / `sm:py-20` (80px) — inherits existing hero.tsx rhythm; keep as-is
- Touch targets on RecipeSearch trigger: min-height 44px (`min-h-11`) — existing component, keep
- Date badge in event rows: `size-10` (40px) square — existing pattern from sidebar.tsx, keep

---

## Typography

| Role | Size | Weight | Line Height | Font | Token |
|------|------|--------|-------------|------|-------|
| Body / card meta | 14px | 400 (regular) | 1.5 | sans | `text-sm` |
| Body / section text | 16px | 400 (regular) | 1.5 | sans | `text-base` |
| Section label / eyebrow | 12px | 600 (semibold) | 1.0 | sans, uppercase, tracking-[3px] | `text-xs font-semibold tracking-[3px] uppercase` |
| Recipe card title / event title | 18–20px | 400 (regular) | 1.2 | serif | `text-lg font-serif` or `text-xl font-serif` |
| Hero h1 | 36px / 48px (sm) | 400 (regular) | 1.2 | serif | `text-4xl sm:text-5xl font-serif` |

Rules:
- Only two weights in use: 400 (regular) and 600 (semibold). No 500/700.
- Serif font (`--font-serif`) for all headings: Hero h1, recipe card titles, event card titles.
- Sans font (`--font-sans`) for all body copy, labels, metadata, section eyebrows.
- `text-balance` on all headings; `text-pretty` on all body paragraphs with >1 line.
- Eyebrow / section label: `text-xs font-sans font-semibold tracking-[3px] text-primary uppercase` — matches existing SectionHeader `label` mode.

Source: existing hero.tsx, sidebar.tsx, section-header.tsx, event-card.tsx patterns

---

## Color

| Role | Token | Light value | Usage |
|------|-------|-------------|-------|
| Dominant (60%) | `background` | `#faf4ea` (linen) | Page background, section backgrounds |
| Secondary (30%) | `card` | `#fffdf8` (warm white) | All cards: recipe cards, event row container, photos section container |
| Accent (10%) | `accent` | `#d9a24b` (mustard gold) | See reserved list below |
| Destructive | `destructive` | `#b23a2e` (warm brick red) | Not used in this phase (read-only homepage) |
| Muted text | `muted-foreground` | `#7a6a5a` | Section subcopy, card body text, location labels |
| Dim text | `text-dim` | `#9a8c7c` | Timestamps, metadata lines, empty state italic copy |
| Primary | `primary` | `#c0673f` (terracotta) | Section labels (SectionHeader), Hero CTA button bg |
| Border | `border` | `#eadfce` | Card borders, section container borders |

Accent (`text-accent` / `bg-accent/10` / `bg-accent/15`) reserved for:
1. Recipe category badges on featured recipe cards
2. Date display text inside event rows (`text-accent text-sm`)
3. Date icon inside EventCard (`text-accent`)
4. Relative-label badge on EventCard (`text-xs text-accent bg-accent/10 px-2.5 py-1 rounded-full`)
5. Section label eyebrow in Hero (`text-xs tracking-[4px] text-accent` — existing eyebrow "Est. 2024 • Dallas, Texas")

Primary (`text-primary` / `bg-primary` / `bg-primary/10`) reserved for:
1. SectionHeader `label` text (all three section labels: RECIPES / PHOTOS / EVENTS)
2. "Browse Recipes" CTA button background (`bg-primary text-primary-foreground`)
3. RecipeSearch trigger hover border (`hover:border-primary/40`)

No new colors. No raw Tailwind color names. Zero hardcoded hex values in `.tsx` files.

Source: `src/styles/globals.css` `:root` block; CLAUDE.md color system rules

---

## Page Structure Contract

Single-column, full-width, stacked. Order locked by D-03:

```
<page body>
  Hero section          ← reworked per D-04; full-width, centered
  ── Separator ──
  Recipes section       ← HOME-01; CTA + RecipeSearch + featured card row
  ── Separator ──
  Photos section        ← HOME-02; PhotoGridPreview full-width + empty state
  ── Separator ──
  Events section        ← HOME-02; event list full-width + empty state
</page body>
```

**No sidebar rail.** The 320px `<aside>` from Sidebar is retired. All four sections are full-width blocks inside a shared `max-w-5xl mx-auto px-4 sm:px-6` container (or per-section containers — see below).

**Section container pattern** (applies to Recipes, Photos, Events):
```
<section className="max-w-5xl mx-auto px-4 sm:px-6 py-10 sm:py-14">
  <SectionHeader label="..." action={{ text: "View all", href: "..." }} />
  {/* section content */}
</section>
```

**Hero** remains full-width (no `max-w` wrapper) per existing pattern.

---

## Section-by-Section Visual Contract

### 1. Hero Section

**Source:** Rework of `src/components/public/hero.tsx` per D-04

Layout: centered, `text-center`, full-width, `py-16 sm:py-20 px-5`, `motion-safe:animate-fade-in-up`

Elements (top to bottom):
1. **Eyebrow** — `text-xs tracking-[4px] text-accent font-sans uppercase mb-3` → copy: `Est. 2024 • Dallas, Texas`
2. **H1** — `text-4xl sm:text-5xl font-serif text-foreground font-normal mb-3 text-balance` → copy: `The Hudson Family`
3. **Subcopy** — `text-muted-foreground italic text-base sm:text-lg max-w-xl mx-auto text-pretty mb-8` → copy: `Grandma Hudson's recipes, family photos, and moments that matter — all in one place.` (replaces blog-era "Stories, photos, and life updates...")
4. **CTA row** — `flex flex-col sm:flex-row items-center justify-center gap-3 mt-2`
   - Primary button: `Browse Recipes` → `/recipes` — `bg-primary text-primary-foreground px-6 py-2.5 rounded-lg text-sm font-sans font-semibold hover:bg-primary/90 transition-colors min-h-11`
   - `RecipeSearch` trigger — existing component, rendered inline; already `min-h-11 w-full sm:w-auto sm:max-w-xs`

The RecipeSearch receives the recipe index passed from the Server Component parent (same pattern as `/recipes` page). No separate fetch needed — `getRecipeIndex()` call in `page.tsx`.

---

### 2. Recipes Feature Section

**Locked by D-01 / D-02**

SectionHeader: `label="RECIPES"` + `action={{ text: "View all", href: "/recipes" }}`

Featured card row:
- **4–6 curated recipe cards** resolved from `FEATURED_RECIPE_SLUGS` constant (defined in `page.tsx` or a co-located constants file, e.g. `src/lib/featured-recipes.ts`). Default slugs chosen at implementation time from known crowd-pleaser categories (e.g. Cakes, Salads, Main Dishes, Soups).
- **Grid:** `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4` (2-up mobile, 3-up tablet, 4-up desktop)
- **Each card** — `<Link href="/recipes/[slug]">` wrapping:
  ```
  <div className="bg-card border border-border rounded-xl p-4 hover:border-accent/40 hover:shadow-card transition-all group">
    <p className="text-xs text-accent font-sans font-semibold tracking-[2px] uppercase mb-1.5">
      {recipe.category}
    </p>
    <h3 className="text-base font-serif text-foreground leading-snug text-balance group-hover:text-primary transition-colors">
      {recipe.title}
    </h3>
  </div>
  ```
- **Graceful skip:** if a slug doesn't resolve (returns `undefined` from `getRecipeBySlug`), the card is simply not rendered — no error, no placeholder.
- Cards that don't resolve are silently filtered; if all 6 fail, the entire featured row is hidden (section still renders with CTA + search).

---

### 3. Photos Section

**Locked by D-05**

SectionHeader: `label="PHOTOS"` + `action={{ text: "View all", href: "/photos" }}`

Container: `bg-card border border-border rounded-xl p-5`

Content (photos.length > 0):
- Render `<PhotoGridPreview photos={photos} />` — existing component unchanged
- Grid is 3-col, aspect-square thumbnails, links to `/photos`, group-hover opacity

Empty state (photos.length === 0):
- Copy: `<p className="text-sm text-text-dim italic">No photos yet</p>`
- No action link, no placeholder slots — just the italic dim text

Source: existing Sidebar pattern, D-05

---

### 4. Events Section

**Locked by D-05**

SectionHeader: `label="EVENTS"` + `action={{ text: "View all", href: "/events" }}`

Container: `bg-card border border-border rounded-xl p-5`

Content (events.length > 0):
- `<ul className="space-y-3">` with one `<li>` per event
- Each row — existing Sidebar event row pattern:
  ```
  <li className="flex gap-3">
    <div className="flex-shrink-0 size-10 rounded-lg bg-accent/15 flex items-center justify-center">
      <span className="text-accent text-xs font-bold font-sans">
        {day number}
      </span>
    </div>
    <div className="min-w-0">
      <p className="text-sm text-foreground font-medium truncate">{title}</p>
      <p className="text-xs text-text-dim">{month short + day} • {location}</p>
    </div>
  </li>
  ```
- Keep existing `event.findMany` query: `visibility: "PUBLIC"`, `startDate gte now`, `orderBy asc`, `take: 5`

Empty state (events.length === 0):
- Copy: `<p className="text-sm text-text-dim italic">No upcoming events</p>`
- No action link

Source: existing Sidebar pattern, D-05

---

## Interaction Contract

| Interaction | Behavior |
|-------------|----------|
| "Browse Recipes" CTA click | Navigate to `/recipes` (Next.js `<Link>`) |
| RecipeSearch trigger click | Opens cmdk CommandDialog (existing RecipeSearch behavior) |
| Cmd/Ctrl+K (anywhere on page) | Opens RecipeSearch dialog (existing global keybind) |
| Featured recipe card click | Navigate to `/recipes/[slug]` |
| "View all" → /recipes | Navigate to `/recipes` index |
| "View all" → /photos | Navigate to `/photos` index |
| "View all" → /events | Navigate to `/events` index |
| PhotoGridPreview click | Navigate to `/photos` (existing group-link behavior) |
| Hover: recipe card | `border-accent/40 + shadow-card` reveal; `text-primary` on title |
| Hover: photo thumbnail | `opacity-80` fade (existing group-hover pattern) |
| Hover: event row container | No hover state needed (list, not card) |
| Destructive actions | None in this phase (read-only homepage) |

Scroll behavior: default browser scroll — no scroll-snap, no parallax.

Animations: `motion-safe:animate-fade-in-up` on Hero section only (existing). No section-level entrance animations added.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Hero eyebrow | `Est. 2024 • Dallas, Texas` |
| Hero H1 | `The Hudson Family` |
| Hero subcopy | `Grandma Hudson's recipes, family photos, and moments that matter — all in one place.` |
| Primary CTA | `Browse Recipes` |
| RecipeSearch trigger label | `Search recipes` (existing aria-label + visible span) |
| RecipeSearch placeholder | `Search by name or category…` (existing) |
| RecipeSearch empty | `No recipes found.` (existing) |
| Recipes section label | `RECIPES` |
| Photos section label | `PHOTOS` |
| Events section label | `EVENTS` |
| Section "View all" link | `View all` (all three sections) |
| Photos empty state | `No photos yet` |
| Events empty state | `No upcoming events` |
| Error state (data fetch fails) | Page-level: Next.js error.tsx boundary — "Something went wrong. Try refreshing the page." (existing (public) error.tsx) |
| Destructive confirmation | None — no destructive actions on this page |

Copy rules:
- Section labels: uppercase, tracked, terracotta (`text-primary`) — rendered via SectionHeader `label` prop
- Empty states: italic, dim (`text-text-dim`), no heading — just the single italic sentence
- Hero subcopy: italic, muted — foregrounds *recipes* first, then family context

---

## Component Inventory

Existing components used (no new components required):

| Component | File | Role |
|-----------|------|------|
| `Hero` | `src/components/public/hero.tsx` | Reworked per D-04 (new subcopy + CTA row) |
| `RecipeSearch` | `src/components/public/recipe-search.tsx` | Embedded in Hero CTA row |
| `SectionHeader` | `src/components/ui/section-header.tsx` | Section labels + "View all" links (label mode) |
| `PhotoGridPreview` | `src/components/public/photo-grid-preview.tsx` | Photos section — unchanged |
| `Separator` | `src/components/ui/separator` | Between sections |
| `Link` | `next/link` | All navigation links |
| `Image` | `next/image` | Inside PhotoGridPreview (existing) |

New code required:
- `FEATURED_RECIPE_SLUGS` constant (in `page.tsx` or `src/lib/featured-recipes.ts`)
- Featured recipe card JSX (inline in `page.tsx` or a thin `<FeaturedRecipeCard>` Server Component)
- `getRecipeIndex()` call added to `page.tsx` (already exists in `/recipes` page — reuse)

Retired:
- `<Sidebar>` component call — replaced by inline sections in `page.tsx`
- `WeatherWidget` — removed with Sidebar (was never prominent; out of scope for Phase 33)

---

## Responsive Behavior

| Breakpoint | Layout |
|------------|--------|
| 0–639px (mobile) | Single column. Hero centered. Recipe cards: 2-col grid. Photos: 3-col grid (unchanged). Events: stacked list. All section padding `px-4 py-10`. |
| 640px+ (sm) | Recipe cards: 3-col grid. Section padding `px-6 py-14`. Hero `sm:py-20`. |
| 1024px+ (lg) | Recipe cards: 4-col grid. Max-width constraint `max-w-5xl` centers content. |

No sidebar column at any breakpoint. Full-width sections only.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | CommandDialog, Separator (existing) | Not required |
| Third-party | None | Not applicable |

All components are from the existing codebase or shadcn official. No new installs needed.

Source: `components.json` — `style: "new-york"`, `iconLibrary: "lucide"`

---

## What Is NOT In Scope (Phase 33)

Per `<scope_guardrail>`:
- Navbar + footer (Phase 35 — separate UI-SPEC)
- `/recipes`, `/photos`, `/events` index pages themselves
- Photo rendering pipeline fix (Phase 34)
- WeatherWidget replacement or any new widget
- Any visual rebrand — Ivory & Terracotta stays unchanged

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
