---
phase: 38-public-surfaces
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/app/(public)/photos/page.tsx
  - src/components/public/album-photo-grid.tsx
  - src/components/public/lightbox.tsx
  - src/__tests__/prod-readiness.test.ts
autonomous: true
requirements: [PHOTOS-01, PHOTOS-02, PHOTOS-03]
must_haves:
  truths:
    - "Visiting /photos shows every album-kind collection as a card at the top of the page"
    - "Below the collection cards, /photos shows an 'All Photos' section sourced from getUncollectedPhotos() (photos in NO album collection), not the old all-published query"
    - "The album photo grid no longer renders a hover title overlay; alt text is generic, never the photo title/filename"
    - "The lightbox no longer renders the title + caption overlay; alt text is generic"
    - "No photo filename, file path, or title text is visible anywhere on /photos or in the lightbox"
  artifacts:
    - path: "src/app/(public)/photos/page.tsx"
      provides: "/photos — collection cards on top (kept) + All Photos via getUncollectedPhotos()"
      contains: "getUncollectedPhotos"
    - path: "src/components/public/album-photo-grid.tsx"
      provides: "Photo grid with hover title overlay removed and generic alt"
    - path: "src/components/public/lightbox.tsx"
      provides: "Lightbox with title/caption overlay removed and generic alt"
    - path: "src/__tests__/prod-readiness.test.ts"
      provides: "Updated source assertions for the new homepage/all-photos behavior + no-filename rule"
  key_links:
    - from: "src/app/(public)/photos/page.tsx"
      to: "src/lib/photo-queries.ts"
      via: "import { getUncollectedPhotos }"
      pattern: "getUncollectedPhotos"
    - from: "src/app/(public)/photos/page.tsx"
      to: "src/components/public/album-photo-grid.tsx"
      via: "<AlbumPhotoGrid photos={allPhotos} />"
      pattern: "AlbumPhotoGrid"
---

<objective>
Reorganize /photos onto the Phase 37 model and strip every rendered filename from the public photo surfaces. Implements PHOTOS-01 (collection cards on top — verify), PHOTOS-02 (All Photos = uncollected photos), and PHOTOS-03 (no filenames anywhere public).

Purpose: "All Photos" must mean photos in no album (the new collection model) rather than "every published photo", and the site must never surface upload filenames (stored as photo.title) — the owner explicitly does not want names rendered.

Output: /photos All-Photos section driven by getUncollectedPhotos(), the album grid + lightbox with their title/caption overlays removed and generic alt text, and updated prod-readiness source assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-public-surfaces/38-CONTEXT.md
@.planning/phases/38-public-surfaces/38-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

From src/lib/photo-queries.ts (Phase 37 — reuse, do not modify):
```typescript
// Returns every Photo with NO album-kind CollectionPhoto row, newest-first.
// Photos only in surface collections (memorial/featured) are still returned.
export async function getUncollectedPhotos(): Promise<Photo[]>;
```

Current /photos All-Photos query to REPLACE (src/app/(public)/photos/page.tsx):
```typescript
prisma.photo.findMany({
  where: { published: true },
  orderBy: { createdAt: "desc" },
  select: { id, title, caption, thumbnailPath, originalPath, width, height, takenAt },
})
```
Note: getUncollectedPhotos returns full Photo records (findMany with no select). AlbumPhotoGrid's Photo prop needs id, title, caption, thumbnailPath, originalPath, width, height, takenAt — full records satisfy that superset, so AlbumPhotoGrid keeps working without a select.

AlbumPhotoGrid prop shape (src/components/public/album-photo-grid.tsx) — unchanged:
```typescript
interface Photo { id; title: string | null; caption: string | null; thumbnailPath; originalPath; width; height; takenAt: Date | null }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap /photos All-Photos to getUncollectedPhotos(); keep collection cards</name>
  <files>src/app/(public)/photos/page.tsx</files>
  <read_first>
    - src/app/(public)/photos/page.tsx (the file being modified — the Promise.all with the collections findMany (kind "album") + the all-published photo.findMany; the Albums section render block lines ~52-136; the All Photos section lines ~138-150)
    - src/lib/photo-queries.ts (getUncollectedPhotos — the replacement query; returns full Photo records)
    - src/components/public/album-photo-grid.tsx (the consumer of allPhotos — confirms the prop fields it reads)
  </read_first>
  <action>
    In the `Promise.all`, replace the second element — the `prisma.photo.findMany({ where: { published: true }, ... select: {...} })` call — with `getUncollectedPhotos()` imported from "@/lib/photo-queries". Rename the destructured result to keep it clear (e.g. `[collections, allPhotos]` stays; `allPhotos` now = uncollected photos). Delete the inline comment that says "Every published photo — so /photos is a real 'all photos' view"; replace it with a one-line note that All Photos = uncollected photos (getUncollectedPhotos, PHOTOS-02). Leave the collections `prisma.collection.findMany({ where: { kind: "album" }, ... })` query and the entire Albums card-rendering section (PHOTOS-01) untouched — it already renders one card per album collection with cover, title, date, count. Keep `await connection()`. Keep the `<AlbumPhotoGrid photos={allPhotos} />` render and the empty-state `<p>No photos yet</p>` branch. If `prisma` is now only used by the collections query, keep the import; if it becomes fully unused, remove it (the collections query still uses it, so it stays).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getUncollectedPhotos' "src/app/(public)/photos/page.tsx"` ≥ 1.
    - `grep -c 'where: { published: true }' "src/app/(public)/photos/page.tsx"` returns 0 (old all-published all-photos query removed).
    - `grep -c 'kind: "album"' "src/app/(public)/photos/page.tsx"` ≥ 1 (collection-cards query retained — PHOTOS-01).
    - `grep -c 'AlbumPhotoGrid' "src/app/(public)/photos/page.tsx"` ≥ 1 (All Photos grid render kept).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>/photos renders album collection cards on top (unchanged) and an All Photos section sourced from getUncollectedPhotos(); the old all-published query is gone; build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove filename overlays + generic alt in album grid and lightbox (PHOTOS-03)</name>
  <files>src/components/public/album-photo-grid.tsx, src/components/public/lightbox.tsx</files>
  <read_first>
    - src/components/public/album-photo-grid.tsx (the file being modified — the hover overlay block `{photo.title && (<div ...><p ...>{photo.title}</p></div>)}` at lines ~43-48, and the Image `alt={photo.title || photo.caption || "Photo"}` at line ~36)
    - src/components/public/lightbox.tsx (the file being modified — the caption overlay block `{(photo.title || photo.caption) && (<div ...>...{photo.title}...{photo.caption}...</div>)}` at lines ~193-203, and the Image `alt={photo.title || photo.caption || "Photo"}` at line ~184)
    - src/app/(public)/photos/page.tsx (caller context — these components still receive title/caption in props; only their RENDERING is removed, the data shape is unchanged)
  </read_first>
  <action>
    In src/components/public/album-photo-grid.tsx: delete the entire hover title overlay block (the `{photo.title && ( ... )}` JSX that renders the gradient `<div>` with `<p ...>{photo.title}</p>`). Change the Image `alt` from `photo.title || photo.caption || "Photo"` to the generic `"The Hudson Family photo"` — never reference photo.title/photo.caption in alt. Leave the Photo interface and the lightbox wiring (still passes title/caption through props, just not rendered) intact.
    In src/components/public/lightbox.tsx: delete the entire caption overlay block (the `{(photo.title || photo.caption) && ( ... )}` JSX containing the title `<p>` and caption `<p>`). Change the Image `alt` from `photo.title || photo.caption || "Photo"` to `"The Hudson Family photo"`. Keep the counter `{currentIndex + 1} / {photos.length}` line — that is not a filename. Keep the LightboxPhoto interface (title/caption still arrive in props; they are simply no longer displayed).
    Do not surface any title/caption text in either component. No fenced implementations here — delete the named JSX blocks and replace the two alt expressions only.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '{photo.title}' src/components/public/album-photo-grid.tsx` returns 0 (hover title overlay removed).
    - `grep -c 'photo.title' src/components/public/album-photo-grid.tsx` returns 0 (title not referenced for alt either).
    - `grep -c '{photo.title}' src/components/public/lightbox.tsx` returns 0 (caption-overlay title removed).
    - `grep -c '{photo.caption}' src/components/public/lightbox.tsx` returns 0 (caption text removed).
    - `grep -c 'The Hudson Family photo' src/components/public/album-photo-grid.tsx` ≥ 1 and `grep -c 'The Hudson Family photo' src/components/public/lightbox.tsx` ≥ 1.
    - `grep -cE 'currentIndex \+ 1' src/components/public/lightbox.tsx` ≥ 1 (counter preserved — not a filename).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Neither the album grid nor the lightbox renders photo.title or photo.caption text; both use generic alt text; the lightbox counter and navigation remain; build is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update prod-readiness assertions for the new homepage/all-photos + no-filename behavior</name>
  <files>src/__tests__/prod-readiness.test.ts</files>
  <read_first>
    - src/__tests__/prod-readiness.test.ts (the file being modified — its source-read assertion style: fs.readFile of a page/component then expect(content).toContain / .not.toContain; the existing memorial/image-route assertions as the pattern to mirror)
    - src/app/(public)/page.tsx (post-Plan-01 homepage — now uses getFeaturedPhotos, no `take: 6`, no `published: true`)
    - src/app/(public)/photos/page.tsx (post-Task-1 — now uses getUncollectedPhotos, no all-published query)
    - src/components/public/album-photo-grid.tsx and src/components/public/lightbox.tsx (post-Task-2 — no rendered titles, generic alt)
  </read_first>
  <action>
    Add a new `describe` block (e.g. "v6.0 Public Surfaces — Phase 38") with source-read assertions, following the existing fs.readFile + expect.toContain / .not.toContain pattern in this file (no DB, no rendering — pure source assertions). Cover:
    - Homepage drives the grid from featured: read src/app/(public)/page.tsx, expect it to contain "getFeaturedPhotos", and expect it to NOT contain "take: 6" and NOT contain "published: true" (the old 6-recent / all-published behavior is gone).
    - /photos All Photos = uncollected: read src/app/(public)/photos/page.tsx, expect it to contain "getUncollectedPhotos" and to NOT contain "where: { published: true }".
    - /photos keeps collection cards: expect src/app/(public)/photos/page.tsx to contain 'kind: "album"' (PHOTOS-01 cards query retained).
    - No filenames in album grid: read src/components/public/album-photo-grid.tsx, expect it to NOT contain "{photo.title}" and to contain "The Hudson Family photo".
    - No filenames in lightbox: read src/components/public/lightbox.tsx, expect it to NOT contain "{photo.title}" and NOT contain "{photo.caption}".
    Then audit the PRE-EXISTING assertions in this file for any that asserted the OLD homepage 6-photo or /photos all-published behavior and update/remove them so they reflect the new behavior (if none reference it, leave the rest untouched). Do not weaken the v5.0 Prune Guard or memorial assertions. Keep all assertions as exact-substring source reads — no subjective language.
  </action>
  <verify>
    <automated>npm test -- src/__tests__/prod-readiness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/__tests__/prod-readiness.test.ts` passes with the new Phase 38 describe block.
    - `grep -c 'getFeaturedPhotos' src/__tests__/prod-readiness.test.ts` ≥ 1.
    - `grep -c 'getUncollectedPhotos' src/__tests__/prod-readiness.test.ts` ≥ 1.
    - `grep -c 'The Hudson Family photo' src/__tests__/prod-readiness.test.ts` ≥ 1.
    - No pre-existing assertion in the file still asserts `take: 6` or all-published homepage behavior as the expected current state.
  </acceptance_criteria>
  <done>prod-readiness.test.ts asserts the new featured-homepage, uncollected-All-Photos, and no-filename behavior; the full file passes; no stale assertions remain.</done>
</task>

</tasks>

<verification>
- `npm test -- src/__tests__/prod-readiness.test.ts` — new Phase 38 source assertions pass; no stale homepage/all-photos assertions remain.
- `npm run build` — clean build for /photos and the public photo components.
- `grep -rn "{photo.title}\|{photo.caption}" src/components/public/album-photo-grid.tsx src/components/public/lightbox.tsx` — zero matches (no filenames rendered).
- `npm test` — full suite green (regression check across photo-queries + prod-readiness).
</verification>

<success_criteria>
- /photos renders every album-kind collection as a card on top (PHOTOS-01) and an All Photos section sourced from getUncollectedPhotos() (PHOTOS-02).
- No photo title/filename/caption text renders on /photos or in the lightbox; both use generic alt text (PHOTOS-03).
- prod-readiness.test.ts encodes the new behavior with source assertions and passes; full test suite green.
- `npm run build` exits 0.
</success_criteria>

<output>
Create `.planning/phases/38-public-surfaces/38-02-SUMMARY.md` when done.
</output>
