---
phase: 38-public-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/photo-queries.ts
  - src/__tests__/lib/photo-queries.test.ts
  - src/components/public/photo-grid-preview.tsx
  - src/app/(public)/page.tsx
autonomous: true
requirements: [FEAT-01]
must_haves:
  truths:
    - "The homepage renders up to 9 featured photos in a 3-column grid"
    - "When fewer than 9 photos are featured, only that many tiles render — no empty placeholder tiles"
    - "When the featured collection row does not exist yet, getFeaturedPhotos() returns [] and the homepage still builds and renders a quiet empty state (no crash, no empty tiles)"
    - "The featured grid links to /photos and the 'View all photos' affordance remains"
    - "No photo title or filename text is rendered in the homepage grid"
  artifacts:
    - path: "src/lib/photo-queries.ts"
      provides: "getFeaturedPhotos() — featured surface collection photos by FEATURED_SLUG, sortOrder asc, capped at FEATURED_MAX, [] when collection absent"
      contains: "export async function getFeaturedPhotos"
    - path: "src/__tests__/lib/photo-queries.test.ts"
      provides: "Unit coverage for getFeaturedPhotos query shape and null-collection -> [] behavior"
      contains: "getFeaturedPhotos"
    - path: "src/components/public/photo-grid-preview.tsx"
      provides: "Featured grid component — up to FEATURED_MAX tiles, no placeholder padding, generic alt"
    - path: "src/app/(public)/page.tsx"
      provides: "Homepage Photos section driven by getFeaturedPhotos()"
      contains: "getFeaturedPhotos"
  key_links:
    - from: "src/app/(public)/page.tsx"
      to: "src/lib/photo-queries.ts"
      via: "import { getFeaturedPhotos }"
      pattern: "getFeaturedPhotos"
    - from: "src/lib/photo-queries.ts"
      to: "src/lib/featured.ts"
      via: "import { FEATURED_SLUG, FEATURED_MAX }"
      pattern: "FEATURED_SLUG"
---

<objective>
Drive the homepage Photos section from a curated `featured` surface collection instead of the 6-most-recent-published query, and render up to 9 featured photos as a 3×3 grid with no empty placeholder tiles. Implements FEAT-01.

Purpose: The homepage becomes an owner-curated grid (curation lands in Phase 39; live seeding in Phase 40). This phase delivers the public rendering so the grid is correct the moment the `featured` collection is populated — and renders gracefully empty until then.

Output: `getFeaturedPhotos()` query + unit tests, a repurposed grid component (up to 9 tiles, no placeholder padding), and a homepage wired to the featured query with a graceful empty state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-public-surfaces/38-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

From src/lib/featured.ts (Phase 37 — reuse, do not modify):
```typescript
export const FEATURED_SLUG = "featured";
export const FEATURED_MAX = 9;
```

From src/lib/photo-queries.ts (existing — add getFeaturedPhotos alongside getUncollectedPhotos):
```typescript
export const COLLECTION_KIND = { ALBUM: "album", SURFACE: "surface" } as const;
export async function getUncollectedPhotos(): Promise<Photo[]>;
```

Canonical analog — the `memorial` surface-collection query in src/app/(public)/richard-hudson-sr/page.tsx:
```typescript
const getMemorialPhotos = cache(async () =>
  prisma.collectionPhoto.findMany({
    where: { collection: { slug: "memorial" } },
    include: { photo: true },
    orderBy: { sortOrder: "asc" },
  })
);
```

CONTEXT-specified shape for getFeaturedPhotos (the locked decision — use this `findUnique`-on-collection form, then map to photos):
```typescript
prisma.collection.findUnique({
  where: { slug: FEATURED_SLUG },
  include: { photos: { include: { photo: true }, orderBy: { sortOrder: "asc" }, take: FEATURED_MAX } },
})
// null collection -> return []
```

prisma mock (src/__tests__/mocks/prisma.ts) already exposes prismaMock.collection.findUnique — tests can mock it directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add getFeaturedPhotos() to photo-queries.ts with unit tests</name>
  <files>src/lib/photo-queries.ts, src/__tests__/lib/photo-queries.test.ts</files>
  <read_first>
    - src/lib/photo-queries.ts (the file being modified — existing getUncollectedPhotos + COLLECTION_KIND export and doc-comment style to match)
    - src/lib/featured.ts (FEATURED_SLUG, FEATURED_MAX — import these, do not hardcode "featured" or 9)
    - src/app/(public)/richard-hudson-sr/page.tsx lines 30-36 (canonical surface-collection query analog)
    - src/__tests__/lib/photo-queries.test.ts (existing getUncollectedPhotos test patterns — prismaMock usage, beforeEach clearAllMocks, the `expect.objectContaining` assertion style to mirror)
    - src/__tests__/mocks/prisma.ts (prismaMock.collection.findUnique is available)
  </read_first>
  <behavior>
    - Test: getFeaturedPhotos calls prisma.collection.findUnique with where slug === FEATURED_SLUG (assert FEATURED_SLUG, not the literal "featured").
    - Test: the include.photos query orders by sortOrder asc and uses take === FEATURED_MAX.
    - Test: when findUnique resolves null (featured collection row absent), getFeaturedPhotos returns [] (no throw).
    - Test: when findUnique resolves a collection with N CollectionPhoto join rows, getFeaturedPhotos returns the N underlying Photo records (mapped from join.photo), in the order returned.
  </behavior>
  <action>
    Add and export an async function `getFeaturedPhotos()` in src/lib/photo-queries.ts. Import FEATURED_SLUG and FEATURED_MAX from "@/lib/featured" (never hardcode the slug string or the 9 cap). Query with `prisma.collection.findUnique({ where: { slug: FEATURED_SLUG }, include: { photos: { include: { photo: true }, orderBy: { sortOrder: "asc" }, take: FEATURED_MAX } } })`. If the result is null (the surface collection row has not been seeded — expected until Phase 40), return an empty array. Otherwise map the CollectionPhoto join rows to their `.photo` records and return that Photo array. Add a doc comment in the same style as getUncollectedPhotos noting: surface collection (not album-kind), drives the homepage FEAT-01 grid, graceful-empty when the row is absent, capped at FEATURED_MAX. Write the four behavior tests in src/__tests__/lib/photo-queries.test.ts in a new `describe('getFeaturedPhotos', ...)` block mirroring the existing getUncollectedPhotos block (import getFeaturedPhotos, beforeEach vi.clearAllMocks, mock prismaMock.collection.findUnique). Do NOT use fenced implementation here — write directive tests + the mapping logic only.
  </action>
  <verify>
    <automated>npm test -- src/__tests__/lib/photo-queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/__tests__/lib/photo-queries.test.ts` passes including the new getFeaturedPhotos describe block.
    - `grep -n "export async function getFeaturedPhotos" src/lib/photo-queries.ts` matches.
    - `grep -nE "FEATURED_SLUG|FEATURED_MAX" src/lib/photo-queries.ts` matches both (slug + cap are imported, not literals).
    - `grep -c 'getFeaturedPhotos' src/__tests__/lib/photo-queries.test.ts` ≥ 4 (import + assertions).
  </acceptance_criteria>
  <done>getFeaturedPhotos() is exported, imports FEATURED_SLUG/FEATURED_MAX, returns [] when the collection is absent and the mapped Photo[] otherwise, and the four unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Repurpose PhotoGridPreview into the featured grid (drop 6-cap + placeholder padding)</name>
  <files>src/components/public/photo-grid-preview.tsx</files>
  <read_first>
    - src/components/public/photo-grid-preview.tsx (the file being modified — current 6-slice, grid-cols-3, and the Array.from placeholder-padding block to delete)
    - src/components/public/album-photo-grid.tsx lines 27-42 (analog grid + generic-alt approach to mirror for the no-filename rule)
    - src/app/(public)/page.tsx (the consumer — confirm the prop shape it passes)
  </read_first>
  <action>
    Repurpose the component so it renders the featured grid for FEAT-01. Remove the `const displayPhotos = photos.slice(0, 6)` 6-cap — render every photo passed in (the query already caps at FEATURED_MAX=9). Delete the entire placeholder-padding block (`{Array.from({ length: Math.max(0, 6 - displayPhotos.length) }).map(...)}` and the placeholder `<div>` it produces) — no empty tiles ever render. Keep `grid grid-cols-3 gap-1.5`, the `aspect-square` square tiles, the `<Link href="/photos">` wrapper (preserve the click → /photos affordance), and `unoptimized`. Set the Image `alt` to a generic non-filename string (`"The Hudson Family photo"`) — do NOT use `photo.title` (titles are upload filenames; PHOTOS-03). Drop `title` from the Photo interface if it is no longer referenced, or keep the field but stop rendering it. Keep this a Server-Component-friendly module (no "use client"; it has no client state). The empty-array case (no photos) is handled by the homepage caller in Task 3 — this component is only rendered when photos.length > 0, so it does not need its own empty branch.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'placeholder' src/components/public/photo-grid-preview.tsx` returns 0 (placeholder padding removed).
    - `grep -c 'slice(0, 6)' src/components/public/photo-grid-preview.tsx` returns 0.
    - `grep -c 'photo.title' src/components/public/photo-grid-preview.tsx` returns 0 (no filename in alt).
    - `grep -c 'The Hudson Family photo' src/components/public/photo-grid-preview.tsx` ≥ 1.
    - `grep -c 'grid-cols-3' src/components/public/photo-grid-preview.tsx` ≥ 1 (3-column grid preserved).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The grid renders exactly photos.length square tiles in 3 columns with generic alt text, no placeholder tiles, no rendered titles, and links to /photos.</done>
</task>

<task type="auto">
  <name>Task 3: Wire homepage Photos section to getFeaturedPhotos() with graceful empty state</name>
  <files>src/app/(public)/page.tsx</files>
  <read_first>
    - src/app/(public)/page.tsx (the file being modified — current Promise.all with prisma.photo.findMany 6-recent query, the PhotoGridPreview usage, SectionHeader "View all photos" action, and the existing `photos.length > 0 ? ... : "No photos yet"` empty branch)
    - src/lib/photo-queries.ts (getFeaturedPhotos signature added in Task 1)
    - src/components/public/photo-grid-preview.tsx (repurposed grid from Task 2)
  </read_first>
  <action>
    Replace the homepage's `prisma.photo.findMany({ where: { published: true }, take: 6, ... })` call inside the `Promise.all` with `getFeaturedPhotos()` (import from "@/lib/photo-queries"). Keep `await connection()` and keep the parallel `getRecipeIndex()` fetch — do not reintroduce a waterfall. Keep the `<SectionHeader label="PHOTOS" action={{ text: "View all photos", href: "/photos" }} />`. Rename the local variable from `photos` to `featuredPhotos` for clarity. The card wrapper stays; inside it, when `featuredPhotos.length > 0` render `<PhotoGridPreview photos={featuredPhotos} />`, otherwise render a single quiet empty line — keep/adapt the existing `<p className="text-sm text-text-dim italic">No photos yet</p>` (acceptable copy, or "No featured photos yet"). Critically: getFeaturedPhotos() returns [] when the `featured` collection row does not exist (it will NOT exist until Phase 40), so the homepage MUST build and render the empty state cleanly with zero featured photos — that is the expected default for this phase, not a bug. Remove the `prisma` import if it is no longer used anywhere else in the file. Do not render any photo title/filename text in this section.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getFeaturedPhotos' src/app/(public)/page.tsx` ≥ 1.
    - `grep -c 'take: 6' "src/app/(public)/page.tsx"` returns 0 (old 6-recent query removed).
    - `grep -c 'published: true' "src/app/(public)/page.tsx"` returns 0 (homepage no longer queries all-published).
    - `grep -c 'View all photos' "src/app/(public)/page.tsx"` ≥ 1 (affordance preserved).
    - `grep -c 'await connection' "src/app/(public)/page.tsx"` ≥ 1 (Cache Components contract kept).
    - `npm run build` exits 0 and the homepage route compiles with zero featured photos.
  </acceptance_criteria>
  <done>Homepage fetches featured photos via getFeaturedPhotos() in parallel with the recipe index, renders the 3×3 grid when non-empty and a quiet empty line otherwise, preserves the "View all photos" link, and builds clean with zero featured photos.</done>
</task>

</tasks>

<verification>
- `npm test -- src/__tests__/lib/photo-queries.test.ts` — getFeaturedPhotos coverage passes (query shape, FEATURED_MAX cap, null-collection -> []).
- `npm run build` — clean build; homepage route compiles and renders with an empty featured set (no featured collection seeded yet).
- `grep -rn "photo.title" src/components/public/photo-grid-preview.tsx "src/app/(public)/page.tsx"` — no rendered filenames in the homepage grid path.
</verification>

<success_criteria>
- getFeaturedPhotos() exists in src/lib/photo-queries.ts, imports FEATURED_SLUG/FEATURED_MAX, returns the featured surface collection's photos (sortOrder asc, capped at FEATURED_MAX) and [] when the collection row is absent.
- The homepage renders up to 9 featured photos in a 3-column grid with no placeholder tiles and no rendered titles.
- With zero featured photos, the homepage builds and shows a quiet empty state — no crash, no empty tiles.
- The "View all photos" affordance linking to /photos remains.
</success_criteria>

<output>
Create `.planning/phases/38-public-surfaces/38-01-SUMMARY.md` when done.
</output>
