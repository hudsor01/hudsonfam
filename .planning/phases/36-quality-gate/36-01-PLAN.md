---
phase: 36-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/prod-readiness.test.ts
  - src/components/dashboard/data-table.tsx
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]
must_haves:
  truths:
    - "npm run lint reports 0 problems (0 errors, 0 warnings)"
    - "npm run build exits 0 with no references to removed features"
    - "npm test passes with >=232 tests and zero failures"
    - "A regression test fails loudly if any removed v5.0 identifier returns to production source"
  artifacts:
    - path: "src/components/dashboard/data-table.tsx"
      provides: "TanStack Table data table with the react-hooks/incompatible-library warning suppressed via a targeted eslint-disable-next-line on the useReactTable call"
      contains: "eslint-disable-next-line react-hooks/incompatible-library"
    - path: "src/__tests__/prod-readiness.test.ts"
      provides: "v5.0 Prune Guard describe block asserting zero dead-identifier matches in production source"
      contains: "v5.0 Prune Guard"
  key_links:
    - from: "src/__tests__/prod-readiness.test.ts"
      to: "src/ production source (excluding src/__tests__)"
      via: "recursive file read + regex scan for dead identifiers"
      pattern: "BlogPost|FamilyUpdate"
---

<objective>
Close the two code-level items of the v5.0 quality gate: (1) add a permanent regression guard test that fails if any pruned v5.0 identifier (BlogPost, FamilyUpdate, lib/blog, /blog, /family, PostStatus) ever returns to production source, and (2) suppress the single `react-hooks/incompatible-library` ESLint warning on the mandated TanStack Table data table so `npm run lint` reports 0 problems. Then re-confirm all three static gates (build, test, lint) are green.

Purpose: QUAL-01 (build) and QUAL-02 (tests) are already green and must stay green; QUAL-03 has exactly one code change — the lint warning — plus a permanence guard so the months-long prune cannot silently regress.
Output: One-line eslint-disable + explanatory comment in data-table.tsx; a `v5.0 Prune Guard` describe block in prod-readiness.test.ts; all three gates confirmed green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-quality-gate/36-RESEARCH.md
@.planning/phases/36-quality-gate/36-VALIDATION.md

<interfaces>
<!-- Key facts the executor needs — extracted from the codebase. No exploration required. -->

The lint warning (live-confirmed) — src/components/dashboard/data-table.tsx:
- Line 45 calls `const table = useReactTable({` (inside the `DataTable<TData, TValue>` function, "use client" component).
- The rule `react-hooks/incompatible-library` (from eslint-plugin-react-hooks@7.0.1, ships via eslint-config-next@16.2.6) flags this because React Compiler (enabled by `cacheComponents: true` in next.config.ts) cannot memoize TanStack Table's returned functions. This is by design, not a bug. CLAUDE.md mandates TanStack Table — the component must NOT be removed or replaced.
- The eslint-disable-next-line MUST sit on the line IMMEDIATELY before line 45 (the `const table = useReactTable({` line). Explanatory comment lines go ABOVE the disable comment, never between the disable comment and the code.

The prune-guard test target — src/__tests__/prod-readiness.test.ts:
- Existing file. Already imports `fs from 'fs/promises'` and `path from 'path'` (lines 13-14) and uses the `describe / it / expect` + `fs.readFile` pattern throughout (e.g. lines 480-493, 590-607).
- Add a NEW `describe('v5.0 Prune Guard', ...)` block at the END of the file (do NOT create a new test file).

CRITICAL scope detail (live-confirmed): A whole-tree grep of `src/` for the dead identifiers returns 6 matches — ALL of them legitimate negative-assertion tests in `src/__tests__/nav-footer.test.ts` (e.g. `expect(footerSection).not.toContain('href="/blog"')`). Production source scanned WITHOUT `src/__tests__/` returns ZERO matches. The guard MUST therefore scan `src/` while EXCLUDING the `src/__tests__/` directory — otherwise it self-invalidates on those legitimate test string literals AND on its own DEAD_IDENTIFIERS array.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v5.0 Prune Guard regression test (Wave 0)</name>
  <files>src/__tests__/prod-readiness.test.ts</files>
  <read_first>
    - src/__tests__/prod-readiness.test.ts (entire file — reuse its existing fs.readFile + describe/it/expect pattern; the file already imports `fs from 'fs/promises'` and `path from 'path'`)
    - .planning/phases/36-quality-gate/36-RESEARCH.md §"QUAL-03 Grep Guard — Dead-Code Permanence" and §"Common Pitfalls" Pitfall 2
  </read_first>
  <action>
    Append a new `describe('v5.0 Prune Guard', ...)` block to the END of src/__tests__/prod-readiness.test.ts. Do NOT create a new test file. Define a `DEAD_IDENTIFIERS` array containing exactly the six strings: BlogPost, FamilyUpdate, lib/blog, /blog, /family, PostStatus. Add one `it(...)` that recursively reads every `.ts` and `.tsx` file under `src/` using `fs.readdir` with `{ withFileTypes: true, recursive: true }` (or an equivalent recursive walk), EXCLUDING any path segment equal to `__tests__` (production source only — the existing nav-footer.test.ts contains six legitimate negative-assertion references to `/blog` and `/family`, and this guard block itself lists the identifiers; both live under src/__tests__ and MUST be excluded so the guard does not self-invalidate). For each surviving production file, read its contents and collect any file path whose contents contain any DEAD_IDENTIFIERS entry. Assert the collected list is empty, and on failure include the offending file paths and matched identifiers in the assertion message so a future regression is immediately diagnosable. Use pure Node fs (no child_process / grep subprocess) to avoid Pitfall 2 (grep exit-code-2 masquerading as a pass). Confirm the test PASSES against the current tree (production source is clean).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/prod-readiness.test.ts -t "v5.0 Prune Guard"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/prod-readiness.test.ts -t "v5.0 Prune Guard"` passes (the guard asserts 0 dead-identifier matches in production source).
    - The guard scans `.ts`/`.tsx` under `src/` while excluding the `src/__tests__/` directory; it uses pure Node fs (no grep subprocess).
    - The DEAD_IDENTIFIERS array contains exactly: BlogPost, FamilyUpdate, lib/blog, /blog, /family, PostStatus.
    - No new test file is created — the block lives inside src/__tests__/prod-readiness.test.ts.
  </acceptance_criteria>
  <done>The v5.0 Prune Guard describe block exists in prod-readiness.test.ts, scopes its scan to production source (excludes __tests__), and passes against the current clean tree. QUAL-03 re-introduction guard satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Suppress the react-hooks/incompatible-library lint warning on the TanStack data table</name>
  <files>src/components/dashboard/data-table.tsx</files>
  <read_first>
    - src/components/dashboard/data-table.tsx (lines 35-63 — the DataTable function and the useReactTable call at line 45)
    - .planning/phases/36-quality-gate/36-RESEARCH.md §"QUAL-03 Lint Warning Fix" (Option A is the chosen approach) and §"Common Pitfalls" Pitfall 1 (disable-comment placement)
  </read_first>
  <action>
    On the line immediately before the `const table = useReactTable({` call (currently line 45), add `// eslint-disable-next-line react-hooks/incompatible-library`. Directly ABOVE that disable comment, add a short explanatory comment (2-4 lines) stating that TanStack Table's useReactTable returns functions React Compiler cannot safely memoize by design, that CLAUDE.md mandates TanStack Table for all data tables so the component cannot be removed, and that this is a targeted local suppression that keeps the rule active everywhere else. Do NOT use a `"use no memo"` directive (over-broad — opts the whole component out of React Compiler). Do NOT add a flat-config override in eslint.config.mjs (no inline rationale at the call site). Do NOT remove or replace the TanStack Table data table. Keep the explanatory comment ABOVE the eslint-disable line — nothing may sit between the disable comment and the `useReactTable` call, or the suppression will not apply (Pitfall 1).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` reports "0 problems" (0 errors, 0 warnings) — the `react-hooks/incompatible-library` warning at data-table.tsx is gone.
    - data-table.tsx still imports and calls `useReactTable` from `@tanstack/react-table` (TanStack Table NOT removed or replaced).
    - The suppression is `// eslint-disable-next-line react-hooks/incompatible-library` placed on the line directly preceding the `useReactTable` call, with the explanatory comment above it (no `"use no memo"` directive, no eslint.config.mjs change).
  </acceptance_criteria>
  <done>`npm run lint` exits with 0 problems; the only warning in the codebase is suppressed at exactly one call site via a self-documenting eslint-disable; the mandated TanStack Table data table is unchanged behaviorally. QUAL-03 lint gate satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Re-confirm all three static gates green (build + test + lint)</name>
  <files></files>
  <read_first>
    - .planning/phases/36-quality-gate/36-VALIDATION.md §"Per-Task Verification Map" (the gate commands + pass conditions)
    - .planning/ROADMAP.md §"Phase 36: Quality Gate" success criteria 1-3
  </read_first>
  <action>
    Run all three static gates back to back and capture the authoritative signals for the SUMMARY: `npm run lint` (expect "0 problems"), `npm test` (expect >=232 passed, 0 failed), and `npm run build` (expect exit 0, ~1036 pages, zero references to removed features — no TypeScript errors, no missing-module errors). This is a verification step, not a code change — QUAL-01 and QUAL-02 are already green and this task re-confirms them after the Task 1 + Task 2 edits, and confirms the new prune-guard test is counted in the suite total. If any gate is red, fix the regression introduced by Task 1/Task 2 (do NOT introduce new feature work) and re-run until all three are green. Record the exact pass counts and build page count in the SUMMARY.
  </action>
  <verify>
    <automated>npm run lint && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` → "0 problems" (QUAL-03 lint).
    - `npm test` → >=232 passed, 0 failed, including the new v5.0 Prune Guard test (QUAL-02 + QUAL-03 guard).
    - `npm run build` → exit 0, no TypeScript errors, no missing-module errors, no references to removed features (QUAL-01).
  </acceptance_criteria>
  <done>All three static gates (lint 0 problems, test >=232/0, build exit 0) confirmed green in a single run; exact counts recorded in the SUMMARY. QUAL-01, QUAL-02, and the static portion of QUAL-03 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan adds a read-only test and a one-line lint suppression. No new input crosses any trust boundary; no new network, auth, data, or package surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | eslint-disable comment in data-table.tsx | accept | The suppression is scoped to one line and one rule (`react-hooks/incompatible-library`, a performance/compiler advisory). It disables no security rule; the rule remains active everywhere else in the codebase. Zero new attack surface. |
| T-36-02 | Information Disclosure | v5.0 Prune Guard test in prod-readiness.test.ts | accept | The guard is read-only (fs.readFile over local source); it runs only in the Vitest test process, never ships to production, and exposes nothing at runtime. |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No packages are installed in this phase (RESEARCH.md §"Package Legitimacy Audit" = N/A). No supply-chain surface added; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run lint` → "0 problems" (0 errors, 0 warnings).
- `npm test` → >=232 passed, 0 failed; the `v5.0 Prune Guard` test is present and green.
- `npm run build` → exit 0, no TypeScript / missing-module errors, no references to removed features.
- `grep -rnE "BlogPost|FamilyUpdate|lib/blog|/blog|/family|PostStatus" src/ --exclude-dir=__tests__` → zero matches (production source clean; enforced permanently by the guard test).
</verification>

<success_criteria>
- QUAL-01: `npm run build` completes with zero errors and zero references to removed features.
- QUAL-02: `npm test` passes (>=232) with the surviving-feature suite green and the new prune-guard test included.
- QUAL-03: `npm run lint` passes with zero warnings; dead v5.0 identifiers are absent from production source and a permanent regression guard enforces that absence.
</success_criteria>

<output>
Create `.planning/phases/36-quality-gate/36-01-SUMMARY.md` when done. Record: exact `npm test` pass count, `npm run build` page count, confirmation that `npm run lint` = 0 problems, and the data-table.tsx line where the eslint-disable was placed.
</output>
