---
phase: 34-photo-pipeline-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fix-photo-data.ts
autonomous: true
requirements: [PHOTO-02]
must_haves:
  truths:
    - "The NAS-era orphan d9c2e950 (0 R2 objects) is removed from the database so no broken/placeholder image surfaces publicly"
    - "The real photo f77dbd54 (3 confirmed R2 objects) is assigned to the Moving to Dallas album so the public auth gate passes — it is NOT deleted"
    - "The data fix is idempotent and safe to re-run"
  artifacts:
    - path: "scripts/fix-photo-data.ts"
      provides: "Idempotent data-fix: delete d9c2e950 (D-01 orphan), assign f77dbd54 to album (D-01 fix-not-delete)"
      min_lines: 25
  key_links:
    - from: "scripts/fix-photo-data.ts"
      to: "prisma.photo (delete + update)"
      via: "exact photo IDs + album ID"
      pattern: "f77dbd54-1b1d-4c8f-9d11-16eb1b6a0def"
---

<objective>
Apply the two D-01 data fixes the researcher root-caused against the live Neon DB: DELETE the album-less NAS-era orphan d9c2e950 (no R2 objects exist — it can only ever render a broken image or placeholder publicly), and ASSIGN the real R2-backed photo f77dbd54 to the existing "Moving to Dallas" album so it passes the intentional album-less auth gate.

Purpose: Bug 1 (the observed broken-ALT-TEXT symptom) is a data problem: both Photo rows have albumId=null, so the public homepage <img> requests are 401'd by the auth gate. Fixing the data (delete the orphan, give the real photo an album) is the correct fix per D-01 — NOT weakening the auth gate. Deleting d9c2e950 is reversible via FUTURE-01.

Output: One idempotent data-fix script. This plan runs in parallel with Plan 01 (no shared files); Plan 03 verifies both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-photo-pipeline-fix/34-RESEARCH.md

<interfaces>
<!-- Extracted from codebase + research live DB probe. -->

Prisma client default export: `import prisma from "@/lib/prisma"` (from scripts/, use the relative path that resolves to src/lib/prisma).

Live DB state (verified 2026-06-02, RESEARCH §"Live DB State"):
- d9c2e950-f0e6-4bf4-85f4-1793e87ab8ec — albumId=null, originalPath=/data/... (NAS path), 0 R2 objects -> DELETE (D-01 orphan).
- f77dbd54-1b1d-4c8f-9d11-16eb1b6a0def — albumId=null, originalPath=originals/unassigned/f77dbd54-....webp, 3 R2 objects confirmed -> UPDATE albumId only (D-01 fix-not-delete).

Album: "Moving to Dallas" = cmn8hinqw0005p1ttk12g9wa8 (exists, 0 photos).

deleteImageFiles(photoId, originalKey) from src/lib/images.ts silently ignores NoSuchKey — safe to call on the orphan (but a plain prisma.photo.delete is also safe since the orphan has 0 R2 objects).

CRITICAL (RESEARCH §"Pitfall 2"): when assigning f77dbd54 to the album, change ONLY the albumId DB field. Do NOT change originalPath — the R2 object key is `originals/unassigned/f77dbd54-....webp` and that key does NOT move. resolveImageKey uses derived/{photoId}-*.webp (photoId-only) for thumbnail/medium; the originalPath key is only used for size=original. Rewriting originalPath to an album-scoped key would point at a non-existent R2 object.

Scripts run with bun: bun run scripts/<name>.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the d9c2e950 NAS-era orphan (D-01)</name>
  <read_first>
    - src/lib/images.ts (deleteImageFiles — confirm it swallows NoSuchKey, in case you choose the deletePhoto path)
    - .planning/phases/34-photo-pipeline-fix/34-RESEARCH.md §"Fix 1: Delete d9c2e950 DB row" + §"Live DB State"
    - .planning/phases/34-photo-pipeline-fix/34-CONTEXT.md §D-01 (orphan policy — delete is reversible via FUTURE-01)
  </read_first>
  <action>
    In a new script scripts/fix-photo-data.ts, delete the Photo row d9c2e950-f0e6-4bf4-85f4-1793e87ab8ec via prisma.photo.delete (or prisma.photo.deleteMany with the exact id for idempotency — deleteMany returns count 0 instead of throwing if already gone). This photo has 0 R2 objects so no R2 cleanup is required; if you prefer belt-and-suspenders, call deleteImageFiles first (it silently ignores NoSuchKey). Make the operation idempotent: re-running must not throw if the row is already absent. Per D-01 this is reversible — the original photo returns when FUTURE-01 re-seeds NAS data. Print a line confirming the deletion (or "already absent").
  </action>
  <verify>
    <automated>bun run scripts/fix-photo-data.ts && bun run scripts/fix-photo-data.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/fix-photo-data.ts deletes Photo d9c2e950-f0e6-4bf4-85f4-1793e87ab8ec.
    - Running the script twice in a row both exit 0 (idempotent — no "record not found" throw on the second run).
    - After running, prisma.photo.findUnique for d9c2e950 returns null (verified later by Plan 03's verify-db-state).
    - No R2 DeleteObjects error surfaces (orphan has 0 objects; NoSuchKey swallowed).
  </acceptance_criteria>
  <done>d9c2e950 is removed from the DB; the operation is idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Assign f77dbd54 to the Moving to Dallas album (D-01 fix-not-delete)</name>
  <read_first>
    - .planning/phases/34-photo-pipeline-fix/34-RESEARCH.md §"Fix 2: Assign f77dbd54" + §"Pitfall 2" (do NOT change originalPath) + §"Live DB State"
    - .planning/phases/34-photo-pipeline-fix/34-CONTEXT.md §D-01 (do not delete photos that DO have an R2 object)
    - src/app/api/images/[...path]/route.ts lines 61-78 (the auth gate this assignment satisfies — requiresAuth becomes false once albumId is set)
  </read_first>
  <action>
    In the same scripts/fix-photo-data.ts, update Photo f77dbd54-1b1d-4c8f-9d11-16eb1b6a0def via prisma.photo.update to set albumId = "cmn8hinqw0005p1ttk12g9wa8" (the Moving to Dallas album). Change ONLY albumId. Do NOT touch originalPath — the R2 key originals/unassigned/f77dbd54-....webp must stay as-is (Pitfall 2). After this, requiresAuth = !photo.albumId becomes false, so the public homepage <img> request passes the auth gate. Make idempotent: re-running and setting the same albumId is a no-op-safe update. Print a line confirming the new albumId. This is the D-01 "fix, don't delete" path because f77dbd54 has 3 confirmed R2 objects.
  </action>
  <verify>
    <automated>bun run scripts/fix-photo-data.ts</automated>
  </verify>
  <acceptance_criteria>
    - After running, prisma.photo.findUnique for f77dbd54 returns a row with albumId === "cmn8hinqw0005p1ttk12g9wa8".
    - The same row's originalPath is still "originals/unassigned/f77dbd54-1b1d-4c8f-9d11-16eb1b6a0def.webp" (unchanged — confirm via the printed line or a findUnique select).
    - Re-running the script exits 0 (idempotent).
    - f77dbd54 is NOT deleted (it has R2 objects; D-01 says fix, not delete).
  </acceptance_criteria>
  <done>f77dbd54 has albumId set to Moving to Dallas; originalPath unchanged; idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data-fix script -> Neon DB | A maintenance script mutates production photo rows |
| photo.albumId state -> image proxy auth gate | Setting albumId converts a private (auth-gated) photo to public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Information Disclosure | assigning f77dbd54 to an album (makes it public) | accept | Per the single-trusted-family model, putting a photo in an album = intentionally publishing it. f77dbd54 ("me and jr") is a family photo meant for the public family site. This is the intended privacy semantics, not a leak. The auth gate is preserved; only this specific photo's intended visibility is set. |
| T-34-05 | Tampering / data loss | deleting d9c2e950 | mitigate | Deletion is targeted to one exact UUID and is reversible via FUTURE-01 (re-seed when NAS returns) per D-01. Idempotent deleteMany avoids accidental broad deletes. No other rows are touched. |
| T-34-06 | Repudiation | unlogged DB mutation | mitigate | Script prints each mutation it performs; the SUMMARY records before/after state for an audit trail. |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase. No install task exists. |
</threat_model>

<verification>
- `bun run scripts/fix-photo-data.ts` exits 0 and is idempotent on a second run.
- Plan 03's `bun run scripts/verify-db-state.ts` will assert d9c2e950 absent + f77dbd54 albumId set.
- Manual DB select confirms f77dbd54.originalPath is unchanged.
</verification>

<success_criteria>
- d9c2e950 deleted (D-01 orphan, reversible).
- f77dbd54 assigned to Moving to Dallas album, originalPath untouched (D-01 fix-not-delete).
- Script is idempotent and prints each mutation.
- Auth gate not weakened — the fix is data-side.
</success_criteria>

<output>
Create `.planning/phases/34-photo-pipeline-fix/34-02-SUMMARY.md` when done.
</output>
