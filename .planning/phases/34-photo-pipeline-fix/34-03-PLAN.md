---
phase: 34-photo-pipeline-fix
plan: 03
type: execute
wave: 2
depends_on: ["34-01", "34-02"]
files_modified:
  - .planning/phases/34-photo-pipeline-fix/34-03-SUMMARY.md
autonomous: false
requirements: [PHOTO-01, PHOTO-02, PHOTO-03, PHOTO-04]
user_setup:
  - service: cloudflare-r2
    why: "R2_ENDPOINT env var on Vercel includes the bucket name as a path segment (Bug 2). The code guard from Plan 01 makes this harmless, but cleaning up the env var is belt-and-suspenders."
    env_vars:
      - name: R2_ENDPOINT
        source: "Vercel Dashboard -> Project Settings -> Environment Variables (Production) -> set to https://<account-id>.r2.cloudflarestorage.com WITHOUT the /hudsonfam-photos suffix"
must_haves:
  truths:
    - "The full test suite is green after both the code fix (Plan 01) and the data fix (Plan 02) have landed"
    - "verify-db-state confirms d9c2e950 absent and f77dbd54 assigned to the Moving to Dallas album"
    - "round-trip-verify proves an existing R2 key streams as image/webp with no 307 placeholder redirect"
    - "The homepage Photos section renders f77dbd54 as a real image (not broken-image alt text, not the SVG placeholder)"
    - "The /photos no-albums state and /photos/[album] empty-album state render as intentional text empty states"
  artifacts:
    - path: ".planning/phases/34-photo-pipeline-fix/34-03-SUMMARY.md"
      provides: "End-to-end verification record + browser smoke results + Vercel env checkpoint outcome"
      min_lines: 20
  key_links:
    - from: "browser homepage <img src=/api/images/f77dbd54?size=thumbnail>"
      to: "R2 derived/f77dbd54-...-thumbnail.webp"
      via: "proxy: auth gate pass (albumId set) + normalized endpoint GetObject"
      pattern: "image/webp"
---

<objective>
Prove the phase goal end-to-end now that both root causes are fixed: run the full automated suite + the two Wave 0 scripts, perform the browser smoke that reproduces the original symptom (homepage Photos rendered as broken ALT TEXT), and clean up the Vercel R2_ENDPOINT env var as belt-and-suspenders for Bug 2.

Purpose: Plan 01 (code) and Plan 02 (data) each fix one of the two compounding bugs. Neither alone makes the photo render. This plan is the goal-backward gate that confirms all four PHOTO requirements are satisfied together, and that the intentional auth gate was preserved (the fix made f77dbd54 public by giving it an album, not by weakening the gate).

Output: A verification SUMMARY with command outputs, browser smoke results, and the Vercel env-var checkpoint outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-photo-pipeline-fix/34-RESEARCH.md
@.planning/phases/34-photo-pipeline-fix/34-VALIDATION.md
@.planning/phases/34-photo-pipeline-fix/34-01-SUMMARY.md
@.planning/phases/34-photo-pipeline-fix/34-02-SUMMARY.md

<interfaces>
<!-- What this plan exercises. No new code; pure verification + one human checkpoint. -->

Scripts created by Plan 01: scripts/round-trip-verify.ts (PHOTO-01/03), scripts/verify-db-state.ts (PHOTO-02). Run with bun.
Data fix by Plan 02: d9c2e950 deleted, f77dbd54.albumId = cmn8hinqw0005p1ttk12g9wa8.
Endpoint normalization by Plan 01: getR2Client strips trailing /<bucket>.

Browser smoke targets (RESEARCH §"PHOTO-04 Empty State Patterns" — empty states already correct, just confirm):
- Homepage "/" Photos section: f77dbd54 thumbnail must render as a real image, NOT alt text, NOT SVG placeholder.
- "/photos": album grid (Moving to Dallas now has 1 photo) + "No albums yet" text state when zero albums.
- "/photos/moving-to-dallas": shows f77dbd54; empty-album text "This album is empty." when zero photos.

Dev server: npm run dev (port from next default). The proxy auth gate passes for f77dbd54 because albumId is now set (public).

Vercel env var (RESEARCH §"Fix 3 Option A", Assumption A1): R2_ENDPOINT in Vercel currently = https://<account>.r2.cloudflarestorage.com/hudsonfam-photos. Target = strip the /hudsonfam-photos suffix. The Plan 01 code guard already makes deploys safe; this is cleanup only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification (suite + both Wave 0 scripts)</name>
  <read_first>
    - .planning/phases/34-photo-pipeline-fix/34-VALIDATION.md §"Per-Task Verification Map" + §"Sampling Rate"
    - scripts/round-trip-verify.ts (created in Plan 01)
    - scripts/verify-db-state.ts (created in Plan 01)
    - .planning/phases/34-photo-pipeline-fix/34-02-SUMMARY.md (confirm the data fix landed before running verify-db-state)
  </read_first>
  <action>
    Run the full Nyquist gate in order and capture each output for the SUMMARY: (1) npm test — full suite must be green (covers the endpoint-normalization unit case + all existing images/dashboard tests); (2) bun run scripts/verify-db-state.ts — must PASS both assertions (d9c2e950 absent, f77dbd54 albumId=cmn8hinqw0005p1ttk12g9wa8) now that Plan 02 has landed; (3) bun run scripts/round-trip-verify.ts — must exit 0 with the PASS line (image/webp + non-zero bytes, no 307) and the CLEANUP line. If verify-db-state FAILs, Plan 02 did not land — stop and report; do not proceed to the browser smoke. Do NOT modify any source to make these pass — this is a gate, not a fix step (escalate to a gap-closure plan if a gate is red).
  </action>
  <verify>
    <automated>npm test && bun run scripts/verify-db-state.ts && bun run scripts/round-trip-verify.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green).
    - `bun run scripts/verify-db-state.ts` prints PASS for both assertions and exits 0.
    - `bun run scripts/round-trip-verify.ts` exits 0 with a PASS line (image/webp, bytes > 0, no 307) and a CLEANUP line.
    - The three command outputs are captured verbatim in 34-03-SUMMARY.md.
  </acceptance_criteria>
  <done>Full suite + db-state + round-trip all green; outputs recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 normalized the R2 endpoint in code and filtered album-less photos off the homepage; Plan 02 deleted the orphan d9c2e950 and assigned f77dbd54 to the Moving to Dallas album. Both root causes (auth-gate-via-null-album, malformed R2 endpoint) are fixed. The browser must now render the real photo where it previously showed broken ALT TEXT.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`.
    2. Open the homepage `http://localhost:3000/` and scroll to the Photos section. EXPECTED: the f77dbd54 thumbnail ("me and jr") renders as a REAL IMAGE — NOT broken-image alt text, NOT the SVG placeholder. (This is the exact symptom from the 2026-06-02 repro.)
    3. Open `http://localhost:3000/photos`. EXPECTED: the "Moving to Dallas" album card renders (it now has 1 photo) with a real cover image, no broken image, no React error.
    4. Open `http://localhost:3000/photos/moving-to-dallas`. EXPECTED: f77dbd54 renders as a real image in the grid; clicking it opens the lightbox with the medium size as a real image.
    5. Confirm the empty states are intact (PHOTO-04): if you can view an album with zero photos it shows "This album is empty."; the no-albums case shows "No albums yet. Check back soon." (research confirmed these are already correct — just confirm they still render text, not a broken image or error).
    6. Confirm a non-image response is NOT served: open the network tab, the /api/images/f77dbd54?size=thumbnail request returns 200 with Content-Type image/webp (not 401 JSON, not a 307 redirect to /placeholder).
  </how-to-verify>
  <resume-signal>Type "approved" if all photos render as real images and empty states are intact, or describe what is broken (alt text? placeholder? 401? error?).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The Plan 01 code guard already strips a trailing /<bucket> from R2_ENDPOINT at runtime, so production deploys are safe regardless of the Vercel env var. This checkpoint is belt-and-suspenders: clean up the Vercel R2_ENDPOINT value so the env var itself is also correct (RESEARCH Fix 3 Option A, Assumption A1 — this is a manual Vercel dashboard step an agent cannot perform).
  </what-built>
  <how-to-verify>
    1. Open Vercel Dashboard -> the hudsonfam project -> Settings -> Environment Variables (Production scope).
    2. Find R2_ENDPOINT. If its value ends with `/hudsonfam-photos`, edit it to remove that suffix so it is `https://<account-id>.r2.cloudflarestorage.com` (account id unchanged).
    3. Also update R2_ENDPOINT in your local `.env.local` to the same suffix-free value (local-dev parity, RESEARCH Runtime State Inventory).
    4. Trigger a redeploy (push to main auto-deploys, or `vercel --prod`).
    5. After deploy, load the production homepage https://thehudsonfam.com/ Photos section and confirm the photo renders as a real image in production too.
    NOTE: If you prefer to rely solely on the code guard and skip the env-var cleanup, that is acceptable — the guard handles it. In that case reply "skip — relying on code guard" and record that decision in the SUMMARY. Do NOT block the phase on this if the code guard is verified working.
  </how-to-verify>
  <resume-signal>Type "approved" (env var cleaned + prod renders), or "skip — relying on code guard", or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public browser -> production /api/images proxy | Unauthenticated production request fetches R2 objects |
| operator -> Vercel env vars | Human edits a production secret/config value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Information Disclosure | production homepage rendering f77dbd54 | accept | f77dbd54 is intentionally public (now in an album). The auth gate still 401s any album-less photo to unauthenticated requests — verified unchanged in Plan 01. The smoke confirms no private (album-less) photo leaked onto the public surface. |
| T-34-08 | Tampering | Vercel R2_ENDPOINT edit | mitigate | The checkpoint specifies the exact suffix-free target value and a post-deploy production smoke so a mistyped endpoint is caught immediately; the code guard is the fallback if the env var is wrong. |
| T-34-09 | Denial of Service | redeploy after env change | accept | Vercel rolling deploy; no downtime expected. The code guard means even a wrong env value does not break image serving. |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase. No install task exists. |
</threat_model>

<verification>
- `npm test` green.
- `bun run scripts/verify-db-state.ts` PASS (PHOTO-02).
- `bun run scripts/round-trip-verify.ts` PASS (PHOTO-01, PHOTO-03).
- Browser smoke: homepage + /photos + /photos/moving-to-dallas render real images; empty states intact (PHOTO-04).
- /api/images/f77dbd54?size=thumbnail returns 200 image/webp (not 401, not 307).
- Vercel env-var checkpoint resolved (cleaned or explicitly relying on code guard).
</verification>

<success_criteria>
- All four PHOTO requirements verified together (PHOTO-01..04).
- The original broken-ALT-TEXT symptom no longer reproduces.
- A GetObject for an existing key never triggers the 307 placeholder.
- Intentional album-less auth gate preserved (private photos still 401 to anon; only album-assigned photos public).
- Empty states render as intentional text, not broken images.
</success_criteria>

<output>
Create `.planning/phases/34-photo-pipeline-fix/34-03-SUMMARY.md` when done.
</output>
