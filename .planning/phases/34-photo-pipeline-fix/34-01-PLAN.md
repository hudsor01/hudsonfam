---
phase: 34-photo-pipeline-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/images.ts
  - src/app/(public)/page.tsx
  - src/__tests__/lib/images.test.ts
  - scripts/round-trip-verify.ts
  - scripts/verify-db-state.ts
autonomous: true
requirements: [PHOTO-01, PHOTO-03]
must_haves:
  truths:
    - "getR2Client() builds an S3Client whose endpoint never contains the bucket name as a path segment"
    - "A GetObject for a key that exists in R2 returns image/webp bytes and never triggers the 307 placeholder redirect"
    - "The public homepage Photos query excludes album-less photos so private (album-less) photos never reach the public auth gate"
    - "The D-03 round-trip script proves processImage -> R2 PutObject -> GetObject returns a real WebP, then cleans up after itself"
  artifacts:
    - path: "src/lib/images.ts"
      provides: "Endpoint-normalizing getR2Client (strips trailing /<bucket> from R2_ENDPOINT)"
      contains: "endsWith"
    - path: "src/app/(public)/page.tsx"
      provides: "Homepage photo query filtered to albumId: { not: null }"
      contains: "albumId"
    - path: "scripts/round-trip-verify.ts"
      provides: "Programmatic upload->R2->GetObject round-trip with cleanup (D-03)"
      min_lines: 30
    - path: "scripts/verify-db-state.ts"
      provides: "Asserts d9c2e950 absent and f77dbd54 has the Moving to Dallas albumId (PHOTO-02 gate)"
      min_lines: 20
    - path: "src/__tests__/lib/images.test.ts"
      provides: "Unit case proving getR2Client strips the bucket suffix"
      contains: "R2_ENDPOINT"
  key_links:
    - from: "src/lib/images.ts getR2Client"
      to: "@aws-sdk/client-s3 S3Client endpoint"
      via: "normalized endpoint string"
      pattern: "endsWith\\(.*bucket"
    - from: "scripts/round-trip-verify.ts"
      to: "src/lib/images.ts processImage"
      via: "import + invocation on sample JPEG"
      pattern: "processImage"
---

<objective>
Fix Bug 2 (R2_ENDPOINT contains the bucket name as a path segment, so every R2 request resolves to /bucket/bucket/key -> NoSuchKey) at the code layer, add the public-surface recurrence guard for album-less photos, and create the Wave 0 validation scaffolding the VALIDATION.md contract requires.

Purpose: The endpoint normalization is the *deployable* fix for Bug 2 — it ships via the codebase and makes verification pass regardless of the Vercel env var state. The homepage query filter prevents album-less (intentionally private) photos from ever hitting the public auth gate again. The scaffolding (round-trip script, db-state script, endpoint-normalization unit case) is the Nyquist contract every later task verifies against.

Output: Normalized getR2Client, filtered homepage query, one new unit test case, two new scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-photo-pipeline-fix/34-RESEARCH.md
@.planning/phases/34-photo-pipeline-fix/34-VALIDATION.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly — no exploration needed. -->

From src/lib/images.ts (current getR2Client — the function to normalize):
```typescript
function getR2Client(): S3Client {
  return new S3Client({
    region: "auto",
    endpoint: process.env.R2_ENDPOINT!,
    credentials: {
      accessKeyId: process.env.R2_ACCESS_KEY_ID!,
      secretAccessKey: process.env.R2_SECRET_ACCESS_KEY!,
    },
  });
}
const R2_BUCKET = () => process.env.R2_BUCKET!;
```

Exports from src/lib/images.ts the scripts will import:
```typescript
export interface ImageMetadata {
  originalPath: string; thumbnailPath: string; mediumPath: string;
  width: number; height: number; takenAt: Date | null;
}
export async function processImage(buffer: Buffer, photoId: string, albumId: string, _originalFilename?: string): Promise<ImageMetadata>;
export function resolveImageKey(photoId: string, size: "thumbnail" | "medium" | "original", originalKey?: string): string;
export function getS3Client(): S3Client; // delegates to getR2Client
```

Current homepage photo query in src/app/(public)/page.tsx (inside Promise.all, lines 30-38):
```typescript
prisma.photo.findMany({
  orderBy: { createdAt: "desc" },
  take: 6,
  select: { id: true, thumbnailPath: true, title: true },
})
```

Prisma client import pattern (default export): `import prisma from "@/lib/prisma"`.
R2 env vars present in .env.local: R2_ENDPOINT, R2_BUCKET (hudsonfam-photos), R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, DATABASE_URL, DIRECT_DATABASE_URL.
Sample image for round-trip: public/images/recipes/_inbox/IMG_2716.jpeg.
Album for assignment (used only by Plan 02; referenced here for the db-state assertion): Moving to Dallas = cmn8hinqw0005p1ttk12g9wa8.
Photo IDs: orphan = d9c2e950-f0e6-4bf4-85f4-1793e87ab8ec; real = f77dbd54-1b1d-4c8f-9d11-16eb1b6a0def.
Scripts run with bun (package.json scripts already use bun): bun run scripts/<name>.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize R2 endpoint in getR2Client + add unit case</name>
  <read_first>
    - src/lib/images.ts (the getR2Client function being modified + R2_BUCKET accessor)
    - src/__tests__/lib/images.test.ts (existing @aws-sdk/client-s3 mock + processImage/resolveImageKey/deleteImageFiles suites — add a case alongside)
    - .planning/phases/34-photo-pipeline-fix/34-RESEARCH.md §"Bug 2" and §"Code Examples" (the verified normalization snippet)
  </read_first>
  <behavior>
    - Given R2_ENDPOINT = "https://acct.r2.cloudflarestorage.com/hudsonfam-photos" and R2_BUCKET = "hudsonfam-photos", getR2Client constructs an S3Client whose endpoint is "https://acct.r2.cloudflarestorage.com" (bucket suffix stripped).
    - Given R2_ENDPOINT = "https://acct.r2.cloudflarestorage.com" (already correct), getR2Client leaves the endpoint unchanged.
    - The bucket suffix is only stripped when it is a trailing "/<bucket>" segment — a bucket name appearing mid-path or as a substring is not stripped.
  </behavior>
  <action>
    In src/lib/images.ts, modify getR2Client so it reads process.env.R2_ENDPOINT and process.env.R2_BUCKET, then computes a normalized endpoint that strips exactly one trailing "/<bucket>" segment when present (use endsWith("/" + bucket) then slice off bucket.length + 1 chars), otherwise uses the raw endpoint. Pass the normalized value to the S3Client endpoint. Do not change region "auto" or the credentials block. This is the deployable fix for Bug 2 per D-02 (debug-in-place) — do NOT touch the auth gate in the proxy route.
    In src/__tests__/lib/images.test.ts, add a describe/it block named "getR2Client endpoint normalization" that exercises both branches. Since getR2Client is not exported, assert through an exported path that constructs the client: call processImage (already mocked S3Client.send) under both R2_ENDPOINT values and assert the S3ClientMock was constructed with the normalized endpoint — extend the existing @aws-sdk/client-s3 mock so S3ClientMock captures its constructor args (e.g. record the endpoint passed in), then assert the captured endpoint equals the stripped value in the bucket-suffix case and the raw value in the already-correct case. Set/restore process.env.R2_ENDPOINT and R2_BUCKET in the test with beforeEach/afterEach so it does not leak.
  </action>
  <verify>
    <automated>npm test -- src/__tests__/lib/images.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "endsWith" src/lib/images.ts returns the normalization line inside getR2Client.
    - `npm test -- src/__tests__/lib/images.test.ts` exits 0 with the new "getR2Client endpoint normalization" case green and all existing cases still green.
    - The captured S3Client endpoint for the bucket-suffix input is exactly "https://acct.r2.cloudflarestorage.com" (no "/hudsonfam-photos").
    - The auth gate in src/app/api/images/[...path]/route.ts is unchanged (git diff shows no change to that file).
  </acceptance_criteria>
  <done>getR2Client strips a trailing /<bucket> from R2_ENDPOINT; the unit case proves both branches; existing images.test.ts cases remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Filter album-less photos out of the public homepage query</name>
  <read_first>
    - src/app/(public)/page.tsx (the prisma.photo.findMany inside Promise.all, lines 30-38, plus the photos.length>0 render branch at line ~82)
    - src/app/api/images/[...path]/route.ts (the requiresAuth = !photo.albumId gate — confirm WHY album-less photos must be filtered: they are intentionally private and would 401 the public <img>)
    - .planning/phases/34-photo-pipeline-fix/34-RESEARCH.md §"Pitfall 4" and §"Homepage Query — Exclude Album-Less Photos"
  </read_first>
  <action>
    In src/app/(public)/page.tsx, add a where clause `albumId: { not: null }` to the prisma.photo.findMany call inside the Promise.all so only album-assigned (intentionally public) photos surface on the homepage Photos section. This is the public-surface recurrence guard the researcher flagged as in-scope for PHOTO-01: it ensures album-less photos never hit the public auth gate regardless of upload behavior. Do NOT modify the upload form to require album selection (CONTEXT.md out-of-scope: new photo features) — at most leave a one-line deferred-follow-up note in the SUMMARY. Leave the existing photos.length>0 / "No photos yet" empty-state branch exactly as-is (PHOTO-04 confirms it is already correct).
  </action>
  <verify>
    <automated>grep -n "albumId" "src/app/(public)/page.tsx" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "albumId: { not: null }" "src/app/(public)/page.tsx" matches inside the photo.findMany where clause.
    - `npm run build` exits 0 (no TypeScript errors).
    - The "No photos yet" text empty state markup (text-text-dim italic) is unchanged in git diff.
    - The upload form (src/app/(dashboard)/.../upload) is NOT modified.
  </acceptance_criteria>
  <done>Homepage photo query excludes albumId=null rows; build is green; empty state untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 scripts — round-trip-verify + verify-db-state</name>
  <read_first>
    - src/lib/images.ts (processImage signature + resolveImageKey + the ImageMetadata return shape — the round-trip script imports these)
    - src/lib/prisma.ts (default export shape — `import prisma from "@/lib/prisma"`; scripts use a relative import path from scripts/)
    - .planning/phases/34-photo-pipeline-fix/34-RESEARCH.md §"D-03 Round-Trip Verification Script Pattern" and §"Code Examples" (the verified script body)
    - .planning/phases/34-photo-pipeline-fix/34-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <action>
    Create scripts/round-trip-verify.ts: read public/images/recipes/_inbox/IMG_2716.jpeg from disk, generate a random UUID test photoId and a throwaway test albumId, call processImage(buffer, testId, testAlbumId) to PutObject the 3 keys to R2, then construct an S3Client with the SAME endpoint-normalization logic getR2Client uses (strip trailing /<bucket>) and GetObjectCommand the returned thumbnailPath. Collect the Body stream to a Buffer. Assert ContentType === "image/webp" and Buffer length > 0 and that no 307/redirect occurred (direct S3 call returns bytes, not a redirect). Then DeleteObjectsCommand the 3 keys (originalPath, thumbnailPath, mediumPath) to clean up. processImage does not write to the DB, so no DB row cleanup is needed — exit 0 on success, exit 1 with the assertion message on failure. Covers PHOTO-01 + PHOTO-03.
    Create scripts/verify-db-state.ts: connect via prisma, assert prisma.photo.findUnique for d9c2e950-f0e6-4bf4-85f4-1793e87ab8ec returns null (deleted), and prisma.photo.findUnique for f77dbd54-1b1d-4c8f-9d11-16eb1b6a0def returns a row whose albumId === "cmn8hinqw0005p1ttk12g9wa8". Print PASS/FAIL per assertion; exit 1 if either fails. This script is the PHOTO-02 gate that Plan 03 runs AFTER Plan 02's data fix lands — it will legitimately FAIL until then; that is expected and documented. Both scripts run with bun (bun run scripts/<name>.ts).
  </action>
  <verify>
    <automated>bun run scripts/round-trip-verify.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/round-trip-verify.ts and scripts/verify-db-state.ts both exist.
    - `bun run scripts/round-trip-verify.ts` exits 0 and prints a PASS line containing the byte count and ContentType=image/webp.
    - After round-trip-verify runs, the 3 test R2 keys are deleted (a follow-up HeadObject would 404 — script logs "CLEANUP: R2 objects deleted").
    - `bun run scripts/verify-db-state.ts` runs without a TypeScript/import error (it may report FAIL on the assertions until Plan 02 lands — that is the expected pre-data-fix state, not a script defect).
    - Neither script imports or requires any auth/BETTER_AUTH env var (they call processImage and the S3 SDK directly, not the HTTP proxy).
  </acceptance_criteria>
  <done>round-trip-verify proves the pipeline end-to-end and self-cleans; verify-db-state is the runnable PHOTO-02 gate for Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser <img> -> /api/images proxy | Unauthenticated public request crosses into server R2 access |
| server code -> Cloudflare R2 | Endpoint string drives which object key is fetched/written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | image proxy auth gate (`requiresAuth = !photo.albumId`) | accept (preserve) | The album-less auth gate is INTENTIONAL (single-trusted-family privacy model). This plan does NOT weaken it. Instead it filters album-less photos out of the public homepage query so private photos never reach the public surface. Weakening the gate would expose private (album-less) photos publicly — explicitly avoided. |
| T-34-02 | Tampering | R2_ENDPOINT normalization | mitigate | Strip only a trailing "/<bucket>" segment; do not blindly rewrite arbitrary host/path so a poisoned endpoint env cannot redirect requests to an attacker host. |
| T-34-03 | Denial of Service | round-trip-verify R2 writes | mitigate | Script uses a random UUID test key and DeleteObjectsCommand cleanup so it cannot accumulate orphaned objects or collide with real photos. |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH §"Package Legitimacy Audit": N/A). No install task exists. |
</threat_model>

<verification>
- `npm test -- src/__tests__/lib/images.test.ts` green (endpoint normalization + existing cases).
- `npm run build` exits 0.
- `bun run scripts/round-trip-verify.ts` exits 0 with PASS line.
- `bun run scripts/verify-db-state.ts` runs without import error.
- git diff shows NO change to src/app/api/images/[...path]/route.ts (auth gate preserved).
</verification>

<success_criteria>
- getR2Client strips a trailing /<bucket> from R2_ENDPOINT (Bug 2 deployable fix).
- Homepage photo query filtered to albumId: { not: null } (public-surface recurrence guard).
- Round-trip script proves processImage -> R2 -> GetObject returns real WebP bytes and self-cleans (PHOTO-01, PHOTO-03).
- verify-db-state script exists and is runnable as the PHOTO-02 gate.
- Endpoint-normalization unit case green; auth gate untouched.
</success_criteria>

<output>
Create `.planning/phases/34-photo-pipeline-fix/34-01-SUMMARY.md` when done.
</output>
