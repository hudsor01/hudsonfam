---
phase: 15-uat-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - UAT-01
  - UAT-02
  - UAT-03

must_haves:
  truths:
    - "An automated browser session navigates to /login and the user completes Google OAuth, landing on /dashboard"
    - "The automated session navigates to /admin/jobs and the page loads without JavaScript errors in the console"
    - "The automated session asserts the kanban board is visible and contains at least one job card"
  artifacts: []
  key_links: []
---

<objective>
Run automated browser tests against production to verify the end-to-end flow: login → navigate to /admin/jobs → kanban board renders with job cards. Uses Chrome MCP browser automation tools.

Purpose: Prove the critical user path works without manual intervention (beyond initial OAuth login).

Output: Test results for UAT-01, UAT-02, UAT-03 with pass/fail evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-uat-automation/15-CONTEXT.md

# Test environment:
# - Production URL: https://thehudsonfam.com
# - Auth: Google OAuth (owner role, rhudsontspr@gmail.com)
# - Image: ghcr.io/hudsor01/hudsonfam:20260408173607
# - Browser tools: mcp__claude-in-chrome__* (navigate, read_page, find, computer, read_console_messages)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT-01 — Automated login flow</name>
  <what-built>Production deployment with Google OAuth login.</what-built>
  <how-to-verify>
**Step 1:** Create a new browser tab and navigate to https://thehudsonfam.com/login
**Step 2:** The user completes Google OAuth login manually (automation cannot handle Google's OAuth)
**Step 3:** After login, verify the browser lands on /dashboard by checking the URL
**Step 4:** Verify the dashboard page rendered by checking for dashboard navigation links via read_page

**Pass criteria:** URL is https://thehudsonfam.com/dashboard and dashboard elements are present.
  </how-to-verify>
  <resume-signal>Type "logged in" after completing Google OAuth login in the browser.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: UAT-02 — Navigate to /admin/jobs without errors</name>
  <files>N/A (browser automation only)</files>
  <action>
Using the authenticated browser session from Task 1:

1. Navigate to https://thehudsonfam.com/admin/jobs
2. Wait 2 seconds for page load
3. Read console messages and check for JavaScript errors:
   - Use read_console_messages with pattern "error|Error|exception|Exception"
   - Filter out acceptable entries (404 for missing static files)
4. Verify the page URL is /admin/jobs (not redirected)
5. Verify the page contains the "Job Search" heading via read_page

**Pass criteria:** 
- URL is https://thehudsonfam.com/admin/jobs
- No JavaScript errors in console
- "Job Search" heading is visible
  </action>
  <verify>
    <automated>Use read_page to confirm "Job Search" heading exists and URL is /admin/jobs</automated>
  </verify>
  <done>Page loaded at /admin/jobs with no JS errors and Job Search heading visible.</done>
</task>

<task type="auto">
  <name>Task 3: UAT-03 — Assert kanban board renders with job cards</name>
  <files>N/A (browser automation only)</files>
  <action>
Using the authenticated browser session:

1. Switch to kanban view by clicking the "Kanban view" button
2. Wait 1 second for render
3. Use read_page to check for kanban column headers (New, Interested, Applied, Interview, Offer, Rejected)
4. Use find to locate job cards within the kanban columns
5. Assert at least one job card is present (has title text, source badge, score)
6. Take a screenshot as evidence

**Pass criteria:**
- Kanban view is active
- At least one column header is visible
- At least one job card with title text is present
  </action>
  <verify>
    <automated>Use find to locate "job card" elements — must find at least 1</automated>
  </verify>
  <done>Kanban board visible with column headers and at least one job card rendered.</done>
</task>

</tasks>

<verification>
Phase 15 is complete when ALL of the following are true:

1. UAT-01: Browser session completed OAuth login and landed on /dashboard
2. UAT-02: /admin/jobs loaded without JavaScript errors, "Job Search" heading visible
3. UAT-03: Kanban board rendered with at least one job card visible
</verification>

<success_criteria>
- UAT-01: Automated browser test completes Google OAuth login → lands on /dashboard
- UAT-02: /admin/jobs loads without JavaScript errors or HTTP error status
- UAT-03: Kanban board is visible with at least one job card
</success_criteria>

<output>
After completion, create `.planning/phases/15-uat-automation/15-01-SUMMARY.md` with:
- Pass/fail for each UAT requirement
- Console output evidence (any errors found)
- Screenshot evidence of kanban render
- Any issues encountered
</output>
