---
phase: 10.5-tanstack-table
type: execute
---

<objective>
Create photos and members tables with sorting, filtering, and pagination.

Purpose: With the DataTable foundation from 10.5-01, this plan extends coverage to photos and members list views, then adds sorting, column filtering, and pagination features across all data tables for a complete admin table experience.
Output: All admin list views use TanStack Table with sorting, filtering, and pagination.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/dashboard/data-table.tsx
@src/app/(dashboard)/dashboard/posts/columns.tsx
@src/app/(dashboard)/dashboard/photos/page.tsx
@src/app/(dashboard)/dashboard/members/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PhotosTable and MembersTable with column definitions</name>
  <files>src/app/(dashboard)/dashboard/photos/columns.tsx (new), src/app/(dashboard)/dashboard/photos/page.tsx, src/app/(dashboard)/dashboard/members/columns.tsx (new), src/app/(dashboard)/dashboard/members/page.tsx</files>
  <action>Create PhotosTable:
  - src/app/(dashboard)/dashboard/photos/columns.tsx with columns: Thumbnail (small image preview), Title/Filename, Album, Upload Date, Actions (view/delete)
  - Update photos/page.tsx to use DataTable with photo columns
  - Consider a grid-compatible view toggle (table vs grid) if the page currently uses a grid layout — keep grid as default but offer table view for bulk management

  Create MembersTable:
  - src/app/(dashboard)/dashboard/members/columns.tsx with columns: User (avatar + name composite cell), Role (badge), Status (active/invited/disabled badge), Actions (edit role/remove dropdown)
  - Update members/page.tsx to use DataTable with member columns
  - Ensure proper formatting for composite cells (avatar + text in User column)</action>
  <verify>bun run build passes, photos and members pages render tables</verify>
  <done>PhotosTable and MembersTable created with typed column definitions, replacing manual list rendering</done>
</task>

<task type="auto">
  <name>Task 2: Add sorting, filtering, and pagination to all data tables</name>
  <files>src/components/dashboard/data-table.tsx, src/app/(dashboard)/dashboard/posts/columns.tsx, src/app/(dashboard)/dashboard/events/columns.tsx, src/app/(dashboard)/dashboard/photos/columns.tsx, src/app/(dashboard)/dashboard/members/columns.tsx</files>
  <action>Enhance the DataTable component with TanStack Table features:

  Sorting:
  - Add getSortedRowModel to useReactTable
  - Add sorting state management (React.useState for SortingState)
  - Make column headers clickable with sort direction indicators (arrow up/down icons)
  - Enable sorting on appropriate columns (Title, Date, Status, Role)

  Filtering:
  - Add getFilteredRowModel to useReactTable
  - Add a toolbar area above the table with filter controls
  - Add status/visibility filter dropdowns using shadcn Select for posts (draft/published), events (public/family/private), members (active/invited)
  - Add a text search input for filtering by title/name

  Pagination:
  - Add getPaginationRowModel to useReactTable
  - Add pagination controls below the table (Previous/Next buttons, page size selector)
  - Default page size of 10 with options for 10/20/50
  - Show "Showing X of Y" count

  Update all column definition files to mark sortable columns with enableSorting and filterable columns with filterFn.</action>
  <verify>bun run build passes, sorting/filtering/pagination work on all tables</verify>
  <done>All data tables support column sorting, status filtering, text search, and pagination with consistent UX</done>
</task>

</tasks>

<verification>
- [ ] bun run build succeeds
- [ ] Photos page renders DataTable with thumbnail, title, album, date, actions columns
- [ ] Members page renders DataTable with user, role, status, actions columns
- [ ] Clicking column headers sorts data ascending/descending
- [ ] Status filter dropdowns filter table rows correctly
- [ ] Text search filters by title/name across tables
- [ ] Pagination controls navigate between pages
- [ ] Page size selector changes rows per page
- [ ] Empty state displays when filters produce no results
</verification>

<success_criteria>
- All admin list views (posts, events, photos, members) use TanStack Table
- Sorting works on relevant columns with visual indicators
- Filtering by status/visibility and text search functional
- Pagination with configurable page size
- Consistent table UX across all admin pages
- Build passes with no type errors
</success_criteria>

<output>
After completion, create .planning/phases/10.5-tanstack-table/10.5-02-SUMMARY.md
</output>
