---
phase: 10.5-tanstack-table
type: execute
---

<objective>
Install TanStack Table and create posts/events data tables.

Purpose: Current admin list views use manual mapping with inconsistent layouts and no sorting, filtering, or pagination. TanStack Table provides a headless, type-safe table engine that pairs with shadcn's data-table pattern for consistent, feature-rich admin tables.
Output: Reusable DataTable component, PostsTable and EventsTable with typed column definitions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@src/app/(dashboard)/dashboard/posts/page.tsx
@src/app/(dashboard)/dashboard/events/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TanStack Table and create reusable DataTable + PostsTable</name>
  <files>package.json, src/components/dashboard/data-table.tsx (new), src/app/(dashboard)/dashboard/posts/columns.tsx (new), src/app/(dashboard)/dashboard/posts/page.tsx</files>
  <action>Install TanStack Table:
  - bun add @tanstack/react-table

  Create src/components/dashboard/data-table.tsx — a reusable DataTable component following the shadcn data-table pattern:
  - Accept columns and data as generic typed props
  - Use useReactTable with getCoreRowModel
  - Render shadcn Table + TableHeader + TableBody + TableRow + TableCell
  - Support optional empty state message
  - Prepare extension points for sorting, filtering, pagination (added in 10.5-02)

  Create PostsTable:
  - src/app/(dashboard)/dashboard/posts/columns.tsx defining column definitions: Title (link to edit), Status (badge), Date (formatted), Actions (edit/delete dropdown)
  - Update posts/page.tsx to pass data to DataTable with the posts columns
  - Replace manual list rendering with the DataTable component</action>
  <verify>bun run build passes, posts page renders table correctly</verify>
  <done>TanStack Table installed, reusable DataTable component created, PostsTable renders with typed columns for Title, Status, Date, Actions</done>
</task>

<task type="auto">
  <name>Task 2: Create EventsTable with column definitions</name>
  <files>src/app/(dashboard)/dashboard/events/columns.tsx (new), src/app/(dashboard)/dashboard/events/page.tsx</files>
  <action>Create EventsTable:
  - src/app/(dashboard)/dashboard/events/columns.tsx defining column definitions: Title (link to edit), Visibility (badge showing public/family/private), Date (formatted event date), Actions (edit/delete dropdown)
  - Update events/page.tsx to replace manual list rendering with DataTable + events columns
  - Ensure the Actions column uses the same dropdown menu pattern as PostsTable for consistency
  - Handle any event-specific display logic (e.g., past events dimmed, upcoming events highlighted)
  - Ensure proper TypeScript types for the event data shape match what the server component provides</action>
  <verify>bun run build passes, events page renders table correctly</verify>
  <done>EventsTable renders with typed columns for Title, Visibility, Date, Actions, consistent with PostsTable pattern</done>
</task>

</tasks>

<verification>
- [ ] bun run build succeeds
- [ ] @tanstack/react-table in package.json dependencies
- [ ] DataTable component is generic and reusable
- [ ] Posts page renders DataTable with correct columns
- [ ] Events page renders DataTable with correct columns
- [ ] Actions dropdowns work (edit links, delete triggers)
- [ ] Empty state displays when no data
</verification>

<success_criteria>
- TanStack Table installed and configured
- Reusable DataTable component works with any typed column definition
- PostsTable displays Title, Status, Date, Actions columns
- EventsTable displays Title, Visibility, Date, Actions columns
- Manual list rendering replaced in both pages
- Build passes with no type errors
</success_criteria>

<output>
After completion, create .planning/phases/10.5-tanstack-table/10.5-01-SUMMARY.md
</output>
