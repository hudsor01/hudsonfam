---
phase: 31-recipes-experience
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/components/public/recipe-checklist.tsx
  - src/components/public/recipe-print-button.tsx
  - src/app/(public)/recipes/[slug]/page.tsx
  - src/styles/globals.css
autonomous: true
requirements: [RECIPE-02, RECIPE-03]
must_haves:
  truths:
    - "On a recipe detail page each ingredient and each step is tappable to cross it off with a visible checked state"
    - "Checked state persists per recipe across reload, keyed by slug in localStorage"
    - "A reset/clear-checks affordance clears all checks for that recipe"
    - "A Print button triggers the browser print dialog"
    - "Print output is one clean page: title + ingredients + numbered steps only, no nav/chrome"
  artifacts:
    - path: "src/components/public/recipe-checklist.tsx"
      provides: "client checklist for ingredients + steps with localStorage persistence"
      contains: "use client"
    - path: "src/components/public/recipe-print-button.tsx"
      provides: "client Print button calling window.print()"
      contains: "use client"
    - path: "src/styles/globals.css"
      provides: "@media print rules hiding chrome and printing the recipe cleanly"
      contains: "@media print"
  key_links:
    - from: "src/components/public/recipe-checklist.tsx"
      to: "localStorage"
      via: "per-slug key read/write in useEffect"
      pattern: "localStorage"
    - from: "src/app/(public)/recipes/[slug]/page.tsx"
      to: "recipe-checklist.tsx"
      via: "renders checklist with ingredients/instructions props"
      pattern: "RecipeChecklist"
---

<objective>
Add tap-to-cross-off ingredient/step checkboxes (RECIPE-02) and a clean print/kitchen view (RECIPE-03).

Purpose: While cooking, the family can tick off ingredients and steps (surviving reload), and print a single readable page with no site chrome.
Output: A client checklist component replacing the static ingredient/step lists on the detail page, a print button, and `@media print` rules in globals.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-recipes-experience/31-CONTEXT.md

@src/app/(public)/recipes/[slug]/page.tsx
@src/components/public/mobile-nav.tsx
@src/styles/globals.css

<interfaces>
<!-- Extracted from codebase. -->

From src/app/(public)/recipes/[slug]/page.tsx (detail page, after 31-01):
- Has `ingredients: string[]` and `instructions: string[]` from frontmatter.
- Currently renders ingredients as a `<ul>` and instructions as an `<ol>` inside `<section>` blocks (lines ~160-194 pre-31-01). Those static lists are what this plan replaces with the client checklist.
- After 31-01 the page also renders Breadcrumb + prev/next nav.

From src/components/ui/checkbox.tsx (installed):
- Exports `Checkbox` (Radix-based). May be used, but native large tappable rows are acceptable for >=44px targets.

Client pattern: mobile-nav.tsx — `"use client"`, useState/useEffect.
globals.css: single `@theme` block at top; print rules go OUTSIDE `@theme`, after it. Tokens: text-foreground, text-muted-foreground, success (checked), border-border, bg-card.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RecipeChecklist client component with per-slug localStorage persistence</name>
  <files>src/components/public/recipe-checklist.tsx</files>
  <read_first>
    - src/app/(public)/recipes/[slug]/page.tsx (existing ingredient `<ul>` / instruction `<ol>` markup + classes to preserve readable styling)
    - src/components/public/mobile-nav.tsx (client component pattern)
    - src/components/ui/checkbox.tsx (Checkbox API if used)
    - src/styles/globals.css (tokens: success for checked state, muted-foreground)
  </read_first>
  <action>
    Create src/components/public/recipe-checklist.tsx with `"use client"` first line. Props: `{ slug: string; ingredients: string[]; instructions: string[] }`. State: two sets/records of checked indices, one for ingredients and one for steps. localStorage key scheme: `recipe-checks:${slug}` storing a JSON object `{ ingredients: number[]; steps: number[] }`. On mount (useEffect) hydrate from localStorage (guard for SSR — read only in effect, not during render, to avoid hydration mismatch; initialize state empty then sync in effect). On every toggle, persist the updated object.
    Render two sections mirroring the existing detail markup (Ingredients `<ul>`, Instructions `<ol>`) but each item is a large tappable row (full-row button/label, min-h-11 / py-3 for >=44px target) that toggles its checked state; checked items get strikethrough (line-through) + dimmed/success color via tokens. Keep the large readable type (text-lg sm:text-xl, leading-relaxed) from the current lists. Each row is keyboard-operable (button or label+input) and has an accessible checked state (aria-checked or a real checkbox input).
    Add a "Reset checks" affordance (a button, min-h-11) that clears both sets and removes/empties the localStorage entry. Colors via tokens only; no raw Tailwind color names. Do not remove shadcn components.
  </action>
  <verify>
    <automated>grep -q "use client" src/components/public/recipe-checklist.tsx && grep -q "recipe-checks:" src/components/public/recipe-checklist.tsx && grep -q "localStorage" src/components/public/recipe-checklist.tsx && echo OK</automated>
  </verify>
  <done>Component is a client component; toggling persists to `recipe-checks:${slug}`; reset clears state and storage; rows are >=44px with strikethrough checked state using theme tokens. Initial render state is empty (arrays), NOT read from localStorage in render or in a useState lazy initializer; localStorage hydration happens in useEffect only (so server-rendered HTML matches the client's first paint — no SSR/hydration crash).</done>
</task>

<task type="auto">
  <name>Task 2: Add Print button + wire checklist into the detail page</name>
  <files>src/components/public/recipe-print-button.tsx, src/app/(public)/recipes/[slug]/page.tsx</files>
  <read_first>
    - src/app/(public)/recipes/[slug]/page.tsx (where ingredient/step sections + header live)
    - src/components/ui/button.tsx (Button + variants)
  </read_first>
  <action>
    Create src/components/public/recipe-print-button.tsx with `"use client"` first line, a single Button (from "@/components/ui/button") with an onClick calling `window.print()`, labeled "Print recipe", min-h-11. Mark it with a stable class/id (e.g. `data-print-hide` or class `print-button`) so the print CSS in Task 3 can hide it — actually it should remain hidden in print along with other chrome; give it `print:hidden`-equivalent via a class the print CSS targets (use a shared `no-print` className).
    Edit src/app/(public)/recipes/[slug]/page.tsx: import RecipeChecklist and RecipePrintButton. Replace the static ingredients `<ul>` and instructions `<ol>` sections with `<RecipeChecklist slug={slug} ingredients={ingredients} instructions={instructions} />` (the component renders both sections). Render `<RecipePrintButton />` in the header area near the breadcrumbs. Wrap site-chrome elements (back link, breadcrumbs, prev/next nav, footer, draft banner, MDX story, print button) with a `no-print` className so Task 3's print CSS can hide them, and give the printable core (title + checklist) a `print-recipe` className/region. Keep the page a server component; only the two new children are client components.
  </action>
  <verify>
    <automated>grep -q "RecipeChecklist" src/app/\(public\)/recipes/\[slug\]/page.tsx && grep -q "window.print" src/components/public/recipe-print-button.tsx && grep -q "no-print" src/app/\(public\)/recipes/\[slug\]/page.tsx && echo OK</automated>
  </verify>
  <done>Detail page renders RecipeChecklist + RecipePrintButton; chrome marked `no-print`, printable core marked `print-recipe`; Print button calls window.print().</done>
</task>

<task type="auto">
  <name>Task 3: Add @media print rules to globals.css for a clean one-page printout</name>
  <files>src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (confirm @theme block boundaries; print rules go after it, not inside @theme)
  </read_first>
  <action>
    Append (after the existing `@theme {...}` block) a plain CSS `@media print { ... }` section. Rules: hide `.no-print` (display:none !important) so nav, breadcrumbs, prev/next, footer, draft banner, print button, and the global public nav/footer are excluded; ensure `.print-recipe` (title + ingredients + steps) prints in black-on-white readable type; reset strikethrough so checked items still print legibly (or keep — but ensure ingredient/step text is fully visible, not dimmed to invisibility in print); remove background colors/shadows; set a sensible base font-size and margins for one-page output. Also hide the public layout `<nav>` and `<footer>` (they wrap children from layout.tsx) — target them via the `no-print` class added in Task 2 OR by element if no class is available; since layout.tsx chrome is not editable in this plan, rely on a print rule `body * { } ` approach is too broad — instead hide `header, nav, footer` that are not inside `.print-recipe`. Prefer the explicit approach: `@media print { nav, footer, .no-print { display: none !important; } }`.
    Do not modify the `@theme` block or any existing token. No raw Tailwind utility additions in tsx here — this is CSS only.
  </action>
  <verify>
    <automated>grep -q "@media print" src/styles/globals.css && grep -q "no-print" src/styles/globals.css && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>globals.css contains an `@media print` block hiding `.no-print`, nav, footer and printing `.print-recipe` cleanly; build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → localStorage | per-recipe check state written from the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | localStorage check payload | accept | Read defensively (JSON.parse in try/catch, validate arrays of numbers); corrupt/missing data falls back to empty state. No security impact — display-only |
| T-31-04 | Denial of Service | unbounded localStorage writes | accept | One small JSON object per recipe slug; bounded by recipe count; negligible |
| T-31-SC | Tampering | npm installs | mitigate | No new packages (checkbox/button already present) |
</threat_model>

<verification>
- `grep -rn "localStorage" "src/app/(public)/recipes"` or the checklist component returns the persistence write.
- `grep -q "@media print" src/styles/globals.css`.
- `npm run build` exits 0; `npm test` exits 0.
</verification>

<success_criteria>
- RECIPE-02: ingredients and steps are tappable (>=44px), cross off with visible checked state, persist per slug in localStorage, with a reset affordance.
- RECIPE-03: Print button opens the print dialog; `@media print` produces a one-page title + ingredients + steps view with no chrome.
</success_criteria>

<output>
Create `.planning/phases/31-recipes-experience/31-02-SUMMARY.md` when done
</output>
