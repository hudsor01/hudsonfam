---
phase: 30-cloud-re-platform
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/lib/images.ts
  - src/app/api/photos/route.ts
  - src/app/api/images/[...path]/route.ts
  - src/__tests__/lib/images.test.ts
  - package.json
  - .env.example
  - .env.local
  - next.config.ts
  - src/app/(admin)/admin/page.tsx
  - src/app/(admin)/admin/admin-client.tsx
  - src/app/api/dashboard/route.ts
  - src/app/(dashboard)/layout.tsx
  - src/lib/dashboard/health.ts
  - src/lib/dashboard/prometheus.ts
  - src/lib/dashboard/server.ts
  - src/lib/dashboard/ups.ts
  - src/lib/dashboard/media.ts
  - src/lib/dashboard/weather.ts
  - src/lib/dashboard/types.ts
  - src/components/dashboard/cluster-metrics.tsx
  - src/components/dashboard/service-monitor.tsx
  - src/components/dashboard/server-stats.tsx
  - src/components/dashboard/ups-status.tsx
  - src/components/dashboard/weather-widget.tsx
  - src/components/dashboard/media-stats.tsx
  - src/components/dashboard/bookmarks.tsx
  - src/__tests__/production-bugs.test.ts
  - src/__tests__/lib/dashboard/health.test.ts
  - src/__tests__/lib/dashboard/media.test.ts
  - src/__tests__/lib/dashboard/prometheus.test.ts
  - src/__tests__/lib/dashboard/weather.test.ts
  - CLAUDE.md
autonomous: false
requirements: [CLOUD-03, CLOUD-04]
user_setup:
  - service: cloudflare-r2
    why: "Object storage for photo originals + derivatives (replaces NAS/PVC)"
    env_vars:
      - name: R2_ACCOUNT_ID
        source: "Cloudflare Dashboard -> R2 -> account ID"
      - name: R2_ACCESS_KEY_ID
        source: "Cloudflare Dashboard -> R2 -> Manage API Tokens -> S3 token"
      - name: R2_SECRET_ACCESS_KEY
        source: "Cloudflare Dashboard -> R2 -> Manage API Tokens -> S3 token"
      - name: R2_BUCKET
        source: "Cloudflare Dashboard -> R2 -> bucket name"
      - name: R2_ENDPOINT
        source: "https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com"
      - name: R2_PUBLIC_URL
        source: "Cloudflare Dashboard -> R2 -> bucket public URL (or custom domain); optional if reads stay proxied"
    dashboard_config:
      - task: "Create an R2 bucket and an S3 API token (read+write) scoped to it"
        location: "Cloudflare Dashboard -> R2"
must_haves:
  truths:
    - "Uploading a photo writes original + thumbnail + medium objects to R2"
    - "The public image read path streams bytes from R2 (no filesystem access)"
    - "A missing R2 object (e.g. the 1 unmigrated NAS photo) degrades gracefully to a placeholder, not an unhandled error"
    - "No /admin homelab-monitoring route resolves and no homelab nav link appears"
    - "The app boots with SONARR/RADARR/JELLYFIN env vars unset"
  artifacts:
    - path: "src/lib/images.ts"
      provides: "R2-backed processImage / read / delete; no fs paths"
      excludes: "fs/promises, /data/hudsonfam/originals, /data/thumbnails"
    - path: "src/app/api/images/[...path]/route.ts"
      provides: "image read path served from R2 with graceful NoSuchKey fallback"
  key_links:
    - from: "src/app/api/photos/route.ts"
      to: "R2 (via src/lib/images.ts processImage)"
      via: "S3 PutObject through the R2 client"
      pattern: "processImage"
    - from: "src/app/api/images/[...path]/route.ts"
      to: "R2 object"
      via: "S3 GetObject stream"
      pattern: "@/lib/images"
---

<objective>
Move the photo pipeline off the NAS/PVC filesystem onto Cloudflare R2 (with graceful fallback for the one unmigrated NAS photo), and remove the homelab-monitoring admin entirely.

Purpose: The homelab is offline; R2 (free tier) holds all new photo objects and the public read path serves from R2. The single restored Photo row points at a NAS path that is not in R2, so the read path must degrade gracefully (placeholder) rather than throw. The Glance-replacement admin dashboard depends on a Prometheus/UPS/media stack that no longer exists, so it is deleted outright (not parked as a placeholder) per CONTEXT, and its env vars become non-blocking.

Output: R2-backed images.ts + routes with NoSuchKey graceful degradation, deleted homelab admin route/widgets/lib/tests, removed admin nav link, and a green build/test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cloud-re-platform/30-CONTEXT.md

@src/lib/images.ts
@src/app/api/photos/route.ts
@src/app/api/images/[...path]/route.ts
@src/app/api/images/placeholder/[id]/route.ts
@next.config.ts

<interfaces>
<!-- Extracted from codebase. Executor should use these directly. -->

ACTUAL route folder names (verified on disk — note the literal brackets):
  - src/app/api/images/[...path]/route.ts        (the R2 read path being rewritten)
  - src/app/api/images/placeholder/[id]/route.ts (EXISTING SVG placeholder; the graceful-degradation target — KEEP, do not modify)

src/lib/images.ts current exports (the contract consumers depend on — preserve signatures):
  export interface ImageMetadata { originalPath; thumbnailPath; mediumPath; width; height; takenAt }
  export async function processImage(buffer, photoId, albumId, _originalFilename?): Promise<ImageMetadata>
  export function resolveImagePath(photoId, size, originalPath?): string   // currently returns a filesystem path
  export async function deleteImageFiles(photoId, originalPath): Promise<void>
  async function extractExifDate(buffer): Date | null   // EXIF logic — KEEP as-is

Consumers (keep working against the same export names):
  - src/app/api/photos/route.ts: imports processImage — POST upload handler. Stores returned originalPath/thumbnailPath on the Photo row; returns thumbnailUrl/mediumUrl/originalUrl as `/api/images/{id}?size=...`.
  - src/app/api/images/[...path]/route.ts: imports resolveImagePath — GET read handler. Currently reads the resolved fs path with fs.readFile and applies path-traversal guards + content-type + cache headers.
  - src/__tests__/lib/images.test.ts: imports processImage, resolveImagePath, deleteImageFiles.

EXISTING placeholder route (graceful-degradation fallback for missing objects):
  - src/app/api/images/placeholder/[id]/route.ts returns a 400x400 SVG (navy-gold palette, "HF" mark) with image/svg+xml + immutable cache headers. The R2 read route should fall back to this on NoSuchKey.

Photo model fields used: id, title, caption, albumId, originalPath, thumbnailPath, width, height, takenAt, uploadedById. (originalPath/thumbnailPath remain string columns — now hold R2 object keys instead of fs paths.)
The 1 restored Photo (CONTEXT) has originalPath `…/originals/unassigned/d9c2e950-f0e6-4bf4-85f4-1793e87ab8ec.jpg` — a NAS path with NO matching R2 object. Its read MUST degrade gracefully (placeholder), not throw.

HOMELAB-MONITORING DELETION SET (admin-only, verified by import graph — none have non-admin consumers):
  Route:   src/app/(admin)/admin/page.tsx, src/app/(admin)/admin/admin-client.tsx
  API:     src/app/api/dashboard/route.ts  (imports all 6 lib/dashboard modules)
  Lib:     src/lib/dashboard/{health,prometheus,server,ups,media,weather,types}.ts
  Widgets: src/components/dashboard/{cluster-metrics,service-monitor,server-stats,ups-status,weather-widget,media-stats,bookmarks}.tsx
  Tests:   src/__tests__/lib/dashboard/{health,media,prometheus,weather}.test.ts

DO NOT DELETE (these only share a name/path prefix):
  - src/lib/dashboard-actions.ts  (content CRUD — kept)
  - src/components/public/weather-widget.tsx  (public homepage widget — different file, kept)
  - src/components/dashboard/{collapsible-card,metric-card,widget-card,app-sidebar,breadcrumbs,data-table}.tsx  (shared primitives — kept)
  - the entire src/app/(dashboard)/ route group (family content management — kept)
  - src/app/api/images/placeholder/[id]/route.ts  (placeholder fallback — kept)

src/app/(dashboard)/layout.tsx line ~41 pushes the owner Admin nav link:
  navLinks.push({ href: "/admin", label: "Admin" });   // remove this line

src/__tests__/production-bugs.test.ts imports BOTH kept and deleted modules:
  KEEP:   from '@/lib/dashboard-actions' (line ~84)
  REMOVE: imports of '@/lib/dashboard/health|media|weather|prometheus' (lines ~88-91) and any tests exercising them.
</interfaces>

<r2_docs>
Use the AWS S3-compatible SDK (@aws-sdk/client-s3) against the R2 endpoint, region "auto"; optionally @aws-sdk/lib-storage Upload for buffers. Resolve current SDK call shapes via Context7 (@aws-sdk/client-s3) before wiring. Pin any new package to a >=48h-aged version per CLOUD-09 (30-01 policy) and regenerate bun.lock with `bun install` (no skip flag).
@aws-sdk/client-s3 surfaces a missing object as an error whose `name`/`Code` is `NoSuchKey` (also possible `NotFound`/HTTP 404 from R2); the read route must catch this specifically.
</r2_docs>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Owner provisions Cloudflare R2 + populates .env.local</name>
  <what-built>Nothing yet — this gate unblocks the auto image-wiring task, which cannot run without R2 credentials.</what-built>
  <how-to-verify>
    Owner actions (Claude cannot do these — they require the Cloudflare dashboard):
    1. Cloudflare Dashboard -> R2: create a bucket (private) for photo objects. Note the bucket name (R2_BUCKET) and account ID (R2_ACCOUNT_ID).
    2. Cloudflare Dashboard -> R2 -> Manage API Tokens: create an S3 API token with read+write scoped to that bucket. Note R2_ACCESS_KEY_ID and R2_SECRET_ACCESS_KEY.
    3. Add these to .env.local (they will also be entered in Vercel during 30-03):
       R2_ACCOUNT_ID=...
       R2_ACCESS_KEY_ID=...
       R2_SECRET_ACCESS_KEY=...
       R2_BUCKET=...
       R2_ENDPOINT=https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com
       R2_PUBLIC_URL=...   (optional — only if you expose a public bucket URL; reads stay proxied through /api/images otherwise)
  </how-to-verify>
  <resume-signal>Type "r2-ready" once the bucket + S3 token exist and all R2_* vars are in .env.local, or describe blockers.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite images.ts + photos/images routes to use Cloudflare R2 (with NoSuchKey graceful degradation)</name>
  <read_first>src/lib/images.ts, src/app/api/photos/route.ts, src/app/api/images/[...path]/route.ts, src/app/api/images/placeholder/[id]/route.ts, src/__tests__/lib/images.test.ts, .env.example</read_first>
  <files>src/lib/images.ts, src/app/api/photos/route.ts, src/app/api/images/[...path]/route.ts, src/__tests__/lib/images.test.ts, package.json, .env.example, next.config.ts</files>
  <behavior>
    - processImage(buffer, photoId, albumId) sharp-resizes (original 2400px WebP q85, thumbnail 400px q80, medium 1200px q85) and PutObjects three keys to R2: originals/{albumId}/{photoId}.webp, derived/{photoId}-thumbnail.webp, derived/{photoId}-medium.webp; returns ImageMetadata whose originalPath/thumbnailPath are the R2 object keys; takenAt from EXIF preserved.
    - resolveImageKey(photoId, size, originalKey?) returns the R2 object key for the requested size (replacing the fs-path resolver). Default size = medium.
    - The GET read route fetches the R2 object by key and streams its body with correct content-type + existing cache-control headers; the FAMILY-visibility auth check is preserved.
    - GRACEFUL DEGRADATION (CLOUD-03): when R2 GetObject throws NoSuchKey (the 1 unmigrated NAS photo, or any missing derivative), the route does NOT throw an unhandled error — it returns a graceful fallback: redirect (307) to /api/images/placeholder/{photoId} (the existing SVG placeholder route), OR return the placeholder SVG body directly with image/svg+xml. The gallery then renders a placeholder, never a 500.
    - deleteImageFiles(photoId, originalKey) DeleteObjects all three R2 keys; missing keys are ignored (NoSuchKey on delete is swallowed).
  </behavior>
  <action>
  Per CONTEXT CLOUD-03 (R2 receives new uploads; remove all NAS/PVC fs paths; the 1 NAS-only photo degrades gracefully).
  1. Add an R2/S3 client in src/lib/images.ts using @aws-sdk/client-s3 configured from env: R2_ENDPOINT, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, region "auto". Confirm SDK call shapes via Context7 (@aws-sdk/client-s3). Add @aws-sdk/client-s3 (and @aws-sdk/lib-storage if used for buffer upload) to package.json pinned to a >=48h-aged version; regenerate bun.lock with `bun install` (no skip flag).
  2. Rewrite processImage to PutObject the three sharp-generated WebP buffers to R2 under the keys in <behavior>. Drop fs/promises, path, ORIGINALS_DIR/THUMBNAILS_DIR, ensureDir. Keep extractExifDate verbatim. Return ImageMetadata with originalPath/thumbnailPath set to the R2 object keys (NOT urls — the existing /api/images/{id}?size=... URL scheme is unchanged).
  3. Replace resolveImagePath with resolveImageKey (same signature shape) returning R2 keys; update the GET route to fetch via GetObjectCommand and stream the body. Remove the fs.access/fs.readFile and the path.resolve traversal guards (no filesystem). Keep the photoId DB lookup, the FAMILY-only auth gate, content-type map, and cache headers.
  4. GRACEFUL DEGRADATION: wrap the GetObjectCommand in try/catch. When the error is NoSuchKey (check `err?.name === "NoSuchKey"` and/or `err?.$metadata?.httpStatusCode === 404` / `NotFound`), return a graceful fallback instead of an error — `NextResponse.redirect(new URL("/api/images/placeholder/" + photoId, request.url), 307)` (or stream the placeholder SVG directly). Only genuinely unexpected errors (auth/network) may surface as 5xx. This covers the 1 restored NAS photo whose object is not in R2.
  5. Update photos/route.ts only if a symbol name changed; processImage signature is preserved so it should need no change beyond confirming it still compiles.
  6. Update src/__tests__/lib/images.test.ts to mock the S3 client (no fs) and assert PutObject keys, resolveImageKey outputs, DeleteObjects behavior, AND the NoSuchKey path: a mocked GetObject throwing NoSuchKey results in the placeholder fallback (assert the 307 redirect to /api/images/placeholder/{id} or the SVG response), NOT a thrown error / 500.
  7. In next.config.ts images.remotePatterns: keep the thehudsonfam.com /api/images/** pattern (read path is still our API route). Remove only NAS-specific commentary.
  8. Add R2_* vars (R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, R2_ENDPOINT, R2_PUBLIC_URL) to .env.example.
  </action>
  <verify>
    <automated>set -e; test $(rg -c "fs/promises|/data/hudsonfam/originals|/data/thumbnails" src/lib/images.ts | head -1 || echo 0) -eq 0; test $(rg -c "fs/promises|/data/hudsonfam/originals|/data/thumbnails" 'src/app/api/images/[...path]/route.ts' 2>/dev/null || echo 0) -eq 0; rg -q "NoSuchKey" 'src/app/api/images/[...path]/route.ts' && echo "graceful NoSuchKey handling present"; rg -q "placeholder|NoSuchKey" src/__tests__/lib/images.test.ts && echo "fallback test present"; npx vitest run src/__tests__/lib/images.test.ts</automated>
  </verify>
  <done>
    - images.ts + the [...path] read route contain zero `fs/promises` and zero `/data/...` paths (rg counts == 0; bracketed path quoted so the shell does not glob-expand it).
    - processImage PutObjects three keys to R2; the read route GetObjects from R2.
    - The read route catches NoSuchKey and returns the placeholder fallback (307 to /api/images/placeholder/{id} or the SVG body) — never an unhandled 500. The 1 restored NAS-only photo renders as a placeholder (CLOUD-03 graceful degradation).
    - images.test.ts passes against a mocked S3 client and includes a NoSuchKey → placeholder assertion.
    - @aws-sdk/client-s3 is an aged-pinned direct dep; bun.lock regenerated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete the homelab-monitoring admin route, lib, widgets, tests, and nav link</name>
  <read_first>src/app/(admin)/admin/page.tsx, src/app/(admin)/admin/admin-client.tsx, src/app/api/dashboard/route.ts, src/app/(dashboard)/layout.tsx, src/__tests__/production-bugs.test.ts, src/lib/dashboard/media.ts</read_first>
  <files>src/app/(admin)/admin/page.tsx, src/app/(admin)/admin/admin-client.tsx, src/app/api/dashboard/route.ts, src/lib/dashboard/health.ts, src/lib/dashboard/prometheus.ts, src/lib/dashboard/server.ts, src/lib/dashboard/ups.ts, src/lib/dashboard/media.ts, src/lib/dashboard/weather.ts, src/lib/dashboard/types.ts, src/components/dashboard/cluster-metrics.tsx, src/components/dashboard/service-monitor.tsx, src/components/dashboard/server-stats.tsx, src/components/dashboard/ups-status.tsx, src/components/dashboard/weather-widget.tsx, src/components/dashboard/media-stats.tsx, src/components/dashboard/bookmarks.tsx, src/__tests__/production-bugs.test.ts, src/__tests__/lib/dashboard/health.test.ts, src/__tests__/lib/dashboard/media.test.ts, src/__tests__/lib/dashboard/prometheus.test.ts, src/__tests__/lib/dashboard/weather.test.ts, src/app/(dashboard)/layout.tsx, .env.example, CLAUDE.md</files>
  <action>
  Per CONTEXT CLOUD-04 (REMOVE ENTIRELY — the /admin route 404s; not a placeholder). Delete the DELETION SET listed in <interfaces>:
  1. Delete the admin route files: src/app/(admin)/admin/page.tsx and admin-client.tsx so visiting /admin 404s. The (admin) layout.tsx/error.tsx/loading.tsx stay. If an empty admin/ directory triggers a build/lint error, remove the now-orphaned admin/ directory; do NOT touch the rest of the (admin) group.
  2. Delete src/app/api/dashboard/route.ts (it imports all 6 homelab lib modules).
  3. Delete src/lib/dashboard/{health,prometheus,server,ups,media,weather,types}.ts.
  4. Delete the 7 admin-only widgets: src/components/dashboard/{cluster-metrics,service-monitor,server-stats,ups-status,weather-widget,media-stats,bookmarks}.tsx. Do NOT delete collapsible-card/metric-card/widget-card/app-sidebar/breadcrumbs/data-table (kept primitives) or src/components/public/weather-widget.tsx.
  5. Delete the homelab tests: src/__tests__/lib/dashboard/{health,media,prometheus,weather}.test.ts.
  6. Edit src/__tests__/production-bugs.test.ts: remove the @/lib/dashboard/{health,media,weather,prometheus} imports and their test cases; KEEP the @/lib/dashboard-actions import and its tests.
  7. Edit src/app/(dashboard)/layout.tsx: remove the navLinks.push({ href: "/admin", label: "Admin" }) line so no Admin link renders anywhere.
  8. Make SONARR_API_KEY/RADARR_API_KEY/JELLYFIN_API_KEY non-blocking by removing every reference: purge them from .env.example AND from the CLAUDE.md Environment Variables block (they were only read by the deleted media.ts). After this, `grep -rn` for those three names across src/ returns nothing. Add a CLAUDE.md note that homelab monitoring is parked (FUTURE-02).
  9. Grep the whole src tree to confirm no remaining import of any deleted module/widget/route.
  </action>
  <verify>
    <automated>set -e; test ! -f "src/app/(admin)/admin/page.tsx"; test ! -f "src/app/(admin)/admin/admin-client.tsx"; test ! -d src/lib/dashboard; test $(rg -l "@/lib/dashboard/|api/dashboard|components/dashboard/(cluster-metrics|service-monitor|server-stats|ups-status|media-stats|bookmarks)" src/ | grep -v "dashboard-actions" | wc -l | tr -d ' ') -eq 0; test $(rg -l "components/dashboard/weather-widget" src/ | wc -l | tr -d ' ') -eq 0; test $(rg -c "SONARR_API_KEY|RADARR_API_KEY|JELLYFIN_API_KEY" src/ 2>/dev/null | wc -l | tr -d ' ') -eq 0; test $(grep -c '/admin' "src/app/(dashboard)/layout.tsx") -eq 0</automated>
  </verify>
  <done>
    - The /admin route 404s: src/app/(admin)/admin/page.tsx and admin-client.tsx are gone (CLOUD-04 removed entirely).
    - src/lib/dashboard/ is gone; all 7 admin widgets + api/dashboard route + 4 homelab tests removed.
    - production-bugs.test.ts keeps only the dashboard-actions import/tests.
    - No `/admin` nav link remains in src/app/(dashboard)/layout.tsx (`grep -c '/admin'` == 0).
    - `rg` for SONARR_API_KEY/RADARR_API_KEY/JELLYFIN_API_KEY across src/ returns nothing (unreferenced; app boots without them).
    - No dangling imports of any deleted module/widget/route; the kept primitives + public weather-widget remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify live R2 upload/render + /admin removal</name>
  <what-built>Photo pipeline now writes to and reads from Cloudflare R2 (with placeholder fallback for missing objects); homelab admin fully removed. Build + tests are green.</what-built>
  <how-to-verify>
    1. R2 env vars are already set in .env.local from Task 1.
    2. Run `npm run dev`, sign in, upload a photo via the dashboard photos page.
    3. Confirm in the Cloudflare R2 dashboard that originals/<albumId>/<id>.webp + derived/<id>-thumbnail.webp + derived/<id>-medium.webp objects appear.
    4. Confirm the uploaded photo renders in the gallery (read path streams from R2).
    5. Confirm the 1 restored (pre-R2) photo renders as a placeholder, not a broken image / 500 (graceful degradation).
    6. Visit /admin — it must 404 (no homelab dashboard). Confirm no "Admin" link shows in the dashboard nav for the owner.
  </how-to-verify>
  <resume-signal>Type "approved" once R2 upload+render works, the unmigrated photo degrades to a placeholder, and /admin is gone, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/photos | untrusted multipart upload (file type/size validated) |
| app → Cloudflare R2 | S3 credentials in R2_* env; objects over TLS |
| client → /api/images | public/authenticated read; FAMILY-only photos gated by session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Spoofing | /api/images FAMILY-only photos | mitigate | Preserve the existing session auth gate for photos with no album; do not weaken during R2 rewrite |
| T-30-05 | Tampering | upload content (arbitrary bytes → R2) | mitigate | Keep ALLOWED_TYPES + MAX_FILE_SIZE checks in photos route; re-encode via sharp before PutObject |
| T-30-06 | Elevation of Privilege | object key path traversal | mitigate | Keys are server-generated from photoId/albumId (no client-supplied path); fs traversal guards removed because there is no filesystem |
| T-30-07 | Information Disclosure | R2 bucket exposure | mitigate | Bucket private; reads proxied through /api/images with auth gate; no public bucket URL in client |
| T-30-12 | Denial of Service | missing-object read path | mitigate | NoSuchKey is caught and degrades to the static placeholder route; no unbounded retry / unhandled 5xx on the 1 unmigrated photo |
| T-30-SC | Tampering | new @aws-sdk/client-s3 install | mitigate | Verify on npmjs.com; aged-pin >=48h per CLOUD-09; bun install with no skip flag |
</threat_model>

<verification>
- `rg -n "fs/promises|/data/hudsonfam|/data/thumbnails" src/lib/images.ts 'src/app/api/images/[...path]/route.ts'` returns nothing.
- The read route catches NoSuchKey and falls back to /api/images/placeholder/{id}; images.test.ts asserts this.
- `npm test` passes (no orphaned imports from deleted modules).
- `npm run build` exits 0 with R2 client + admin removal.
- `/admin` 404s (page.tsx + admin-client.tsx deleted); no Admin nav link (`grep -c '/admin' src/app/(dashboard)/layout.tsx` == 0); SONARR/RADARR/JELLYFIN absent from src/, .env.example + CLAUDE.md.
- Owner verifies live R2 upload + render + placeholder fallback via the checkpoint.
</verification>

<success_criteria>
- CLOUD-03: photos write originals + derivatives to R2; public read path serves from R2; all NAS/PVC fs paths removed; a missing R2 object (the 1 unmigrated NAS photo) degrades gracefully to a placeholder instead of erroring.
- CLOUD-04: the homelab-monitoring admin is removed entirely — /admin 404s, the nav link is gone, and SONARR/RADARR/JELLYFIN env are unreferenced so the app boots without them.
</success_criteria>

<output>
Create `.planning/phases/30-cloud-re-platform/30-02-SUMMARY.md` when done.
</output>
