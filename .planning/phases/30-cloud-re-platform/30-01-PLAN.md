---
phase: 30-cloud-re-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth.ts
  - src/lib/prisma.ts
  - package.json
  - bun.lock
  - renovate.json
  - .env.example
autonomous: true
requirements: [CLOUD-01, CLOUD-02, CLOUD-09]
must_haves:
  truths:
    - "App runtime connects to Neon via @prisma/adapter-pg over the pooled DATABASE_URL"
    - "No Redis code path, dependency, or env reference remains; better-auth uses Postgres-backed sessions"
    - "bun install resolves under the Aikido Safe Chain age gate with no skip flag and bun.lock matches package.json"
    - "Renovate is configured security-only with a >=48h minimum release age"
  artifacts:
    - path: src/lib/auth.ts
      provides: "better-auth config with no Redis / secondaryStorage block"
      contains: "betterAuth"
    - path: renovate.json
      provides: "security-only Renovate policy with minimumReleaseAge"
      contains: "minimumReleaseAge"
    - path: bun.lock
      provides: "lockfile regenerated to match pinned package.json"
  key_links:
    - from: src/lib/prisma.ts
      to: "Neon pooled endpoint"
      via: "process.env.DATABASE_URL → PrismaPg adapter"
      pattern: "PrismaPg|DATABASE_URL"
    - from: src/lib/auth.ts
      to: "prismaAdapter"
      via: "database: prismaAdapter(prisma)"
      pattern: "prismaAdapter"
---

<objective>
Verify the app's runtime database path connects to Neon, remove Redis entirely so better-auth runs on Postgres-backed sessions, and put the dependency situation on a permanent footing: pin to versions that clear the Aikido Safe Chain 48h age gate, regenerate bun.lock, and add a security-only Renovate policy.

Purpose: These are the three foundational, file-local changes the rest of Phase 30 builds on. CLOUD-01's migrate/seed is already done (during discuss-phase); this plan only verifies the runtime adapter connection and locks down deps.
Output: Redis-free auth.ts, verified Neon runtime connection, pinned package.json + regenerated bun.lock, renovate.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cloud-re-platform/30-CONTEXT.md

@src/lib/auth.ts
@src/lib/prisma.ts
@prisma.config.ts
@package.json

<interfaces>
<!-- Already-established contracts the executor uses directly. -->

From src/lib/prisma.ts (DO NOT change the adapter shape — only verify it connects):
```typescript
// PrismaPg adapter reads process.env.DATABASE_URL (pooled Neon endpoint), max:10, min:2
const prisma: PrismaClient // default export
```

From src/lib/auth.ts (current — the secondaryStorage + Redis block is the removal target):
```typescript
export const auth = betterAuth({ database: prismaAdapter(prisma, { provider: "postgresql" }), ... });
export type Session = typeof auth.$Infer.Session;
```
better-auth's default session store is the configured database adapter (Postgres). Removing `secondaryStorage` makes sessions Postgres-backed with no further wiring.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Redis from auth.ts (CLOUD-02)</name>
  <read_first>src/lib/auth.ts, src/lib/prisma.ts</read_first>
  <files>src/lib/auth.ts, package.json, .env.example</files>
  <action>
    Per D (CONTEXT.md "Redis (CLOUD-02)"): strip every Redis artifact from src/lib/auth.ts.
    Remove: the `import Redis from "ioredis"` line; the `redisAvailable` flag; the `globalForRedis` global; the `new Redis(...)` construction (including the hardcoded `redis://redis.homelab.svc.cluster.local:6379` fallback); the `redis.on(...)` event handlers; the `redis.connect().catch(...)` call; and the entire `secondaryStorage: { get, set, delete }` block passed to betterAuth.
    Keep everything else in the betterAuth config unchanged (baseURL, secret, database: prismaAdapter, emailAndPassword, socialProviders, plugins, databaseHooks, session.cookieCache). Keep the `export type Session` line.
    Remove `"ioredis"` from package.json dependencies.
    Remove the `REDIS_URL` line from .env.example.
    Do NOT add any new session-store config — better-auth uses the existing prismaAdapter (Postgres) as the session store by default once secondaryStorage is gone.
  </action>
  <verify>
    <automated>rg -n "ioredis|REDIS_URL|secondaryStorage|redis\.homelab|new Redis" src/ package.json .env.example | grep -v '^#' ; test -z "$(rg -l 'ioredis|REDIS_URL|secondaryStorage|redis\.homelab' src/ package.json .env.example 2>/dev/null)" && echo CLEAN</automated>
  </verify>
  <done>`rg` for ioredis/REDIS_URL/secondaryStorage/redis.homelab across src/, package.json, .env.example returns nothing (prints CLEAN). auth.ts still exports `auth` and `Session`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify Neon runtime connection (CLOUD-01 verification)</name>
  <read_first>src/lib/prisma.ts, prisma.config.ts, .planning/phases/30-cloud-re-platform/30-CONTEXT.md (CLOUD-01 DONE EARLY section)</read_first>
  <files>src/__tests__/lib/neon-connection.test.ts</files>
  <behavior>
    - prisma singleton exports a PrismaClient built from PrismaPg using process.env.DATABASE_URL (pooled endpoint).
    - A runtime query against the live Neon pooled URL returns the restored seed: at least 1 user, 1 album, 1 photo, 5 events.
    - The adapter raises no pool error during the query.
  </behavior>
  <action>
    CLOUD-01's migrate/seed is ALREADY COMPLETE (see CONTEXT.md "CLOUD-01 — DONE EARLY"). This task ONLY verifies the runtime adapter connects.
    Create src/__tests__/lib/neon-connection.test.ts as an integration-style check guarded by env availability: if `process.env.DATABASE_URL` is unset, the test skips cleanly (use `it.skipIf`) so CI without secrets stays green. When DATABASE_URL is set, import the prisma default export and assert `prisma.user.count()`, `prisma.album.count()`, `prisma.photo.count()`, `prisma.event.count()` resolve to >=1, >=1, >=1, >=5 respectively (the restored seed counts from CONTEXT.md). Close the client/pool in afterAll.
    Also run a one-off manual confirmation via a `next build` (Task wiring confirms the adapter compiles against the pooled URL). Do NOT re-run `prisma migrate deploy` or re-seed.
    Reference D (CONTEXT.md): seed = 1 user, 1 album, 1 photo, 5 events.
  </action>
  <verify>
    <automated>npm test -- neon-connection 2>&1 | tail -5</automated>
  </verify>
  <done>neon-connection.test.ts passes when DATABASE_URL is set (seed counts match) and skips cleanly when unset. `next build` exits 0 with no Prisma pool error in output.</done>
</task>

<task type="auto">
  <name>Task 3: Aged-pin deps + Renovate security-only + regenerate bun.lock (CLOUD-09)</name>
  <read_first>package.json, .planning/phases/30-cloud-re-platform/30-CONTEXT.md (Dependency strategy section)</read_first>
  <files>package.json, bun.lock, renovate.json</files>
  <action>
    Per D (CONTEXT.md "Dependency strategy (CLOUD-09)"): make `bun install` resolve under the Aikido Safe Chain shim with NO `--safe-chain-skip-minimum-package-age` flag.
    1. First identify the auto-bumper: search the repo and dev environment for whatever rewrites package.json/bun.lock to same-day-latest. Check for: a `renovate.json`/`.github/renovate.json`/dependabot config (none currently in repo — confirm), any postinstall/preinstall script in package.json, any `.agents/` hook, and any local cron/launchd referencing this repo's `bun install`/`update`. Record findings in the SUMMARY. If a same-day auto-bump mechanism is found, disable it (remove the offending config/script); if none is found, document that the churn source is interactive `bun install` resolving caret ranges to latest, and the fix is the pins below.
    2. Pin dependencies: convert caret (`^`) ranges in package.json to exact versions for packages that would otherwise resolve to a release younger than 48h. For each pinned package use the newest version that is at least 48h old (the Safe Chain `minimumPackageAgeHours` default). Leave packages already on aged versions as-is. Do NOT downgrade across a major version; pick the latest patch/minor that clears the gate. NEVER leave a pre-release/canary caret range (e.g. `^x.y.z-rc.1`) — those can never satisfy the age gate; pin them to an aged stable version.
    3. Regenerate bun.lock from the pinned package.json by running `bun install` (under the Safe Chain shim, NO skip flag). The install MUST complete with zero age-gate suppression errors or skip text.
    4. Create renovate.json at repo root configured security-only: extend a security baseline, restrict to vulnerability-driven updates only (`vulnerabilityAlerts` enabled, `osvVulnerabilityAlerts` true), set package-rule `minimumReleaseAge: "48 hours"` (mirrors Safe Chain), and `automerge: true` after the cooldown for security updates. Disable routine (non-security) version bumps.
  </action>
  <verify>
    <automated>set -e; OUT=$(bun install 2>&1); echo "$OUT" | tail -5; if echo "$OUT" | grep -Eiq "safe-chain-skip|skip-minimum-package-age|age-gate suppress"; then echo "FAIL: age-gate suppression text present in install output"; exit 1; fi; bun install --frozen-lockfile 2>&1 | tail -3; if ! git diff --quiet bun.lock 2>/dev/null; then echo "bun.lock regenerated"; else echo "bun.lock unchanged (already pinned to aged versions)"; fi; rg -n "minimumReleaseAge" renovate.json; node -e "const p=require('./package.json');const all={...p.dependencies,...p.devDependencies};const bad=Object.entries(all).filter(([k,v])=>typeof v==='string'&&v.startsWith('^')&&/-(alpha|beta|rc|canary|next|dev)\b/i.test(v));if(bad.length){console.error('Pre-release caret ranges remain (cannot clear 48h gate):',bad);process.exit(1)}console.log('No pre-release caret ranges remain')"</automated>
  </verify>
  <done>
    - `bun install` completes with NO `--safe-chain-skip-minimum-package-age` flag and no age-gate suppression/skip text in its output (CLOUD-09: deps clear the Aikido Safe Chain 48h gate cleanly).
    - `bun install --frozen-lockfile` exits 0 — bun.lock matches the pinned package.json.
    - bun.lock was regenerated from the pinned package.json (verify reports "bun.lock regenerated", or "unchanged" only when every range already resolves to an aged release).
    - No pre-release/canary caret range survives in package.json (the `node -e` check exits non-zero if one does).
    - renovate.json exists at repo root with `minimumReleaseAge` ("48 hours") and a security-only policy (vulnerabilityAlerts/osvVulnerabilityAlerts enabled, routine version bumps disabled).
    - The same-day auto-bump churn source is identified and recorded in the SUMMARY (offending config/script disabled if found, else documented as interactive caret-range resolution).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app runtime → Neon | pooled DATABASE_URL over TLS (sslmode=require); creds in env only |
| dev/CI → npm registry | `bun install` resolves third-party packages (supply-chain surface) |
| better-auth → Postgres sessions | session store moves from Redis to the DB adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | DATABASE_URL / BETTER_AUTH_SECRET | mitigate | Secrets stay in env (.env.local / Vercel env), never committed; the hardcoded Redis homelab fallback is removed so no stale internal host leaks |
| T-30-02 | Tampering | Postgres-backed sessions after Redis removal | accept | better-auth's default DB session store is authoritative; no second store to desync; sessions persisted in Neon |
| T-30-03 | Denial of Service | Neon pooled endpoint exhaustion | mitigate | PrismaPg pool capped (max:10) against the pooled (PgBouncer) endpoint; migrations use unpooled DIRECT_DATABASE_URL |
| T-30-SC | Tampering | `bun install` of pinned/Renovate-updated deps | mitigate | Aikido Safe Chain 48h age gate enforced with NO skip flag; Renovate restricted to security-only with `minimumReleaseAge` >=48h; bun.lock pinned + frozen-install verified |
</threat_model>

<verification>
- `rg -n "ioredis|REDIS_URL|secondaryStorage|redis\.homelab|new Redis" src/ package.json .env.example` returns nothing.
- `neon-connection.test.ts` passes when DATABASE_URL is set (seed counts: >=1 user, >=1 album, >=1 photo, >=5 events) and skips cleanly when unset.
- `bun install` resolves with no age-gate suppression/skip text; `bun install --frozen-lockfile` exits 0.
- renovate.json exists with `minimumReleaseAge` and a security-only policy.
- `next build` exits 0 with no Prisma pool error.
</verification>

<success_criteria>
- CLOUD-01: the app runtime connects to Neon via the @prisma/adapter-pg pooled DATABASE_URL and reads the restored seed (migrate/seed already done during discuss-phase; this plan verifies the runtime connection).
- CLOUD-02: Redis is gone entirely — no ioredis dependency, no REDIS_URL, no secondaryStorage, no homelab Redis fallback; better-auth runs on Postgres-backed sessions.
- CLOUD-09: dependencies are aged-pinned so `bun install` clears the Aikido Safe Chain 48h gate with no skip flag, bun.lock matches package.json, and a security-only Renovate policy (minimumReleaseAge >=48h) is in place.
</success_criteria>

<output>
Create `.planning/phases/30-cloud-re-platform/30-01-SUMMARY.md` when done.
</output>
