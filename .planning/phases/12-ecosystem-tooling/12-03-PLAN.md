---
phase: 12-ecosystem-tooling
type: execute
---

<objective>
Install and integrate additional shadcn components.

Purpose: Install remaining useful shadcn components (Checkbox, Radio Group, Collapsible, Hover Card, Toggle Group, Resizable) and integrate Collapsible and Hover Card into existing pages for immediate value.
Output: Additional components installed and integrated.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/ — Existing shadcn component directory
@src/app/(dashboard)/dashboard/page.tsx — Dashboard overview with widget sections
@src/app/(dashboard)/dashboard/members/page.tsx — Members listing page with user avatars

**Components to install:**
- Checkbox — form input for multi-select options
- Radio Group — form input for single-select options
- Collapsible — expandable/collapsible content sections
- Hover Card — rich content preview on hover
- Toggle Group — grouped toggle buttons
- Resizable — resizable panel layouts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install remaining shadcn components</name>
  <files>src/components/ui/</files>
  <action>Install the following shadcn components using bunx shadcn@latest add: checkbox, radio-group, collapsible, hover-card, toggle-group, resizable. Verify each component is created in src/components/ui/ with proper imports and theme token usage. Confirm no conflicts with existing components.</action>
  <verify>bun run build passes without errors</verify>
  <done>All six components installed in src/components/ui/ and building cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Collapsible for dashboard widget groups and Hover Card for member avatars</name>
  <files>dashboard components, members page</files>
  <action>Integrate Collapsible into the dashboard overview page to create collapsible widget groups/sections (e.g., collapsible "Recent Activity" or "Quick Stats" sections with Collapsible, CollapsibleTrigger, and CollapsibleContent). Integrate Hover Card on the members listing page so hovering over a user avatar shows a Hover Card with user details (name, email, role, join date). Use HoverCard, HoverCardTrigger, and HoverCardContent.</action>
  <verify>bun run build passes without errors</verify>
  <done>Dashboard has collapsible widget sections, member avatars show user details on hover</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] All six components exist in src/components/ui/
- [ ] Dashboard widget sections are collapsible
- [ ] Member list avatars show Hover Card with user details
- [ ] Components use theme tokens consistently
</verification>

<success_criteria>
- All six shadcn components installed and available
- Collapsible integrated into dashboard for section management
- Hover Card integrated into members page for user detail preview
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-ecosystem-tooling/12-03-SUMMARY.md`
</output>
