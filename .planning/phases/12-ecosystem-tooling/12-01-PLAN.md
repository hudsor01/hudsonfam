---
phase: 12-ecosystem-tooling
type: execute
---

<objective>
Install shadcn skills and migrate to unified radix package.

Purpose: Add AI-aware project context via shadcn skills and consolidate individual @radix-ui/* packages into the unified radix-ui package using the official shadcn migration tool.
Output: shadcn skills installed, radix packages unified.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json — Current dependencies including individual @radix-ui/* packages
@components.json — shadcn configuration file

**Migration reference:**
- `pnpm dlx skills add shadcn/ui` installs AI-aware project context for shadcn
- `bunx shadcn info` validates current shadcn configuration
- `bunx shadcn migrate radix` consolidates @radix-ui/react-dialog, @radix-ui/react-popover, etc. into unified radix-ui package and updates all imports
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn skills and validate configuration</name>
  <files>project config</files>
  <action>Run `pnpm dlx skills add shadcn/ui` to install AI-aware project context for shadcn. Then run `bunx shadcn info` to validate the current shadcn configuration and ensure everything is properly set up. Review the output for any configuration warnings or issues that need addressing.</action>
  <verify>bunx shadcn info outputs valid configuration without errors</verify>
  <done>shadcn skills installed and configuration validated</done>
</task>

<task type="auto">
  <name>Task 2: Migrate to unified radix-ui package</name>
  <files>package.json, all component imports</files>
  <action>Run `bunx shadcn migrate radix` to consolidate individual @radix-ui/* packages (react-dialog, react-popover, react-select, etc.) into the unified radix-ui package. This migration updates package.json dependencies and rewrites all import statements across the codebase. After migration, verify all imports have been updated correctly and no stale @radix-ui/* references remain.</action>
  <verify>bun run build && bun run test both pass without errors</verify>
  <done>All @radix-ui/* packages consolidated into unified radix-ui, all imports updated</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] `bun run test` passes without errors
- [ ] `bunx shadcn info` shows valid configuration
- [ ] No individual @radix-ui/* packages remain in package.json
- [ ] All imports reference radix-ui instead of @radix-ui/*
</verification>

<success_criteria>
- shadcn skills installed and providing AI-aware context
- All @radix-ui/* packages consolidated into unified radix-ui
- All component imports updated to use radix-ui
- Build and tests pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-ecosystem-tooling/12-01-SUMMARY.md`
</output>
