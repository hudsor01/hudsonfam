---
phase: 22-salary-intelligence-defensive-render
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - src/lib/jobs-db.ts
  - src/__tests__/lib/jobs-db-zod.test.ts
autonomous: true
requirements:
  - AI-DATA-02
tags:
  - currency
  - cascade
  - d-12
  - salary

must_haves:
  truths:
    - "`?? \"USD\"` default is removed from src/lib/jobs-db.ts — line ~349 now reads `salary_currency: row.cr_salary_currency,`"
    - "CompanyResearch TS interface has salary_currency: string | null"
    - "When salary_currency is null in the DB row, parseOrLog still succeeds (schema is .nullable() after Plan 22-01)"
    - "Grep gate G-6 holds: `grep -n '?? \"USD\"' src/lib/jobs-db.ts` returns zero matches"
  artifacts:
    - path: src/lib/jobs-db.ts
      provides: currency-null passthrough (no implicit USD default)
      contains: "salary_currency: row.cr_salary_currency"
    - path: src/__tests__/lib/jobs-db-zod.test.ts
      provides: end-to-end null-currency parseOrLog smoke test
      contains: "salary_currency: null"
  key_links:
    - from: src/lib/jobs-db.ts (getJobDetail row assembly)
      to: CompanyResearchSchema (flipped to .nullable() in Plan 22-01)
      via: parseOrLog delivers salary_currency: null through to JobDetail
      pattern: "salary_currency: row.cr_salary_currency"
---

<objective>
Execute the D-12 currency cascade: remove `?? "USD"` default at `src/lib/jobs-db.ts:349` and flip the `CompanyResearch.salary_currency` TS interface from `string` to `string | null`. (The Zod schema was already flipped to `.nullable()` in Plan 22-01.) Add a targeted test asserting that a DB row with `null` currency now passes through cleanly via parseOrLog.

Purpose: ROADMAP SC #5 / D-12 posture — when `salary_currency` is null, the salary block hides entirely at the render layer (Plan 22-07 adds the guards on existing formatSalary call sites). Today's scope is purely the server-side edit: no implicit `"USD"` default means future GBP/EUR rows never mislabel as `$`.

Output: Single-line edit at jobs-db.ts:349 removing `?? "USD"`; CompanyResearch interface field type flipped to nullable; 1 new test asserting the null-currency round-trip; grep gate G-6 satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-salary-intelligence-defensive-render/22-CONTEXT.md
@.planning/phases/22-salary-intelligence-defensive-render/22-RESEARCH.md
@.planning/phases/22-salary-intelligence-defensive-render/22-PATTERNS.md
@.planning/phases/22-salary-intelligence-defensive-render/22-02-SUMMARY.md

<interfaces>
<!-- Exact edits in this plan -->

From src/lib/jobs-db.ts (current — line 349 after Plan 22-02 may have shifted by 0 lines; grep to confirm):
```typescript
salary_currency: row.cr_salary_currency ?? "USD",   // ← THIS LINE, `?? "USD"` to remove
```
Becomes:
```typescript
salary_currency: row.cr_salary_currency,
```

From src/lib/jobs-db.ts (current — CompanyResearch at lines 60-74):
```typescript
export interface CompanyResearch {
  // ...
  salary_currency: string;   // ← flip to string | null
  // ...
}
```

Plan 22-01 already flipped the Zod schema at jobs-schemas.ts:37 to `z.string().nullable()`. No re-edit needed there.

RENDER LAYER GUARDS — deferred to Plan 22-07 (header `detail.salary_currency` guard; company-research `detail.company_research.salary_currency` guard; NOT part of this plan).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n Postgres → JobDetail render | Non-USD currency rows (GBP/EUR/etc.) must not be coerced to `"USD"` string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering | Currency mislabeling — `$` symbol displayed for GBP figure | mitigate | Remove `?? "USD"` default; downstream renderers guard on `salary_currency` truthiness (Plan 22-07) so the whole block hides rather than mislabel |
| T-22-03-02 | Integrity | `CompanyResearch` type contract promises `string` but runtime emits `null` | mitigate | Flip interface to `string | null`; TypeScript will flag any consumer that depends on the non-null invariant, forcing explicit handling |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 22-03-01: Remove `?? "USD"` default + flip CompanyResearch.salary_currency TS interface to nullable</name>
  <files>src/lib/jobs-db.ts</files>
  <read_first>
    - src/lib/jobs-db.ts (ALL of it — specifically lines 60-74 for the CompanyResearch interface + line 349 for the `?? "USD"` default; after Plan 22-02 the line numbers may shift by ~11 lines due to the added SalaryIntelligence interface and SELECT additions — grep to confirm actual line)
    - src/lib/jobs-schemas.ts (after Plan 22-01 — CompanyResearchSchema.salary_currency is `z.string().nullable()`)
    - .planning/phases/22-salary-intelligence-defensive-render/22-RESEARCH.md §Data Layer → `?? "USD"` removal — ACTUAL LINE IS 349
    - .planning/phases/22-salary-intelligence-defensive-render/22-RESEARCH.md §Common Pitfalls → Pitfall 1 + Pitfall 4 (cascade edits)
    - .planning/phases/22-salary-intelligence-defensive-render/22-CONTEXT.md §Decisions D-12
  </read_first>
  <behavior>
    - `getJobDetail` row assembly no longer coerces null `cr_salary_currency` to `"USD"`.
    - CompanyResearch TS interface at line 67 shows `salary_currency: string | null`.
    - When a test fixture passes `salary_currency: null` through parseOrLog with the CompanyResearchSchema (nullable), the result has `salary_currency: null` (not `"USD"`).
    - `npm run build` exits 0 — if any consumer of `detail.company_research.salary_currency` assumed non-null, TS flags it. Per PATTERNS.md §4, only two call sites consume this (job-detail-sheet.tsx:321-330 and the header at 158-163 for `detail.salary_currency`); both gain `&& detail.*.salary_currency` truthy-guards in Plan 22-07.
    - No render-layer changes this plan — Plan 22-07 owns the job-detail-sheet guards.
  </behavior>
  <action>
    1. Open `src/lib/jobs-db.ts`.
    2. Locate the CompanyResearch interface block (starts around line 60 `export interface CompanyResearch {`). Find the `salary_currency: string;` field (around line 67). Change to:
       ```typescript
       salary_currency: string | null;
       ```
    3. Locate the `?? "USD"` default in the `companyResearch` parseOrLog block (after Plan 22-02 this is around line 349 ± 11; verify via `grep -n '?? "USD"' src/lib/jobs-db.ts`). The line currently reads:
       ```typescript
       salary_currency: row.cr_salary_currency ?? "USD",
       ```
       Change to:
       ```typescript
       salary_currency: row.cr_salary_currency,
       ```
    4. Do NOT touch any other use of `?? "USD"` — if grep finds other matches unrelated to salary_currency, leave them. (Spot check: RESEARCH says there's only one match tied to salary_currency in jobs-db.ts.)
    5. Do NOT touch the render layer (`job-detail-sheet.tsx`) this plan. Plan 22-07 owns the truthy-guard additions on the 2 formatSalary call sites. Build may warn/error if a downstream consumer relies on the old string type — if so, this is EXPECTED and will be fixed in Plan 22-07. Document any build errors encountered in the SUMMARY so Plan 22-07 executor knows to address.

       **However:** if `npm run build` fails with a TS error in `job-detail-sheet.tsx` on line 321-330 (Company Intel salary cell) or line 158-163 (sheet header `detail.salary_currency`) because `salary_currency` is now nullable — Plan 22-07 will add the guards. If the error blocks the commit step at Task 22-03-03, proceed anyway: the guard work is scoped to Plan 22-07, but a build-failing interim state for 1 commit is acceptable IF a clean commit of the 2 nullable-cascade edits is landed here. (Rule: if build breaks, confirm that the breakage is confined to lines 158-163 and 321-330 of job-detail-sheet.tsx, otherwise escalate.)

       **Note:** `detail.salary_currency` (on `Job`) was ALREADY `string | null` before this plan (see jobs-db.ts:41 `salary_currency: string | null;`). So the header consumer at line 161 in job-detail-sheet.tsx ALREADY operates on a nullable type — no TS break there. Only Company Intel's `detail.company_research.salary_currency` (line 321-330) potentially breaks. Confirm via `npm run build`.
    6. Implements D-12 (currency cascade, server-side half).
  </action>
  <verify>
    <automated>grep -c '?? "USD"' src/lib/jobs-db.ts; echo "---"; npm run build 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '?? "USD"' src/lib/jobs-db.ts` returns zero matches (grep gate G-6).
    - `grep -n "salary_currency: row.cr_salary_currency" src/lib/jobs-db.ts` returns exactly 1 match — on the line that previously had `?? "USD"`, now WITHOUT the coalesce.
    - `grep -n "salary_currency: string | null" src/lib/jobs-db.ts` returns at least 2 matches (Job interface at line 41 existing + CompanyResearch interface at ~line 67 new).
    - `grep -n "salary_currency: string;" src/lib/jobs-db.ts` returns 0 matches (old non-nullable version removed from CompanyResearch).
    - `npm run build` exits 0 (type-check passes). **If it fails in job-detail-sheet.tsx at lines 321-330 because `detail.company_research.salary_currency` is now nullable AND the existing truthy-guards on `salary_range_min || salary_range_max` don't filter null currency: this is expected — Plan 22-07 fixes. But per RESEARCH §5 "Behavioral implication" the current code only reads salary_currency for Zod parse (not for rendering), so build SHOULD exit 0 today. If not, flag in SUMMARY.**
  </acceptance_criteria>
  <done>D-12 cascade complete at the DB boundary. Grep gate G-6 satisfied. Company Intel salary rendering may show the currency figure without a currency symbol when null — Plan 22-07 adds the truthy-guard that hides the whole cell in that case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 22-03-02: Add null-currency round-trip test to jobs-db-zod.test.ts</name>
  <files>src/__tests__/lib/jobs-db-zod.test.ts</files>
  <read_first>
    - src/__tests__/lib/jobs-db-zod.test.ts (after Plans 22-01 + 22-02 — has SalaryIntelligenceSchema describe block + CompanyResearchSchema nullable cascade block)
    - src/lib/jobs-schemas.ts (after Plan 22-01 — salary_currency is nullable)
  </read_first>
  <behavior>
    - Locate the `describe("CompanyResearchSchema — salary_currency nullable cascade (D-12)", ...)` block added in Plan 22-01 Task 22-01-02. Add ONE additional `it(...)` case asserting that `parseOrLog(CompanyResearchSchema, { ..., salary_currency: null }, "company_research", 42)` returns an object whose `salary_currency` is `null` — NOT `"USD"`.
  </behavior>
  <action>
    1. Open `src/__tests__/lib/jobs-db-zod.test.ts`.
    2. Find the `describe("CompanyResearchSchema — salary_currency nullable cascade (D-12)", ...)` block. Inside it, append one more `it(...)`:
       ```typescript
       it("parseOrLog passes null salary_currency through without coercing to USD (D-12 server-side)", () => {
         const result = parseOrLog(
           CompanyResearchSchema,
           { ...validCompanyResearch, salary_currency: null },
           "company_research",
           99
         );
         expect(result).not.toBeNull();
         expect(result!.salary_currency).toBeNull();
         expect(result!.salary_currency).not.toBe("USD");
       });
       ```
    3. No other edits.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/lib/jobs-db-zod.test.ts -t "D-12"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/__tests__/lib/jobs-db-zod.test.ts -t "D-12"` exits 0.
    - Test output shows 3 passing tests in the nullable cascade block (2 from Plan 22-01 + 1 new).
    - Full suite: `npm test -- --run` exits 0.
  </acceptance_criteria>
  <done>Round-trip test locked. The null-currency behavior is test-covered — a future accidental re-introduction of `?? "USD"` fails the test immediately.</done>
</task>

<task type="auto">
  <name>Task 22-03-03: Commit — feat(22-03): remove `?? "USD"` default + flip CompanyResearch.salary_currency nullable (D-12)</name>
  <files>(git commit)</files>
  <read_first>(none)</read_first>
  <action>
    Run:
    ```bash
    gsd-sdk query commit "feat(22-03): remove ?? \"USD\" currency default + CompanyResearch.salary_currency nullable (D-12 cascade; grep gate G-6)" \
      src/lib/jobs-db.ts \
      src/__tests__/lib/jobs-db-zod.test.ts
    ```
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "feat(22-03)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` shows `feat(22-03)` and mentions `USD` or `currency`.
    - `git diff HEAD~1 HEAD -- src/lib/jobs-db.ts | grep -c '\\?\\? "USD"'` shows exactly 1 line removed (`-` side) and 0 added.
  </acceptance_criteria>
  <done>Commit landed. Wave 1 of Phase 22 (data layer) complete. Plan 22-04 (schema-drift EXPECTED map) is parallel-safe with everything in Wave 1 (different file, no overlap).</done>
</task>

</tasks>

<verification>
- Grep gate G-6: `grep -n '?? "USD"' src/lib/jobs-db.ts` returns 0 matches.
- Full suite green.
- `npm run build` exits 0.
</verification>

<success_criteria>
- D-12 server-side cascade complete.
- Grep gate G-6 satisfied.
- One new test proves null-currency round-trip works without USD coercion.
- CompanyResearch.salary_currency TS type is `string | null` (matches the Zod schema).
</success_criteria>

<output>
After completion, create `.planning/phases/22-salary-intelligence-defensive-render/22-03-SUMMARY.md` with:
- What shipped (the 2-line edit — `?? "USD"` removal + interface nullable flip)
- One new test case
- Note: Plan 22-07 owns the render-layer truthy-guards that finish the D-12 cascade (`&& detail.salary_currency` on header line 158-163; `&& detail.company_research.salary_currency` on Company Intel line 321-330).
- Any build warnings encountered (should be 0 — per RESEARCH §5 Behavioral implication, formatSalary doesn't read salary_currency today).
</output>
