---
phase: 22-salary-intelligence-defensive-render
plan: 08
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03", "22-04", "22-05", "22-06", "22-07"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/22-salary-intelligence-defensive-render/22-SUMMARY.md
autonomous: true
requirements:
  - AI-RENDER-03
  - AI-RENDER-07
  - AI-DATA-01
  - AI-DATA-02
tags:
  - meta
  - roadmap
  - requirements
  - state
  - finalization

must_haves:
  truths:
    - "ROADMAP.md Phase 22 SC #3 wording updated — removes stale 'job_id and company_name keying' language; replaces with 'defensive LEFT JOIN LATERAL skeleton (WHERE FALSE) tolerating zero rows today and tolerating future schema shape via 1-line predicate edit'"
    - "ROADMAP.md Phase 22 SC #5 line number updated: `328` → `349`"
    - "ROADMAP.md Phase 22 Plans count + plan list populated (8 plans, 22-01 → 22-08 with goal summaries and 2026-04-22 completion dates)"
    - "ROADMAP.md Progress table shows Phase 22 → `8/8` Complete with today's date"
    - "REQUIREMENTS.md traceability table marks 4 REQs complete for Phase 22 with plan cross-references"
    - "REQUIREMENTS.md v1 checklist: `[x] AI-RENDER-03`, `[x] AI-RENDER-07`, `[x] AI-DATA-01`, `[x] AI-DATA-02`"
    - "STATE.md Current Position advances to Phase 22 complete; What's Next points to Phase 23"
    - "22-SUMMARY.md created with full phase deliverables list, test-count delta, commit list, and notes"
  artifacts:
    - path: .planning/ROADMAP.md
      provides: updated SC #3 wording + SC #5 line number + Phase 22 plan list + progress row
    - path: .planning/REQUIREMENTS.md
      provides: 4 REQ checkmarks + traceability table updates
    - path: .planning/STATE.md
      provides: Phase 22 → complete position advance
    - path: .planning/phases/22-salary-intelligence-defensive-render/22-SUMMARY.md
      provides: final phase summary
      contains: "Phase 22 — Salary Intelligence (Defensive Render)"
  key_links:
    - from: ROADMAP.md Phase 22 entry
      to: REQUIREMENTS.md AI-RENDER-03 / AI-RENDER-07 / AI-DATA-01 / AI-DATA-02 traceability rows
      via: plan-level cross-references (`Phase 22 / Plan 22-XX`)
      pattern: "Phase 22 / Plan 22"
---

<objective>
Meta-doc finalization for Phase 22. Update ROADMAP.md with corrected SC #3 wording + SC #5 line number + plan list; update REQUIREMENTS.md traceability for the 4 Phase 22 REQs; advance STATE.md to Phase 22 complete; create 22-SUMMARY.md rolling up the phase's deliverables.

Purpose: Lock the phase's meta-state so future agents (Phase 23 planner, v3.5 executor) inherit accurate context. The stale SC #3 wording ("job_id and company_name keying") reflected a pre-discuss-phase assumption about the n8n workflow's final column shape; the live-DB inspection during /gsd-discuss-phase 22 revealed the table is keyed on `search_date` with no `job_id` / `company_name` columns. D-03 locked the defensive posture; this plan sanitizes the roadmap to match. SC #5 line number went `328` → `347` → `349` over Phases 20/21/22 as jobs-db.ts grew.

Output: 4 doc files updated/created; commit landed with all 4 files; Phase 22 closes code-complete per the v3.0 milestone sequencing decision (production UAT deferred to v3.5 per Phase 21 precedent — same deploy pipeline block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-salary-intelligence-defensive-render/22-CONTEXT.md
@.planning/phases/22-salary-intelligence-defensive-render/22-RESEARCH.md
@.planning/phases/22-salary-intelligence-defensive-render/22-01-SUMMARY.md
@.planning/phases/22-salary-intelligence-defensive-render/22-02-SUMMARY.md
@.planning/phases/22-salary-intelligence-defensive-render/22-03-SUMMARY.md
@.planning/phases/22-salary-intelligence-defensive-render/22-04-SUMMARY.md
@.planning/phases/22-salary-intelligence-defensive-render/22-05-SUMMARY.md
@.planning/phases/22-salary-intelligence-defensive-render/22-06-SUMMARY.md
@.planning/phases/22-salary-intelligence-defensive-render/22-07-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

(None — doc-only updates; no untrusted inputs.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08-01 | Integrity | Stale SC #3 wording propagates to v3.5 retroactive UAT, causing confusion | mitigate | Rewrite SC #3 as part of this plan before Phase 22 closes |
| T-22-08-02 | Integrity | REQUIREMENTS.md traceability fails a future `gsd-sdk query verify` check because a REQ is unmapped | mitigate | Explicit traceability update with Phase 22 / Plan 22-NN citations for each REQ |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 22-08-01: Update ROADMAP.md — SC #3 wording, SC #5 line number, Phase 22 plan list, Progress row</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase 22 section starts at line 178 `#### Phase 22:` and runs through the Success Criteria + Plans block; Progress table is near line 215-222)
    - .planning/phases/22-salary-intelligence-defensive-render/22-CONTEXT.md §Phase Boundary (the authoritative explanation of the SC #3 correction)
    - .planning/phases/22-salary-intelligence-defensive-render/22-RESEARCH.md §Data Layer → `?? "USD"` removal — ACTUAL LINE IS 349
    - Each 22-NN-SUMMARY.md for plan list wording
  </read_first>
  <action>
    1. Open `.planning/ROADMAP.md`.
    2. **Edit A — SC #3 wording.** Find the Phase 22 Success Criteria list. Item #3 currently reads (approximately):
       ```
       3. When zero `salary_intelligence` rows exist for a job, the detail sheet shows the AI-RENDER-04 empty-state messaging for that section and does NOT crash — the defensive `LEFT JOIN LATERAL` returns null cleanly for both `job_id` and `company_name` keying
       ```
       Replace with:
       ```
       3. When zero `salary_intelligence` rows exist for a job, the detail sheet shows the AI-RENDER-04 empty-state messaging for that section and does NOT crash — the defensive `LEFT JOIN LATERAL` skeleton (WHERE FALSE) returns null cleanly today, and tolerates any future schema shape the n8n workflow produces via a 1-line predicate edit (the live `salary_intelligence` table is keyed on `search_date` with no `job_id` / `company_name` columns; the original SC wording was based on a pre-CONTEXT.md assumption about upstream — corrected 2026-04-22 during Phase 22 planning per `.planning/phases/22-salary-intelligence-defensive-render/22-CONTEXT.md` §Phase Boundary)
       ```
    3. **Edit B — SC #5 line number.** Find SC #5 — currently contains `jobs-db.ts:328`. Replace `328` with `349`:
       ```
       5. The `?? "USD"` currency default at `jobs-db.ts:349` is removed; when `salary_currency` is null the salary block hides entirely rather than mislabeling a GBP/EUR figure with `$`
       ```
    4. **Edit C — Phase 22 Plans block.** Find the `**Plans**: TBD` line (near line 188). Replace the `TBD` line with:
       ```
       **Plans:** 8 plans
       Plans:
       - [x] 22-01-PLAN.md — SalaryIntelligenceSchema + CompanyResearchSchema nullable cascade (D-01, D-12 prep) — 2026-04-22
       - [x] 22-02-PLAN.md — LEFT JOIN LATERAL + SalaryIntelligence type + tri-field attachFreshness (AI-DATA-01 + AI-DATA-02) — 2026-04-22
       - [x] 22-03-PLAN.md — Remove `?? "USD"` + flip CompanyResearch.salary_currency nullable (D-12 cascade; grep gate G-6) — 2026-04-22
       - [x] 22-04-PLAN.md — Schema-drift EXPECTED map gains salary_intelligence (D-04) — 2026-04-22
       - [x] 22-05-PLAN.md — ProvenanceTag primitives (provenanceColor/Label + component) (AI-RENDER-07 foundation) — 2026-04-22
       - [x] 22-06-PLAN.md — SalaryIntelligenceSection + EMPTY_STATE_COPY + parseSalaryHeadline + formatSingleSalary (AI-RENDER-03) — 2026-04-22
       - [x] 22-07-PLAN.md — Mount in job-detail-sheet + provenance retrofits + D-12 currency guards (AI-RENDER-03 + AI-RENDER-07) — 2026-04-22
       - [x] 22-08-PLAN.md — Meta-doc finalization (this plan) — 2026-04-22
       ```
    5. **Edit D — Progress table row.** Find the Progress table in v3.0 section (around line 216). Update the Phase 22 row from:
       ```
       | 22. Salary Intelligence (Defensive Render) | 0/0 | Not started | - |
       ```
       To:
       ```
       | 22. Salary Intelligence (Defensive Render) | 8/8 | Complete | 2026-04-22 |
       ```
       **Note on production UAT:** Per the v3.0 sequencing decision in STATE.md (locked 2026-04-22 after Phase 21 CI/CD investigation), Phase 22 code ships code-complete; production UAT of the accumulated v3.0 backlog executes retroactively in v3.5-P4. If the owner expects the deferred-UAT marker: use `| 22. Salary Intelligence (Defensive Render) | 8/8 | Code complete (prod UAT deferred to v3.5) | 2026-04-22 |` — match the Phase 21 precedent.
    6. No other ROADMAP.md edits.
  </action>
  <verify>
    <automated>grep -c "22-01-PLAN.md" .planning/ROADMAP.md; grep -c "jobs-db.ts:349" .planning/ROADMAP.md; grep -c "jobs-db.ts:328" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "jobs-db.ts:349" .planning/ROADMAP.md` returns at least 1 match.
    - `grep -n "jobs-db.ts:328" .planning/ROADMAP.md` returns 0 matches (stale reference removed).
    - `grep -n "job_id.*company_name.*keying" .planning/ROADMAP.md` returns 0 matches (stale SC #3 wording removed — OR modified to the new wording which mentions these phrases only in the correction context; use `grep -E "(WHERE FALSE|1-line predicate edit)" .planning/ROADMAP.md` → at least 1 match from the new wording).
    - `grep -c "22-0[1-8]-PLAN.md" .planning/ROADMAP.md` returns at least 8 matches (one per plan in the new list).
    - Progress table has a row with `22. Salary Intelligence (Defensive Render)` and `8/8`.
    - No other unintended edits — `git diff .planning/ROADMAP.md` shows only the 4 regions touched.
  </acceptance_criteria>
  <done>ROADMAP.md reflects Phase 22 state.</done>
</task>

<task type="auto">
  <name>Task 22-08-02: Update REQUIREMENTS.md — 4 REQ checkmarks + traceability rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — 4 Phase 22 REQs all `[ ]` unchecked; traceability table near line 86-111 has them marked Pending)
  </read_first>
  <action>
    1. Open `.planning/REQUIREMENTS.md`.
    2. **Edit A — v1 checklist.** Find each of the 4 Phase 22 REQ lines (AI-RENDER-03, AI-RENDER-07, AI-DATA-01, AI-DATA-02) in the `## v1 Requirements` section. Flip each from `- [ ]` to `- [x]`. Keep the descriptive text unchanged.
    3. **Edit B — Traceability table.** Find the traceability table in `## Traceability` section. Update these 4 rows:

       From:
       ```
       | AI-RENDER-03 | Phase 22 | Pending |
       | AI-RENDER-07 | Phase 22 | Pending |
       | AI-DATA-01 | Phase 22 | Pending |
       | AI-DATA-02 | Phase 22 | Pending |
       ```

       To:
       ```
       | AI-RENDER-03 | Phase 22 (22-06, 22-07) | Code complete (2026-04-22) — prod UAT deferred to v3.5 |
       | AI-RENDER-07 | Phase 22 (22-05, 22-07) | Code complete (2026-04-22) — prod UAT deferred to v3.5 |
       | AI-DATA-01 | Phase 22 (22-02) | Code complete (2026-04-22) — prod UAT deferred to v3.5; WHERE FALSE skeleton pending n8n task #11 upstream fix |
       | AI-DATA-02 | Phase 22 (22-01, 22-02) | Code complete (2026-04-22) — prod UAT deferred to v3.5 |
       ```
    4. **Edit C — Footer timestamp.** Update the `*Last updated:* ...` line at the bottom of REQUIREMENTS.md to:
       ```
       *Last updated: 2026-04-22 — Phase 22 (4 REQs: AI-RENDER-03, AI-RENDER-07, AI-DATA-01, AI-DATA-02) marked "Code complete — prod UAT deferred to v3.5" per the same CI/CD pipeline block documented in `.planning/notes/ci-cd-fragility-analysis.md`. Phase 22 ships defensive LEFT JOIN LATERAL skeleton (WHERE FALSE today; 1-line predicate edit when n8n task #11 lands) + SalaryIntelligence Zod schema + SalaryIntelligenceSection renderer + ProvenanceTag primitives + D-12 currency cascade complete. All 4 REQs code-complete + full suite green.*
       ```
    5. No other edits.
  </action>
  <verify>
    <automated>grep -c "\\[x\\] \\*\\*AI-RENDER-03" .planning/REQUIREMENTS.md; grep -c "\\[x\\] \\*\\*AI-RENDER-07" .planning/REQUIREMENTS.md; grep -c "\\[x\\] \\*\\*AI-DATA-01" .planning/REQUIREMENTS.md; grep -c "\\[x\\] \\*\\*AI-DATA-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\[x\\] \\*\\*AI-RENDER-03" .planning/REQUIREMENTS.md` → at least 1.
    - `grep -c "\\[x\\] \\*\\*AI-RENDER-07" .planning/REQUIREMENTS.md` → at least 1.
    - `grep -c "\\[x\\] \\*\\*AI-DATA-01" .planning/REQUIREMENTS.md` → at least 1.
    - `grep -c "\\[x\\] \\*\\*AI-DATA-02" .planning/REQUIREMENTS.md` → at least 1.
    - Traceability table has 4 rows mentioning `Phase 22 (22-*)` with "Code complete (2026-04-22)".
    - `grep -n "n8n task #11" .planning/REQUIREMENTS.md` → at least 1 (AI-DATA-01 explanatory note).
    - Footer mentions Phase 22 + 2026-04-22.
  </acceptance_criteria>
  <done>REQUIREMENTS.md reflects 4/4 Phase 22 REQs code-complete with clear upstream-dependency notes.</done>
</task>

<task type="auto">
  <name>Task 22-08-03: Update STATE.md — Current Position → Phase 22 complete; What's Next → Phase 23</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — the Current Position block still points to Phase 21 complete; What's Next still says "discuss-phase 22")
  </read_first>
  <action>
    1. Open `.planning/STATE.md`.
    2. **Edit A — frontmatter.** Update `last_updated` to today's ISO timestamp. Update `last_activity` to mention Phase 22 closure. Update `progress.completed_plans` by +8 (reflect the 8 new Phase 22 plans).
    3. **Edit B — Current Position block.** Replace the existing `## Current Position` body from the line starting `Phase: 21 ...` through `Last activity: ...` with a new block:
       ```
       Phase: 22 (Salary Intelligence — Defensive Render) — **CODE COMPLETE; prod UAT DEFERRED-to-v3.5**
       Plan: 8/8 plans complete (22-01 through 22-08). Phase 22 closes code-complete; production UAT is deferred to v3.5-P4 per the v3.0 sequencing decision (same CI/CD pipeline block as Phase 21).
       Status: 4/4 Phase 22 REQs (AI-RENDER-03, AI-RENDER-07, AI-DATA-01, AI-DATA-02) code-complete; full suite green; zero hardcoded Tailwind colors; grep gates G-1/G-2/G-3/G-4/G-5/G-6/G-7 all enforced by tests or CI. Defensive LEFT JOIN LATERAL skeleton (WHERE FALSE predicate) in getJobDetail tolerates zero rows today (pending n8n task #11 upstream fix) + future column shape via 1-line predicate edit. Phase 22 new artifacts: src/lib/provenance.ts + src/lib/parse-salary-report.ts + src/lib/format-salary.ts + src/app/(admin)/admin/jobs/salary-intelligence-section.tsx + src/app/(admin)/admin/jobs/provenance-tag.tsx (plus existing-file edits to jobs-db.ts, jobs-schemas.ts, empty-state-copy.ts, attach-freshness.ts, job-actions.ts, check-jobs-schema.ts, job-detail-sheet.tsx). D-12 currency cascade complete: `?? "USD"` removed at jobs-db.ts:349; CompanyResearch.salary_currency is now `string | null`; header + Company Intel salary blocks guard on `&& detail.*.salary_currency` to hide cleanly when null.
       Last activity: 2026-04-22 — Phase 22 closed code-complete; 4 REQs shipped; meta-docs finalized
       ```
    4. **Edit C — What's Next block.** Replace the existing `## What's Next` body — specifically the `### Immediate next step` block — with:
       ```
       ### Immediate next step

       **`/gsd-discuss-phase 23`** — Owner-Triggered Workflows (Pattern Setter). 5 REQs: AI-ACTION-03 + AI-ACTION-04 + AI-SAFETY-02 + AI-SAFETY-03 + AI-SAFETY-04. Depends only on Phase 20. Establishes the HMAC-SHA256 + X-Idempotency-Key + sentinel-error scrubbing pattern that Phase 24 then copies to salary/resume regenerate.
       ```
    5. **Edit D — Sequenced milestone order.** Update the v3.0 progress list to reflect Phase 22 now complete:
       ```
       - **v3.0 AI Integration** (in progress, 3/5 phases code-complete):
         - Phase 20: Foundation — COMPLETE (2026-04-21)
         - Phase 21: Polish — CODE COMPLETE 2026-04-22, prod UAT deferred to v3.5-P4
         - Phase 22: Salary Intelligence — CODE COMPLETE 2026-04-22, prod UAT deferred to v3.5-P4
         - **Phase 23: Owner-Triggered Workflows — NEXT**
         - Phase 24: Regenerate Expansion — depends on Phase 22 + 23
       ```
    6. **Edit E — Last Session entry.** Prepend a new entry at the top of the `## Last Session` block:
       ```
       2026-04-22 <TIME> UTC — Phase 22 executed end-to-end. 8/8 plans complete. 4 REQs shipped code-complete: AI-RENDER-03 (SalaryIntelligenceSection renders with Streamdown prose + optional headline row + 3-branch shell), AI-RENDER-07 (ProvenanceTag primitives + 2 retrofits in job-detail-sheet.tsx + 3 tags inside the headline row = every dollar figure source-labeled per Pitfall 5 mitigation), AI-DATA-01 (defensive LEFT JOIN LATERAL with WHERE FALSE skeleton — pending n8n task #11 upstream fix for real rows), AI-DATA-02 (SalaryIntelligenceSchema + SalaryIntelligence TS interface). D-12 currency cascade complete: `?? "USD"` removed at jobs-db.ts:349; CompanyResearch.salary_currency → `string | null`. Grep gates G-1 through G-7 all enforced. Five new source files (provenance.ts + provenance-tag.tsx + salary-intelligence-section.tsx + parse-salary-report.ts + format-salary.ts) + 7 existing files edited. ROADMAP.md SC #3 wording corrected (stale `job_id and company_name keying` → accurate `WHERE FALSE skeleton + 1-line predicate edit`); SC #5 line number updated 328 → 349. Production UAT deferred to v3.5-P4 per the same CI/CD pipeline block as Phase 21. See `.planning/phases/22-salary-intelligence-defensive-render/22-SUMMARY.md`.
       ```
    7. No other edits to STATE.md.
  </action>
  <verify>
    <automated>grep -c "Phase 22.*CODE COMPLETE" .planning/STATE.md; grep -c "gsd-discuss-phase 23" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Phase 22.*CODE COMPLETE" .planning/STATE.md` returns at least 1 match.
    - `grep -n "gsd-discuss-phase 23" .planning/STATE.md` returns at least 1 match.
    - The Sequenced milestone order v3.0 list shows 3/5 phases code-complete.
    - Last Session block has a new entry dated 2026-04-22 mentioning Phase 22.
    - Frontmatter `last_updated` matches today's ISO date.
  </acceptance_criteria>
  <done>STATE.md advances to Phase 22 complete.</done>
</task>

<task type="auto">
  <name>Task 22-08-04: Create 22-SUMMARY.md — phase rollup</name>
  <files>.planning/phases/22-salary-intelligence-defensive-render/22-SUMMARY.md</files>
  <read_first>
    - All 22-NN-SUMMARY.md files from Plans 22-01 through 22-07
    - .planning/phases/20-foundation-freshness-zod-tailored-resume/20-SUMMARY.md (analog — what a phase-level SUMMARY looks like, if present; otherwise mirror the plan-level SUMMARY structure scaled to the phase)
    - .planning/phases/21-polish-copy-pdf-empty-states-link-out/21-09-SUMMARY.md (analog meta-finalization SUMMARY)
  </read_first>
  <action>
    1. Create `.planning/phases/22-salary-intelligence-defensive-render/22-SUMMARY.md`:

       ```markdown
       ---
       phase: 22-salary-intelligence-defensive-render
       closed: 2026-04-22
       status: code-complete (prod UAT deferred to v3.5-P4)
       plans_complete: 8/8
       requirements_complete: 4/4 (AI-RENDER-03, AI-RENDER-07, AI-DATA-01, AI-DATA-02)
       test_count_delta: +30 (estimate; exact count in per-plan SUMMARYs)
       ---

       # Phase 22 — Salary Intelligence (Defensive Render)

       ## Outcome

       All 4 REQs shipped code-complete. Defensive `salary_intelligence` renderer ships BEFORE n8n task #11 lands — the `LEFT JOIN LATERAL` skeleton (`WHERE FALSE`) returns zero rows today, so every job renders the `null` branch with `EMPTY_STATE_COPY.salary_intelligence.missing` copy. When task #11 fills rows, the predicate tightens via a 1-line edit in `jobs-db.ts` (no other code changes required — the Zod schema + renderer + freshness dispatch all accept the real shape).

       ## Deliverables

       ### Source files created
       - `src/lib/provenance.ts` — pure `provenanceColor` / `provenanceLabel` + `ProvenanceSource` type union
       - `src/lib/parse-salary-report.ts` — `parseSalaryHeadline` pure type-guard + `HeadlineShape` / `HeadlineFigure` types
       - `src/lib/format-salary.ts` — `formatSingleSalary(n, currency)` pure formatter (null-currency → empty string per D-12)
       - `src/app/(admin)/admin/jobs/salary-intelligence-section.tsx` — client component with 3-branch render (missing / empty / populated)
       - `src/app/(admin)/admin/jobs/provenance-tag.tsx` — reusable Badge+Tooltip wrapper
       - `src/__tests__/lib/provenance.test.ts` — 11+ pure-function cases
       - `src/__tests__/components/salary-intelligence-section.test.tsx` — 12+ component cases (branches + headline + drift guard)
       - `src/__tests__/components/job-detail-sheet.test.tsx` — grep-gate G-1 + currency-null + ProvenanceTag render cases

       ### Source files edited
       - `src/lib/jobs-schemas.ts` — +SalaryIntelligenceSchema; CompanyResearchSchema.salary_currency → `.nullable()`
       - `src/lib/jobs-db.ts` — +SalaryIntelligence TS interface; +LEFT JOIN LATERAL subquery (WHERE FALSE today); +parseOrLog wiring; +FreshJobDetail extension; CompanyResearch.salary_currency → `string | null`; `?? "USD"` default removed (D-12)
       - `src/lib/empty-state-copy.ts` — +salary_intelligence const entry (2 verbatim strings)
       - `src/lib/attach-freshness.ts` — dual-field → tri-field dispatch (now handles generated_at / search_date / created_at)
       - `src/lib/job-actions.ts` — +attachFreshness<SalaryIntelligence> call in fetchJobDetail
       - `src/app/(admin)/admin/jobs/job-detail-sheet.tsx` — +2 imports; +SalaryIntelligenceSection mount (SectionErrorBoundary wrapped, inserted between Tailored Resume and Company Intel with new leading Separator); +2 <ProvenanceTag> retrofits (header `source="scraped"`; Company Intel `source="company_research"`); +2 D-12 currency guards
       - `scripts/check-jobs-schema.ts` — EXPECTED map gains salary_intelligence entry (7 columns: id, search_date, report_json, raw_results, llm_analysis, created_at, updated_at)
       - `src/__tests__/lib/jobs-db-zod.test.ts` — +SalaryIntelligenceSchema describe block (12+ cases) +CompanyResearchSchema null-currency cascade block (3 cases)
       - `src/__tests__/lib/attach-freshness.test.ts` — +tri-field dispatch describe block (3 cases)

       ### Test count delta
       ~+30 vitest cases across the phase (exact count per-plan summaries).

       ### Commits
       - `feat(22-01): add SalaryIntelligenceSchema + flip CompanyResearch.salary_currency to nullable`
       - `feat(22-02): LEFT JOIN LATERAL salary_intelligence (WHERE FALSE skeleton) + SalaryIntelligence type + tri-field attachFreshness`
       - `feat(22-03): remove ?? "USD" currency default + CompanyResearch.salary_currency nullable`
       - `feat(22-04): add salary_intelligence to schema-drift EXPECTED map`
       - `feat(22-05): provenanceColor + provenanceLabel + ProvenanceTag component`
       - `feat(22-06): SalaryIntelligenceSection component + EMPTY_STATE_COPY.salary_intelligence + parseSalaryHeadline + formatSingleSalary`
       - `feat(22-07): mount SalaryIntelligenceSection in job-detail-sheet + 2 ProvenanceTag retrofits + D-12 currency guards`
       - `docs(22-08): meta-doc finalization — ROADMAP SC #3/#5 + REQUIREMENTS traceability + STATE + SUMMARY`

       ### Grep gates enforced

       - **G-1** (provenance-tag adjacency): every `formatSalary(` in `job-detail-sheet.tsx` is within 5 lines of `<ProvenanceTag` OR `<Badge variant="outline"` (or inside a self-tagging component). Enforced by unit test.
       - **G-2** (no raw Tailwind color classes in Phase 22 code): zero matches in provenance.ts / provenance-tag.tsx / salary-intelligence-section.tsx.
       - **G-3** (EMPTY_STATE_COPY source of truth): both salary_intelligence strings imported, never inlined. Enforced by unit test drift guard.
       - **G-4** (SectionErrorBoundary wrap): `<SalaryIntelligenceSection>` wrapped in `<SectionErrorBoundary section="salary_intelligence">`. Enforced by unit test.
       - **G-5** (anti-CTA on empty-state copy): both strings pass the imperative-verb + `!` + single-period regex gate.
       - **G-6** (no `?? "USD"` resurrection): zero matches in jobs-db.ts. Verified by command + expected to be re-verified in v3.5-P4 UAT.
       - **G-7** (tabular-nums on headline figures): every figure `<span>` carries `tabular-nums` class.

       ## Deferred to v3.5-P4

       - Production UAT on `https://thehudsonfam.com/admin/jobs` — blocked by the same CI/CD pipeline break documented in `.planning/notes/ci-cd-fragility-analysis.md`. Phase 22 code is 100% green locally; UAT happens retroactively once v3.5 migrates deploy to GitHub Actions + GHCR.

       ## Awaiting upstream (homelab repo)

       - n8n task #11 (batch-INSERT `$N`-collision bug) — when fixed, the `WHERE FALSE` predicate in `getJobDetail` tightens to a real match condition (e.g., `si.report_json->>'company_name' ILIKE j.company OR si.report_json->>'job_id' = j.id::text`). Until then, every detail sheet renders the `null` branch (missing-state copy).

       ## Follow-up notes for Phase 23 planner

       - ProvenanceTag primitive + src/lib/provenance.ts are reusable for Phase 23/24 surfaces where a figure's source needs labeling.
       - SectionErrorBoundary label `salary_intelligence` is already in the SECTION_LABELS map — Phase 23 regenerate UI can refer to it if needed.
       - D-12 cascade is complete at the render layer — any future null-currency edge cases are already handled.

       ## Known non-issues

       - `raw_results` column is present in SalaryIntelligence interface but never rendered (D-06 — it's a debugging text dump from the raw web-search results, not owner-value).
       - `updated_at` may be null on first insert — schema accepts; renderer doesn't consume.
       - Salary regenerate button is explicitly deferred to Phase 24 AI-ACTION-06.
       ```
    2. No other file changes.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-salary-intelligence-defensive-render/22-SUMMARY.md &amp;&amp; grep -c "^## " .planning/phases/22-salary-intelligence-defensive-render/22-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/22-salary-intelligence-defensive-render/22-SUMMARY.md` exists.
    - File has at least 7 `##` section headings (Outcome / Deliverables / Grep gates / Deferred / Awaiting / Follow-up / Known).
    - Mentions each of the 4 REQs by ID.
    - Mentions all 8 plan commits.
  </acceptance_criteria>
  <done>Phase-level SUMMARY in place. Future agents can orient on Phase 22 closure in one file.</done>
</task>

<task type="auto">
  <name>Task 22-08-05: Commit — docs(22-08): meta-doc finalization</name>
  <files>(git commit)</files>
  <read_first>(none)</read_first>
  <action>
    Run:
    ```bash
    gsd-sdk query commit "docs(22-08): Phase 22 meta-doc finalization — ROADMAP SC #3 corrected + SC #5 line 328→349 + 4 REQ traceability + STATE advance + 22-SUMMARY" \
      .planning/ROADMAP.md \
      .planning/REQUIREMENTS.md \
      .planning/STATE.md \
      .planning/phases/22-salary-intelligence-defensive-render/22-SUMMARY.md
    ```
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "docs(22-08)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` shows `docs(22-08)` and mentions `meta-doc` or `SC`.
    - `git diff HEAD~1 HEAD --name-only` lists exactly 4 files (all 4 meta-docs).
  </acceptance_criteria>
  <done>Phase 22 closed. Commit landed. Next phase: `/gsd-discuss-phase 23`.</done>
</task>

</tasks>

<verification>
- `grep -c "22-0[1-8]-PLAN.md" .planning/ROADMAP.md` → at least 8.
- `grep -n "jobs-db.ts:349" .planning/ROADMAP.md` → at least 1; `grep -n "jobs-db.ts:328" .planning/ROADMAP.md` → 0.
- All 4 Phase 22 REQs show `[x]` in REQUIREMENTS.md v1 checklist.
- STATE.md frontmatter `last_updated` is today's ISO timestamp.
- 22-SUMMARY.md exists and has the required sections.
</verification>

<success_criteria>
- ROADMAP SC #3 wording reflects reality (no `job_id and company_name keying` assumption).
- SC #5 line number updated 328 → 349.
- Phase 22 plan list fully populated.
- All 4 REQs traceably mapped to their contributing plans.
- STATE.md position advanced.
- Phase-level SUMMARY written and committed.
- Phase 22 closes as 8/8 plans complete.
</success_criteria>

<output>
After commit, no further per-plan SUMMARY is needed for 22-08 beyond the commit itself — the phase-level 22-SUMMARY.md IS the closure artifact. (Optional: if a 22-08-SUMMARY.md is desired for symmetry with other plans, keep it to ~20 lines listing the 4 meta-doc edits.)
</output>
