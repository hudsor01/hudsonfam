---
phase: 37-data-model-actions-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/photo-queries.ts
  - src/__tests__/lib/photo-queries.test.ts
  - src/app/api/photos/route.ts
  - src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx
  - src/__tests__/api/photos-published-default.test.ts
autonomous: true
requirements: [VIS-01, VIS-02]
must_haves:
  truths:
    - "A reusable query returns exactly the photos with no album-kind CollectionPhoto, newest-first"
    - "Adding a photo to an album removes it from the uncollected set; removing it returns it"
    - "A newly uploaded photo with no published field defaults to published:true"
    - "The /api/images published auth gate is left intact (no change)"
  artifacts:
    - path: "src/lib/photo-queries.ts"
      provides: "getUncollectedPhotos() — All-Photos query shared by Phases 38/39"
      exports: ["getUncollectedPhotos"]
    - path: "src/__tests__/lib/photo-queries.test.ts"
      provides: "Unit coverage for the uncollected query shape + ordering"
      contains: "getUncollectedPhotos"
    - path: "src/app/api/photos/route.ts"
      provides: "Upload default published:true (canonical default)"
      contains: "published"
  key_links:
    - from: "src/lib/photo-queries.ts"
      to: "prisma.photo"
      via: "findMany where collections none album-kind, orderBy createdAt desc"
      pattern: "none|createdAt"
    - from: "src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx"
      to: "/api/photos"
      via: "published default true"
      pattern: "published"
---

<objective>
Two locked deliverables, both data/code-layer only:

1. All-Photos helper — add a reusable query `getUncollectedPhotos()` in a new lib module so Phase 38 (`/photos`) and Phase 39 (dashboard) share one definition of "All Photos". "All Photos" = photos with NO `CollectionPhoto` row pointing at an `album`-kind collection (surface memberships like memorial/featured do NOT remove a photo from All Photos). Order newest-first (`createdAt desc`).

2. Visibility default flip (VIS-01 / VIS-02 code layer) — new uploads default to public. The upload path must treat `published: true` as the canonical default so a photo is publicly visible the moment it's uploaded, with no owner publish step. The form already initializes the toggle to true; make the server-side default explicit and unambiguous, and keep the form default true. The publish-toggle UI removal is Phase 39 and the existing-row backfill is Phase 40 — do NOT do those here.

Purpose: gives the public/dashboard surfaces in Phases 38/39 a single uncollected-photos query to import, and makes "uploaded = visible" true at the code layer.

OUT OF PHASE (do NOT do here): removing the `published` column, changing the `/api/images` auth gate, removing the publish-toggle UI (Phase 39), backfilling existing rows to published:true (Phase 40, gated live-DB), any Prisma schema change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-data-model-actions-foundation/37-CONTEXT.md

<interfaces>
<!-- The memorial media page already shows the "all library photos minus this collection" pattern. -->
<!-- All-Photos is the inverse: photos NOT in ANY album-kind collection. From prisma/schema.prisma: -->
- Photo has `collections CollectionPhoto[]`. CollectionPhoto.collection has `kind` ("album" | "surface").
- Prisma relational filter for "no album membership": photo.findMany({ where: { collections: { none: { collection: { kind: "album" } } } }, orderBy: { createdAt: "desc" } }).
- A photo that is ONLY in surface collections (memorial/featured) still counts as uncollected → appears in All Photos.

<!-- Current upload published resolution (from src/app/api/photos/route.ts) -->
```ts
const publishedRaw = formData.get("published") as string | null;
// missing OR "on" OR "true" → true; "false" → false
const published =
  publishedRaw === null || publishedRaw === "on" || publishedRaw === "true";
// Photos added to a collection are always published
const finalPublished = collectionId ? true : published;
```

<!-- Current form state (from upload-form.tsx) -->
```ts
const [publishNow, setPublishNow] = useState(true);
formData.append("published", publishNow ? "true" : "false");
```

<!-- Test harness facts -->
- prismaMock.photo.findMany is a vi.fn(); assert the where/orderBy passed to it.
- For the route test, mock @/lib/auth getSession, @/lib/images processImage, @/lib/prisma (via mocks/prisma), and next/headers. See src/__tests__/lib/dashboard-actions.test.ts for the mocking style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add getUncollectedPhotos() All-Photos helper</name>
  <files>src/lib/photo-queries.ts, src/__tests__/lib/photo-queries.test.ts</files>
  <read_first>
    - src/app/(dashboard)/dashboard/memorial/media/page.tsx (the surface-collection library-photos pattern this generalizes; note `orderBy: { createdAt: "desc" }`)
    - prisma/schema.prisma (Photo.collections, CollectionPhoto.collection.kind)
    - src/lib/prisma.ts (import the singleton as default)
    - src/__tests__/lib/collection-actions.test.ts (mocking style: `import '../mocks/prisma'` then `import { prismaMock }`)
    - src/__tests__/mocks/prisma.ts (photo.findMany is available)
  </read_first>
  <behavior>
    - getUncollectedPhotos() returns photos where the relation `collections` has NONE whose related collection.kind === "album".
    - Results are ordered createdAt descending (newest first).
    - A photo in only surface collections (memorial/featured) is still returned (it has no album membership).
    - A photo in at least one album-kind collection is excluded.
    - The helper is a plain async query function (no requireRole — it's a read used by both public and dashboard callers; callers gate their own pages). It must NOT carry a "use server" action directive; it is an importable query, not a mutation.
  </behavior>
  <action>
    Create src/lib/photo-queries.ts exporting an async `getUncollectedPhotos()` that calls prisma.photo.findMany with a relational `where` excluding any album-kind membership (`collections: { none: { collection: { kind: "album" } } }`) and `orderBy: { createdAt: "desc" }`. Keep the album-kind slug/string as a shared constant if useful, mirroring the kind values in schema.prisma. Do NOT add "use server" (this is a query helper, not a server action). Optionally accept a `select`/`include` passthrough only if trivial — otherwise return full Photo rows; keep it minimal and documented so Phases 38/39 can import it directly.
    Write src/__tests__/lib/photo-queries.test.ts asserting: (a) findMany is called with a where that uses `collections: { none: { collection: { kind: "album" } } }`; (b) orderBy createdAt desc; (c) the function returns whatever findMany resolves (pass-through).
  </action>
  <verify>
    <automated>npm test -- src/__tests__/lib/photo-queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/__tests__/lib/photo-queries.test.ts` exits 0.
    - Source assertion: `grep -n "none" src/lib/photo-queries.ts` shows the relational `none` filter on album-kind collections.
    - Source assertion: `grep -n "createdAt" src/lib/photo-queries.ts` shows `orderBy` newest-first.
    - Source assertion: `grep -c '"use server"' src/lib/photo-queries.ts` returns 0 (it is a query helper, not an action).
    - A test proves a photo in only surface collections would be returned (where filter targets kind "album" only).
  </acceptance_criteria>
  <done>`getUncollectedPhotos()` exists, returns album-uncollected photos newest-first, is importable by Phases 38/39, and is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Default new uploads to published:true (VIS-01/VIS-02 code layer)</name>
  <files>src/app/api/photos/route.ts, src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx, src/__tests__/api/photos-published-default.test.ts</files>
  <read_first>
    - src/app/api/photos/route.ts (the `published` resolution + `finalPublished` lines; this is where the canonical default lives)
    - src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx (the `publishNow` state, already `useState(true)`)
    - src/app/api/images/[...path]/route.ts (the published auth gate — read to confirm it is LEFT INTACT; do NOT modify)
    - .planning/phases/37-data-model-actions-foundation/37-CONTEXT.md (VIS decision: flip default to true; toggle UI removal is Phase 39, backfill is Phase 40)
  </read_first>
  <behavior>
    - A POST to /api/photos with NO `published` form field results in a Photo created with published:true (default-public).
    - A POST with `published:"false"` and no collection still respects false (the explicit owner override path remains, since the toggle UI is removed only in Phase 39).
    - A POST with a collectionId still forces published:true (existing behavior preserved).
    - The upload form continues to send the default true (state stays `useState(true)`); no UI element is removed in this phase.
    - The /api/images auth gate is unchanged.
  </behavior>
  <action>
    In src/app/api/photos/route.ts, make published:true the canonical default for new uploads: keep the resolution so a missing `published` field yields true, and keep the existing "collection forces published" rule. Ensure the default is explicit and documented as the v6.0 default-public behavior (reference VIS-01/VIS-02). Do NOT remove the `published` column, do NOT touch the /api/images gate. In upload-form.tsx leave the default true (state already initializes to true) — do NOT remove the publish-now checkbox (that removal is Phase 39); only confirm/keep the default-true behavior.
    Add src/__tests__/api/photos-published-default.test.ts covering the published resolution: missing field → true; "false" → false; collectionId present → true regardless. Mock auth/getSession, processImage, prisma (mocks/prisma), and next/headers per the dashboard-actions mocking style; assert prisma.photo.create is called with data.published as expected. (Test the route handler's published-resolution outcome; you may exercise it via the POST handler with a stubbed File/FormData, or factor the resolution into a tiny pure helper in the route module and test that — whichever is cleaner without changing public behavior.)
  </action>
  <verify>
    <automated>npm test -- src/__tests__/api/photos-published-default.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/__tests__/api/photos-published-default.test.ts` exits 0.
    - A test proves a missing `published` field yields a Photo created with published:true.
    - A test proves an explicit `published:"false"` (no collection) yields published:false (override path preserved for Phase 39).
    - A test proves a collectionId forces published:true.
    - Source assertion: `git diff --quiet src/app/api/images/[...path]/route.ts` (the images auth gate is unmodified).
    - Source assertion: upload-form.tsx `publishNow` state still defaults to true (`grep -n "useState(true)" src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx`).
  </acceptance_criteria>
  <done>New uploads default to published:true; the explicit-false override and collection-forces-true rules are preserved; the images gate and toggle UI are untouched; tests green.</done>
</task>

</tasks>

<verification>
- `npm test -- src/__tests__/lib/photo-queries.test.ts src/__tests__/api/photos-published-default.test.ts` passes.
- `npx tsc --noEmit` reports no new type errors in `src/lib/photo-queries.ts` or `src/app/api/photos/route.ts`.
- `prisma/schema.prisma` is unchanged; `src/app/api/images/[...path]/route.ts` is unchanged.
- No publish-toggle UI element is removed and no existing DB rows are mutated by this plan.
</verification>

<success_criteria>
- All-Photos query: `getUncollectedPhotos()` returns exactly the photos with no album-kind membership, newest-first, importable by Phases 38/39 (COLL-01 read side / supports VIS surfaces).
- Default-public uploads: a new upload is published:true by default with no owner action (VIS-01); the toggle removal and existing-row backfill are correctly deferred to Phases 39/40 (VIS-02 scope boundary respected).
</success_criteria>

<output>
Create `.planning/phases/37-data-model-actions-foundation/37-02-SUMMARY.md` when done.
</output>
