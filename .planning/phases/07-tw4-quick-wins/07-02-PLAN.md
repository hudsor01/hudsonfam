---
phase: 07-tw4-quick-wins
type: execute
---

<objective>
Add accent-color/caret-color to form elements, add open/not-* variants.

Purpose: Theme native form controls with project accent colors and leverage Tailwind v4 state variants for cleaner conditional styling.
Output: Checkboxes, radios, and range inputs use accent-primary; inputs/textareas show themed caret; details elements and list dividers use open:/not-* variants.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/input.tsx — Input component
@src/components/ui/select.tsx — Select component
@src/app/(public)/events/page.tsx — Events page with past events section (potential details/summary)
@src/app/(dashboard)/ — Dashboard pages with form components and list pages

**Tailwind v4 utilities to apply:**
- `accent-primary` — CSS `accent-color` theming for checkboxes, radios, range inputs
- `caret-primary` — CSS `caret-color` theming for text input cursors
- `open:` variant — Styles applied when a details element is open
- `not-last:` variant — Styles applied to all but the last child (replaces manual last:hidden dividers)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accent-primary to checkboxes/radios/range and caret-primary to inputs/textareas</name>
  <files>form components, src/components/ui/input.tsx, src/components/ui/select.tsx</files>
  <action>Add accent-primary class to all checkbox, radio, and range input elements. Add caret-primary class to all text input and textarea elements. Update the base Input component and any inline input/textarea usage in dashboard forms. Search for all checkbox/radio/range elements across the codebase to ensure comprehensive coverage.</action>
  <verify>bun run build passes without errors</verify>
  <done>All native form controls use project accent color, text inputs show themed caret</done>
</task>

<task type="auto">
  <name>Task 2: Add open: variant to details elements and not-last: for list dividers</name>
  <files>src/app/(public)/events/page.tsx, dashboard list pages</files>
  <action>Add open: variant styling to the details element on the events page (past events section) for smooth expand/collapse visual feedback — e.g., open:bg-muted/50, open:border-primary. Replace manual last-child divider logic (conditional border-b, last:border-0 patterns) with not-last:border-b across dashboard list pages and public list pages. Search for existing last: conditional divider patterns and replace with not-last: equivalents.</action>
  <verify>bun run build passes without errors</verify>
  <done>Details elements styled with open: variant, list dividers use not-last: instead of manual conditionals</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] Checkboxes/radios/range inputs show accent-primary color
- [ ] Text inputs and textareas show caret-primary color
- [ ] Details elements have open: variant styling
- [ ] List dividers use not-last: variant
</verification>

<success_criteria>
- All form controls themed with accent-primary
- Input cursors use caret-primary
- open: variant applied to collapsible sections
- not-last: replaces manual divider conditionals
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-tw4-quick-wins/07-02-SUMMARY.md`
</output>
