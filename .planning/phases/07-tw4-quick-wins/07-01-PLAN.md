---
phase: 07-tw4-quick-wins
type: execute
---

<objective>
Add text-balance to headings and text-pretty to body text, add field-sizing-content to textareas.

Purpose: Leverage Tailwind v4 typography utilities for visually balanced headings, prettier paragraph wrapping, and auto-expanding textareas.
Output: All headings use text-balance, body text uses text-pretty, textareas auto-size to content.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/public/hero.tsx — Hero section with h1 heading
@src/components/ui/section-header.tsx — Reusable section header with h2
@src/app/(public)/richard-hudson-sr/page.tsx — Memorial page with titles and descriptions
@src/app/(public)/blog/page.tsx — Blog listing with header and excerpts
@src/app/(public)/events/page.tsx — Events page with header and event descriptions

**Tailwind v4 utilities to apply:**
- `text-balance` — CSS `text-wrap: balance` for headings (h1/h2), distributes text evenly across lines
- `text-pretty` — CSS `text-wrap: pretty` for body text, avoids orphans/widows in paragraphs
- `field-sizing-content` — CSS `field-sizing: content` for textareas, auto-expands to fit content
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add text-balance to all h1/h2 elements and text-pretty to body text</name>
  <files>src/components/public/hero.tsx, src/components/ui/section-header.tsx, src/app/(public)/richard-hudson-sr/page.tsx, src/app/(public)/blog/page.tsx, src/app/(public)/events/page.tsx, plus other pages with headings</files>
  <action>Add text-balance class to all h1 and h2 elements across Hero, SectionHeader, memorial titles, blog header, and events header. Add text-pretty class to body text including blog excerpts, memorial descriptions, and event descriptions. Search for all h1/h2 elements and paragraph text across public pages to ensure comprehensive coverage.</action>
  <verify>bun run build passes without errors</verify>
  <done>All h1/h2 headings have text-balance applied, body text paragraphs have text-pretty applied</done>
</task>

<task type="auto">
  <name>Task 2: Add field-sizing-content to all textarea elements in forms</name>
  <files>all form components in src/app/(dashboard)/</files>
  <action>Add field-sizing-content class to all textarea elements in dashboard form components. Search for all textarea usage across form pages (posts new/edit, events new/edit, updates new, memorial content/memories, etc.) and add the utility class. This allows textareas to auto-expand based on their content without JavaScript.</action>
  <verify>bun run build passes without errors</verify>
  <done>All textarea elements auto-expand to fit content using field-sizing-content</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] All h1/h2 elements have text-balance class
- [ ] Body text paragraphs have text-pretty class
- [ ] All textarea elements have field-sizing-content class
</verification>

<success_criteria>
- All headings visually balanced across lines
- Body text wraps without orphaned words
- Textareas auto-expand to fit content
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-tw4-quick-wins/07-01-SUMMARY.md`
</output>
