---
phase: 29-decommission-job-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/jobs/
  - src/app/api/jobs/
  - src/app/(admin)/layout.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true
requirements: [JOB-01, JOB-02]
must_haves:
  truths:
    - "No /admin/jobs route resolves (directory deleted)"
    - "No /api/jobs route resolves (directory deleted, including PDF routes)"
    - "Admin nav shows Overview / Site / Dashboard only — no Jobs link"
    - "Owner dashboard nav shows no Jobs link (the navLinks.push for /admin/jobs is gone)"
    - "next build succeeds with no references to deleted job UI/API"
  artifacts:
    - path: "src/app/(admin)/layout.tsx"
      provides: "Admin nav without Jobs entry"
      contains: "Overview"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Owner dashboard nav without Jobs entry"
      contains: "Admin"
  key_links:
    - from: "src/app/(admin)/layout.tsx"
      to: "removed /admin/jobs Link"
      via: "deletion of nav line 37"
      pattern: "admin/jobs"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "removed /admin/jobs navLinks.push"
      via: "deletion of the owner navLinks.push line"
      pattern: "admin/jobs"
---

<objective>
Delete the job-search admin UI and job API routes, and remove the "Jobs" entry from BOTH navs that render for the owner (the admin nav and the owner's dashboard nav). This is the first of two pure-deletion plans for Phase 29. It runs first so the importers (UI + API route handlers) are gone before Plan 29-02 deletes the lib modules they import — keeping every intermediate state buildable.

Purpose: Shrink the codebase surface before the v4.0 cloud re-platform; the job-search subsystem is being retired entirely (not migrated).
Output: `src/app/(admin)/admin/jobs/` deleted, `src/app/api/jobs/` deleted, both navs cleaned. App still builds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md

<interfaces>
<!-- Admin nav structure the executor edits. From src/app/(admin)/layout.tsx lines 35-40: -->
<!-- A horizontal nav with four Links: Overview (/admin), Jobs (/admin/jobs), Site (/), Dashboard (/dashboard). -->
<!-- Remove ONLY the Jobs Link (line 37). Keep the other three and the surrounding <div className="flex gap-4 ...">. -->

<!-- Owner dashboard nav. From src/app/(dashboard)/layout.tsx lines 38-43: inside the
     `if (userRole === "owner")` block, navLinks.push() adds Members, Memorial, Admin, AND Jobs.
     Remove ONLY `navLinks.push({ href: "/admin/jobs", label: "Jobs" })` (line 42). Keep the
     Members / Memorial / Admin pushes and the rest of the base navLinks array. -->

<!-- Job admin directory (delete entire dir) — src/app/(admin)/admin/jobs/ contains 14 files:
     page.tsx, jobs-dashboard.tsx, kanban-board.tsx, columns.tsx, filters-sidebar.tsx,
     stats-bar.tsx, job-detail-sheet.tsx, salary-intelligence-section.tsx,
     tailored-resume-section.tsx, regenerate-button.tsx, trigger-company-research-button.tsx,
     provenance-tag.tsx, freshness-badge.tsx, section-error-boundary.tsx -->

<!-- Job API directory (delete entire dir) — src/app/api/jobs/ contains:
     [id]/cover-letter-pdf/route.ts, [id]/tailored-resume-pdf/route.ts -->

<!-- NOTE on remaining `admin/jobs` references after THIS plan: deleting the two directories +
     both nav entries does NOT zero out `admin/jobs` across src/. The string still lives in
     src/lib/job-actions.ts (7 `revalidatePath("/admin/jobs")` hits) and in ~11 job test files —
     ALL of those are deleted in Plan 29-02 (wave 2). So the broad `grep -rn "admin/jobs" src/` → 0
     gate belongs to the END of 29-02, NOT here. In 29-01 the gate is scoped to the two nav files only. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the job admin UI directory and remove the Jobs nav entry from both owner-facing navs</name>
  <files>src/app/(admin)/admin/jobs/, src/app/(admin)/layout.tsx, src/app/(dashboard)/layout.tsx</files>
  <read_first>
    - List `src/app/(admin)/admin/jobs/` to confirm the 14 files are present before deleting (page.tsx, jobs-dashboard.tsx, kanban-board.tsx, columns.tsx, filters-sidebar.tsx, stats-bar.tsx, job-detail-sheet.tsx, salary-intelligence-section.tsx, tailored-resume-section.tsx, regenerate-button.tsx, trigger-company-research-button.tsx, provenance-tag.tsx, freshness-badge.tsx, section-error-boundary.tsx).
    - Read `src/app/(admin)/layout.tsx` lines 35-40 to confirm the Jobs Link is at line 37 and the other three nav Links (Overview, Site, Dashboard) surround it.
    - Read `src/app/(dashboard)/layout.tsx` lines 38-43 to confirm the owner `navLinks.push({ href: "/admin/jobs", label: "Jobs" })` is at line 42, inside the `if (userRole === "owner")` block alongside the Members / Memorial / Admin pushes.
  </read_first>
  <action>
    Delete the entire directory `src/app/(admin)/admin/jobs/` (all 14 files). This satisfies JOB-01.
    Then edit `src/app/(admin)/layout.tsx`: remove ONLY the line `<Link href="/admin/jobs" className="hover:text-foreground transition-colors">Jobs</Link>` (currently line 37). Keep the Overview, Site, and Dashboard Links and the enclosing `<div className="flex gap-4 text-sm text-muted-foreground">`.
    Then edit `src/app/(dashboard)/layout.tsx`: remove ONLY the line `navLinks.push({ href: "/admin/jobs", label: "Jobs" })` (currently line 42). Keep the other three owner pushes (Members, Memorial, Admin) and the base navLinks array. JOB-01 requires the Jobs entry gone from the owner's nav — both layouts render for the owner, so both must be cleaned.
    Do not touch any other file in the (admin) or (dashboard) route groups — the homelab dashboard (`src/app/(admin)/admin/page.tsx`) and the family dashboard pages stay.
  </action>
  <acceptance_criteria>
    - `test ! -d "src/app/(admin)/admin/jobs"` exits 0 (directory gone)
    - `grep -rn "admin/jobs" "src/app/(admin)/layout.tsx" "src/app/(dashboard)/layout.tsx"` returns nothing (both nav references removed)
    - `grep -c "Jobs" "src/app/(admin)/layout.tsx"` returns 0
    - `grep -c "admin/jobs" "src/app/(dashboard)/layout.tsx"` returns 0
    - `grep -q "Overview" "src/app/(admin)/layout.tsx"` exits 0 (other admin nav entries preserved)
    - `grep -q "Admin" "src/app/(dashboard)/layout.tsx"` exits 0 (other owner dashboard pushes preserved)
  </acceptance_criteria>
  <done>The /admin/jobs route directory is deleted, and neither the admin nav nor the owner dashboard nav renders a Jobs link, while the rest of both layouts (homelab Overview/Site/Dashboard; family Members/Memorial/Admin) is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the job API routes and verify the build</name>
  <files>src/app/api/jobs/</files>
  <read_first>
    - List `src/app/api/jobs/` to confirm `[id]/cover-letter-pdf/route.ts` and `[id]/tailored-resume-pdf/route.ts` are present before deleting.
    - `grep -rn "api/jobs" src/` to confirm no caller outside the deleted job admin UI references these routes (the admin UI that referenced them was deleted in Task 1).
  </read_first>
  <action>
    Delete the entire directory `src/app/api/jobs/` (both PDF route handlers and the `[id]` subtree). This satisfies JOB-02. Do not touch any other route under `src/app/api/`.
    After deletion, run `next build` to confirm no dangling imports remain from the UI/API deletion. NOTE: the lib modules these routes imported (`src/lib/jobs-db.ts`, `src/lib/job-actions.ts`, etc.) still exist on disk at this wave — they are deleted in Plan 29-02. The build must still pass because nothing references them anymore once the UI + API are gone (orphaned-but-unimported lib files do not break the build). If `next build` surfaces an import error pointing at a file NOT in the job cluster, stop and report it (Rule 3 deviation).
  </action>
  <acceptance_criteria>
    - `test ! -d "src/app/api/jobs"` exits 0 (directory gone)
    - `grep -rn "api/jobs" src/` returns nothing
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>The job API routes are deleted and `next build` succeeds, confirming the job UI + API surface is fully removed without breaking the remaining app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure deletion — no new code, no new input paths, no new dependencies. Attack surface only shrinks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | Accidental deletion of non-job admin/API files | mitigate | `read_first` glob confirms exact file set; deletions scoped to `src/app/(admin)/admin/jobs/` and `src/app/api/jobs/` only; nav edits scoped to single lines; grep gates assert other nav entries (Overview, Admin) survive |
| T-29-02 | Denial of Service | Build breaks from dangling import after partial deletion | mitigate | `next build` is a Task 2 acceptance gate; wave ordering (UI/API before libs) keeps every intermediate state buildable |
</threat_model>

<verification>
- `test ! -d "src/app/(admin)/admin/jobs"` — job admin UI gone
- `test ! -d "src/app/api/jobs"` — job API routes gone
- `grep -rn "admin/jobs" "src/app/(admin)/layout.tsx" "src/app/(dashboard)/layout.tsx"` — returns nothing (both navs cleaned)
- `grep -rn "api/jobs" src/` — returns nothing
- `grep -c "Jobs" "src/app/(admin)/layout.tsx"` — returns 0; `grep -q "Overview"` exits 0
- `grep -c "admin/jobs" "src/app/(dashboard)/layout.tsx"` — returns 0; `grep -q "Admin"` exits 0
- `npx next build` — exits 0
</verification>

<success_criteria>
- JOB-01 satisfied: `src/app/(admin)/admin/jobs/` deleted; no Jobs nav entry in EITHER owner-facing nav (admin layout + owner dashboard layout)
- JOB-02 satisfied: `src/app/api/jobs/` deleted including both PDF routes
- `next build` green; no references to deleted UI/API anywhere in src
- NOTE: the broad `grep -rn "admin/jobs" src/` → 0 gate is owned by Plan 29-02 (job-actions.ts revalidatePath hits + job test files still reference the string until then).
</success_criteria>

<output>
Create `.planning/phases/29-decommission-job-pipeline/29-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
