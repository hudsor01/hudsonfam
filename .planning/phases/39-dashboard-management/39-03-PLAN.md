---
phase: 39-dashboard-management
plan: 03
type: execute
wave: 3
depends_on: ["39-01", "39-02"]
files_modified:
  - src/app/(dashboard)/dashboard/photos/photo-actions.tsx
  - src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx
  - src/app/(dashboard)/dashboard/photos/page.tsx
  - src/__tests__/prod-readiness.test.ts
autonomous: true
requirements: [FEAT-02, FEAT-03, COLL-02]
must_haves:
  truths:
    - "No publish toggle, publish checkbox, or publish-related control appears in the dashboard photo actions menu or the upload form"
    - "New uploads are always sent published:true; the owner is never asked to choose to publish"
    - "The dashboard Photos grid shows a quiet collection/featured status per tile instead of the raw upload filename, and links to the Featured manager"
    - "Source-level tests assert the publish toggle is gone, the featured manager page exists and uses FEATURED_SLUG, and the album page has the add-from-library UI"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/photos/photo-actions.tsx"
      provides: "Photo actions menu with the publish toggle removed (collection + delete kept)"
    - path: "src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx"
      provides: "Upload form with the Publish-now checkbox removed, always published:true"
    - path: "src/app/(dashboard)/dashboard/photos/page.tsx"
      provides: "Photos grid with a quiet status label (not filename) and a Featured manager link"
      contains: "/dashboard/photos/featured"
    - path: "src/__tests__/prod-readiness.test.ts"
      provides: "Source-level v6.0 Phase-39 assertions"
  key_links:
    - from: "src/app/(dashboard)/dashboard/photos/page.tsx"
      to: "/dashboard/photos/featured"
      via: "link to the featured manager"
      pattern: "/dashboard/photos/featured"
    - from: "src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx"
      to: "/api/photos (published field)"
      via: "always append published=true"
      pattern: "published"
---

<objective>
Remove the now-obsolete publish step from the dashboard (ROADMAP 39 success criterion 4 / VIS-02 UI half — all photos are public by default since Phase 37), tidy the dashboard Photos grid to show a quiet status instead of raw filenames + link to the new Featured manager, and add source-level regression assertions covering this phase's three pages. The `setPhotoPublished` server action and the `published` DB column stay in place as plumbing — only the owner-facing toggle is removed.

Wave note: this plan is wave 3 (after Plans 01 + 02) because its Task 3 source-level tests assert on the featured manager page (Plan 01) and the album manage page (Plan 02). The publish-removal edits (Tasks 1-2) touch only this plan's files and have no overlap with Plans 01/02.

Purpose: The owner never thinks about "publishing" — uploads are visible immediately. Filenames are noise (owner doesn't care about names); a collection/featured status is the useful at-a-glance signal.
Output: edited `photo-actions.tsx`, `upload-form.tsx`, `photos/page.tsx`, plus new assertions in `prod-readiness.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-dashboard-management/39-CONTEXT.md

# Files to modify
@src/app/(dashboard)/dashboard/photos/photo-actions.tsx
@src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx
@src/app/(dashboard)/dashboard/photos/page.tsx
# Test patterns (source-level fs.readFile + toContain/not.toContain assertions)
@src/__tests__/prod-readiness.test.ts

<interfaces>
From src/lib/featured.ts:
```typescript
export const FEATURED_SLUG = "featured"; // assertion target: featured page must reference it
```

photos/page.tsx already fetches per-photo `collections: { select: { collectionId } }` and the full `collections` list — so a tile can map its first album membership to a collection title (or "All Photos" when uncollected) without a new query. The featured surface membership is a separate concept; a "Featured" marker is optional polish, but the primary status replaces the filename label.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the publish toggle from photo actions and the upload form</name>
  <files>src/app/(dashboard)/dashboard/photos/photo-actions.tsx, src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx</files>
  <read_first>
    - src/app/(dashboard)/dashboard/photos/photo-actions.tsx (the file being modified — the publish control is the `DropdownMenuCheckboxItem checked={published}` block + `handlePublishToggle` + the `setPhotoPublished` import)
    - src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx (the file being modified — the "Publish now checkbox" block + `publishNow` state + the `formData.append("published", publishNow ? "true" : "false")` line)
    - src/lib/collection-actions.ts (confirm setPhotoPublished STAYS — it is just no longer called from the UI)
  </read_first>
  <action>
    In `photo-actions.tsx`: delete the "Published" `DropdownMenuCheckboxItem` (the one bound to `checked={published}` / `onCheckedChange={handlePublishToggle}`), delete the `handlePublishToggle` function, drop `setPhotoPublished` from the `@/lib/collection-actions` import, and remove the now-unused `published` prop from `PhotoActionsProps` and the component signature. Keep "View Full Size", the "Add to collection" submenu, and Delete intact. The only caller is `photos/page.tsx` (Task 2) — note that it must stop passing `published` so the prop removal stays consistent. In `upload-form.tsx`: delete the "Publish now checkbox" block (the `<div>` with `id="publish-now"`), delete the `publishNow` state (`useState(true)`), and change the form data line so it always sends published true (`formData.append("published", "true")`) — the server default already yields true; the form no longer offers the choice. Do not touch the collection selector, file input, preview, upload button, progress, or results. Keep `setPhotoPublished` and the `published` column untouched in the codebase (out of scope to delete). Theme tokens only.
  </action>
  <verify>
    <automated>grep -ric "publishNow\|handlePublishToggle\|setPhotoPublished\|publish-now\|Published" "src/app/(dashboard)/dashboard/photos/photo-actions.tsx" "src/app/(dashboard)/dashboard/photos/upload/upload-form.tsx" | grep -v ':0$' || echo "publish UI removed from both files"</automated>
  </verify>
  <acceptance_criteria>
    - photo-actions.tsx: no `DropdownMenuCheckboxItem` bound to publish, no `handlePublishToggle`, no `setPhotoPublished` import, no `published` prop in `PhotoActionsProps` or the component signature; "Add to collection" and Delete remain.
    - upload-form.tsx: no `publish-now` input, no `publishNow` state; the upload `formData.append("published", ...)` always sends "true".
    - `setPhotoPublished` still exists in src/lib/collection-actions.ts (not deleted — plumbing kept).
    - `npx tsc --noEmit` is clean (no dangling `published` references after the prop removal — coordinate with Task 2's caller change).
  </acceptance_criteria>
  <done>The owner sees no publish control in the actions menu or upload form; uploads are always published:true.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the filename label with a quiet status + add the Featured manager link</name>
  <files>src/app/(dashboard)/dashboard/photos/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/dashboard/photos/page.tsx (the file being modified — the per-tile `<p>{photo.title || "Untitled"}</p>` label and the top quick-links row; it already loads `collections` and per-photo `collections: { select: { collectionId } }`)
    - src/app/(dashboard)/dashboard/photos/photo-actions.tsx (Task 1 removed its `published` prop — stop passing it here)
  </read_first>
  <action>
    Replace the per-tile filename label (`<p className="text-xs text-foreground truncate">{photo.title || "Untitled"}</p>`) with a quiet status: resolve the photo's album from its `collections` memberships against the fetched `collections` list (find the first album-kind collection the photo belongs to and show its title; if it belongs to no album, show "All Photos"). Render this as muted, lightweight text (`text-xs text-muted-foreground truncate`) — do NOT render the raw upload filename/title as the primary label. (A "Featured" marker is optional polish; the required change is dropping the filename in favor of the collection/All-Photos status.) In the top quick-links row (next to "Manage Collections" / "Upload Photos"), add a link to the Featured manager: `<Link href="/dashboard/photos/featured">` labeled e.g. "Manage Featured" using the same muted card-link styling as the "Manage Collections" link. Update the `<PhotoActions ... />` usage to stop passing the removed `published` prop (Task 1). Keep everything else (SectionHeader, empty state, grid, image, PhotoActions wiring for collections + delete) intact. Theme tokens only; the page stays a Server Component.
  </action>
  <verify>
    <automated>grep -c "/dashboard/photos/featured" "src/app/(dashboard)/dashboard/photos/page.tsx" && ! grep -q 'photo.title || "Untitled"' "src/app/(dashboard)/dashboard/photos/page.tsx" && echo "status label + featured link in place"</automated>
  </verify>
  <acceptance_criteria>
    - The literal `photo.title || "Untitled"` primary filename label is gone; tiles show the photo's album title or "All Photos" in muted text.
    - A link to `/dashboard/photos/featured` exists in the page (the Featured manager link).
    - `<PhotoActions>` no longer receives a `published` prop (matches Task 1's prop removal).
    - The page remains a Server Component; no raw Tailwind color names; SectionHeader / empty state / grid / delete wiring unchanged.
  </acceptance_criteria>
  <done>The Photos grid shows a quiet collection/All-Photos status per tile and links to the Featured manager; no raw filename label.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Source-level regression assertions for Phase 39</name>
  <files>src/__tests__/prod-readiness.test.ts</files>
  <read_first>
    - src/__tests__/prod-readiness.test.ts (the file being modified — copy the existing `fs.readFile(path.join(...)) + expect(src).toContain/not.toContain` source-assertion pattern, e.g. the "image route gates visibility" and "memorial uses the DB hero photo" tests)
    - src/app/(dashboard)/dashboard/photos/featured/page.tsx (Plan 01 output — assertion subject)
    - src/app/(dashboard)/dashboard/photos/albums/[id]/page.tsx (Plan 02 output — assertion subject)
  </read_first>
  <behavior>
    - Test: photo-actions.tsx source does NOT contain `setPhotoPublished` or a publish `DropdownMenuCheckboxItem` (publish toggle removed).
    - Test: upload-form.tsx source does NOT contain `publish-now` / `publishNow` (publish checkbox removed) and DOES append `published` as "true".
    - Test: src/app/(dashboard)/dashboard/photos/featured/page.tsx exists and its source contains `FEATURED_SLUG` and references `SortablePhotoGrid` + `PhotoLibraryPicker`.
    - Test: albums/[id]/page.tsx source contains `getUncollectedPhotos` and `PhotoLibraryPicker` (add-from-library UI present).
    - Test: photos/page.tsx source contains `/dashboard/photos/featured` and does NOT contain `photo.title || "Untitled"` (filename label replaced).
  </behavior>
  <action>
    Add a new `describe('v6.0 Phase 39 — Dashboard Management', ...)` block to prod-readiness.test.ts following the existing source-level pattern (read each file with `fs.readFile(path.join(process.cwd(), 'src/app/...'), 'utf-8')`, assert with `toContain` / `not.toContain`). Implement the five behavior assertions above. These are static source assertions — no Prisma mocking, no rendering. Use exact `path.join` paths matching the route-group directory names (the `(dashboard)` segment is a literal directory). Keep assertions resilient to formatting (match on identifiers like `FEATURED_SLUG`, `getUncollectedPhotos`, `PhotoLibraryPicker`, `/dashboard/photos/featured`, not whole lines). Do not assert on the publish checkbox in a way that also matches the kept "published" DB plumbing in unrelated files — scope each read to the specific file under test.
  </action>
  <verify>
    <automated>npm test -- prod-readiness 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - A new describe block "v6.0 Phase 39 — Dashboard Management" exists in prod-readiness.test.ts with the five assertions from `<behavior>`.
    - The featured-page assertion checks for `FEATURED_SLUG` + `SortablePhotoGrid` + `PhotoLibraryPicker`.
    - The publish-removal assertions read photo-actions.tsx and upload-form.tsx specifically and assert the toggle/checkbox identifiers are absent.
    - `npm test -- prod-readiness` exits 0 with the new tests passing.
  </acceptance_criteria>
  <done>prod-readiness.test.ts locks in: publish toggle gone, featured page exists + uses FEATURED_SLUG, album page has add-from-library, photos grid links to Featured and dropped the filename label.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean across the three touched app files (prop removals consistent).
- `grep` confirms no publish toggle/checkbox identifiers remain in photo-actions.tsx / upload-form.tsx; `setPhotoPublished` still present in collection-actions.ts.
- photos/page.tsx links to `/dashboard/photos/featured` and no longer renders the `photo.title || "Untitled"` filename label.
- `npm test -- prod-readiness` passes (Task 3 assertions reference Plan 01/02 files — this plan is wave 3, so those files exist).
</verification>

<success_criteria>
- ROADMAP 39 success criterion 4 met: no publish toggle, publish button, or publish-related UI element appears anywhere in the dashboard photo or upload flows.
- VIS-02 UI half satisfied; `setPhotoPublished` + `published` column retained as plumbing (out of scope to delete).
- Dashboard Photos grid shows a quiet status instead of filenames and exposes the Featured manager link.
- Source-level regression tests lock in FEAT-02/FEAT-03/COLL-02 page contracts.
</success_criteria>

<output>
Create `.planning/phases/39-dashboard-management/39-03-SUMMARY.md` when done.
</output>
