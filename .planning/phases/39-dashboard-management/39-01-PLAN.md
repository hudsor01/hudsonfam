---
phase: 39-dashboard-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/dashboard/memorial/media/photo-library-picker.tsx
  - src/app/(dashboard)/dashboard/photos/featured/page.tsx
autonomous: true
requirements: [FEAT-02, FEAT-03]
must_haves:
  truths:
    - "Owner opens /dashboard/photos/featured and sees a live 3-column preview mirroring the public homepage featured grid in current order"
    - "Owner can add any photo that is not already featured from the library and drag to reorder the featured set"
    - "The featured manager surfaces the max-9 limit (shows count, disables/blocks add at 9) and toasts the server error gracefully when the cap is hit"
    - "When the featured collection row does not exist yet, the page renders a quiet 'not set up yet' state instead of crashing"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/photos/featured/page.tsx"
      provides: "Featured manager server page: resolve by FEATURED_SLUG, live 3-col preview, SortablePhotoGrid + PhotoLibraryPicker, graceful-absent"
      min_lines: 60
      contains: "FEATURED_SLUG"
    - path: "src/app/(dashboard)/dashboard/memorial/media/photo-library-picker.tsx"
      provides: "Collection-agnostic library picker reused by featured + album managers"
      contains: "addPhotoToCollection"
  key_links:
    - from: "src/app/(dashboard)/dashboard/photos/featured/page.tsx"
      to: "prisma.collection (slug = FEATURED_SLUG)"
      via: "findUnique resolving the featured surface collection"
      pattern: "FEATURED_SLUG"
    - from: "src/app/(dashboard)/dashboard/photos/featured/page.tsx"
      to: "SortablePhotoGrid + PhotoLibraryPicker"
      via: "render with collectionId = featured collection id"
      pattern: "SortablePhotoGrid|PhotoLibraryPicker"
---

<objective>
Build the featured-grid manager at `/dashboard/photos/featured` (FEAT-02, FEAT-03): a live preview that mirrors the public homepage 3×3 grid, plus drag-to-reorder and add-from-library, reusing the exact memorial/media pattern pointed at the `featured` surface collection. Generalize the existing memorial `PhotoLibraryPicker` so it is collection-agnostic (no hardcoded "memorial" copy) and can serve both the featured manager and the per-collection manager (Plan 02 consumes it).

Purpose: Give the owner real control over what visitors see on the homepage without touching files or re-uploading. The featured collection row is seeded in Phase 40 — until then the page must show a graceful "not set up yet" state, mirroring how memorial/media handles a missing collection.
Output: `src/app/(dashboard)/dashboard/photos/featured/page.tsx` (new) + generalized `photo-library-picker.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-dashboard-management/39-CONTEXT.md

# THE reuse template — surface-collection manager (copy this structure for featured)
@src/app/(dashboard)/dashboard/memorial/media/page.tsx
# The picker to generalize (currently hardcoded "memorial" copy)
@src/app/(dashboard)/dashboard/memorial/media/photo-library-picker.tsx
# Drag-reorder grid (reuse as-is; no changes)
@src/components/dashboard/sortable-photo-grid.tsx
# The public homepage featured grid the live preview must mirror (grid-cols-3)
@src/components/public/photo-grid-preview.tsx

<interfaces>
<!-- Phase 37 — reuse, do not re-implement. -->

From src/lib/featured.ts:
```typescript
export const FEATURED_SLUG = "featured"; // resolve the featured surface collection by this slug
export const FEATURED_MAX = 9;           // server-enforced cap; surface it in the UI
```

From src/lib/collection-actions.ts (server actions):
```typescript
// Surface target (featured): adds as a reference, does NOT move the photo out of its album/All-Photos home.
// Throws "Featured is limited to 9 photos..." when count >= FEATURED_MAX. PhotoLibraryPicker already toasts the thrown message.
export async function addPhotoToCollection(collectionId: string, photoId: string): Promise<void>;
export async function reorderCollectionPhoto(collectionId: string, photoIds: string[]): Promise<void>;
export async function removePhotoFromCollection(collectionId: string, photoId: string): Promise<void>;
```

From src/components/dashboard/sortable-photo-grid.tsx (reuse unchanged):
```typescript
// Renders drag-reorder grid; per-tile remove + layout select; calls reorderCollectionPhoto / removePhotoFromCollection.
export function SortablePhotoGrid(props: {
  collectionId: string;
  items: { photoId: string; caption: string | null; layout: string }[];
}): JSX.Element;
```

From src/components/public/photo-grid-preview.tsx (the homepage grid shape to mirror):
```typescript
// grid-cols-3 gap-1.5, aspect-square tiles, /api/images/{id}?size=thumbnail. Wrapped in <Link href="/photos">.
export function PhotoGridPreview(props: { photos: { id: string; thumbnailPath: string }[] }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize PhotoLibraryPicker to be collection-agnostic</name>
  <files>src/app/(dashboard)/dashboard/memorial/media/photo-library-picker.tsx</files>
  <read_first>
    - src/app/(dashboard)/dashboard/memorial/media/photo-library-picker.tsx (the file being modified — currently hardcodes "memorial" copy on lines 28 and 42)
    - src/app/(dashboard)/dashboard/memorial/media/page.tsx (the analog caller — passes collectionId + photos)
  </read_first>
  <action>
    Make the picker reusable for featured + album managers WITHOUT changing its current memorial behavior. Add two optional props to `PhotoLibraryPickerProps`: a label used in the success/empty copy (default keeps the memorial wording so the existing memorial caller is unchanged), and an optional `disabled` boolean that, when true, blocks all add buttons (the featured manager passes this when the set is full at FEATURED_MAX). Replace the hardcoded toast string "Photo added to memorial" and the empty-state string "All library photos are already in the memorial collection." with versions built from the new label prop, defaulting to the memorial text. When `disabled` is true, render the add buttons as non-interactive (disabled) and do not call `addPhotoToCollection`. Keep the existing success/error toast flow (the thrown FEATURED_MAX message from `addPhotoToCollection` must still surface via the existing catch → `toast.error(err.message)`). Do NOT rename the file or move it; both new callers import it from its current path. Keep the `"use client"` directive.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "photo-library-picker" || echo "no picker type errors"</automated>
  </verify>
  <acceptance_criteria>
    - File still starts with `"use client"` and still imports `addPhotoToCollection` from `@/lib/collection-actions`.
    - `PhotoLibraryPickerProps` exposes an optional label prop and an optional `disabled` prop; both default to preserving current memorial behavior.
    - grep shows the literal "added to memorial" success string is now derived from the label prop (default text unchanged), not a bare hardcoded string in the add handler.
    - With `disabled` true, the add button is rendered disabled and `handleAdd` is not invoked.
    - The existing memorial caller (memorial/media/page.tsx) still type-checks with no prop changes required.
  </acceptance_criteria>
  <done>Picker is collection-agnostic, defaults preserve memorial copy, and a `disabled` prop blocks adds when the set is full.</done>
</task>

<task type="auto">
  <name>Task 2: Create the featured manager page with live preview, reorder, and add-from-library</name>
  <files>src/app/(dashboard)/dashboard/photos/featured/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/dashboard/memorial/media/page.tsx (THE template — copy its structure: requireRole, findUnique by slug, library = all photos minus this collection, SortablePhotoGrid + PhotoLibraryPicker, graceful-absent block)
    - src/components/public/photo-grid-preview.tsx (the 3-col homepage grid the live preview must mirror — reuse this component directly for the preview)
    - src/lib/featured.ts (FEATURED_SLUG, FEATURED_MAX)
    - src/lib/collection-actions.ts (addPhotoToCollection surface behavior + max-9 throw)
  </read_first>
  <action>
    Create a Server Component page (no `"use client"`; page.tsx is a server component) at route `/dashboard/photos/featured`. Call `await connection()` then `await requireRole(["owner"])` exactly like memorial/media. Resolve the featured collection with `prisma.collection.findUnique({ where: { slug: FEATURED_SLUG }, include: { photos: { include: { photo: true }, orderBy: { sortOrder: "asc" } } } })`. Build `photoItems` from the collection's photos (photoId, caption, layout) the same way memorial/media does, and build the library = all photos whose id is NOT already in the featured set (fetch via `prisma.photo.findMany({ orderBy: { createdAt: "desc" }, select: { id, title, caption } })` then filter out featured ids — mirror memorial/media; note featured is a surface ref so the library is "all photos minus featured", a photo may be featured AND in an album). Render: (1) breadcrumbs + a SectionHeader titled "Featured" with subtitle showing the count and the cap (e.g. "{n} / {FEATURED_MAX} featured"); (2) a "Live Preview" block that renders the featured photos through the `PhotoGridPreview` component (grid-cols-3) so the owner sees exactly the public homepage shape — pass the featured photos mapped to `{ id, thumbnailPath }`; (3) the `SortablePhotoGrid` with `collectionId = collection.id` and `items = photoItems`; (4) the generalized `PhotoLibraryPicker` with `collectionId = collection.id`, `photos = library`, a featured-appropriate label, and `disabled = photoItems.length >= FEATURED_MAX` so adds are blocked at the cap. When `findUnique` returns null (row not seeded until Phase 40), render the graceful "Featured collection is not set up yet" card (mirror the memorial-missing block in memorial/media — a bordered card with muted copy) and DO NOT render the grid/picker. Link target note: the dashboard Photos page links here in Plan 03 — no action needed in this file. Use theme tokens only (text-muted-foreground, bg-card, border-border, text-primary) — no raw Tailwind color names.
  </action>
  <verify>
    <automated>test -f "src/app/(dashboard)/dashboard/photos/featured/page.tsx" && npx tsc --noEmit 2>&1 | grep -i "photos/featured" || echo "featured page compiles"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/(dashboard)/dashboard/photos/featured/page.tsx and is a Server Component (no `"use client"` line).
    - grep shows the file imports and references `FEATURED_SLUG` and resolves the collection via `prisma.collection.findUnique({ where: { slug: FEATURED_SLUG ... } })`.
    - grep shows it imports `SortablePhotoGrid`, `PhotoLibraryPicker`, and `PhotoGridPreview`; the preview block uses the grid-cols-3 PhotoGridPreview.
    - The library passed to the picker excludes photos already in the featured set (filter by featured photo ids).
    - The picker `disabled` prop is bound to `photoItems.length >= FEATURED_MAX` and the header surfaces the count and cap.
    - A null-collection branch renders a graceful "not set up yet" card and skips the grid/picker (no crash, no `collection.id` deref on null).
    - `await requireRole(["owner"])` gates the page; no raw Tailwind color names (text-green-*, bg-blue-*, etc.) appear.
  </acceptance_criteria>
  <done>Featured manager renders live preview + reorder + capped add-from-library when the collection exists, and a graceful not-set-up card when it is absent.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` is clean for the two touched files.
- `/dashboard/photos/featured` route file exists and uses FEATURED_SLUG, SortablePhotoGrid, PhotoLibraryPicker, PhotoGridPreview.
- With the featured row absent (current DB state — seeded in Phase 40), the page renders the graceful state, not an error.
- The generalized picker leaves memorial/media behavior unchanged (defaults preserve memorial copy).
</verification>

<success_criteria>
- ROADMAP 39 success criteria 1 + 2 met: featured manager shows a live preview mirroring the homepage grid in current order; owner can add from the full library and drag to reorder with the max-9 cap enforced (server) and surfaced (UI), no duplicates.
- FEAT-02 (manager with live preview) and FEAT-03 (add-from-library + drag reorder) satisfied at the dashboard layer.
- No public-page change, no schema change, no live-DB seeding.
</success_criteria>

<output>
Create `.planning/phases/39-dashboard-management/39-01-SUMMARY.md` when done.
</output>
