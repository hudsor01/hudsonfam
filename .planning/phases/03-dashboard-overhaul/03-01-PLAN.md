---
phase: 03-dashboard-overhaul
type: execute
---

<objective>
Replace inline Edit/Delete actions with DropdownMenu (three-dot) row actions on dashboard list pages.

Purpose: Consistent action patterns across dashboard, cleaner row layout, extensible menu.
Output: Posts, events, and photos list pages use DropdownMenu for row actions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(dashboard)/dashboard/posts/post-actions.tsx — existing DropdownMenu pattern (AlertDialog for delete confirm)
@src/app/(dashboard)/dashboard/posts/page.tsx — posts list with inline Edit/DeleteButton
@src/app/(dashboard)/dashboard/events/page.tsx — events list with inline Edit/DeleteButton (two sections: upcoming/past)
@src/app/(dashboard)/dashboard/photos/page.tsx — photos grid with hover DeleteButton

**What's already done (Phase 3):**
- DashboardBreadcrumbs component exists at src/components/dashboard/breadcrumbs.tsx, used on all sub-pages
- Tabs (Pending/Approved) already on memorial memories page
- PostActions component exists at src/app/(dashboard)/dashboard/posts/post-actions.tsx with DropdownMenu + AlertDialog pattern

**Pattern to follow:**
PostActions uses: DropdownMenu with MoreHorizontal trigger, Edit item (router.push), Delete item (opens AlertDialog with confirm), toast on success/error.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline actions with PostActions on posts list page</name>
  <files>src/app/(dashboard)/dashboard/posts/page.tsx</files>
  <action>Convert posts list page to use PostActions component. Remove the inline Edit link and DeleteButton from each post row. Replace with PostActions component passing postId and deleteAction. The page is a server component, so wrap the row actions area to pass server action. Keep the existing row layout (flex items-center justify-between) but replace the right-side actions div content with just date + PostActions.</action>
  <verify>bun run build passes without errors</verify>
  <done>Posts list shows three-dot menu per row with Edit and Delete options instead of inline links</done>
</task>

<task type="auto">
  <name>Task 2: Create EventActions and PhotoActions components, wire into list pages</name>
  <files>src/app/(dashboard)/dashboard/events/event-actions.tsx, src/app/(dashboard)/dashboard/events/page.tsx, src/app/(dashboard)/dashboard/photos/photo-actions.tsx, src/app/(dashboard)/dashboard/photos/page.tsx</files>
  <action>Create EventActions following exact PostActions pattern — DropdownMenu with MoreHorizontal trigger, Edit (router.push to /dashboard/events/{id}), Delete with AlertDialog confirm, toast feedback. Wire into events list page replacing inline Edit link + DeleteButton in both upcoming and past sections. Create PhotoActions similarly — DropdownMenu with View (open image in new tab via window.open), Delete with AlertDialog. Wire into photos grid replacing the hover DeleteButton overlay. Keep the photo grid layout intact but add the three-dot button in the top-right corner of each photo tile (absolute positioned, always visible or visible on group-hover).</action>
  <verify>bun run build passes without errors</verify>
  <done>Events and photos list pages show three-dot dropdown menus for row actions</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] Posts list page uses PostActions dropdown
- [ ] Events list page uses EventActions dropdown (both sections)
- [ ] Photos list page uses PhotoActions dropdown
</verification>

<success_criteria>
- All three list pages use DropdownMenu pattern
- No inline Edit/Delete links remain on these pages
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-overhaul/03-01-SUMMARY.md`
</output>
