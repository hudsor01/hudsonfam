---
phase: 10-tanstack-form
type: execute
---

<objective>
Install TanStack Form + zod infrastructure and create form utility wrappers.

Purpose: Current forms use uncontrolled inputs with manual validation scattered across server actions. TanStack Form + zod provides type-safe client-side validation, field-level error tracking, and form state management that integrates cleanly with shadcn Field patterns.
Output: TanStack Form and zod installed, form utility wrappers created, zod schemas defined for all form types.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@src/app/(dashboard)/dashboard/posts/new/page.tsx
@src/app/(dashboard)/dashboard/events/new/page.tsx
@src/app/(dashboard)/dashboard/updates/new/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TanStack Form, zod, adapter, and create form utility wrappers</name>
  <files>package.json, src/lib/form-utils.ts (new)</files>
  <action>Install the required packages:
  - bun add @tanstack/react-form zod @tanstack/zod-form-adapter
  - Add shadcn Label and Textarea components if not already installed (npx shadcn@latest add label textarea)

  Create src/lib/form-utils.ts with utility wrappers that integrate TanStack Form with shadcn Field patterns:
  - FormField wrapper component that connects TanStack Form's field API to shadcn Label + Input/Textarea/Select with error display
  - FieldError component that renders validation messages in consistent style
  - useAppForm hook that pre-configures TanStack Form with zod validator adapter
  - Type helpers for form field props to ensure type safety across form components</action>
  <verify>bun run build passes, imports resolve correctly</verify>
  <done>TanStack Form + zod installed, form-utils.ts provides reusable wrappers for field rendering, error display, and form creation</done>
</task>

<task type="auto">
  <name>Task 2: Create zod schemas for all form types</name>
  <files>src/lib/schemas/ (new directory)</files>
  <action>Create zod schemas matching current server-side constraints for each form:
  - src/lib/schemas/post.ts — PostFormSchema: title (required, min 1), content (required, min 1), status (enum: draft/published), visibility (enum)
  - src/lib/schemas/event.ts — EventFormSchema: title (required), description (optional), date (required, valid date), location (optional), visibility (enum)
  - src/lib/schemas/update.ts — UpdateFormSchema: title (required), content (required), type (enum)
  - src/lib/schemas/album.ts — AlbumFormSchema: title (required), description (optional)
  - src/lib/schemas/memory.ts — MemoryFormSchema: title (required), content (required), author (optional), date (optional)
  - src/lib/schemas/index.ts — barrel export all schemas

  Inspect existing server actions and Prisma model to ensure schema constraints match actual database requirements.</action>
  <verify>bun run build passes, all schemas export correctly</verify>
  <done>Zod schemas defined for PostForm, EventForm, UpdateForm, AlbumForm, MemoryForm, all matching server-side constraints</done>
</task>

</tasks>

<verification>
- [ ] bun run build succeeds
- [ ] @tanstack/react-form, zod, @tanstack/zod-form-adapter in package.json dependencies
- [ ] src/lib/form-utils.ts exports useAppForm, FormField, FieldError
- [ ] All schema files in src/lib/schemas/ export valid zod schemas
- [ ] Schema constraints match Prisma model and server action validation
</verification>

<success_criteria>
- TanStack Form + zod installed and importable
- Form utility wrappers integrate TanStack Form field API with shadcn components
- Zod schemas defined for all five form types
- All schemas match existing server-side constraints
- Build passes with no type errors
</success_criteria>

<output>
After completion, create .planning/phases/10-tanstack-form/10-01-SUMMARY.md
</output>
