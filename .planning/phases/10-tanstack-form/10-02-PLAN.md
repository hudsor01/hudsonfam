---
phase: 10-tanstack-form
type: execute
---

<objective>
Migrate all forms to TanStack Form + zod validation.

Purpose: With TanStack Form infrastructure and zod schemas in place from 10-01, this plan migrates every dashboard form to use client-side validation with field-level error display. This eliminates the current pattern of submitting invalid data to server actions and provides immediate user feedback.
Output: All five form components use TanStack Form with zod validation and shadcn Field patterns.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/form-utils.ts
@src/lib/schemas/index.ts
@src/app/(dashboard)/dashboard/posts/new/page.tsx
@src/app/(dashboard)/dashboard/posts/[id]/page.tsx
@src/app/(dashboard)/dashboard/events/new/page.tsx
@src/app/(dashboard)/dashboard/events/[id]/page.tsx
@src/app/(dashboard)/dashboard/updates/new/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate PostForm and EventForm to TanStack Form with zod validation</name>
  <files>src/app/(dashboard)/dashboard/posts/post-form.tsx, src/app/(dashboard)/dashboard/events/event-form.tsx</files>
  <action>Migrate PostForm:
  - Extract form logic into a dedicated post-form.tsx component if not already separated
  - Use useAppForm hook from form-utils.ts with PostFormSchema from schemas/post.ts
  - Replace uncontrolled inputs with TanStack Form field subscriptions
  - Wire field-level validation errors to display below each input using FieldError component
  - Maintain existing server action submission — call form.handleSubmit() for validation, then invoke server action on success
  - Support both create (new) and edit ([id]) modes with defaultValues

  Migrate EventForm similarly:
  - Use EventFormSchema, handle date field with proper date input/picker
  - Wire validation errors, maintain server action integration
  - Support create and edit modes</action>
  <verify>bun run build passes, form validation shows errors before submission</verify>
  <done>PostForm and EventForm fully migrated to TanStack Form with zod validation, field-level error display, server action integration preserved</done>
</task>

<task type="auto">
  <name>Task 2: Migrate UpdateForm, AlbumForm, and MemoryForm</name>
  <files>src/app/(dashboard)/dashboard/updates/update-form.tsx, src/app/(dashboard)/dashboard/photos/albums/album-form.tsx, src/app/(dashboard)/dashboard/memorial/memories/memory-form.tsx</files>
  <action>Migrate UpdateForm:
  - Use UpdateFormSchema, connect to TanStack Form with useAppForm
  - Wire field-level errors, maintain server action submission
  - Handle the update type enum field with Select component

  Migrate AlbumForm:
  - Use AlbumFormSchema (title + optional description)
  - Simpler form, ensure consistent patterns with other forms

  Migrate MemoryForm:
  - Use MemoryFormSchema, handle optional date and author fields
  - Ensure content field uses Textarea component

  For all three: ensure server actions still work correctly after migration, form submissions produce the same data shape as before, and loading/submitting states are handled with form.state.isSubmitting.</action>
  <verify>bun run build && bun run test</verify>
  <done>All five forms migrated to TanStack Form + zod, consistent validation UX across all dashboard forms</done>
</task>

</tasks>

<verification>
- [ ] bun run build succeeds
- [ ] bun run test passes
- [ ] All forms show field-level validation errors before submission
- [ ] Server actions still receive correct data and execute successfully
- [ ] Create and edit modes work for PostForm and EventForm
- [ ] Form submission disabled while submitting (isSubmitting state)
- [ ] No console errors or type errors
</verification>

<success_criteria>
- All five forms use TanStack Form with zod validation
- Field-level error messages display immediately on blur/change
- Server action integration preserved — forms submit successfully
- Consistent validation UX pattern across all forms
- Both create and edit modes functional
- Build and tests pass
</success_criteria>

<output>
After completion, create .planning/phases/10-tanstack-form/10-02-SUMMARY.md
</output>
