---
phase: 05-advanced
type: execute
---

<objective>
Enhance command palette with Quick Actions group, add hover effects to photo thumbnails.

Purpose: Power-user navigation and visual polish on photo grids.
Output: Command palette has Pages/Dashboard/Quick Actions groups. Photo thumbnails have brightness/saturation hover effects.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/command-palette.tsx — existing CommandPalette with Pages and Dashboard groups
@src/components/providers.tsx — already includes CommandPalette
@src/app/(public)/photos/[album]/album-photo-grid.tsx — album photo grid with hover scale-105 effect
@src/app/(public)/richard-hudson-sr/page.tsx — memorial photo gallery with hover scale-105 effect
@src/app/(dashboard)/dashboard/photos/albums/[id]/page.tsx — dashboard album detail with photo grid
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Quick Actions group to command palette</name>
  <files>src/components/command-palette.tsx</files>
  <action>Add a third command group "Quick Actions" after the Dashboard group with a CommandSeparator before it. Quick Actions:
  - New Post (href: /dashboard/posts/new, icon: FilePlus or PlusCircle)
  - Upload Photos (href: /dashboard/photos/upload, icon: Upload)
  - New Event (href: /dashboard/events/new, icon: CalendarPlus)
  - New Update (href: /dashboard/updates/new, icon: MessageSquarePlus)
  Import the needed lucide-react icons. Follow the same navigate() pattern as existing items.</action>
  <verify>bun run build passes</verify>
  <done>Command palette shows three groups: Pages, Dashboard, Quick Actions</done>
</task>

<task type="auto">
  <name>Task 2: Add hover:brightness-110 hover:saturate-110 to photo thumbnails</name>
  <files>src/app/(public)/photos/[album]/album-photo-grid.tsx, src/app/(public)/richard-hudson-sr/page.tsx, src/app/(dashboard)/dashboard/photos/albums/[id]/page.tsx</files>
  <action>Add hover brightness and saturation effects to photo thumbnail img elements:
  1. album-photo-grid.tsx: Add "hover:brightness-110 hover:saturate-110 transition-all" to the img className (which already has group-hover:scale-105 transition-transform). Change transition-transform to transition-all so brightness/saturate also animate.
  2. richard-hudson-sr/page.tsx: Add "hover:brightness-110 hover:saturate-110" to the memorial gallery img elements (line ~328). Change transition-transform to transition-all.
  3. Dashboard albums/[id]/page.tsx: Add the same hover effects to photo thumbnails in the album detail view if they have img elements.</action>
  <verify>bun run build passes</verify>
  <done>Photo thumbnails across album pages, memorial gallery, and dashboard album detail have brightness/saturation hover effects</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds
- [ ] Command palette has three groups (Pages, Dashboard, Quick Actions)
- [ ] Photo thumbnails in albums have hover:brightness-110 hover:saturate-110
- [ ] Memorial gallery photos have the same effect
</verification>

<success_criteria>
- Command palette fully functional with three command groups
- Photo hover effects applied consistently
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-advanced/05-01-SUMMARY.md`
</output>
