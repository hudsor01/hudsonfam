---
phase: 35-navbar-footer-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/nav-footer.test.ts
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, FOOT-01, FOOT-02]

must_haves:
  truths:
    - "A test file exists asserting the exact nav link set, order, and Recipes label"
    - "A test asserts both desktop NavLink and mobile link emit aria-current=page when active"
    - "A test asserts the footer contains Recipes/Photos/Events/In Memory and no Blog/Family"
    - "Running the nav-footer suite produces failing (RED) assertions until Plan 02 ships"
  artifacts:
    - path: "src/__tests__/nav-footer.test.ts"
      provides: "Nyquist Wave 0 coverage for NAV-01/02/03 + FOOT-01/02"
      min_lines: 60
      contains: "aria-current"
  key_links:
    - from: "src/__tests__/nav-footer.test.ts"
      to: "src/app/(public)/layout.tsx"
      via: "fs.readFile source assertion"
      pattern: "readFile.*layout\\.tsx"
    - from: "src/__tests__/nav-footer.test.ts"
      to: "src/components/public/nav-link.tsx"
      via: "RTL render with mocked usePathname"
      pattern: "vi\\.mock.*next/navigation"
---

<objective>
Create the Wave 0 test file `src/__tests__/nav-footer.test.ts` that encodes the Nyquist
VALIDATION.md contract for Phase 35. This is the RED step: the file-read and RTL render
assertions describe the target state (correct nav order, "Recipes" label, aria-current on
active links, four footer links). The tests are expected to FAIL until Plan 02 implements
the component changes.

Purpose: Encode the 5 ROADMAP success criteria as automated assertions BEFORE implementation,
so Plan 02 has an executable, unambiguous contract and the phase gate is machine-verifiable.
Output: One new test file. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-navbar-footer-ia/35-RESEARCH.md
@.planning/phases/35-navbar-footer-ia/35-VALIDATION.md

<interfaces>
<!-- Contracts the test must assert against. Plan 02 implements these. -->

Target navLinks array (src/app/(public)/layout.tsx) — exact order + labels Plan 02 produces:
  Home (/) → Recipes (/recipes) → Photos (/photos) → Events (/events) → In Memory (/richard-hudson-sr)
  NOTE: label is "Recipes" — NOT "Grandma Hudson's Recipes".

Target footer link set (src/app/(public)/layout.tsx) — Plan 02 produces:
  /recipes, /photos, /events, /richard-hudson-sr  (NO /blog, NO /family)
  Footer row keeps class "flex-col sm:flex-row".

New component Plan 02 creates (src/components/public/nav-link.tsx):
  "use client"; export function NavLink({ href, children }): renders <Link> with
  aria-current="page" when usePathname() matches (root-exact for "/", startsWith otherwise).

Mobile link (src/components/public/mobile-nav.tsx) — Plan 02 adds:
  aria-current={isActive ? "page" : undefined} on each rendered <Link>.

Existing test patterns to mirror:
  - File-read assertions: src/__tests__/prod-readiness.test.ts:930 — uses
    `fs.readFile(path.join(process.cwd(), 'src', 'app', '(public)', 'layout.tsx'), 'utf-8')`
    then `expect(layout).toContain(...)`. Import `fs from 'fs/promises'` and `path from 'path'`.
  - RTL render: src/__tests__/components/ui.test.tsx — `import { render, screen } from '@testing-library/react'`.
  - Mock usePathname: `vi.mock('next/navigation', () => ({ usePathname: () => '/recipes' }))`
    (mock at module top, before importing the component under test).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave 0 nav-footer test file (RED)</name>
  <files>src/__tests__/nav-footer.test.ts</files>
  <read_first>
    - src/__tests__/prod-readiness.test.ts (lines 1-20 for imports; lines 930-940 for the layout.tsx file-read assertion pattern)
    - src/__tests__/components/ui.test.tsx (lines 1-60 for the RTL render/screen pattern)
    - src/app/(public)/layout.tsx (current navLinks array + footer markup — the source under assertion)
    - src/components/public/mobile-nav.tsx (current mobile link render — target for aria-current)
    - .planning/phases/35-navbar-footer-ia/35-VALIDATION.md (the Per-Task Verification Map — each row becomes a test)
  </read_first>
  <behavior>
    - NAV-01 order: layout.tsx source has "/recipes" appearing before "/photos" in the navLinks region.
    - NAV-01 label: layout.tsx navLinks region contains the label "Recipes" and does NOT contain "Grandma Hudson's Recipes".
    - NAV-01 count: exactly 5 nav hrefs present — "/", "/recipes", "/photos", "/events", "/richard-hudson-sr".
    - NAV-02 mobile: rendering MobileNav with all 5 links yields 5 reachable links plus Sign In.
    - NAV-03 desktop: NavLink rendered with usePathname mocked to its href emits aria-current="page"; rendered for a non-matching path emits no aria-current.
    - NAV-03 mobile: an active mobile link emits aria-current="page".
    - FOOT-01 present: layout.tsx footer region contains hrefs "/recipes", "/photos", "/events", "/richard-hudson-sr".
    - FOOT-01 absent: layout.tsx does NOT contain "/blog" or href to "/family".
    - FOOT-02 responsive: layout.tsx footer contains the class "flex-col sm:flex-row".
  </behavior>
  <action>
    Create `src/__tests__/nav-footer.test.ts`. Import `describe, it, expect, vi` from vitest,
    `fs from 'fs/promises'`, `path from 'path'`, and `render, screen` from '@testing-library/react'.
    Mirror the file-read pattern at prod-readiness.test.ts:930 — read
    `src/app/(public)/layout.tsx` once via fs.readFile and assert against its source string for
    NAV-01 (order, label, count) and FOOT-01/FOOT-02. For the order assertion, compare the index of
    "/recipes" to the index of "/photos" in the layout source and assert recipes index is lower.
    Assert source `.toContain('"Recipes"')` style for the short label and `.not.toContain("Grandma Hudson's Recipes")`.
    For NAV-03 desktop, write RTL render tests for the NavLink component at
    `@/components/public/nav-link` — mock 'next/navigation' usePathname per case (active vs inactive)
    using vi.mock and assert the rendered link's getAttribute("aria-current") equals "page" when active
    and is null when inactive. For NAV-02 + NAV-03 mobile, render MobileNav from
    `@/components/public/mobile-nav` with the 5-link array and mocked usePathname, assert all 5 labels
    render and the active link carries aria-current="page". Since `nav-link.tsx` does not exist yet and
    layout/mobile are not yet fixed, these assertions are EXPECTED TO FAIL (RED) — that is correct for
    Wave 0. Do NOT use watch mode. Use only globals.css token class names in any class assertions
    (e.g. "text-foreground", never raw Tailwind color names). Keep the test file self-contained — no new packages.
  </action>
  <acceptance_criteria>
    - File `src/__tests__/nav-footer.test.ts` exists.
    - Contains a file-read block reading `src/app/(public)/layout.tsx` via fs.readFile (mirrors prod-readiness.test.ts:930).
    - Contains assertions: "/recipes" index < "/photos" index; source contains `"Recipes"`; source does NOT contain "Grandma Hudson's Recipes".
    - Contains assertions for all 4 footer hrefs ("/recipes","/photos","/events","/richard-hudson-sr") and `.not.toContain('/blog')` + no "/family" link.
    - Contains assertion that footer source contains "flex-col sm:flex-row".
    - Contains an RTL render test importing NavLink from `@/components/public/nav-link` asserting `aria-current="page"` for the active path and null for an inactive path, with usePathname mocked via vi.mock('next/navigation', ...).
    - Contains an RTL render test importing MobileNav asserting all 5 link labels render and the active link has `aria-current="page"`.
    - `npm test -- src/__tests__/nav-footer.test.ts` runs and reports failing assertions (RED) — failure is the expected Wave 0 outcome.
    - No watch-mode flag used; no new package installed.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/__tests__/nav-footer.test.ts 2>&1 | grep -Eiq 'fail|✗|✖|expected' &amp;&amp; echo "RED-as-expected" || (echo "TESTS DID NOT FAIL — assertions too weak or already passing" &amp;&amp; exit 1)</automated>
  </verify>
  <done>The test file exists, encodes all 8 VALIDATION.md rows, and runs RED (failing) because Plan 02 has not yet shipped the implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Test-only file. No runtime code, no user input, no network, no data access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | src/__tests__/nav-footer.test.ts | accept | Test file only; no production surface, no input, no auth/data. No new packages → no supply-chain vector. |
</threat_model>

<verification>
- `npm test -- src/__tests__/nav-footer.test.ts` executes and reports failing assertions (RED).
- File contains all 8 behaviors from 35-VALIDATION.md Per-Task Verification Map.
- No production files modified (git diff shows only the new test file).
</verification>

<success_criteria>
- New test file `src/__tests__/nav-footer.test.ts` committed.
- All 5 ROADMAP success criteria represented by at least one automated assertion.
- Suite runs RED (expected) — becomes GREEN after Plan 02.
- No new packages; no watch mode; no raw Tailwind color names in class assertions.
</success_criteria>

<output>
Create `.planning/phases/35-navbar-footer-ia/35-01-SUMMARY.md` when done
</output>
