---
phase: 35-navbar-footer-ia
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - src/components/public/nav-link.tsx
  - src/app/(public)/layout.tsx
  - src/components/public/mobile-nav.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, FOOT-01, FOOT-02]

must_haves:
  truths:
    - "Navbar links are exactly Home, Recipes, Photos, Events, In Memory in that order at every viewport"
    - "The recipes nav label reads Recipes (short form), not Grandma Hudson's Recipes"
    - "The active route is visually indicated and emits aria-current=page in desktop nav and mobile drawer"
    - "Mobile drawer opens and exposes all five links plus Sign In with no overflow"
    - "Footer links are Recipes, Photos, Events, In Memory with no Blog or Family and stack on mobile"
  artifacts:
    - path: "src/components/public/nav-link.tsx"
      provides: "Client active-route NavLink with usePathname + aria-current"
      min_lines: 20
      contains: "aria-current"
    - path: "src/app/(public)/layout.tsx"
      provides: "Corrected navLinks order/label, NavLink import, 4-link footer"
      contains: "nav-link"
    - path: "src/components/public/mobile-nav.tsx"
      provides: "Mobile link aria-current on active route"
      contains: "aria-current"
  key_links:
    - from: "src/app/(public)/layout.tsx"
      to: "src/components/public/nav-link.tsx"
      via: "import { NavLink }"
      pattern: "from \"@/components/public/nav-link\""
    - from: "src/components/public/nav-link.tsx"
      to: "next/navigation"
      via: "usePathname()"
      pattern: "usePathname\\(\\)"
    - from: "src/app/(public)/layout.tsx"
      to: "/richard-hudson-sr"
      via: "footer In Memory Link"
      pattern: "/richard-hudson-sr"
---

<objective>
Implement the seven Phase 35 changes that turn Plan 01's RED tests GREEN: correct the
`navLinks` order and label in `layout.tsx`, extract an active-route `NavLink` client leaf
(`nav-link.tsx`) with `usePathname()` + `aria-current="page"`, wire it into `layout.tsx`,
add `aria-current` to the mobile drawer's active link, and add the missing footer links
(Recipes, In Memory). This is a UI refinement of existing components — no rebrand, no new
packages, all colors via globals.css tokens.

Purpose: Surface only the surviving IA (Home . Recipes . Photos . Events . In Memory),
give accessible active-route feedback on desktop and mobile, and align the footer with the
real IA — satisfying NAV-01/02/03 and FOOT-01/02.
Output: One new client component; two edited files. The nav-footer suite goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-navbar-footer-ia/35-RESEARCH.md
@.planning/phases/35-navbar-footer-ia/35-VALIDATION.md

<interfaces>
<!-- Exact current-state contracts the executor edits. No codebase exploration needed. -->

src/app/(public)/layout.tsx (Server Component) — current navLinks (lines 10-16):
  [ Home(/), Photos(/photos), Recipes:"Grandma Hudson's Recipes"(/recipes),
    Events(/events), In Memory(/richard-hudson-sr) ]
  Inline NavLink function lives at lines 95-110 (Server, no usePathname).
  Footer link group (lines 67-74) currently renders only Photos + Events with
  className "hover:text-muted-foreground transition-colors".
  Footer row class (line 60): "flex flex-col sm:flex-row" — already correct, do not change.
  External "Built by" link (lines 80-87) has target="_blank" rel="noopener noreferrer" — PRESERVE.
  MobileNav is rendered at line 50 inside Suspense and receives navLinks via the `links` prop —
  fixing navLinks once fixes desktop AND mobile.

src/components/public/mobile-nav.tsx ("use client") — already calls usePathname() and computes:
  isActive = link.href === "/" ? pathname === "/" : pathname.startsWith(link.href)
  Active styling already applied: "text-foreground bg-background". MISSING: aria-current.

src/lib/utils.ts — exports `cn(...inputs)` for class merging.

globals.css tokens available for the active state: text-foreground, font-medium, border-primary,
text-muted-foreground, bg-background. NO raw Tailwind color names (e.g. never text-green-400).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract active-route NavLink client component</name>
  <files>src/components/public/nav-link.tsx</files>
  <read_first>
    - src/app/(public)/layout.tsx (lines 95-110 — the inline NavLink being replaced; its className is the inactive baseline)
    - src/components/public/mobile-nav.tsx (lines 60-78 — the canonical isActive root-guard pattern to mirror exactly)
    - src/lib/utils.ts (the cn helper signature)
    - src/styles/globals.css (confirm token names: text-foreground, text-muted-foreground, border-primary)
  </read_first>
  <behavior>
    - Active path (usePathname returns the link href, or a child path for non-root): link emits aria-current="page" and active class (text-foreground font-medium).
    - Inactive path: no aria-current attribute; inactive class (text-muted-foreground hover:text-foreground).
    - Root link "/" is active only on exact "/" (not on every path via startsWith).
  </behavior>
  <action>
    Create src/components/public/nav-link.tsx with "use client" as the first line. Import Link from
    "next/link", usePathname from "next/navigation", and cn from "@/lib/utils". Export a function
    NavLink({ href, children }: { href: string; children: React.ReactNode }). Compute isActive using the
    EXACT root-guard pattern from mobile-nav.tsx: href === "/" then pathname === "/" else pathname.startsWith(href).
    Render a Link with aria-current set to "page" when isActive and undefined otherwise, and a cn() className:
    shared base "transition-colors contrast-more:underline", active branch "text-foreground font-medium",
    inactive branch "text-muted-foreground hover:text-foreground". Use ONLY globals.css token class names — no
    raw Tailwind color names. Optionally add "border-b border-primary" to the active branch for a terracotta
    underline accent consistent with the Ivory and Terracotta language; keep it token-based. Do not add any new package.
  </action>
  <acceptance_criteria>
    - File src/components/public/nav-link.tsx exists with "use client" as line 1.
    - Imports usePathname from "next/navigation" and cn from "@/lib/utils".
    - Exports NavLink accepting href and children.
    - isActive uses href === "/" then pathname === "/" else pathname.startsWith(href).
    - Rendered Link sets aria-current to "page" when active and undefined when inactive.
    - Active class includes text-foreground and font-medium; inactive class includes text-muted-foreground.
    - No raw Tailwind color name appears (grep finds none of text-green/red/blue/etc.).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/public/nav-link.tsx','utf8'); if(!/^\"use client\"/.test(s.trim()))throw 'use-client'; if(!s.includes('usePathname'))throw 'pathname'; if(!s.includes('aria-current'))throw 'aria'; if(/text-(red|green|blue|yellow|purple|orange|teal|emerald)-[0-9]/.test(s))throw 'raw-color'; console.log('OK')"</automated>
  </verify>
  <done>nav-link.tsx exists as a client leaf with usePathname-driven aria-current and token-only active styling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix navLinks order/label, wire NavLink, add footer links in layout.tsx</name>
  <files>src/app/(public)/layout.tsx</files>
  <read_first>
    - src/app/(public)/layout.tsx (full file — navLinks lines 10-16, inline NavLink 95-110, footer 59-90)
    - src/components/public/nav-link.tsx (the new export to import — created in Task 1)
    - .planning/phases/35-navbar-footer-ia/35-RESEARCH.md (section "Exact Gap Summary" rows 1,2,4,6 — the authoritative change list)
  </read_first>
  <action>
    Edit src/app/(public)/layout.tsx. (1) Reorder the navLinks array to exactly: Home "/", Recipes
    "/recipes", Photos "/photos", Events "/events", In Memory "/richard-hudson-sr" — Recipes BEFORE Photos.
    (2) Change the recipes label from "Grandma Hudson's Recipes" to exactly "Recipes". (3) Remove the inline
    NavLink function (lines 95-110) and add the import { NavLink } from "@/components/public/nav-link" near the
    top imports — the desktop map at lines 37-41 already calls NavLink, so it now resolves to the client leaf.
    (4) In the footer link group (the flex-wrap div), add two Links matching the EXISTING className
    "hover:text-muted-foreground transition-colors": Recipes to "/recipes" and In Memory to "/richard-hudson-sr".
    Order the footer links Recipes, Photos, Events, In Memory. Do NOT add /blog or /family. Do NOT change the
    footer row "flex-col sm:flex-row" class or the flex-wrap container. PRESERVE the external "Built by" anchor
    exactly including target="_blank" rel="noopener noreferrer". Keep layout.tsx a Server Component (no "use client").
  </action>
  <acceptance_criteria>
    - navLinks order is Home then Recipes then Photos then Events then In Memory (index of "/recipes" before index of "/photos").
    - navLinks recipes label is exactly "Recipes"; "Grandma Hudson's Recipes" no longer appears in the file.
    - File imports NavLink from "@/components/public/nav-link"; the inline NavLink function is gone.
    - layout.tsx has NO "use client" directive (still a Server Component).
    - Footer link group contains Links to "/recipes", "/photos", "/events", "/richard-hudson-sr" using class "hover:text-muted-foreground transition-colors".
    - File does NOT contain "/blog"; no footer link to "/family".
    - Footer row still has "flex-col sm:flex-row".
    - "Built by" anchor still has target="_blank" and rel="noopener noreferrer".
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/__tests__/nav-footer.test.ts</automated>
  </verify>
  <done>navLinks corrected, NavLink wired from the client leaf, footer has all four IA links, external link rel preserved, file remains a Server Component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add aria-current to the active mobile drawer link</name>
  <files>src/components/public/mobile-nav.tsx</files>
  <read_first>
    - src/components/public/mobile-nav.tsx (lines 60-78 — the link map where isActive is already computed)
    - src/components/public/nav-link.tsx (mirror the same aria-current expression for consistency)
  </read_first>
  <behavior>
    - When a mobile link's isActive is true, the rendered Link emits aria-current="page".
    - When isActive is false, no aria-current attribute is present.
  </behavior>
  <action>
    Edit src/components/public/mobile-nav.tsx. On the mapped Link inside the nav (the one already styled
    with the isActive ternary), add the attribute aria-current set to "page" when isActive and undefined
    otherwise. Do not change the existing isActive computation, the onClick that calls setOpen(false), the
    className token styling, or anything else. The Sign In link is not a route nav item — leave it unchanged.
    No new package.
  </action>
  <acceptance_criteria>
    - The mapped nav Link in mobile-nav.tsx sets aria-current to "page" when isActive and undefined otherwise.
    - The isActive root-guard computation is unchanged.
    - The onClick calling setOpen(false) is still present on each link.
    - No raw Tailwind color names introduced; file still begins with "use client".
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/__tests__/nav-footer.test.ts</automated>
  </verify>
  <done>The active mobile link emits aria-current="page"; existing behavior unchanged; the full nav-footer suite is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser -> static nav/footer markup | Nav/footer hrefs are static internal routes. No user input is read or rendered, so there is no injection vector. The only external link ("Built by") is preserved with rel="noopener noreferrer". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02 | Tampering | navLinks / footer hrefs (layout.tsx) | accept | All hrefs are static string literals to internal routes. No dynamic interpolation, no user input -> no injection surface. |
| T-35-03 | Information Disclosure | external "Built by" anchor (layout.tsx) | mitigate | Preserve target="_blank" rel="noopener noreferrer" exactly (Task 2 acceptance asserts rel present) to prevent reverse-tabnabbing / window.opener leakage. |
| T-35-04 | Elevation of Privilege | usePathname() client hook (nav-link.tsx / mobile-nav.tsx) | accept | Read-only route awareness; no privilege boundary, no auth/data access. aria-current is presentational only. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH section Package Legitimacy Audit: zero new packages). No supply-chain action required. |
</threat_model>

<verification>
- `npm test -- src/__tests__/nav-footer.test.ts` is GREEN (all 8 VALIDATION.md rows pass).
- `npm test` full suite remains green (207-test regression baseline + new file).
- `npm run build` compiles (layout.tsx remains a Server Component; nav-link.tsx is a client leaf — no usePathname-in-Server error).
- Manual (deferred to /gsd:verify-work per VALIDATION.md): 375px drawer opens with all 5 links and no overflow; Tab traverses nav and focus returns to the hamburger on Esc; active route visually distinct on desktop and mobile; footer stacks cleanly at 375px.
</verification>

<success_criteria>
- Navbar links are exactly Home, Recipes, Photos, Events, In Memory in that order (NAV-01).
- Recipes nav label is the short form "Recipes" (NAV-01).
- Mobile drawer surfaces all five links plus Sign In, no overflow (NAV-02).
- Desktop NavLink and mobile link both emit aria-current="page" on the active route with token-based active styling (NAV-03).
- Footer links are Recipes, Photos, Events, In Memory; no Blog/Family; footer stacks on mobile (FOOT-01, FOOT-02).
- External "Built by" link keeps rel="noopener noreferrer"; layout.tsx stays a Server Component; no new packages; all colors via globals.css tokens.
- Plan 01's RED suite is now GREEN.
</success_criteria>

<output>
Create `.planning/phases/35-navbar-footer-ia/35-02-SUMMARY.md` when done
</output>
